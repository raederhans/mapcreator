# Development loop simplification

## Stage 2 verification cost reduction authorized on 2026-09-06

Proceed with the four-phase roadmap's second stage: calibrate local runtime estimates using fixed measured workflows, improve ordinary multi-file WIP selection, and inspect repeated control-plane execution for shared catalog edits. Stage 1 policy acceptance remains incomplete and is not part of this stage's success claim. Root owns implementation, shared catalog/runner/tests, measurement and records. One user-visible helper audits duplication read-only against the original dirty checkout. Acceptance: deterministic complete coverage for fixed cases, no duplicate execution leaves, unknown paths fail closed, existing reserved/owner/budget authority retained, and repeated local timings reported with their actual scope. No production-module changes, P4 producer, baseline refresh, broad admission, commit, push or deployment.

## Stage 1 closure authorized on 2026-09-06

Close current writer-policy synchronization after the prior governance batches. Preserve all existing WIP and historical baselines. Root owns shared contract/scanner/producer integration and generated policy; two read-only subagents first inspect actual special-zone runtime contracts and the narrow generation/check path. Choose the smallest source-backed fix before assigning implementation ownership. Do not assume shallow projection or read-only normalization produces detached values. Acceptance is meaningful target regressions, current policy generation/check success, unchanged frozen history, and a documented local closeout. No stage 2-4 work, remote operation or formal full-P4 admission is included.

## Follow-up priorities 1-4 authorized on 2026-09-05

Implement the four selected opportunities on the current checkout while preserving all earlier uncommitted stages: retire unused scenario activation code, remove redundant renderer source-shape tests, clarify current governance documentation, and consolidate Supervisor execution plus historical catalog entrypoints. Four user-visible same-directory tasks and a read-only dependency subagent use Astra 6 medium; root owns shared package/catalog integration and these records.

Acceptance is actual scoped deletion or responsibility consolidation, preserved canonical behavior and failure semantics, accurate current versus historical status, and the narrowest sufficient checks. Keep frozen historical evidence intact; update current generated policy through its supported producer if required. No commits, remote changes, data migration, Pages/dist work or broader scenario lifecycle rewrite is included. Earlier plan sections below retain their original scope and acceptance history.

## Goal and scope

The user authorized rapid implementation after the 2026-09-05 audit: shorten everyday verification, remove renderer forwarding registries and the special-zone import cycle, and consolidate border state ownership. Preserve existing product behavior and the local P4.4 candidate at `12d967fc`.

## Implementation slices

1. Restrict default core verification to local-safe work; retain explicit full/CI entrypoints and make existing local feedback usable during action edits.
2. Remove four internal renderer facade registries without changing public exports; eliminate the special-zone model/actions cycle.
3. Consolidate a bounded border dirty/timer/cache lifecycle in existing owners.

## Acceptance

- Demonstrate changed default command selection and measured targeted feedback time without running the historical full-policy chain.
- Confirm the four registries and their imports are removed, the special-zone cycle is gone, and focused behavior tests pass.
- Demonstrate fewer border-state writers with cancellation/invalidation behavior preserved.
- Integrate all owned changes in the current checkout and report measured improvements separately from unverified runtime performance.

## Boundaries

No push, deployment, full-policy/performance/nightly runs, new frameworks, unrelated cleanup, or changes to data. Do not weaken explicit release authority or claim local feedback is formal P4 admission. Use targeted checks; one integration owner handles shared files and control-plane follow-through.

## Authorized second batch (2026-09-05)

The user accepted four priorities and requested four user-visible implementation tasks:

1. Retire duplicate handwritten verification metadata and routine legacy-shadow maintenance; retain independent discovery, schema and coverage validation.
2. Simplify a bounded renderer source-shape test family where existing behavior coverage proves the retained contract.
3. Consolidate sidebar country grouping/tree-model responsibilities and reduce cross-module callbacks.
4. Separate ordinary current-change verification from historical governance proof, based on actual remaining trigger paths.

Acceptance: each task delivers actual scoped code, minimal meaningful checks, measurable maintenance/command reductions, and explicit residual gaps. P1 owns shared metadata/package entries; P4 owns execution runners; root coordinates requests between them. Preserve the entire first batch and do not claim local checks establish historical admission or release readiness.

## Authorized third batch (2026-09-05)

The user requested another analysis and explicitly authorized dispatching the determined work without another confirmation. Priorities for the next rounds: broaden useful existing local behavior coverage; consolidate complete UI/renderer responsibilities; then consider domain-based catalog and mixed-test organization. Do not start a broad scenario/state rewrite merely to shrink files.

This batch starts with local routes for existing renderer/UI owners, an independent commit-runner test file, and HOI4 catalog state/render ownership within existing sidebar helpers. Renderer scope depends on the bounded read-only candidate map. Preserve all first/second-batch uncommitted work. Root coordinates canonical/package changes and acceptance; no automatic release or full historical admission is implied.

## Authorized fourth batch (2026-09-05)

The user selected verification catalog organization and requested higher-reasoning visible tasks. Split the 19k-line authored source by domain, separate normalization from authored data, preserve one logical authority and public exports, and update actual dependency consumers and existing navigation documentation. Preserve all 476 normalized records, commands, ordering and policies; only explicitly justified sourceRefs may change to cover the new modules. Root compares the complete before/after objects and integrates bounded checks. No broad test run or budget-policy change is included.

## Authorized local integration and commits (2026-09-05)

The user requested one integration/commit round covering the earlier improvements, with review and small evidence-backed fixes for remaining redundancy. All four batches are already in the same checkout on `codex/runtime-architecture-reset-r1-integration-20260831`; integrate locally on that branch. Root owns staging and commits. Parallel owners review/fix renderer and sidebar slices; a read-only verification reviewer reports issues for root to repair. Reuse existing successful checks, rerun affected behavior only after changes, and perform the necessary cross-slice import/catalog checks. Remote publication, main-branch promotion, worktree cleanup and full historical admission are outside this local closeout.

## Authorized deeper stages 1-4 (2026-09-05)

Starting from the committed, clean `348a952e`, the user authorizes higher-reasoning implementation tasks and root supervision to finish stages sequentially:

1. Renderer: retire the RuntimeContext intermediary while retaining effective owner contracts; then consolidate hover transient state and scheduling in its existing owner.
2. Sidebar/toolbar: consolidate Project support diagnostics mounting and behavior, then Quick Fill policy/events/state in its existing controller.
3. Verification: deduplicate overlapping commit/local-infra execution; separate small contracts from repository/historical scanning and retire obsolete architecture shape checks with effective coverage retained.
4. Scenario: physically separate quick/heavy test imports; then implement bounded request cancellation only with shared in-flight reuse, retry and generation safety preserved. Keep promotion/rollback transactions unchanged.

One implementation stage at a time, with independent bounded review before moving on. Root alone edits shared package/catalog/records and handles Git integration. Test the affected behavior and actual execution selection; do not require unrelated whole-repository or historical admission runs. No production publication, dist regeneration, data migration or new framework is included.


## Stage 1 policy closeout (2026-09-06)

The user requested continued repair and closeout. Reconcile the deeper-stage changes with the existing state-writer policy without refreshing frozen baselines, increasing diagnostic budgets, or treating the previous local acceptance as historical admission. Preserve all other uncommitted stages and worktrees.

Root owns integration and the serial producer/checker runs. Focused subagent work covers scanner soundness and read-only borrowed-reference analysis. The completion gate is the existing producer with `--phase P4.4 --previous-policy-revision HEAD --write`, followed by the policy checker with `--phase P4.4`. No commit, push, dist or production promotion is part of this request.


## Roadmap stage 3 authorized on 2026-09-06

Begin the original four-phase roadmap stage 3 (functional consolidation, distinct from the earlier deeper-stage verification batch). First slice: consolidate regional preset filtering, disabled state, rendering and application validation into one sidebar controller; reuse existing ownership/color mutations and preserve failure results, history reasons and click-time state. Replace the three ordinary/scenario-ownership/scenario-visual call-site recipes with one functional entry. Preserve all inherited WIP. Validate focused behavior and import/routing boundaries. Root owns source/catalog/records; no commit, publication, baseline refresh or whole-stage completion claim. Remaining scenario and renderer opportunities follow only after this slice is accepted.


## Stage 3 continuation — resolved core presets, 2026-09-06

The user authorized continued optimization and repair. Consolidate core preset resolution and presence checks in the existing regional preset controller, and give resolved presets a direct application path shared with indexed presets. Fix generated boundary-variant visual application (index -1 has no backing array record) and restore sparse-array enumeration compatibility. Retain ownership, companion transfer and shell orchestration. Validate the actual Sidebar visual caller, detached arrays, mode/count semantics and the source-bound reader gate. Preserve inherited WIP; no publication, baseline refresh or broad state migration.


## Stage 3 scenario focus — historical transfers, 2026-09-06

The user explicitly requested a scenario-focused continuation. Consolidate manual historical transfers, automatic companions and their UI in one controller, using the existing owner/controller transaction and visibility filter. Remove the single-use explicit-transfer wrapper and duplicate result bookkeeping; propagate silent behavior through empty/unavailable target preflight. Preserve fatal-lock authority, history/render order, hidden UI versus automatic eligibility, and final core shell/list refresh. Verify focused controller behavior and the actual core caller, with no state-policy exemptions or broad lifecycle rewrite.

Stage 3 larger scenario wave (2026-09-06): user authorized complete autonomous consolidation across core planning, ownership/variant workflow and entire scenario inspector. Implemented and locally verified; task.md records actual evidence and limits. Preserve inherited WIP and existing state mutation owners. Renderer work remains a separate remaining stage 3 responsibility.

Renderer wave (2026-09-06): user authorized moving stage 3 to Renderer after scenario acceptance. Consolidate complete renderer responsibilities and repair demonstrated cache/lifecycle defects. Root exclusively integrates map_renderer.js/catalog; legend owner and tests are an independent lane, read-only analysis identifies a second useful surface. Preserve existing WIP, export behavior and state mutation owners. Validate affected behavior and boundaries; no broad policy regeneration, publication or performance claim without measurements.

Renderer round2 (2026-09-06): repair demonstrated projected-bounds overflow and collection/invalid-path fallbacks; simplify primary index preparation by removing discarded culling payload; reuse whole-water-feature fallback bounds once per synchronous feature build. Preserve geometry order, projection freshness, state/index ownership and existing invalidation. Bounds lane owns geometry module/test; root owns spatial source and integration; independent tests lane owns two new spatial behavior suites. No cross-frame caching, remote effects or FPS claim.

Renderer round3 (2026-09-06): user prioritizes code simplification, responsibility splits and incorrect merge/integration. First repair the inverted viewport signature boundary (root snapshots projection values; viewport owner formats signature) and align P38 architecture check with the already canonical hit-canvas state action. Root alone edits shared facade/checker/catalog. Parallel read-only mapping identifies additional complete responsibilities. Preserve existing WIP, scalar snapshot isolation and exact signatures; no blanket checker exemption, cache or compatibility layer.

Closeout (2026-09-06): review the integrated governance and scenario/renderer work, repair demonstrated integration defects, validate current source and tracked Pages artifacts, commit with personal .codex/config.toml excluded, push a PR to protected main and wait for required checks before merge. Preserve seven worktrees with independent commits. Remove only ancestor-only clean worktrees and merged task branches after remote receipt. Main root remains the working checkout. This is repository integration, not formal P4 B admission.
