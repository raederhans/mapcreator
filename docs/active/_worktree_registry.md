# Worktree Registry

## P4 authority and Nightly topology execution — 2026-08-28

- Integration worktree: `C:\Users\raede\.codex\worktrees\p4-nightly-topology-20260828\mapcreator`, branch `codex/p4-nightly-topology-20260828`, frozen base `origin/main@aa57f7ab8a5c2fd8329c23dadc4b97575992d679`.
- Integration owner: the root agent owns all commits, merges, shared/long verification, remote workflow actions, registry updates, and cleanup. Parallel agents begin read-only and may receive later file-bounded implementation worktrees only after overlap analysis.
- Parent WIP protection: `C:\Users\raede\Desktop\dev\mapcreator` remains on `codex/preserved-main-wip-20260823@9869698d` with its 20 tracked modifications untouched.
- Planned sequence: M1 single checker authority -> M2 parallel P4 authorities -> M3 Nightly decoupling -> M4 five-run same-SHA measurement -> M5 catalog projection closure -> M6 artifact/checkout/dependency/Pages footprint closeout.
- Current status: M1 is integrated as `d56ba108`, M2 as `c0027f26`, M3 as `0da10cd2`, and M4 passes at `69fceb02`. M5 implementation `d57d1647` has clean focused/shared gates and an independent PASS review; its first exact commit/tree catalog-shadow receipt is green, so legacy retirement is only `1/10` and remains ineligible. M6 frozen repair `c993cbbc` passed three exact-SHA/tree full Nightly Pages shadows and reached tracked-dist retirement eligibility, but tracked `dist` remains deliberately retained. The final local `--no-ff` integration merge closes M1-M6. No main push, deployment, history rewrite, branch-protection change, worktree cleanup, legacy retirement, or tracked-dist removal has occurred.
- M4 heavy-work repair lane: `C:\Users\raede\.codex\worktrees\p4-nightly-m4-heavy-repair-20260828\mapcreator`, branch `codex/p4-nightly-m4-heavy-repair-20260828`, based on integration `54094da3`. Commit `cf0ce328` replaces quadratic declaration-record membership with ordered Set membership and prepares source-only mutation-owner proof once per scan while retaining binding-local matching and fail-closed findings. Focused tests pass 69/69, paired 20/100-binding outputs are deep/JSON/hash equal with 20.6%/29.7% improvement, independent review is clear, canonical full passes 430/430 in 426,366 ms, and the canonical checker passes in about 451 seconds with semantic report fields unchanged. Root owns integration, new exact-SHA remote measurement, and documentation.
- M5 catalog projection lane: `C:\Users\raede\.codex\worktrees\p4-nightly-m5-catalog-projection-20260829\mapcreator`, branch `codex/p4-nightly-m5-catalog-projection-20260829`, based on integration `69fceb02`. Implementation `d57d1647` is complete; receipt #1 binds SHA `d57d1647`, tree `14720de6`, and canonical identity `b7d7b19d...`. All legacy projections are retained until at least ten consecutive same-identity green receipts and a separate explicit retirement decision.
- M6 Pages footprint lane: `C:\Users\raede\.codex\worktrees\p4-nightly-m6-pages-footprint-20260829\mapcreator`, branch `codex/p4-nightly-m6-pages-footprint-20260829`, based on integration `cd090299`. Implementation commits `61f3090c`, `e8a2b690`, and `c993cbbc`; the repaired frozen identity `c993cbbc` / `4f3b6c9b` passed serial runs `33212369000`, `33215161411`, and `33216626971`. Chained receipt `b3acee49...` is 3/3 green and retirement-eligible with `legacyTrackedDistRetained=true`. Root owns the enclosing no-fast-forward merge; the lane and remote measurement ref are retained as recovery evidence because cleanup/deletion was not authorized.

## M7-M12 controlled continuation — 2026-08-29

- Integration worktree: `C:\Users\raede\.codex\worktrees\bf46\mapcreator`, branch `codex/sc-m7-m12-integration-20260829`, frozen base `7cb8d49d90c24bd8bc8775f2ebe14af972294dd9`. Root is the sole integration owner.
- Parent WIP protection: `C:\Users\raede\Desktop\dev\mapcreator` remains on `codex/preserved-main-wip-20260823@9869698d` with 20 tracked modifications and is excluded from all integration actions.
- M8 delivery: `C:\Users\raede\.codex\worktrees\8750\mapcreator`, branch `codex/sc-m8-selective-repair`, clean commit `2d12c6f7` based directly on `7cb8d49d`; ready for serial cherry-pick.
- M9 delivery: `C:\Users\raede\.codex\worktrees\dfe6\mapcreator`; active Git-backed analysis-bundle and real shadow-equivalence work, not yet committed.
- M10 delivery: `C:\Users\raede\.codex\worktrees\9562\mapcreator`, branch `codex/sc-m10-artifact-root-contract`, clean commit `86ad3542` based directly on `7cb8d49d`; shared catalog/route files are excluded from the final commit.
- M11 delivery: `C:\Users\raede\.codex\worktrees\ce97\mapcreator`; active dependency/checkout profile work, not yet committed.
- M12 delivery: `C:\Users\raede\.codex\worktrees\2e3c\mapcreator`, branch `codex/sc-m12-migration-ledger` at the frozen base; active/queued ledger ingestion work, not yet committed.
- Integration order: M8 -> M10 -> M9 -> M11 -> M12, subject to a fresh overlap check before every cherry-pick. Final catalog/route/cross-receipt wiring is root-owned and comes only after worker commits are admitted.
- Retention boundary: all worker and historical P4 worktrees, branches, refs, receipts, tracked `dist`, and legacy projections remain retained. No push, PR, workflow dispatch, deployment, cleanup, or retirement is part of this phase.

## Worktree convergence snapshot — 2026-08-27

- Main identity: local `main@efd76074` exactly matched `origin/main@efd76074` before this registry closeout. The clean audit checkout passed `tests.test_global_transport_builder_contracts` 63/63, `test:node:verification-metadata` 41/41, and `git diff --check HEAD^1..HEAD`.
- Parent WIP protection: `C:\Users\raede\Desktop\dev\mapcreator` remains on `codex/preserved-main-wip-20260823@9869698d`, 166 commits behind the pre-closeout main, with its 20 tracked modifications untouched.
- Cleanup: all 22 clean auxiliary worktrees were removed after exact-path, dirty-state, ancestry, patch-equivalence, supersession, and revert checks. Their three associated merged local branches were deleted after proving `origin/main` containment.
- Coverage decisions: `7ecfac08` is patch-equivalent to `b8363e4d`; `28e0328c` and `7606db91` landed as current-catalog adaptations `e1d12a7d` and `0a0ef303`; `af3a57c7` was superseded by `94acc4df` plus `ab30b9e4`; `de7c9815` was intentionally reverted by `7dce59d2` and was not reintroduced.
- Recovery refs: `refs/archive/worktrees/20260827/{sc-v2-p0-p1-covered,sc-v2-nightly-superseded,repository-footprint-covered,pr-cost-covered,export-pipeline-reverted}` preserve the five detached or reverted tips that were not literal ancestors of current main.
- Current topology: after this closeout's temporary clean-main checkout is removed, the preserved desktop checkout is the only registered worktree. Future task worktrees should be bounded to active work and retired after integration or explicit supersession.

## Landing home revamp integration snapshot — 2026-08-25

- Integration candidate: `C:\Users\raede\.codex\worktrees\landing-home-revamp-integration-20260825\mapcreator` on `codex/landing-home-revamp-integration-20260825`, based on `origin/main@f118a101d30373c507075da32267969b22197338`.
- Integration action: published and fast-forwarded to `origin/main@28215c12`; this P3 integration now preserves that accepted landing tree through an ordinary merge. Force-push and remote-history rewriting are not authorized.
- Parent WIP protection: `C:\Users\raede\Desktop\dev\mapcreator` remains on `codex/preserved-main-wip-20260823@9869698d` with its unrelated tracked modifications untouched.
- Accepted evidence: map asset contracts `10/10`, Pages shell contracts `62/62`, Landing view contracts `20/20`, sample-project contracts `18/18`, verification-route checks `6/6`, selector `379` routes, and desktop/tablet/mobile localhost acceptance all pass.
- Scope boundary: the static showcase, canonical map assets and Pages mirror are included. Remote CI completion, production-manual deployment and public-host verification are not part of this closeout.
- Cleanup: the three completed landing implementation tasks are archived; their clean detached worktrees and the integration worktree are removed. Local and remote `codex/landing-home-revamp-integration-20260825` are deleted after ancestry verification; recovery remains available from `origin/main` and the integrated tip `28215c12`.

## SC P3 serial execution snapshot — 2026-08-24

- Supervisor/integration: thread `01a028e9-78d6-7a31-ae01-d81532018212`, `C:\Users\raede\.codex\worktrees\sc-p0-p3-integration-20260824\mapcreator`, branch `codex/sc-p0-p3-integration-20260824@2e41e4ae` before this final task-record commit; sole owner of integration, shared/long checks, remote reconciliation, push, and cleanup decisions.
- Sole production implementer: thread `01a03372-ff2a-7281-af36-65e58e243f9c`, worktree `d871`, branch `codex/sc-p3-5-political-partial-worker-engine@9c2dc32f`; all five P3 slices committed and stopped clean, no merge/push/cleanup.
- Read-only reviewer: thread `01a03372-ff2a-7281-af36-65fed97d06ee`, detached worktree `42a1`; final P3.5 exact-commit review `CLEAR`.
- Independent verifier: thread `01a03372-ff0c-72e3-addf-b25981aa9311`, detached worktree `5152`; final P3.5 candidate and test-only follow-up both `PASS`.
- Active boundary: P3.1 through P3.5 are integrated, reviewed, verified, merged with the accepted landing revamp, and normally pushed. No production slice remains open. Merge `2e41e4ae` matched local, `origin/main`, and `ls-remote`; this documentation-only closeout is the final delta. No live/shared process is currently running.
- Parent protection: `C:\Users\raede\Desktop\dev\mapcreator` remains on `codex/preserved-main-wip-20260823` with its 20 tracked modifications outside this execution scope.
- Baseline identity: the renderer blob at planned `a6833008` and current start `f118a101` is identical (`b5990105850cc7c79e5506c6f89dbee1ab111fae`), preserving the 23,153 split-line budget.
- P3.1 admission: final implementation `fa015b53` received reviewer `CLEAR` and verifier `PASS`; supervisor commits `79746feb` through `dc256b8f` produce the same committed tree. Day/Night ownership, state-action guardrails, raw/current scanner separation, P4.3 direct routes, selector identity, and zero-spawn planning are closed. The Water river argv conflict is inherited from `f118a101` and is non-blocking for this slice.
- P3.2 admission: implementation `dcc83e8d` received reviewer `CLEAR` and verifier `PASS`; supervisor commit `41b3f538` reproduces the same five files. Visual Effects owns paper/graticule/draft-grid/texture-label rendering and all four texture cache families; the exact selector, P4.3 routes, architecture, import graph, and zero-spawn plan pass with no owner conflict.
- P3.3 admission: implementation `d2420643` plus checkout-stability fix `039acb7d` received reviewer `CLEAR` and verifier `PASS`; supervisor commits `5d6543fa` and `1c2f92f8` reproduce the same code, tests, tools, and generated policy. Click transaction ownership, live-state hydration refresh, canonical action authority, source-bound fail-closed proofs, CRLF-stable architecture matching, and P4.3 routing are closed.
- P3.4 admission: implementation `56c40a91` plus contract/route follow-ups `208dc652` and `a9f63bd6` received reviewer `CLEAR` and verifier `PASS`; supervisor commits `c8deacb9`, `5dabccaf`, and `19c0ddf4` reproduce the same committed tree. Political background cache/deferred ownership, source/color provenance, stale-work fences, ocean/intensity effect delegation, source-bound contracts, canonical/legacy route parity, and the 18-command owner selector are closed. Blank-line trailing whitespace is a non-blocking cleanup carried into the single final P3.5 policy refresh.
- P3.5 admission: implementation `5bc0782f` plus recovery/source-contract follow-ups `da539359`, `5c83cc29`, and `9c2dc32f` received final reviewer `CLEAR` and verifier `PASS`; supervisor commits `7cd7d254`, `da93be85`, `9ecb1793`, and `29bdcf40` reproduce the same committed code and tests. Partial/worker ownership, fail-closed recovery, normal/noop commit exception propagation, Scenario dual-source proofs, root facade/DI, worker protocol identity, zero-action policy, and historical P3.3b inventory semantics are closed. Post-integration focused checks pass through metadata 37/37 and portfolio 54/54.
- Final merge: fresh `origin/main@28215c12` was merged without rebase or force as `2e41e4ae`. The combined authority has 340 scripts, 428 records, 387 routes, and 343 commands; unique selector order is landing 379 and Day/Night 380/381. Metadata 38/38, portfolio 54/54, landing assets 10/10, P3 focused suites, selector schema, portfolio consistency, and both-parent diff checks pass before the first exact SHA synchronization.
- Cleanup: the P3 implementer, reviewer, verifier, P3.4/P3.5 preflight tasks and the earlier P0/P1-P2/P3-plan tasks are archived. Their eight clean auxiliary worktrees are removed; local P3.1/P3.4/P3.5 branches are deleted only after `git cherry` or exact-tree coverage checks. This registry commit is the supervisor worktree's terminal action; after its ordinary push and exact remote check, that final worktree and local integration branch are removed. Recovery anchors remain `7898dc7e`, `f5d21542`, `039acb7d`, `a9f63bd6`, `9c2dc32f`, merge `2e41e4ae`, and final synchronized main.

## SC P0-P3 integration snapshot — 2026-08-24

- Primary integration worktree: `C:\Users\raede\.codex\worktrees\sc-p0-p3-integration-20260824\mapcreator` on `codex/sc-p0-p3-integration-20260824`, based on `origin/main@a6833008`.
- Integrated lineage: P0 `12b4a243`, P1/P2 `929b0ae1`, combined catalog-authority seam `b69ff0b5`, followed by this task-record closeout.
- Auxiliary user-visible tasks: P0 `01a032dd-a0c8-7203-be07-9dc2c208e265` / worktree `9cb3`; P1/P2 `01a032dd-a0d6-70d2-a358-cfc79b4bcaa5` / worktree `9e29`; P3 plan `01a032dd-a0c8-7203-be07-9de998edb850` / worktree `4760`.
- Parent WIP protection: `C:\Users\raede\Desktop\dev\mapcreator` stays on `codex/preserved-main-wip-20260823` with its 20 tracked modifications untouched.
- Integration action: finish the bounded combined checks, fetch and require `origin/main` to remain at the recorded base, push normally to `origin/main`, verify exact SHA equality, and observe the workflows triggered by that SHA. No force-push, production-manual deployment, or remote-history rewrite is authorized.
- P3 disposition: planning complete only. Start P3.1 serially after this integration closes; the five-slice plan and acceptance/deletion gates live in `docs/active/test-verification-reform-20260813/plan.md`.
- Superseding cleanup: the three auxiliary tasks listed above are archived and worktrees `9cb3`, `9e29`, and `4760` are removed after their output was admitted or fully consumed by the completed P3 execution. The current P3 completion snapshot above supersedes the historical planning disposition.

The older 2026-08-14 registry snapshot below is retained as historical evidence and is not the current topology authority.

Last updated: 2026-08-14. Local `main@a4f18020` is the single checked-out worktree and contains the integrated P4.3 verification-runner lineage, Windows Job V2 containment, projected-bounds cache atomicity repair, P4 policy test parallelization, and resolved archive cleanup. Refreshed `origin/main@5461c24a` is an ancestor of local main by 65 commits. The final clean-main full P4 admission and `origin/main` push remain in progress under the primary integration owner.

## Integration Owner

- Owner: primary Codex agent on `main`; scope is final short gates, the single full P4 admission, push, registry truth, and merged-branch cleanup.
- Live resources: no task-owned process remains. The next full P4 policy command has one owner and one `.runtime/reports/generated/p4-state-actions/P4.0` output lane.
- Recovery bundle: `C:\Users\raede\.codex\recovery\mapcreator-pre-convergence-20260814.bundle`, verified complete with 11 refs. It preserves the three detached tips, two historical P4 lines, the P4.4/P4.5 future line, and five recovery branches.
- Future P4.4/P4.5 line: `codex/state-action-ownership-p4-20260719@65335370` remains as a local and remote branch while its dedicated checkout has been retired.
- Published candidate: `origin/codex/audit-20260814-verification-runner@a4f18020` contains the exact local-main candidate before the registry closeout commit.
- Fresh performance evidence: the five focused policy tests pass in `358589 ms`; the old sequential scan/proof hotspot pair measured about `685602 ms`, yielding a `47.7%` critical-path reduction while preserving the canonical rebuild and closed-world checker contracts.
- Fresh adjacent evidence: worker/cache fakes 13/13; reviewer CLEAR; metadata 30/30; renderer cache 15/15; Pages 47/47 + 18/18 + 18/18; P4 quick 259/259; route schema 367 with zero unmatched changed files.

## Recommended Order

1. Commit this topology closeout on local main.
2. Run the final short control-plane checks and one clean-main full P4 policy admission.
3. Push local main to `origin/main` and verify exact local/remote SHA equality.
4. Delete merged temporary local and remote branches after ancestry checks.
5. Retain the P4.4/P4.5 future branch and the five named recovery branches until their own replay decisions are complete.

## Current Worktrees

Current truth contains one long-lived checkout. Eleven clean auxiliary worktrees were removed after exact-path validation and recovery-bundle verification.

| Worktree | Branch / HEAD | Base | Status | Dirty / hot files | Evidence | Overlap risk | Integration action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `C:\Users\raede\Desktop\dev\mapcreator` | local `main@a4f18020` plus this registry closeout | `origin/main@5461c24a` ancestor | tracked-clean before registry edit; final integration owner | registry closeout only | worktree list contains exactly one entry; candidate branch is published at `a4f18020` | Shared output lanes remain single-owner during final admission | Commit registry, run final admission, push main, verify two-end equality. |

## P3.1 Visual-Effects Pass Delivery Package 2026-07-14

1. Changes: introduced one state-free visual-effects pass owner for `effects`, `lineEffects`, `textureLabels`, and `dayNight`; retained stable `map_renderer.js` entry wrappers and all bottom-level drawing/state effects in the composition root; updated pass inventory, architecture budgets, verification routes, and Pages mirrors; repaired the Texture E2E route to the current Map Content tab.
2. Files: core `js/core/map_renderer.js`, `js/core/renderer/visual_effects_pass_owner.js`; tests `tests/visual_effects_pass_owner_behavior.test.mjs`, `tests/e2e/texture_overlay_regression.spec.js`, existing inventory/boundary/metadata/core-runner contracts; tooling/package `tools/renderer_pass_family_inventory.mjs`, `tools/check_architecture_boundaries.mjs`, `tools/verification/verification_domains.mjs`, `package.json`; docs this active task and registry; generated `dist/app/**` and `dist/pages-dist-manifest.json`; ignored evidence under `.runtime/tests/p3-1/`.
3. Diff from base: the functional checkpoint `80cbb747` contains 17 tracked files. The closeout adds one three-site E2E selector repair plus control-plane truth. `map_renderer.js` is 23,267 split lines, with the architecture ceiling lowered from 23,269 to 23,267; P3.2 owns the substantial next reduction.
4. Commit state: functional implementation is committed at `80cbb747d39f4c4288428513a54444626e21c2f4`; the browser-contract and closeout truth are staged for the next Lore commit. The branch is intentionally retained for P3.2-P3.3 continuation.
5. Divergence: branch base is integrated `origin/main@6fe219182f66c6c22159f8825de2a0cc40f8d873`; branch is three handoff/cleanup commits plus the P3.1 functional commit ahead. The dirty parent remains on its old local main with 57 preserved WIP entries.
6. Conflict assessment: red for concurrent edits to `map_renderer.js`, pass-family inventory, architecture checks, verification metadata, package scripts, generated dist, or P3 docs; yellow for UI-navigation tests that reference Map Content; green for the dirty parent's unrelated archive/landing/localization WIP.
7. Verification: owner 7/7; inventory 6/6; Python boundary 5/5; metadata 20/20; core-runner 8/8; focused texture/city/HGO/catalog tests pass; architecture, 116-file state-write allowlist, 51-spec import graph, supervisor contracts/plan, selector zero-gap, Pages generation, source/dist parity, and clean-head dist drift pass; full core 72/72 and main-thread 76/76 pass; isolated Texture 3/3 and serial City 8/8 pass; schema-2 `perf:gate` passes.
8. Remaining risk: the 12-worker broad layer-regression command overloaded its shared service and produced invalid timeout/connection evidence. Its directly affected Texture and City domains pass under the project's required single-worker ownership. The broad runner's resource policy belongs to a separate test-tooling task.
9. Recommended integration: continue sequentially into P3.2 in the same isolated branch; retain this checkpoint as the recovery boundary; integrate the complete P3 lineage only after P3.3 and final independent reviews.
10. Integration readiness: P3.1 is `ready-for-continuation`. It is fully verified for its phase and intentionally remains unmerged while P3.2-P3.3 share the same renderer hotspot and active control-plane folder.

## P3.2 Context-Pass Orchestration Delivery Package 2026-07-14

1. Changes: introduced one state-free context-pass orchestration owner for `contextBase`, `contextMarkers`, and `contextScenario`; retained all physical, urban, river, transport, strategic, city, region, relief, cache, metric-session, and runtime-state effects in `map_renderer.js` or existing owners; converted three stable entry functions to thin wrappers; atomically retargeted existing source-scan contracts and SF-ATS routes.
2. Files: core `js/core/map_renderer.js`, `js/core/renderer/context_pass_orchestrator_owner.js`; tests `tests/context_pass_orchestrator_owner_behavior.test.mjs`, existing pass inventory/physical/river/scenario/strategic/Python/metadata/core-runner contracts; tooling/package `tools/renderer_pass_family_inventory.mjs`, `tools/check_architecture_boundaries.mjs`, `tools/test_route_registry.mjs`, `tools/verification/verification_domains.mjs`, `package.json`; docs this active task and registry; generated owner/map-renderer mirrors and `dist/pages-dist-manifest.json`; ignored evidence under `.runtime/logs/`, `.runtime/output/perf/`, `.runtime/tests/playwright/`, and `.runtime/browser/`.
3. Diff from base: functional commit `f79ecbf20c4589f3c6b66e3efd2409443073453d` changes 22 tracked files. `map_renderer.js` falls from 23,267 to 23,180 split lines (`-87`), and the architecture ceiling follows the exact post-change count; the owner ceiling is 280.
4. Commit state: functional implementation is committed at `f79ecbf2`; this registry/context/task update is the separate evidence closeout. The branch remains isolated for P3.3a/P3.3b and final P3 integration.
5. Divergence: branch base is integrated `origin/main@6fe219182f66c6c22159f8825de2a0cc40f8d873`; the branch contains the audited P3.0 handoff, P3.1, and P3.2 lineage. The dirty parent remains on its old local main with all 57 WIP entries unchanged.
6. Conflict assessment: red for concurrent edits to `map_renderer.js`, render-pass inventory, architecture checker, verification metadata/routes, package scripts, generated dist, or P3 control docs; green for the dirty parent's unrelated archive/landing/localization WIP.
7. Verification: owner `10/10`; inventory `6/6`; physical/river/scenario/strategic source contracts; combined Python boundary `6/6`; architecture/state-write/import-graph/supervisor/selector gates; adaptive 22-file zero-gap; Pages `927.19 MiB`; source/dist blob parity; clean-head dist drift; core `73/73`; main-thread `77/77`; physical `1/1`; resilience `3/3`; Water `12/12`; City `8/8`; TNO `2/2`; scenario chunk `8/8`; and the original-policy standard performance rerun all pass.
8. Remaining risk: one main-thread attempt encountered simultaneous Chromium `ERR_NETWORK_CHANGED` errors across unrelated module requests; the final full rerun passed. One long-matrix chunk case waited on deferred infrastructure after its political pixel contract was already green; isolated and complete suite reruns passed. The first perf run showed whole-machine timing drift; an independent five-run/three-warmup same-SHA rerun passed with zero contract and render-role mismatches. These artifacts are retained as environment evidence without product retries, timeout changes, or threshold changes.
9. Recommended integration: commit and push the P3.2 evidence closeout, then execute P3.3a as a production-zero political-pass preflight before any political owner extraction.
10. Integration readiness: P3.2 is `ready-for-continuation`. Its functionality and acceptance evidence are complete; whole-P3 integration remains deferred until P3.3b and final independent reviews.

## P3.3a Political-Pass Preflight Delivery Package 2026-07-14

1. Changes: added one production-zero political-pass preflight contract and document; froze the full-pass, worker, progressive-recovery, partial-repaint, fine-loop, result, facade, runtime-context, and state-write boundaries; registered the contract in the default core renderer lane and added the dedicated political progressive-recovery browser lane to inventory truth.
2. Files: tests `tests/renderer_political_pass_orchestration_preflight.test.mjs` plus existing inventory/metadata/core-runner contracts; tooling/package `tools/renderer_pass_family_inventory.mjs`, `tools/verification/verification_domains.mjs`, `package.json`; docs the P3.3a preflight, current coupling matrix, P3 context/task, and this registry. Core runtime, generated dist, architecture checker, route registry, supervisor registry, public facade, RendererRuntimeContext, and state-write allowlist remain byte-identical to `0270eeb8`.
3. Diff from base: production-path diff is empty. The preflight establishes seven static cases and extends the existing inventory/metadata contract without introducing a product import or a second inventory source.
4. Commit state: P3.3a is verified working-tree content on top of pushed P3.2 closeout `0270eeb8`; it will receive one separate Lore commit before P3.3b production work starts.
5. Divergence: branch base remains integrated `origin/main@6fe219182f66c6c22159f8825de2a0cc40f8d873`; the dirty parent remains on its old local main with all 57 WIP entries unchanged.
6. Conflict assessment: red for concurrent political renderer, pass inventory, verification metadata, package, or P3 control-doc edits; green for parent archive/landing/localization WIP.
7. Verification: preflight `7/7`; inventory `6/6`; scenario chunk `57/57`; worker packet `5/5`; political collection `5/5`; cache-host/commit-accounting/pipeline suites; Python pipeline boundary `6/6`; metadata `21/21`; core runner `8/8`; architecture, 116-file state-write allowlist, 51-spec import graph, supervisor contracts/plan, selector schema; clean core `74/74`; Pages `927.19 MiB`; embedded dist drift; diff checks. Adaptive final evidence covers `12` changed files with `205` recommendations (`197` child-safe, `8` main-thread), `unmatchedChangedFiles: []`, and `routeGaps: []`.
8. Remaining risk: P3.3b still needs atomic source-scan retargeting and behavior tests around worker-disabled requests, accepted callback exceptions, four result schemas, coarse/fine error propagation, and opaque visible-items forwarding. Production runtime behavior remains unchanged in P3.3a.
9. Recommended integration: commit and push P3.3a, then implement P3.3b in the same isolated branch under TDD and the full political/browser/performance acceptance matrix.
10. Integration readiness: P3.3a is `ready-for-continuation`; whole-P3 integration remains deferred until P3.3b, independent reviews, final clean-head acceptance, archive, and main integration.

## P3.3b Political-Pass Orchestrator Delivery Package 2026-07-14 / 2026-07-15 closeout

1. Changes: introduced one import-free, state-free political-pass orchestrator; converted `drawPoliticalPass(k)` to a stable thin wrapper; retained identity, viewport, diagnostics, worker, packet, callback, progressive recovery, partial repaint, fine drawing, state writes, metrics, and scheduling in the composition root; atomically upgraded P3.3a and older source-scan contracts.
2. Files: core `js/core/map_renderer.js`, `js/core/renderer/political_pass_orchestrator_owner.js`; tests the owner behavior, P3.3a preflight, pass inventory, scenario chunk, two Python boundaries, metadata, and core runner; tooling/package the inventory, architecture checker, route registry, verification metadata, and `package.json`; docs the P3.3b contract, coupling matrix, P3 context/task, and this registry; generated `dist/app/**` mirrors and `dist/pages-dist-manifest.json`; follow-up test-contract files `tests/e2e/dev/scenario_chunk_exact_after_settle_regression.dev.spec.js` and `tests/e2e/support/playwright-app.js`.
3. Diff from P3.3a: `map_renderer.js` counts 23,156 split lines, down 24 from the 23,180 P3.2/P3.3a ceiling; the architecture ceiling is 23,156 and the political owner ceiling is 280. No formatting compression is used.
4. Commit state: functional Lore commit `a18ee17fe2aba894f3e9455513d166a2a9d7d032` is pushed to the P3 runtime branch. The current closeout WIP records the deferred-infra E2E wait-contract repair and final P3 evidence before rebase/integration.
5. Divergence: branch base remains integrated `origin/main@6fe219182f66c6c22159f8825de2a0cc40f8d873`; remote `main` adds performance-boundary tooling commit `d5695acc13b38c1ae4e77f887a2add69985cbf95`; the dirty parent remains on old local `main@db8bd6c1` with all 57 WIP entries unchanged.
6. Conflict assessment: red for concurrent political renderer, pass inventory, architecture, verification metadata/routes, package, generated dist, or P3 control-doc edits; green for the parent archive/landing/localization WIP.
7. Verification: owner `10/10`; preflight `7/7`; political Python boundary `5/5`; combined pipeline boundary `6/6`; inventory `6/6`; scenario chunk contracts; worker packet and political collection `5/5` each; P2/P3.1/P3.2 owner regressions; metadata `21/21`; core runner `8/8`; architecture; state-write allowlist; import graph; supervisor contracts/plan; route schema; diff checks; adaptive zero unmatched / zero route gaps; Pages `927.20 MiB`; clean dist drift; `verify:core`; `verify:core:main-thread`; dedicated browser matrix; standard `perf:gate`.
8. Follow-up E2E wait-contract repair: current `origin/main` and the P3 branch both reproduced the same complete `test:e2e:dev:scenario-chunk-runtime` deferred-infra wait shape with `pendingInfraPromotion=true` after the political pixel/color contract was already satisfied. `waitForFullPoliticalColorCoverage()` and `waitForChunkIdle()` now keep deferred infra in diagnostics while gating on visual promotion, refresh, and render readiness. Post-repair evidence passes scenario chunk `8/8`, political progressive recovery `3/3`, scenario resilience `3/3`, physical `1/1`, Water `12/12`, City `8/8`, TNO `2/2`, and `perf:gate`.
9. Remaining risk: `verify:core` and `verify:core:main-thread` need one final rerun after the closeout helper/docs commit and the rebase onto current `origin/main`. Williams remains in its dedicated research lane.
10. Integration readiness: `acceptance-complete / final-integration-pending`; commit the wait-contract/docs closeout, rebase over current main, rerun final gates, archive the active P3 folder, fast-forward main, push, and clean the verified isolated worktree.

## P3.0 Render-Pass Family Delivery Package 2026-07-13 (historical; superseded by the 2026-07-14 audit addendum)

1. Changes: froze the 13-pass family/phase/owner/coupling inventory; added one exact central contract and child-safe metadata route; repaired the governed-report unit test with a hermetic 40-run fixture; converted the donor-island hit probe to the existing observational Ctrl gesture with a deterministic owner-stability assertion; completed worker, scenario, bathymetry, coastal-accent, urban, strategic-marker, city-label, color-resolution, and `contextMarkers` runtime-state coupling coverage while preserving production-zero.
2. Files: core tooling `tools/renderer_pass_family_inventory.mjs`, `tools/verification/verification_domains.mjs`, `package.json`; tests `tests/renderer_pass_family_inventory_behavior.test.mjs`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`, `tests/perf_role_governed_report_behavior.test.mjs`, `tests/e2e/tno_open_ocean_rendering.spec.js`; docs `docs/archive/renderer-pass-family-p3-20260713/{plan,context,task,coupling-matrix-p3-0}.md`, this registry, and one appended P3 lesson in `lessons learned.md`; disposable logs under ignored `.runtime/reports/generated/renderer-pass-family-p3-0/` and `.runtime/tests/playwright/`.
3. Diff from base: 14 tracked files form the delivery set. Product runtime changed-file count is zero; the change set is limited to tooling, tests, package metadata, and control-plane/lessons docs.
4. Commit state: runtime candidate `89c7278a3d4e730d3ed1c19945375ec539c42082` contains five Lore-protocol commits for the inventory, hermetic baseline repair, owner-coupling/test corrections, and final `contextMarkers` runtime-state correction. The docs-only closeout is the sixth Lore record and published branch HEAD; runtime validation remains bound to `89c7278a`.
5. Divergence: the worktree base and initial HEAD equal `origin/main@63dd0bb5e23afd340afe2dc0dcc74095cc4cb2cd`; the published branch is six commits ahead and zero behind that base. The dirty parent `main@db8bd6c118d158aaed4dd6734ecdd981fe80f326` has a separate dirty WIP state and remains untouched.
6. Conflict analysis: the stale dirty parent directly overlaps at `lessons learned.md`, producing a red future semantic-reconciliation item; yellow semantic overlap also applies to future work editing `package.json`, verification metadata, verify-core runner tests, or renderer-runtime selector routes. All other current parent WIP paths are disjoint from this P3 delivery. Production `js/`, `dist/`, `css/`, `data/`, `landing/`, `public/`, `scenarios/`, `index.html`, facade, state-write allowlist, and P2 owner paths remain green.
7. Verification: `node --check` passes for the inventory and central test; named inventory contract passes 6/6; verification metadata passes 18/18 with projection 82/41/70/4/5; verify-core runner passes 8/8; architecture boundaries pass; state-write allowlist passes with 116 tracked files; test import graph passes with 51 specs; supervisor schemas/contracts/routing and plan pass; selector route schema passes with 308 routes; verify-core list reports 70 commands; `git diff --check` passes. The repaired render-sample-role package passes 36/36. Exact run `final3-89c7278a` executes all ten commands under candidate `89c7278a`: Pages/dist, dist drift, core 70, main-thread 74, physical 1/1, resilience 3/3, city 8/8, and TNO 2/2 pass; Water finishes 11/12 after one setup-level `pendingInfraPromotion` stall; fresh single and ordered pair Water reruns pass 1/1 and 2/2; `perf:gate` exits 1 at TNO startup `8493.1 ms`, HOI4 startup `8193.0 ms`, and HOI4 render median `746.5 ms`. The unified run and focused reruns end clean with production diff `[]`, empty owned ports/processes, and released locks.
8. Remaining risk: the refreshed final selector dossier records the published docs HEAD, 14 changed files, `unmatchedChangedFiles: []`, and `routeGaps: []`; evidence lives under `.runtime/reports/generated/renderer-pass-family-p3-0/blocked-final/`. Williams crossover remains outside P3.0. Water has one timing-sensitive chunk-infrastructure stall in the complete run with successful focused recovery evidence. The checked-in April oracle uses schema 1, old workload sizes, one render sample, and incomplete exact identity while current reports use schema 2 and canonical-role evidence. This comparability gap leaves current-main regression, workload/harness drift, and machine effects unresolved. The governed analyzer's default core unit test is hermetic; explicit offline reanalysis still depends on archived P2 evidence.
9. Recommended integration: retain the worktree and publish the branch for recovery and review. A separate performance-governance lane should run a clean exact `origin/main@63dd0bb5` control, resolve attribution, then fix any verified regression or record an explicit approval for a dated schema-2 oracle with five measured runs, exact scenario/browser identity, and canonical render-role policy. P3.0 then reruns the full Water lane and complete ten-command sequence before any P3.1 extraction.
10. Integration readiness: `blocked`. Runtime recovery point is `89c7278a3d4e730d3ed1c19945375ec539c42082`; the docs-only closeout is the published branch HEAD; exact base is `63dd0bb5e23afd340afe2dc0dcc74095cc4cb2cd`; runtime evidence is preserved under `final3-89c7278a/`. P3.1 admission requires resolved performance attribution plus a passing complete Water/performance baseline.

## P3.0 audit delivery addendum 2026-07-14

1. Changes: removed the mirrored inventory oracle; separated pass entry hosts from delegated owners; added static dependency reachability; routed every production source read by the contract; replaced repository-global metadata counts with local route assertions; bounded the TNO action-clock wait.
2. Files: core tooling `tools/renderer_pass_family_inventory.mjs` and `tools/verification/verification_domains.mjs`; tests `tests/renderer_pass_family_inventory_behavior.test.mjs`, `tests/verification_metadata_behavior.test.mjs`, `tests/test_e2e_structural_tooling.py`, and `tests/e2e/tno_open_ocean_rendering.spec.js`; control files the P3 matrix/context/task, this registry, and `lessons learned.md`. Runtime evidence stays under ignored `.runtime/**`.
3. Diff from base: the P3 branch remains production-zero. The audit repair itself changes ten tooling/test/control files with no `js/**`, `dist/**`, perf runner/oracle, dependency lock, UI/CSS, data, or scenario asset changes.
4. Commit state: functional audit repair is committed at `d8991c8b58bbb6ce1984959727663ce9c9ff1f40`; this addendum is the docs-only integration handoff.
5. Divergence: before integration the branch is zero behind and seven functional/history commits ahead of `origin/main@63dd0bb5e23afd340afe2dc0dcc74095cc4cb2cd`. The dirty parent remains at `main@db8bd6c118d158aaed4dd6734ecdd981fe80f326` with 57 preserved WIP entries.
6. Conflict analysis: direct file overlap with the dirty parent exists at `lessons learned.md`; semantic overlap exists in verification metadata and package/test ownership. The isolated branch is based exactly on current remote main and is the integration source.
7. Verification: TDD red evidence reproduced all three failures. Green evidence: inventory 6/6, metadata 18/18, E2E structural 35/35, `node --check`, `git diff --check`, clean-head `verify:core` 70/70, and exact changed TNO Water spec 2/2 in 3.6 minutes with one worker. SF-ATS covers 15 branch files with zero unmatched files and zero route gaps.
8. Remaining risk: the April schema-1 performance oracle is red on unchanged current-main bytes. The clean control records TNO startup `6850.3 ms > 6676.1 ms` and HOI4 startup `5993.0 ms > 5986.6 ms`, with zero contract and render-role mismatches. P3.1 admission requires an explicit schema-2 oracle governance decision.
9. Recommended integration: fast-forward the isolated branch to remote main, retain commit hashes as recovery evidence, remove the integrated worktree, and refresh the current-worktree table from a clean integration worktree.
10. Integration readiness: `ready-for-integration`. P3.0 audit and verification are complete; P3.1 performance admission remains a separate blocked decision.

## Recent P2 audit delivery package 2026-07-13

1. Changes: repaired transformed-pass diagnostics on both compositor paths; extracted urban strategic-value policy from the renderer; made Williams power-scheme ownership, GUID mismatch handling, replay, telemetry, and policy adjudication auditable; tightened render-sample validation and verification routing.
2. Files: core files are `js/core/map_renderer.js`, `js/core/renderer/cached_pass_compositor_owner.js`, `js/core/renderer/urban_city_policy.js`, and `tools/perf/**`; tests are the related Node, Python boundary, and Scenario E2E contracts; control files are `package.json`, verification registries, `tools/eslint-rules/state-writer-allowlist.json`, this registry, and checked-in `dist/**` mirrors; disposable evidence stays under `.runtime/**`.
3. Diff from base: functional checkpoint `76af9f68` changes 32 tracked files with 2,427 insertions and 223 deletions; the full integrated chain through the control-plane closeout changes the same 32 files with 2,429 insertions and 225 deletions. Production work is concentrated in compositor diagnostics, urban policy ownership, and Williams measurement governance.
4. Commit state: functional Lore checkpoint `76af9f681f781a90f411995c40b7c64582650f64` is pushed to `origin/main`.
5. Divergence: the functional checkpoint was one commit ahead and zero behind refreshed `origin/main@e195a4531a08870f45c2a3dac00b6ab52ad620fc`; the push fast-forwarded remote `main` to `76af9f681f781a90f411995c40b7c64582650f64`.
6. Overlap: the dirty parent directly overlaps `docs/active/_worktree_registry.md`, `dist/pages-dist-manifest.json`, and other generated Pages surfaces; integration therefore proceeds from this isolated worktree directly to remote main.
7. Verification: clean committed HEAD passes `verify:core` 69/69 with embedded `verify:pages-dist` and `verify:dist-drift`; report SHA256 is `c578e1b70a4b6efb398665b63021f30575ecd3cb965c577105ced424d1e532d4`, combined log SHA256 is `82ac9a12e64c5a9a094273cc223400fff211bd3f31e594a0ceb45fd75e1d8dc4`; selector reports 32 changed files and zero unmatched files; state-write allowlist passes with 116 tracked writers; TNO coverage chain passes including 57/57 chunk contracts; Scenario runtime E2E passes 8/8 and the fresh transformed-frame case passes 1/1; perf gate, Williams governance/job runner/live preflight/live telemetry, renderer boundaries, and urban data contracts pass; four independent static reviews return APPROVE, including the final foreign-GUID deletion security repair.
8. Remaining verification: the 45-minute Williams crossover measurement and nine main-thread/browser lanes remain assigned to their dedicated owners; deterministic integration evidence is complete.
9. Recommendation: retain branch `codex/audit-20260713-recent` and the recorded commit hashes as recovery pointers; reconcile the dirty parent WIP in its own task.
10. Integration readiness: functional repair, registry truth, remote push, and isolated-worktree cleanup are complete.

## P2 final integration decision 2026-07-13

1. The project owner accepted the refreshed current verification state for P2 and deferred specialized performance governance.
2. `origin/main@17aeedf5` was proven to be an ancestor of the P2 recovery branch, enabling a fast-forward integration with no merge conflict.
3. P2 delivers the draw-canvas orchestration owner, cached-pass compositor owner, and transformed-frame compositor owner while preserving public facade, render order, state-write allowlist, and concrete pass drawing ownership.
4. `map_renderer.js` decreased from 23,473 to 23,269 lines, a 204-line reduction.
5. P2 archive location: `docs/archive/renderer-frame-orchestration-p2-20260710/`.
6. Final cleanup removes the isolated P2 worktree and retains the remote recovery branch.

## Williams Rerun08 Harness-Recovery Frozen Contract 2026-07-13

Historical record: the Williams sections below preserve the gate state at each experiment checkpoint. The `P2 final integration decision 2026-07-13` section supersedes their phase-local integration and archival actions.

1. Governance: `rerun08-harness-recovery-governance.md`; protocol `p2-williams-rerun08-harness-recovery-v1`; one dry plan, one execute, zero retry; every result terminal.
2. Frozen identities: candidate `9479e9e6ff8d4b2fa8ba969fdc7b7e2f341d2d40`; control `bd98c5800ac5cca2a93d7f55ac1b0a254ca5028f`; policy `p2-williams-crossover-v4`; role policy `render-sample-role-v2`.
3. Correction boundary: a generated destination GUID owns duplicate/query/activate/get-active/restore/delete/query-absent identity. `/list` is diagnostic-only.
4. Reproduced capability: S0 Low Power Idle host; the tracked helper completed all 11 lifecycle events, including post-delete expected-failure query and original-scheme query; final active scheme returned to Balanced; report `.runtime/reports/generated/p2-2b-williams-rerun08-power-preflight-v3.json`, SHA256 `85b4c6d374fd9c0018529d7851bd75f3619e508e2244852e52838383cfb5082f`.
5. Live ownership: root integration agent owned all rerun08 power, quiet, browser, telemetry, Job Object, and Williams processes. The lane is complete and current live owner is none.
6. Integration gate: only `accepted / exit 0` could open main integration and archival. The terminal `invalid-experiment / exit 3` result keeps both gates closed.

## Williams Rerun08 Terminal Invalid-Experiment Package 2026-07-13

1. Execution identity: protocol `p2-williams-rerun08-harness-recovery-v1`; candidate `9479e9e6ff8d4b2fa8ba969fdc7b7e2f341d2d40`; control `bd98c5800ac5cca2a93d7f55ac1b0a254ca5028f`; one dry plan; one execute; zero retry.
2. Terminal decision: analyzer `invalid-experiment / exit 3`, `admitted=false`, and `regressions=[]`. Invalidity comes from preregistered CPU/telemetry admission, same-side drift, and one direction-veto check. Paired estimates remain diagnostic-only.
3. Evidence completeness: 8/8 blocks completed; 32/32 measured raw JSON files; 115/115 manifest entries. Independent byte-count and SHA256 validation produced zero manifest errors. Runner stderr is zero bytes for every block.
4. Immutable hashes: analysis JSON `3db96ff50d7a7d435e85d09e04d271e879f03bc1136b82ff44caa708088e6272`; analysis Markdown `8eaf26475d91ca5ff64ab444ce56e5cf25cca93c64b003aa59a6516092f42b56`; raw manifest `83ad80a62d0c94cd537b301e5f3e6b0aeea26aa681d7c558e0e74f0b9ae589fd`; execute log `493b36091160379e45c93fbe80ac8433159d1f272b6c0fed2d9db6b23a183947`; operator script `8aa39325fbee8d6dc57bda99c0bc25914f43a45883ffcfc29f630fd7c96d9a1e`.
5. Power lifecycle: original Balanced GUID `381b4222-f694-41f0-9685-ff5bb260df2e`; temporary GUID `43e48295-98a5-4a6d-a46c-d38408fa7cbd`; 11/11 lifecycle events; original restored; temporary absent by expected-failure query; cleanup valid with zero errors.
6. Cleanup topology: both exact detached rerun08 worktrees were clean at their frozen commits; both junction targets matched the P2 dependency tree; junctions were removed non-recursively; worktrees were removed and pruned; ports 8000/8892 are clear; task process count is zero; parent WIP remains unchanged.
7. Terminal action: the frozen rerun08 mapping ends P2 as implementation-complete/performance-unresolved. Main integration, completion, and archival remain closed. Retain the isolated recovery lane and active docs.

## Williams Rerun07 Terminal Harness-Fault Package 2026-07-13

1. Exact identities: candidate `9479e9e6ff8d4b2fa8ba969fdc7b7e2f341d2d40`, control `bd98c5800ac5cca2a93d7f55ac1b0a254ca5028f`, governance `233ff550157de0a5e0e3b09ea6ff00c6bd0fa8c0`, policy `p2-williams-crossover-v4`, and role policy `render-sample-role-v2`.
2. Execution boundary: one dry plan completed; execute count `0`; retry count `0`; quiet interval absent; performance sample count `0`; execute log, operator preregistration, raw root, analysis JSON, and analysis Markdown absent.
3. Fault: `powercfg /duplicatescheme SCHEME_MIN` returned GUID `5b998c51-6f90-428d-870c-cb8d8d279b05` with exit `0`; the immediate scheme list still contained only Balanced, so the exact-one-new-scheme invariant raised `expected exactly one temporary power scheme; found 0`.
4. Cleanup: original Balanced GUID `381b4222-f694-41f0-9685-ff5bb260df2e` is active; the returned temporary GUID is absent; `cleanup.valid=true`; cleanup errors are empty.
5. Immutable evidence: root `.runtime/output/perf/p2-2b-williams-postchange-20260712-rerun07/`; plan log SHA256 `43d9151f25717b609686621535aebcdf28a26872b107990f99856aec545dacd0`; operator finalization SHA256 `55f9810ac2a5456b9a87aadc9df343a472852237e7430542c2731098c0ae8d6f`.
6. Terminal decision: `exit 1 / harness fault`. The final authorization is consumed. P2 implementation evidence stays valid, Williams performance acceptance stays unresolved, and main integration, P2 completion, and archival remain closed.
7. Cleanup topology: both exact detached rerun07 measurement worktrees and their verified `node_modules` junctions are removed and pruned. The tracked-clean control-overlay branch was pushed to `origin/codex/williams-rerun06-control-role-v2-20260712@bd98c580`, then its support worktree was removed and pruned. The isolated P2 worktree remains because its recovery branch is still outside main. The parent checkout remains unchanged with 57 unrelated WIP entries.

## P2 Final Recovery-Branch Verification Package 2026-07-13

1. Terminal-truth checkpoint: `72ccbc3a71cbc4bbea386af867eab67cd8f1bfb0`; tracked-clean before and after verification.
2. Clean-head sequence: `verify:pages-dist -> verify:dist-drift -> verify:core`, exit `0`; 67/67 core results have exit code `0`; seven main-thread lanes remain outside the deterministic core plan.
3. Evidence hashes: core JSON `70947a928c08fa6f0735b321ef2cfcd5f7cf23cfe25bc973b161fdcb5b661f2b`; combined stdout `bb853b56e460c574a1f28fff713f921f33852fae9a16153b73fea6d9d73310dd`.
4. Browser authority: main-thread 71/71, scenario 3/3, physical 1/1, TNO 2/2, water 12/12. The first city result remains preserved in the matrix summary; the later authoritative `p2-2b-city-rendering-final.stdout.log` passed 8/8.
5. Performance evidence: `p2-2b-perf-gate-final.log` reports PASS and hashes to `98796574bd75ed3647cd4d42d7da654556031769bd4e4f626917d4cb1908507e`; that invocation has no separate exit-code artifact.
6. Reviews: code `APPROVE` with zero blocker; first-principles architecture implementation `CLEAR`; evidence/UltraQA implementation `APPROVE`, formal integration `BLOCK`.
7. Architecture: current origin baseline `23,473`, branch `23,269`, reduction `204`; enforced ceiling `23,269`. Runtime context, public facade, and state-write allowlist retain their existing P2 boundaries.
8. Ancestry: after fresh fetch, `origin/main@17aeedf5` is an ancestor of the P2 recovery branch; checkpoint `72ccbc3a` was `0 behind / 47 ahead`.
9. Integration action: keep origin/main unchanged; recovery branch push is verified through `d508e00698cec84b707b589ec0163a07014ebf61`; retain the P2 worktree and active docs; require a new explicit governance decision for any future Williams protocol or integration condition.

## Williams Rerun06 Terminal Package and Rerun07 Final Governance 2026-07-12

1. Exact identities: candidate `9479e9e6ff8d4b2fa8ba969fdc7b7e2f341d2d40`, control `bd98c5800ac5cca2a93d7f55ac1b0a254ca5028f`, policy `p2-williams-crossover-v4`, role policy `render-sample-role-v2`.
2. Execution: one dry plan, one execute, zero retry; 8/8 blocks, 32/32 measured raw files, 115/115 manifest entries, valid Job cleanup, and clear ports.
3. Formal result: `invalid-experiment / exit 3`, `admitted=false`, `regressions=[]`; three frequency checks, one TNO startup internal outlier, and one same-side startup drift check failed.
4. Immutable evidence root: `.runtime/output/perf/p2-2b-williams-postchange-20260712-rerun06/`; canonical hashes are recorded in `rerun07-final-repeat-governance.md`.
5. Cleanup: both rerun06 detached measurement worktrees and dependency junctions are removed and pruned. The parent remains at 57 unrelated WIP entries.
6. Governance: evidence, calculation, and governance reviews validate the terminal classification and authorize one final capped rerun07 under a temporary standard High performance plan, fixed 120-second quiet interval, fresh detached worktrees, one plan, one execute, zero retry, and terminal exit mapping.

## Williams Rerun05 Terminal Package and Role-v2 Repair 2026-07-12

1. Exact identities: candidate `3bbf46ec0fd5dec62d719b1aae31c8b0ef5792b7`, control `ab86b1e24d161edbe6bcc80acb0b316e4bf81942`, matching package-lock blob `df70020f2f930d5692a1ff9febebf86dbb0e0db1`, baseline-runner blob `70453d9b`, and role-policy-v1 blob `c66fe181`.
2. Execution: one dry plan, one execute, zero retry. Block-01/control produced three valid TNO lifecycle samples; v1 rejected the sample count/array/sequence and stopped before candidate execution.
3. Formal result: `invalid-experiment / exit 3`; candidate measurements `0`; no performance conclusion.
4. Immutable evidence root: `.runtime/output/perf/p2-2b-williams-postchange-20260712-rerun05/`. SHA256: raw manifest `3f5e1902c8910f19bce07b0a274c60ae60245ce3fe58781525221cb9146939ed`; analysis JSON `0ec23cfeac7f9f2dda0cced3cb4292cdbace1b600f6a6fbc1a31f8001c787c89`; analysis Markdown `908193d2c9fc4e85b6a3c9bf58b4d2575a7d12793285815ca1bed9ec4846e1c4`; dry-plan log `fe6cb0d1c846f54b40874f1fdb561e169d564af7d557bfcdac8185a936b6d20f`; execute log `67d0ffc7ab373aa270e12e8189a70cbcf8a94de8ac14d41a89c807a6e9b7d102`.
5. Governed correction: `render-sample-role-v2` accepts two-or-more contiguous lifecycle samples only when declared and actual counts match, the canonical sample is unique and last, and every earlier sample predates chunk promotion. Williams identity rotates to `p2-williams-crossover-v4`.
6. TDD/review: render-role `23/23`, Williams `34/34`, Python perf contract `23/23`; independent code and governance reviews `CLEAR`.
7. Rerun06 gate: commit candidate v2/v4, create a one-file control overlay from `ab86b1e2`, verify dual-side blobs, clean rerun05 worktrees, create fresh exact detached worktrees and paths, run one plan and one execute, require full `accepted / exit 0`.

## Williams Rerun04 Terminal Package 2026-07-12

1. Exact identities: candidate `26b1115cc3fe1e66200661e195a4c5abbf4d4c05`, control `ab86b1e24d161edbe6bcc80acb0b316e4bf81942`, matching package-lock Git blob `df70020f2f930d5692a1ff9febebf86dbb0e0db1`.
2. Worktree health: both sides are detached and tracked-clean. The control worktree briefly appeared as `locked initializing` while Git was still completing its checkout; a later process/metadata/status inspection confirmed initialization completed normally, the lock disappeared, and both indexes are healthy. No repair or checkout mutation was required.
3. Dependency identity: both ignored `node_modules` junctions target `C:\Users\raede\.codex\worktrees\mapcreator-renderer-frame-orchestration-p2-20260710\node_modules`.
4. Immutable evidence root: `.runtime/output/perf/p2-2a-williams-live-20260712-rerun04/`; it contains one dry plan, one execute, canonical evidence index, final cleanup index, raw manifest, preregistration, and terminal analysis.
5. Live ownership: root integration agent issued the execute command exactly once. Current live owner is `none`; all task processes are gone and ports 8000/8892 are clear.
6. Terminal admission: analyzer returned `invalid-experiment / exit 3`, `admitted=false`, eight preregistered telemetry invalid reasons, and zero regressions. The result carries no performance verdict and received zero retry.
7. Sole dry plan: exit `0`; policy `p2-williams-crossover-v3`; telemetry schema `3`; one `excluded-warmup` priming capture per window; eight frozen blocks in `A/TH, B/TH, B/HT, A/HT, B/TH, A/TH, A/HT, B/HT` order; 32 measured raw files. Dry-plan JSON SHA256 is `e486b198be5011a266c0b67d45f449724eb3416b31b528042fcc764523e528c3`; pre-execute snapshot SHA256 is `682b909663fc241407f8d252bac69ef5a15b3ab8e0d3752fe15f4e60ba9d8f31`.
8. Execute record: invocation count `1`, automatic retry count `0`, eight completed blocks, 32 measured raw files, 115 validated manifest entries, and eight valid Job cleanup records. Canonical SHA256: execute log `6e9575ef94d1685355a4c624e36d7e728d2371dcc4314d074613a1007d46f29a`; analysis JSON `aa24701dbb7efcc1a184e52eb85a5f222f8124b4d4516e2ae7b1a51d7273bada`; analysis Markdown `d6bcf67309fb19cda24143089c4051cb0b4834d91bdc15b6d66984c55de2752c`; raw manifest `484e6866b86217ebf61127b19dd92efee767a27fe4ae1e2abdb59ce545c765f4`; canonical index `35cda7dfd189b9a6dd7595bbbb9a6df6d67ecaf0e5b8afb4aada0d728cd33251`; cleanup index `8f5d8e4188572cbc3d65ea68330b36104cd21547517b237271700e3f07d2525d`.
9. Invalidating environment values: pre CPU `26.2%`, `31.2%`, `27.6%`, `30.4%` against `25%`; pair frequency differences `9.5808%` and `8.2902%` against `5%`; global frequency drift `15.5689%` against `10%`; one capture duration `1334.4013ms` against `1250ms`.
10. Cleanup: both exact detached worktrees and verified dependency junctions are removed; worktree metadata is pruned; parent checkout remains unchanged with 57 unrelated WIP entries.
11. Governance: evidence and statistics reviews return `BLOCK` for performance admission; architecture review returns `APPROVE` for reversible isolated P2.2b implementation only. Main integration and P2 closeout still require fresh `accepted / exit 0` performance evidence.

## Williams Rerun03 Terminal and Telemetry-v3 Repair Package 2026-07-12

1. Rerun03 identity: candidate `2e05d5a8e7beae2c2200f6d4f3ffdc19e8df16a9`, control `ab86b1e24d161edbe6bcc80acb0b316e4bf81942`, matching package-lock Git blob `df70020f2f930d5692a1ff9febebf86dbb0e0db1`.
2. Terminal result: one dry plan and one execute. Block-01/control completed with valid Job cleanup. Block-02/candidate pre-telemetry recorded a `3617.4604ms` first governed capture, intervals `[3621,629,637,654]ms`, and schedule lag up to `2652.9ms`; quiet admission failed `telemetry.samples.interval`; analyzer returned `invalid-experiment / exit 3`; no retry.
3. Immutable evidence: root `.runtime/output/perf/p2-2a-williams-live-20260712-rerun03/`; execute log SHA256 `4622a91609f260aef829681d4950fd42a0639bf3fdd8089b0dc16a01a7639d99`; analysis JSON `c5e61eef6fdc8a567b1a73f9c1d840930c51d622cbbae47c1ffbfa43a372d6bd`; analysis Markdown `90697fe456ffb603732775ccb0cfec972e7d33176b4a5db8a5bd0bdc2f1cc414`; raw manifest `ede91623f669e684f57e64683f5b547027d3185534e1470b897073eb1bb5c6e4`.
4. Protocol repair: policy `p2-williams-crossover-v3`, telemetry schema 3, exactly one same-counter priming capture before the sampling stopwatch, explicit `excluded-warmup` evidence, five governed samples, measured `captureDurationMs <= 1250`, measured `abs(scheduleLagMs) <= 250`, and unchanged 1000 +/- 250ms capture-start interval admission.
5. Regression evidence: governance 33/33; default Job runner 10 pass plus one explicit live skip; root-owned explicit live lane 2/2 across two successive fresh telemetry windows in 18.903 seconds. Logs live under `.runtime/tests/renderer-frame-orchestration-p2-20260710/williams-telemetry-v3/`.
6. Review state: three independent diagnosis reviews approved this protocol shape. Three final implementation/evidence reviews are complete: architecture `CLEAR / APPROVE`, code `CLEAR`, and evidence `CLEAR` after the historical telemetry-v2 lane and rerun03 terminal task wording were reconciled. The code-review suggestion to count the reader, priming, and measured-loop call sites is implemented; governance remains 33/33. Adaptive selection is `7 changed / 16 recommended / 3 main-thread / 0 unmatched`.
7. Admission: rerun04 is the sole next experiment authorization after a committed telemetry-v3 checkpoint and clean-head core gate. It uses fresh detached paths, a unique evidence root, one dry plan, one execute, and zero automatic retry. P2.2b remains closed until a complete analyzer result returns `accepted / exit 0`.

## P2.2a Williams Rerun02 Terminal Record 2026-07-12

1. Exact identity: candidate `6a2fad24bfe864d47c5d0fc712fb07403ceac98d`, control `ab86b1e24d161edbe6bcc80acb0b316e4bf81942`, matching package-lock Git blob `df70020f2f930d5692a1ff9febebf86dbb0e0db1`.
2. Preflight: clean-head `verify:core` passed 66/66; dry plan exited 0 with eight blocks, four adjacent pairs, and 32 expected measured raw files.
3. Live execution: exactly one `--execute` invocation. Block-01/control/TH baseline exited 0 and produced four measured raw files. Job Object evidence is complete with `remainingPids=[]`, `unverifiedPids=[]`, Job close success, and confirmed root termination.
4. Fail-closed decision: the operator classification is `multiple-admission-failures`. Primary failure: external Edge extension renderer PIDs `99504` and `106292`, children of pre-existing PID `32132`, appeared only in the post snapshot. Additional failure: rounded pre intervals `[1666, 1628, 1655, 1618]ms` and post intervals `[1628, 1635, 1617, 1631]ms` exceeded the 1000±250ms telemetry contract. The harness stopped before block-02 and returned `invalid-experiment` / exit `3`. No four-pair performance verdict exists.
5. Immutable evidence root: `.runtime/output/perf/p2-2a-williams-live-20260712-rerun02/`. SHA256: raw manifest `d9f4c89f01d01d661f38ac81a42ffa71d762842c2dd93d6a4e17ceba1f074b60`; analysis JSON `0c6c229bbfbc7b4df91b0810284aca4ece989e02ad27604deca738adfca4e799`; analysis Markdown `f3a4160887f3a794ad987b06526b029628fc97657ac4e14d0dcae21a4aaab31e`; execute log `3920a243e777a193cba68caade0f2ddc8834d18a74d0b02db9a0c5c6a1f7757d`; cleanup `36a6a103e42574e5d837cf559ab20239fd682688331565353eb159e5bcc6e191`; Job evidence `f0a99b36f74f5bff07b3dae95710141442f0b40745eb84ad7377d5bc728ed586`.
6. Cleanup: both measurement junctions/worktrees removed and pruned; ports 8000/8892 have no listeners; rerun02 task process count is zero. `cleanup-final.json` captured 48 parent WIP entries at experiment cleanup. A later read-only snapshot contains 57 entries (52 deletions and 5 modifications) from unrelated parent work; P2 has kept the parent checkout untouched.
7. Admission: P2.2b remains closed. The live run received no retry. Canonical indexes are `canonical-evidence.json` and `cleanup-final.json` beside the report artifacts.

## Williams Telemetry-v2 Repair Delivery Package 2026-07-12

1. Root cause: the prior collector slept one second after each sequential WMI query pair, producing capture-start intervals near 1.63 seconds. Ambient Edge extension renderers also appeared between environment snapshots despite valid task Job cleanup.
2. Functional delivery: commit `89dfe15e1b28536687e258634bb92324336ff81c` uses a monotonic fixed-rate schedule, actual capture-start timestamps, schema-2 completion/duration/lag fields, policy `p2-williams-crossover-v2`, and a shared cadence validator for quiet-window and final analysis.
3. Cleanup semantics: Job Object containment, confirmed termination, remaining/unverified PIDs, ports, probes, and Git identity remain hard gates. Ambient browser PID churn is retained as diagnostic-only evidence. No browser allowlist, tolerance increase, retry, or historical evidence rewrite was introduced.
4. Test routing: default Job runner keeps the live cadence probe skipped. Named command `test:node:williams-crossover-telemetry-live` runs under a main-thread `perf-pr-gate` route and stays outside default `verify:core`.
5. Verification: governance 32/32; default Job runner 10 pass + 1 explicit skip; live WMI lane 2/2; metadata 16/16; core runner 8/8; perf gate 23/23; render role 17/17; route schema 299; architecture, state-write, import graph, supervisor contracts/plan, syntax, and diff checks pass. Adaptive selection is 9 changed / 198 recommended / 8 main-thread / 0 unmatched.
6. Independent review: code review CLEAR; architecture review APPROVE with WATCH that interval remains admission and lag remains diagnostic; final test/evidence review CLEAR after live-lane isolation, source-formula contracts, valid quiet-window coverage, and workload-admission ordering coverage.
7. Recovery/integration: branch commit is the recovery point. Parent checkout retains 57 unrelated WIP entries and remains untouched. Fresh rerun03 requires a new exact candidate/control pair, unique no-clobber evidence root, one dry plan, and one execute attempt.
8. Admission: P2.2b remains closed until rerun03 completes all eight blocks and the analyzer returns `accepted` / exit `0`.

## P2.2a Cached-Pass Compositor Delivery Package 2026-07-11

1. Core delivery extracts cached transformed-pass drawing and generic cached-pass target composition into `js/core/renderer/cached_pass_compositor_owner.js`; `map_renderer.js` retains narrow wiring, diagnostics writes, stable wrappers, export compatibility, and P2.2b algorithms.
2. Files: core `js/core/map_renderer.js`, new owner, and generated mirrors; tests/contracts `cached_pass_compositor_owner_behavior`, combined Python frame-compositor boundary, receiver/P53/scenario/Pages/metadata/core-runner updates; tooling `package.json`, architecture checker, verification domains; docs P2.2a record, registry, context, and task.
3. Diff/commit: initial extraction Lore commit `2f4ed71d8455bc16ad87ff361ac3f106360aa8c0`; review-fix Lore commit `aa34b8b43ad52590f4c5fc553ff4b13d74fceab4`; final contract microfix Lore commit `76977207`; `map_renderer.js` 23,437 -> 23,376 split lines, net -61; final owner 175 split lines; cumulative P2 extraction 96 lines. The branch has no commits behind `origin/main@17aeedf5b295d08fe08965fa5d6f89b0dfb6426c`.
4. Conflict assessment: red for concurrent `map_renderer.js`, cached-compositor, architecture, verification-metadata, Pages startup/manifest, registry/context/task edits; yellow for P2.2b transformed-frame composition and governed performance tooling; green for unrelated parent WIP, which remains untouched.
5. Verification: TDD red artifacts retained; owner 13/13; P53 11/11; combined Python boundary 4/4; architecture/state/import/selector/supervisor green; Pages 927.17 MiB with startup 47/47, landing 18/18, sample 17/17; clean-head dist drift exit 0; clean-head core 64/64.
6. Routes/parity: final microfix selector 7 changed files / 13 recommended commands / 1 main-thread lane / 0 unmatched; evidence-doc selector 4/9/0/0. Owner source/dist blobs match at `55c3f02bdf6da3f57ba1a7266a4954cd51bed249`; `map_renderer.js` remains unchanged from the prior exact source/mirror pair.
7. Acceptance result: exact-head browser gates pass. Governed A/B produced 40/40 canonical role matches; subsequent paired diagnosis classifies the experiment `invalid-environment-regime` because the machine changed performance regimes during B1 and HOI4 direction reversed. Reports and raw evidence remain byte-preserved.
8. Recommendation: hold P2.2b. Preserve runtime source anchor `8eda8c5c`, rerun01 candidate `f53a44f4`, and all historical evidence hashes. The watcher-capability issue is resolved by committed Job Object candidate `6a2fad24`; rerun02 then terminated invalid for multiple admission failures. A later eligible run still requires a new fresh raw root and complete accepted/exit-0 evidence.

## P2.2a Williams Crossover Governance Delivery Package 2026-07-11

1. Delivery: pure policy `tools/perf/williams_crossover_policy.mjs`, import-safe harness/raw analyzer `tools/perf/run_williams_crossover.mjs`, narrow Windows runtime adapter `tools/perf/williams_crossover_windows_runtime.mjs`, named governance tests, package scripts, verification metadata, and P2 control-plane updates.
2. Contract: eight fixed Williams blocks; one warmup plus two measured runs per scenario; exact 32 measured raw files; adjacent `B-A` pairs `1/2`, `4/3`, `6/5`, `7/8`; fixed same-side/order drift pairs; median block and primary estimators; frozen role `last-post-promotion-idle-scenario-frame-v1`.
3. Admission: startup `+3% AND +75ms`, canonical render `+5% AND +35ms`; one practical pair regression is diagnostic, two invalidates, three or four yields a valid regression failure; symmetric direction veto, internal outlier, telemetry, identity, manifest, raw, and cleanup failures invalidate.
4. Telemetry: five ordered one-second pre and post Windows samples cover CPU, Processor Performance, processor frequency, derived `performanceAdjustedFrequencyMHz`, memory, power, structured ports, processes, server/browser/cwd/probes/git. Frozen admission caps pre CPU at 25%, adjacent CPU difference at 10 percentage points, adjacent frequency at 5%, global frequency drift at 10%, adjacent memory at 5%, and within-block memory change at 5%; power GUID and AC source remain constant. Missing/null/capability/time/probe drift is invalid.
5. Identity/cleanup: both measurement worktrees must be exact detached clean checkouts; runner/role/analyzer/policy/package-lock identities use git blobs and LF-normalized SHA256; raw analysis binds exact manifest entries and current tool identities. The committed Windows Job Object helper creates the workload suspended, assigns it before resume, uses kill-on-close with breakaway disabled, and records direct per-block Job evidence. Cleanup requires confirmed root termination plus zero remaining or unverified captured descendants. Raw/JSON/Markdown outputs are no-clobber; analysis replacement requires `--overwrite-analysis`.
6. Routes: `test:node:williams-crossover-governance` is child-safe, selected for tooling/tests/docs, and included in the deterministic `verify:core` infra group; `perf:williams-crossover:run` is limited to runtime tools plus tracked workload/package inputs, remains main-thread/heavy with `perf-dev-server`, `browser-dev-server`, `playwright-browser`, and `.runtime-output` locks, and stays outside `verify:core`. Analyze writes `.runtime` under one owner.
7. Original evidence: JSON `277dba80fafb796b1d96ee0793e18a4eab1281e790908a778c617857a0dc7ddc`; Markdown `15359b93c75a36d1f5f0a442710f248517eaaa40b995d421b0b4e5e554edec45`; raw manifest `c832d438cef94962ac2fb88623fca6464f3e8d11f94732aa09ffb4a2f1279246`.
8. Current state: analyzer null-safety and Job Object containment are committed through candidate `6a2fad24`; live owner `none`; rerun01 is terminal with zero measured samples; rerun02 is terminal `invalid-experiment` after 4/32 measured raw files; P2.2b remains blocked until a future complete analyzer result exits `0`.
9. Review-fix closeout: all telemetry/preregistration/canonical/workload/process-tree/port/manifest/no-clobber/typed-exit blockers are closed. Independent final static reviews report no remaining live blocker and approve handoff to the first exact Williams run.
10. Deterministic verification: four syntax checks, Williams governance 26/26, verification metadata 16/16, core runner 8/8, route schema 295, import graph 51, supervisor contracts 12/12 plus routing 4/4, supervisor plan 15/15, and `verify:core:list` 65 commands all pass. Adaptive dry-run reports 12 changed files, 196 recommendations, 7 main-thread lanes, and 0 unmatched; `git diff --check` passes.
11. Clean-head follow-up: the first `verify:core` attempt exposed one stale P53 inventory sentence. The test contract now asserts the exact current P2.2a acceptance-lane wording; focused P53/Williams/metadata/core-runner checks pass without changing production or task-document truth.

## P2.2a Williams Rerun01 and Analyzer TDD Follow-up 2026-07-11/12

1. Identity/gates: exact control `ab86b1e24d161edbe6bcc80acb0b316e4bf81942`; exact candidate `f53a44f483403edbd7286db226380f0f0e4664be`; runtime source anchor `8eda8c5ce19f54fd839e72e3031a2424a4e658f3`; mandatory `verify:core` and committed `--plan` exit `0`.
2. Terminal live result: block-01 `Register-CimIndicationEvent` returned permission denied / `HRESULT 0x80041003`; runner never started, `runnerPid=null`, measured raw count `0`, block result invalid/exit `3`, and outer execute exit `1` after the analyzer secondary fault. The fixed sequence, thresholds, and one-warmup/two-measured contract were unchanged.
3. Analyzer root cause: missing later-block telemetry yields `validateTelemetryWindow(...).summary=null`; cross-window order checks treated the optional-chain `undefined` result as present and dereferenced `post.summary.firstAtMs`. This prevented the expected typed invalid report.
4. Static TDD repair: the existing Williams governance test first reproduced the exact TypeError as RED. The policy now normalizes four timestamp reads to `number|null`, compares only present timestamps, preserves `.missing` invalid reasons, and adds no synthetic telemetry. Focused GREEN is 1/1; full governance is 27/27.
5. Evidence: raw root `.runtime/output/perf/p2-2a-williams-live-20260711-rerun01/experiment-20260712T010243Z-raw`; raw manifest SHA256 `65ab4e89a77d40f4a7e8c1361d69be21ca9df5db625ce6f924b1037bf9667727`; cleanup evidence SHA256 `b20617025622387118537543ff474f0ac53c3d90a410a637d0c78f898f14beb9`; execute stderr SHA256 `723dd8ed312ceb611a9a7f53cead2fab71642d0aff484cad0603ff944fbf723a`.
6. Cleanup/integration: processes `0`, ports 8000/8892 clear, both junctions and temporary worktrees removed, candidate head stable, parent status identical to preflight, no commit/push from the live or static executor. Rerun01 remains immutable and will not be selectively resumed.
7. Admission: P2.2b remains blocked. The reviewed Job Object solution supersedes the watcher path. The next eligible experiment requires a fresh evidence root and one complete accepted/exit-0 Williams run.
8. Static verification: focused GREEN 1/1; Williams governance 27/27; metadata 16/16; core runner 8/8; perf-gate contract 23/23; render-role policy 17/17; selector schema 295; supervisor contracts/routing 12/12 + 4/4; syntax and diff check green. SF-ATS dry-run is 5 changed / 14 recommended / 12 child-safe / 2 main-thread / 0 unmatched. The two live perf recommendations remain unrun in this static slice.

## P2.2a Windows Job Object Containment TDD Package 2026-07-12

1. Delivery: tracked C# Job helper, Node compile/run adapter, Williams harness/policy integration, Job source/binary identity, per-block cleanup evidence, behavior/integration coverage, package/verification routing, and P2 control-plane updates.
2. Root cause: normal-user WMI process-start subscriptions fail with `HRESULT 0x80041003`. The Job path creates the workload suspended and hidden, assigns and verifies Job membership before resume, uses kill-on-close without breakaway, and performs no WMI watcher, process snapshot polling, or `taskkill` during measurement.
3. Cleanup contract: restricted inherited handles; descendant synchronization handles captured before Job close; Job close, root termination, timeout termination, remaining PIDs, and unverified PIDs all fail closed. Assign failure terminates and waits for the still-suspended root.
4. Identity contract: tracked source uses git blob plus LF-normalized SHA256; the compiled PE uses SHA256 plus byte count; preregistration, every block identity, cross-block validation, and the raw manifest bind the same descriptors.
5. TDD evidence: initial RED `0/3`; first GREEN `3/3`; cleanup RED `3/4`; cleanup GREEN `4/4`. Combined Job/Williams focused tests pass `29/29`; metadata passes `16/16`; core runner passes `8/8`.
6. Capability evidence: first compile/probe exit `0`, binary `19968` bytes, SHA256 `0c04ead454235f28df5d06db545be9f18e8b59838367fdfb91c930d5738d127d`; Windows integration `1/1` preserves special argv and root exit `7`, records the detached child in Job evidence, then verifies it gone with empty remaining/unverified lists.
7. Current commit status: uncommitted static TDD package on `557cdf34204b30d07f6c260841edc70531527b38`; no add/commit/push from this executor. Current-snapshot P/Invoke review and final static gates remain pending.
8. Verification boundaries: Williams live execution, browser/dev-server/Playwright, perf, Pages, dist, and full `verify:core` remain unrun in this slice. The next eligible live run requires a separately assigned owner and a fresh raw/report root. P2.2b remains blocked until accepted exit `0`.

## P2.1 Legacy-Metric Acceptance Closeout 2026-07-11

Status: `failed/blocked` for P2.2 admission at candidate `7e6ca0159cb5a9d8734a58b2bace5ca898ccaed1` against control `c7fb5cde4d6eb5ec4fc9c7c712b1964f45502f8a`.

Browser acceptance:

- `npm run verify:core:main-thread`: 65/65 passed. Log: `.runtime/tests/renderer-frame-orchestration-p2-20260710/p2-1-acceptance/browser-77855450/01-verify-core-main-thread.log`.
- `npm run test:e2e:physical-layer-runtime-contract`: 1/1 passed. Log: `.runtime/tests/renderer-frame-orchestration-p2-20260710/p2-1-acceptance/browser-77855450/02-physical-layer-runtime-contract.log`.
- `npm run test:e2e:scenario-resilience`: 3/3 passed after test-contract commit `427e68398a67586ef4a330b5304dfde567da917e`. Evidence root: `.runtime/tests/renderer-frame-orchestration-p2-20260710/scenario-resilience-contract-fix/`.

Contemporary A/B protocol and validity:

- Sequence `A1 -> B1 -> B2 -> A2`; every block used scenario order `tno_1962,hoi4_1939`, three warmups, and five measured runs per scenario.
- All four blocks exited 0, matched their expected HEAD, passed their first quiet-window attempt, retained clear ports 8000/8892, and left no task-owned Chromium. Each side therefore contributes ten measured samples per scenario.
- Runner SHA256 `5d3756d9532d83fede372096823129ee4a66b5cac651fee0a049a7eb3465c34d` and package-lock SHA256 `fa60a74b517568ffedd1bcdad5414e5ab3a36380ec1ea511411a267a52766385` match across all blocks. Node/npm/Python/Playwright/Chromium, URL query, scenario order, and workload identity also match.

Primary pooled medians:

| Scenario | Metric | Control A | Candidate B | B-A | B/A delta | Result |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `tno_1962` | startup | 6414.40ms | 6408.15ms | -6.25ms | -0.10% | PASS |
| `tno_1962` | render | 668.10ms | 815.75ms | +147.65ms | +22.10% | FAIL |
| `hoi4_1939` | startup | 5833.95ms | 5804.10ms | -29.85ms | -0.51% | PASS |
| `hoi4_1939` | render | 569.70ms | 573.18ms | +3.48ms | +0.61% | PASS |

Additional classification:

- Outlier rules and opposite-direction veto passed.
- HOI4 promotion largest adjacent gap is `640.60 -> 671.80ms`; its `31.20ms` gap is below the material two-cluster rule. The provisional short/long split is A `9/1`, B `10/0`; the long-path rate difference is 10 percentage points and passes the 30-point limit. The long stratum has fewer than three samples per side, so promotion-stratum attribution remains ancillary inconclusive.
- The historical April baseline and thresholds remain frozen. This contemporary A/B decides the current phase gate.

Artifacts and cleanup:

- Reports: `.runtime/reports/generated/p2-1-performance-ab-20260711.json` and `.runtime/reports/generated/p2-1-performance-ab-20260711.md`.
- Block artifacts: `.runtime/output/perf/p2-1-acceptance/20260711/A/{A1,A2}/` and `.runtime/output/perf/p2-1-acceptance/20260711/B/{B1,B2}/`.
- Cleanup: `.runtime/output/perf/p2-1-acceptance/20260711/cleanup/control-cleanup.json`; the runner was restored, the `node_modules` junction and control worktree were removed, worktree registration was pruned, ports remained clear, and parent WIP retained its original shape.

Historical legacy decision: P2.1 implementation remained recoverable and browser-correct under the exercised matrix while the mixed-role pooled render metric blocked P2.2. The following governed reanalysis supersedes only the phase-admission interpretation; the original report and decision remain preserved as evidence.

## P2.1 Governed Render-Sample Reanalysis 2026-07-11

Status: `accepted-with-governed-reanalysis`; P2.2a admitted.

The original report `.runtime/reports/generated/p2-1-performance-ab-20260711.json` remains unchanged at SHA256 `f601896f26478ae9e023d97d0193e281cb8a0c3931fdcd8fa4bccebe03f4d839` with its legacy `failed/blocked` decision. The new companion reads all 40 existing raw files and applies `render-sample-role-v1` / `last-post-promotion-idle-scenario-frame-v1`. Every run has count 2, sequence `[1,2]`, one unique final canonical sample, matching scenario, idle phase, progressive political background, positive scenario context, and a timestamp at or after visual chunk promotion. Role classification does not inspect duration.

The legacy TNO first-sample composition differs: A blank/scenario `6/4`, B `3/7`. Canonical medians remove that role mixture: TNO A/B `1197.90/1195.35ms`; HOI4 A/B `694.55/694.80ms`. Startup, block validity/quiet windows, control/candidate HEAD, runner, lock, environment, query, workload identity, 40 raw hashes, block drift, outliers, and direction checks pass. HOI4 has no material promotion gap, recorded as `not-applicable/pass`.

Tooling delivery adds the reusable pure policy, import-safe offline analyzer, schema-version-2 runner role output, legacy schema-version-1 baseline compatibility, focused Node/Python contracts, verification metadata, and default core routing. Companion artifacts: `.runtime/reports/generated/p2-1-performance-ab-governed-20260711.json` and `.md`.

Deterministic closeout at functional Lore commit `14878c78937f36f9ddee53a876521494a2214cbb` passed `verify:dist-drift` and full `verify:core` 62/62. Logs: `.runtime/tests/renderer-frame-orchestration-p2-20260710/perf-role-governance-14878c78/01-verify-dist-drift.log` and `02-verify-core.log`. The governed JSON/Markdown are reproducible at SHA256 `6f76b274a7827c1c6b7baac68508e40dbafb521206cdc85c1e06c03c42790245` and `bed108df85b38aaf803287b3354332e0023868235767aea7500c5bad88d52305`; the source legacy report remains `f601896f26478ae9e023d97d0193e281cb8a0c3931fdcd8fa4bccebe03f4d839`.

The historical P2.2 preregistration used the same role policy, A1/B1/B2/A2 order, three warmups, five measured runs, startup `3% + 75ms`, render `5% + 35ms`, block drift `5%/10%`, and frozen historical `1.15/1.25` baseline thresholds. The Williams delivery package governs the replacement P2.2a rerun. Parent checkout WIP remains untouched.

## P2 Upstream Integration 2026-07-11

Status: `ready-for-P2.1-acceptance`. Merge commit `aebb9efd492db711e24f43c39c51b4ef94f59097` has parents `a777d17b9d22a5f1d8dde7aac515ab39d8f69b2a` and `17aeedf5b295d08fe08965fa5d6f89b0dfb6426c`; branch ancestry is `ahead 11 / behind 0 origin/main` before this evidence-only closeout commit.

Integration planning and conflict classification:

- Green: P2 renderer source/dist, draw-canvas owner, package scripts, owner tests, P53 inventory, and Python boundary had no upstream file overlap.
- Yellow: `.gitignore`, `tools/build_pages_dist.py`, `tools/test_route_registry.mjs`, `tests/test_e2e_structural_tooling.py`, and `lessons learned.md` are upstream selector/Pages-policy surfaces retained unchanged from `origin/main`.
- Red: `docs/active/_worktree_registry.md`, `tests/test_pages_dist_startup_shell.py`, and `tools/verification/verification_domains.mjs` overlapped directly. Registry produced one text conflict and was manually reconciled. Pages startup and verification metadata auto-merged; direct source inspection plus tests prove both upstream release guards and P2 owner contracts remain.

Verification evidence:

- Upstream focused Python: Pages startup shell plus structural tooling 80/80.
- P2 focused: draw-canvas owner 14/14, P53 8/8, Python boundary 5/5.
- Shared: verification metadata 14/14, core runner 8/8, architecture boundaries, state-write allowlist 115, import graph 51, E2E layers 47, supervisor schemas/contracts/plan, route schema 286.
- Canonical `verify:pages-dist`: startup 46/46, landing 18/18, sample 17/17, total 927.17 MiB.
- Source/dist blobs: renderer `a78767ade0dd2f416fab95b02d67f007e0a9f79c`, owner `85eb8cc125b3438fd63805fca7c5be371e64076c`, sidebar `6a67865bc5d199c20b7cb639c99ca12cabca1932`.
- Clean merge HEAD: `verify:dist-drift` exit 0; `verify:core` results 61/61 with zero failures and seven explicitly skipped main-thread commands.
- Logs: `.runtime/tests/renderer-frame-orchestration-p2-20260710/p2-upstream-integration/01-upstream-python-focused.log` through `06-clean-head-verify-core.log`; report `.runtime/reports/generated/verify-core.json`.

Parent proof: `C:\Users\raede\Desktop\dev\mapcreator` remains `main@db8bd6c118d158aaed4dd6734ecdd981fe80f326`, behind 17, with its pre-existing WIP shape untouched. The release residue worktree path is absent. Browser, main-thread, and performance remain assigned to their separate acceptance owner and were not run by this integration owner.

## P2.1 Post-Acceptance Code-Review Fix 2026-07-11

Status: `ready-for-new-P2.1-acceptance` at functional Lore commit `3efc43206d04616b82be576eb75ae105fc01dd05`.

- Production behavior fix: previous-pixel continuity now reads phase after transformed-cache and last-good effects complete.
- Hot-path fix: `drawCanvasFrame()` creates a frozen JSON-safe summary only for explicit `{ includeSummary: true }`; the production no-options path returns `undefined` without summary serialization or timings copying.
- Timing contract: `commitLastFrame` keeps the original mutable timings reference; opted-in summaries own independent frozen copies.
- Contract fix: P53 inventory validates public-facade and state-write semantics directly and no longer shells out to a temporary working-tree diff.
- Pre-fix acceptance evidence: `verify:core:main-thread` passed 65/65 at `7b3a8fb4`; cleanup evidence records stopped PID 34784 and clear port 8892. Fresh acceptance is required on the post-fix clean HEAD.
- Live-process owner: this writer owns deterministic and Pages/dist commands. Browser, Playwright, main-thread, and performance lanes remain idle until the later acceptance handoff.
- Deterministic closeout: owner 17/17, P53 8/8, Python boundary 5/5, scenario chunk 57/57, shared gates green, Pages 46/46 + 18/18 + 17/17, route gaps 0, clean-head `verify:dist-drift` exit 0, and clean-head `verify:core` 61/61.
- Logs: `.runtime/tests/renderer-frame-orchestration-p2-20260710/p2-1-review-fix/`; adaptive artifact `.runtime/reports/generated/p2-1-review-fix-adaptive.json`.

## P2.1 Focused Repair Closeout 2026-07-11

Status: functional commit `cc6477e0111568091a8665f76fa13d1083c67426` is committed and verified; clean-head dist/core checks are complete. The worktree is clean at that HEAD, `ahead 9 / behind 1 origin/main@17aeedf`; `origin/main@17aeedf` moved forward by one commit, so the P2 worktree stays unre-based until the integration owner handles the red overlaps.

1. Restored draw-frame read order: defer promotion happens before `frameStart`, phase/defer are freshly read after promotion, commit reads phase before total time before raw transform, and capture reads phase/transform after first-visible mutation.
2. Added owner behavior regressions for synchronous `nowMs` and first-visible hook mutations of phase/transform; kept the frozen JSON-safe summary contract.
3. Applied reviewer-approved wiring reductions: compact singleton return, one-line readiness getter, and positive promotion eligibility guard; source/dist `js/core/map_renderer.js` are 23437 physical lines, owner source/dist are 255, baseline is 23472, net reduction is 35, architecture split-entry budget is 23438, and Python `splitlines()` ceiling is 23437.
4. Fixed P2.1 metadata owner-only routing coverage and kept generated source/dist owner mirrors aligned.
5. Added a performance-risk note to `docs/archive/renderer-frame-orchestration-p2-20260710/renderer-draw-canvas-orchestration-owner-p2-1-20260710.md`; later browser/performance A/B acceptance remains a separate lane.

Files touched:

- Core/runtime: `js/core/map_renderer.js`, `js/core/map_renderer/draw_canvas_orchestration_owner.js`, `dist/app/js/core/map_renderer.js`, `dist/app/js/core/map_renderer/draw_canvas_orchestration_owner.js`.
- Tests/contracts: `tests/draw_canvas_orchestration_owner_behavior.test.mjs`, `tests/renderer_draw_canvas_orchestration_inventory_boundary.test.mjs`, `tests/test_map_renderer_draw_canvas_orchestration_owner_boundary_contract.py`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/test_map_renderer_render_pipeline_passes_boundary_contract.py`, `tests/test_pages_dist_startup_shell.py`, `tests/test_scenario_chunk_refresh_contracts.py`.
- Tooling/metadata: `package.json`, `tools/check_architecture_boundaries.mjs`, `tools/verification/verification_domains.mjs`.
- Docs/generated metadata: `docs/archive/renderer-frame-orchestration-p2-20260710/renderer-draw-canvas-orchestration-owner-p2-1-20260710.md`, `docs/active/_worktree_registry.md`, `dist/pages-dist-manifest.json`.
- Runtime artifacts: `.runtime/reports/generated/supervisor-plan.json`.

Diff summary: the large `drawCanvas()` frame state machine is delegated to the new injected owner; concrete pass drawing, cache composition, public facade, runtime context, state-write allowlist, UI/CSS/data, and scenario data stay outside this owner. The P2.1 tests and metadata route the owner, inventory, Python boundary, architecture, and state-write checks as focused child-safe lanes. The focused repair adjusted reviewer-identified ordering and approved line-count reductions inside the existing P2.1 slice.

Commit status: committed/verified. Functional Lore commit is `cc6477e0111568091a8665f76fa13d1083c67426`; the worktree is clean at that HEAD, `ahead 9 / behind 1 origin/main@17aeedf`. `origin/main@17aeedf` moved one commit and has red direct overlaps in `docs/active/_worktree_registry.md`, `tests/test_pages_dist_startup_shell.py`, and `tools/verification/verification_domains.mjs`; preserve this clean worktree without rebase. Browser, Playwright, main-thread, and performance acceptance belong to the separate acceptance owner.

Deterministic validation passed: owner 14/14; P53 8/8; Python boundary 5/5; render-pipeline 5/5; scenario chunk 57/57; scenario refresh 36/36; metadata 14/14; core-runner 8/8; architecture boundaries; state-write allowlist; import graph; route schema 286; supervisor contracts. `verify:pages-dist` passed startup 44/44, landing 18/18, sample 17/17, total 927.17 MiB. Clean-head `verify:dist-drift` exited 0 with log `.runtime/tests/renderer-frame-orchestration-p2-20260710/p2-1-recovery/clean-head-verify-dist-drift.log`. Clean-head `verify:core` exited 0 with 61/61, report `.runtime/reports/generated/verify-core.json`, and log `.runtime/tests/renderer-frame-orchestration-p2-20260710/p2-1-recovery/clean-head-verify-core.log`. Source/dist blobs match: renderer `a78767ade0dd2f416fab95b02d67f007e0a9f79c`, owner `85eb8cc125b3438fd63805fca7c5be371e64076c`. Pages log: `.runtime/tests/renderer-frame-orchestration-p2-20260710/p2-1-recovery/verify-pages-dist.log`. Old PIDs 27992/18096/35820 are absent and ports 8000/8892 are clear.

Checks still assigned or pending: browser, Playwright, main-thread, and performance belong to the separate acceptance owner.

## P2 Pre-baseline Repair 2026-07-10

- P2.0 docs-only truth reconciliation is complete at `6cd077bd3a732d3bebae0ba84c4dc09dbca462d4`.
- Test-only repair scope was one E2E file plus existing P2 docs and this registry; parent checkout WIP remains untouched.
- Main-thread collection first failed because the Pages release URL was missing; the `/dist` retry through `PLAYWRIGHT_TEST_BASE_URL` was the wrong shape for mixed tests.
- The separated probe used `MAPCREATOR_BASE_URL=http://127.0.0.1:8892`, `SCENARIO_FORGE_PAGES_URL=http://127.0.0.1:8892/dist/`, and no `PLAYWRIGHT_TEST_BASE_URL`, producing four downloads.
- Remaining test drift is the hidden styled-select native control and stale E2E schema expectation `21` while production/unit truth is `22`.
- Static results for committed test-only repair `28bda618` are complete: `node --check tests/e2e/project_save_load_roundtrip.spec.js` exit 0; `git diff --check` exit 0; `npm run verify:test:e2e-layers` exit 0 with 47 manifest specs; `npm run verify:test-import-graph` exit 0 with 51 specs.
- Adaptive dry-run artifact `.runtime/reports/generated/test-adaptive-selection.json` reports `changedFiles=4`, `recommendedCommands=7`, `mainThreadSerialVerification=1`, and `unmatchedChangedFiles=[]`.
- Historical expected-red owner path is `/root/p2_baseline_test_fix`; there is currently no active live-process owner, and root must assign one before browser, main-thread, or perf lanes run. P2.1 admitted under scoped governance waiver.
- Fresh focused browser baseline after `28bda618` is 2/5 and confirms a production disclosure race: `renderPresetTree -> updateScenarioInspectorLayout` forced global Project, Legend, and Diagnostics details closed under an active scenario.
- Production source removed the five-line forced-close block for global Project/Legend/Diagnostics disclosures in `updateScenarioInspectorLayout`; the `scenarioDefaultsKey` gate and `collapseScenarioManagedSections()` remain intact.
- `project_save_load_roundtrip.spec.js` now uses a real visible `#lblProjectLegend` summary click, awaits `state.renderPresetTreeFn`, reasserts open state, and uses `expect.poll` until URL `section` contains `projectLegendSection`.
- Focused Python disclosure coverage is green 1/1.
- Full UI mainline keeps the new disclosure contract green and has two existing out-of-scope HTML substring errors: project sidebar order and `transportProjectSection`.
- Deterministic evidence is green: Node syntax; state-write allowlist 115; architecture boundaries; selector schema 282; E2E layers 47; import graph 51; supervisor contracts and plan.
- Independent static review result is `APPROVE`.
- `verify:pages-dist` exited 0: total 927.16 MiB; startup shell 44/44; landing showcase 18/18; sample project 17/17.
- Generated tracked files are exactly `dist/app/js/ui/sidebar.js` and `dist/pages-dist-manifest.json`.
- Source/dist sidebar hashes both equal `6a67865bc5d199c20b7cb639c99ca12cabca1932`.
- Post-build sidebar/support boundary suite is green 19/19.
- Adaptive result is `changedFiles=8`, `recommendedCommands=61`, `mainThreadSerialVerification=53`, `unmatchedChangedFiles=0`.
- The Conductor hook prevented writing the requested Pages log file; full output remains in the sole live-owner Codex transcript.
- Functional commit is `f5f27d3fe3dc2a928b6de453b2883a3c766daf21`; its Lore trailer records post-commit browser/main-thread/perf baseline as `Not-tested`.
- Clean-head `npm run verify:core` exited 0 from existing post-commit evidence; main-thread evidence is carried from `f5f27d3f`; browser regressions, physical-layer runtime contract, scenario resilience, and future perf investigation remain separate lanes.
- Current functional commit is `61e090388feb0c69887b9947b55b61968d5324de`; branch was ahead 4 and the worktree was clean before this docs-only closeout.
- Windows perf gate readiness failed before measurement because raw `py -3 tools/dev_server.py` made Node track `py.exe` while active server metadata recorded the nested `python.exe` pid.
- Minimal interpreter-resolution decision: keep setup-python and Unix paths unchanged; on Windows without setup-python env, synchronously resolve `sys.executable` with `py -3 -c "import sys; print(sys.executable)"`, fail fast on probe errors/status/blank stdout with `[perf-baseline]` plus truncated stderr, then spawn the resolved Python executable with `["tools/dev_server.py"]`.
- Focused verification so far: TDD red `npm run verify:perf-gate-contract` exit 1 on missing `spawnSync`/raw `py` fallback; green `node --check tools/perf/run_baseline.mjs` exit 0 and `npm run verify:perf-gate-contract` exit 0 with 22 tests.
- Final readiness verification: direct `py -3 -c "import sys; print(sys.executable)"` exit 0 resolved `C:\Users\raede\AppData\Local\Programs\Python\Python312\python.exe`; `git diff --check` exit 0; adaptive selector dry-run for the five changed files exited 0 with `unmatchedChangedFiles=[]`; selector route check, supervisor contracts, perf probe snapshot, polyline benchmark, and draw-canvas inventory all exited 0.

## P2 Perf Readiness Cleanup Classification 2026-07-10

- Functional commit: `61e090388feb0c69887b9947b55b61968d5324de`; branch was ahead 4 from `origin/main` and clean before docs edits.
- Clean-head `npm run verify:core` exited 0 from existing post-commit evidence. This docs-only closeout did not rerun tests, perf, browser, dev server, or Playwright.
- Perf readiness is green: Windows PID/readiness ownership fix reached measurement and generated `.runtime/output/perf/baseline_2026-04-20/perf-gate-current.json`.
- Perf gate is red: `.runtime/tests/renderer-frame-orchestration-p2-20260710/perf-readiness/post-commit/02-perf-gate.exit-code.txt` records `exit_code=1`; baseline is `docs/perf/baseline_2026-04-20.json` via `DEFAULT_BASELINE_JSON`.
- `contractMismatches` is empty. Current/baseline share `win32 10.0.26200`, Node major 22, `chromium-headless`, `warmups=3`, and the same URL query; current Node is `v22.23.0`, baseline Node is `v22.22.0`; current has 3 runs and baseline has 5.
- Threshold failures: `tno_1962.totalStartupMs` 8131.4 vs 5805.3, limit 6676.095; `hoi4_1939.totalStartupMs` 7746.8 vs 5205.7, limit 5986.555; `hoi4_1939.renderSampleMedianMs` 705.05 vs 560.9, limit 701.125, only 3.925 ms / 0.56% above limit.
- Attribution boundary: totalStartup failures are repeatable current-vs-April-baseline shifts; scenarioApplied and applyBundle are substantially faster than baseline; HOI render is near threshold; functional patch touches perf launcher/readiness only and does not attribute red thresholds to product renderer/scenario runtime code.
- Cleanup: before cleanup port 8000 was clear; port 8892 had PID 58444 `python.exe tools/dev_server.py --port 8892`; metadata matched PID/port/cwd/runtime root; only PID 58444 was terminated; parent PID 67120 had exited; ports 8000 and 8892 were clear afterward; worktree status was clean before docs edits.
- Runtime report: `.runtime/tests/renderer-frame-orchestration-p2-20260710/perf-readiness/post-commit/cleanup-classification.md`.
- P2.1 admitted under scoped governance waiver; old April threshold failures and block-drift data are historical evidence only. `test:e2e:physical-layer-runtime-contract` and `test:e2e:scenario-resilience` were not run in this round.
## P2 Contemporary A/B Admission Run 2026-07-10

- Scope: contemporary A/B only; no renderer/product code, perf runner, historical baseline, thresholds, or samples were edited. Browser E2E was not run.
- Control lifecycle: detached control worktree `C:\Users\raede\.codex\worktrees\mapcreator-perf-control-b14165c-20260710` was created from `b14165c0e693a87872361b87ac78dc31cd7a0155`, used only for A1/A2, then removed after deleting its ignored `node_modules` junction with a non-recursive junction-safe operation. Cleanup proof: `.runtime/output/perf/p2-ab/20260710/cleanup/control-cleanup.txt`; control path is gone.
- Recovery evidence: control SHA `b14165c0e693a87872361b87ac78dc31cd7a0155`; P2 SHA `3c54d9298c393a3401fd7279b180eac136c9ae85`; package-lock SHA256 `fa60a74b517568ffedd1bcdad5414e5ab3a36380ec1ea511411a267a52766385` on both sides; control artifacts were copied into P2 `.runtime/output/perf/p2-ab/20260710/A/` before cleanup.
- Environment: Node `v22.23.0`; npm `11.18.0`; Python `Python 3.12.10` at `C:\Users\raede\AppData\Local\Programs\Python\Python312\python.exe`; `pythonLocation=C:\Users\raede\AppData\Local\Programs\Python\Python312`; Playwright `1.58.2`; Chromium `145.0.7632.6` at `C:\Users\raede\AppData\Local\ms-playwright\chromium-1208\chrome-win64\chrome.exe`.
- Blocks:
- `A1` control order `tno_1962,hoi4_1939` exit `0`; log `.runtime\output\perf\p2-ab\20260710\A\A1\runner.log`; baseline `.runtime\output\perf\p2-ab\20260710\A\A1\baseline.json`; raw `.runtime\output\perf\p2-ab\20260710\A\A1\raw`.
- `B1` p2 order `tno_1962,hoi4_1939` exit `0`; log `.runtime\output\perf\p2-ab\20260710\B\B1\runner.log`; baseline `.runtime\output\perf\p2-ab\20260710\B\B1\baseline.json`; raw `.runtime\output\perf\p2-ab\20260710\B\B1\raw`.
- `B2` p2 order `hoi4_1939,tno_1962` exit `0`; log `.runtime\output\perf\p2-ab\20260710\B\B2\runner.log`; baseline `.runtime\output\perf\p2-ab\20260710\B\B2\baseline.json`; raw `.runtime\output\perf\p2-ab\20260710\B\B2\raw`.
- `A2` control order `hoi4_1939,tno_1962` exit `0`; log `.runtime\output\perf\p2-ab\20260710\A\A2\runner.log`; baseline `.runtime\output\perf\p2-ab\20260710\A\A2\baseline.json`; raw `.runtime\output\perf\p2-ab\20260710\A\A2\raw`.
- Combined reports: `.runtime/reports/generated/p2-perf-ab-20260710.json` and `.runtime/reports/generated/p2-perf-ab-20260710.md`.
- Admission checks: readiness_contract_errors=PASS, workload_identity_matches=PASS, block_drift=FAIL, b_vs_a_startup=PASS, b_vs_a_render_median=PASS, new_outlier_or_opposite_direction_anomaly=FAIL.
- Workload identity: scenario id, manifest SHA, runtime topology SHA, bootstrap topology SHA, runtime meta SHA, detail chunk manifest SHA, feature count, owner count, snapshot fingerprint, baseline hash, URL query, package-lock hash, Node major, Playwright package, and Chromium identity all match between A/B.
- Metrics:

| Scenario | Metric | A median | B median | B/A | B-A ms | A2/A1 drift | B2/B1 drift | A p90 | B p90 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `tno_1962` | `totalStartupMs` | 8243.0 | 8369.9 | 1.015 / 1.5% | 126.9 | 1.2% | 1.0% | 8337.5 | 8454.2 |
| `tno_1962` | `scenarioAppliedMs` | 3338.3 | 3400.7 | 1.019 / 1.9% | 62.4 | 0.3% | 0.1% | 3419.3 | 3419.8 |
| `tno_1962` | `applyScenarioBundleMs` | 664.0 | 665.6 | 1.002 / 0.2% | 1.6 | -3.1% | 4.2% | 694.9 | 688.8 |
| `tno_1962` | `refreshScenarioApplyMs` | 331.2 | 339.1 | 1.024 / 2.4% | 7.9 | -8.5% | 8.6% | 356.4 | 354.4 |
| `tno_1962` | `renderSampleMedianMs` | 831.0 | 835.1 | 1.005 / 0.5% | 4.0 | 3.5% | 0.9% | 868.0 | 853.2 |
| `hoi4_1939` | `totalStartupMs` | 7513.2 | 7485.8 | 0.996 / -0.4% | -27.4 | -7.2% | 11.1% | 7830.9 | 7873.5 |
| `hoi4_1939` | `scenarioAppliedMs` | 3474.4 | 3505.3 | 1.009 / 0.9% | 30.9 | 1.9% | 3.1% | 3534.1 | 3540.8 |
| `hoi4_1939` | `applyScenarioBundleMs` | 714.6 | 714.6 | 1.000 / -0.0% | -0.0 | 0.2% | 4.2% | 732.7 | 727.8 |
| `hoi4_1939` | `refreshScenarioApplyMs` | 438.6 | 432.4 | 0.986 / -1.4% | -6.2 | 0.3% | 2.3% | 464.0 | 455.0 |
| `hoi4_1939` | `renderSampleMedianMs` | 701.8 | 685.1 | 0.976 / -2.4% | -16.7 | -1.3% | 0.8% | 708.7 | 699.0 |

- Decision: P2.1 admitted under scoped governance waiver. Old block-drift data (`A2/A1=-7.2%`, `B2/B1=11.1%`) is preserved as historical evidence only; primary contemporary A/B readiness, workload identity, B/A startup thresholds, and B/A render thresholds pass.
- Historical April gate status: April baseline/threshold frozen; `docs/perf/baseline_2026-04-20.json` and `.md` were not modified.
- Parent checkout proof: parent status after cleanup matches preflight WIP shape and remains untouched. Release residue `C:\Users\raede\.codex\worktrees\mapcreator-release-e102a70` was removed and is no longer registered on 2026-07-11; recovery evidence remains commit `b14165c0e693a87872361b87ac78dc31cd7a0155`.

## Audit Release Packaging Guardrails 2026-07-11

Status: `integrated`; audit commit `17aeedf5b295d08fe08965fa5d6f89b0dfb6426c` is current `origin/main`. The temporary release residue worktree `C:\Users\raede\.codex\worktrees\mapcreator-release-e102a70` has been removed; recovery remains in Git history.

What changed:

1. Made Pages dist LF normalization fail-fast when a scanned file disappears, while keeping explicit optional missing-file probes opt-in.
2. Corrected Windows extended UNC path normalization and added direct URL parent-escape coverage.
3. Narrowed local deployment ignore policy to `.vercel/`, preserved `.env.example` and `.env.template`, and routed `.gitignore` through selector contracts.
4. Synchronized route-registry and verification-metadata truth for `.gitignore` changes.
5. Added structural and Pages startup shell regression coverage for the audit findings.

Changed files by group:

- Core/tooling: `.gitignore`, `tools/build_pages_dist.py`, `tools/test_route_registry.mjs`, `tools/verification/verification_domains.mjs`.
- Tests: `tests/test_pages_dist_startup_shell.py`, `tests/test_e2e_structural_tooling.py`.
- Documentation/control: this registry and `lessons learned.md`.
- Temporary/generated: `.runtime/reports/generated/audit-20260711-*.json|md` and `supervisor-plan.json` are runtime artifacts and stay untracked.

Diff summary: small audit patch over `origin/main@b14165c0`; semantic diff is limited to release packaging guards, selector route coverage, and tests. No `dist/**` file remains modified after `verify:dist-drift`.

Commit status: integrated on `origin/main@17aeedf5b295d08fe08965fa5d6f89b0dfb6426c`.

Base divergence: the audit started at `origin/main@b14165c0` and now forms upstream `origin/main@17aeedf5`. Parent checkout remains stale/dirty and untouched. The P2 worktree merges this upstream commit while preserving its isolated renderer history.

Overlap analysis:

- Green: runtime app behavior, UI/CSS, scenario data, and P2 renderer implementation were not edited by this audit.
- Yellow: `.gitignore`, `tools/build_pages_dist.py`, `tools/test_route_registry.mjs`, and `tests/test_e2e_structural_tooling.py` are semantically related to selector/Pages validation and retain upstream authority.
- Red: `docs/active/_worktree_registry.md`, `tools/verification/verification_domains.mjs`, and `tests/test_pages_dist_startup_shell.py` overlap directly with P2; the integration owner reconciles both contracts in the merge.

Validation passed: `node tools/select_verification_targets.mjs --check`; `npm run -s test:node:verification-metadata`; `NODE_PATH=C:\Users\raede\Desktop\dev\mapcreator\node_modules npm run python -- -m unittest tests.test_e2e_structural_tooling -q` with 34 tests; `npm run python -- -m unittest tests.test_pages_dist_startup_shell -q` with 46 tests; adaptive dry-run for changed files with zero unmatched; `npm run python -- -m py_compile tools/build_pages_dist.py tests/test_pages_dist_startup_shell.py tests/test_e2e_structural_tooling.py`; `node --check` for the changed MJS files; `npm run -s verify:dist-drift`; `npm run -s verify:supervisor-contracts`; `npm run -s verify:supervisor-plan`; `npm run -s verify:test-import-graph`; `npm run -s verify:test:e2e-layers`; `npm run -s test:node:verify-core-runner`; `npm run -s test:node:click-selection-transaction-owner`; `npm run -s test:node:renderer-click-selection-transaction-inventory`; `npm run -s test:python:map-renderer-click-selection-transaction-boundary`; and `git diff --check`.

Validation gaps at the audit commit: full `verify:core`, browser/Playwright release smoke, and remote CI were not run locally for the narrow release packaging audit. The P2 integration reruns the combined deterministic and dist gates.

Recommended next step: complete the P2 upstream merge, verify the combined release-packaging and draw-canvas owner contracts, then hand the clean integrated P2.1 HEAD to its acceptance owner.

## Scenario Forge P1 Remaining Renderer Context 2026-07-09

Status: `integrated`; P1.5/P1.6 green; P1.7 complete at functional `5f78f3a545d1cfae2e311019718e25b5397bb218` and evidence `f355546c281da1e51cbbbe651fb96a55801267cc`; P1.8 complete at functional `5a8deeebcccbc3d5a4f06e83f9a33c69baa70f16` and evidence `be9d722748d590a7fa9063d6e69f403b4cb259c5`; Closeout passed, pushed, and cleaned; next approved phase is P2 renderer frame orchestration.

Worktree/branch/base: `C:\Users\raede\.codex\worktrees\mapcreator-p1-remaining-20260709`; `codex/renderer-runtime-context-p1-remaining-20260709`; `origin/main@a8f71822d705fcd3b26c32db1abd417b41264eb0`.

Goal: execute the approved linear P1.5 fresh gate, P1.6 narrow hit/hover receiver, P1.7 canonical click preflight, P1.8 atomic pure owner, and P1 Closeout while preserving effect ownership and behavior.

Current P1.8 changed files are the new pure owner, `map_renderer.js` delegation, owner/P54/Python/metadata/core-runner tests, architecture checker, package/verification metadata, generated Pages mirrors/manifest, original P54 plus new P1.8 phase record, the three task documents, and this registry. Runtime context, public facade, state-write allowlist, dependencies, README, UI/CSS/data, and scenario data remain unchanged in this implementation slice.

Validation truth: P1.7 retains its accepted evidence through checkpoint `f355546c`. P1.8 pre-edit selection reported 19 files / 186 commands / 6 main-thread commands with only the new phase record unmatched. Root confirmed the expected TDD RED at `.runtime/tests/renderer-runtime-context-p1-remaining-20260709/p1-8-owner-red.log`. Focused tests, shared gates, actual selector, Pages generation, `verify:pages-dist`, clean-head `verify:dist-drift`, diff check, focused scenario rerun, clean-head core, committed selector, and three independent reviews are green.

Dependency preparation: the ignored `node_modules` Junction targeted `C:\Users\raede\Desktop\dev\mapcreator\node_modules`; `.gitignore:18` is `node_modules/`; the ignored log directory `.runtime/tests/renderer-runtime-context-p1-remaining-20260709` was scoped to the isolated worktree. Both were removed with the verified P1 worktree cleanup.

P1.5 provenance: the historical section below retains the earlier 52/53 attempt. Fresh root-owned acceptance at clean `a8f71822` passed 53/53 and establishes `green` status.

Recommended integration: use `origin/main@b14165c0e693a87872361b87ac78dc31cd7a0155` as the P1 integrated baseline for P2 startup. Release residue was later removed/no longer registered on 2026-07-11; recovery evidence remains commit `b14165c0e693a87872361b87ac78dc31cd7a0155`.

## Scenario Forge P1.8 Pure Click-Selection Decision Owner 2026-07-09

Status: `integrated`; implementation, deterministic root-owned verification, Closeout, remote push, and isolated worktree cleanup complete.

Worktree/branch/base: `C:\Users\raede\.codex\worktrees\mapcreator-p1-remaining-20260709`; `codex/renderer-runtime-context-p1-remaining-20260709`; initial base was `origin/main@a8f71822d705fcd3b26c32db1abd417b41264eb0`; branch has been pushed to `origin/main` and preserved as `origin/codex/renderer-runtime-context-p1-remaining-20260709`.

What changed:

1. Added one strict pure owner, `resolveClickSelectionDecision(resolvedHit, readonlyModifiers)`, with exact own-key/data-property/scalar validation and the closed `{ decision, target }` result.
2. Added one `handleClick` delegation from an exact four-scalar hit projection and root-frozen four-boolean modifier snapshot.
3. Preserved returned-target admission, original `if (!id)` timing, independent root-owned water ctrl/meta behavior, and land-only consumption of `decision.devSelectionRequested`.
4. Extended behavior, P54 inventory, Python composition-root, architecture, metadata, and core-runner contracts.
5. Registered the named owner behavior command and child-safe default-core route, then recorded phase and integration control truth.

Changed files by group:

- Core: `js/core/map_renderer.js`; new `js/core/map_renderer/click_selection_transaction_owner.js`.
- Tests: new `tests/click_selection_transaction_owner_behavior.test.mjs`; updated `tests/renderer_click_selection_transaction_inventory_boundary.test.mjs`, `tests/test_map_renderer_click_selection_transaction_boundary_contract.py`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`, and `tests/scenario_chunk_contracts.test.mjs`.
- Package/metadata/tooling: `package.json`, `tools/verification/verification_domains.mjs`, and `tools/check_architecture_boundaries.mjs`.
- Documentation: original P54 record, new P1.8 phase record, `renderer-runtime-context-p1-remaining-20260709/{plan,context,task}.md`, and this registry.
- Temporary/generated: the implementation executor wrote no `.runtime` artifact and changed no `dist/**` path. Root owns the generated owner mirror, renderer mirror, and Pages manifest. Source-boundary guards exclude those expected generated paths; canonical Pages checks own their parity and drift.

Diff/commit/divergence: P1.8 functional commit `5a8deeebcccbc3d5a4f06e83f9a33c69baa70f16` plus evidence checkpoint `be9d722748d590a7fa9063d6e69f403b4cb259c5` sit linearly over P1.7. Branch ancestry is seven commits ahead and zero behind current `origin/main@a8f71822`.

Overlap analysis:

- Red: `js/core/map_renderer.js`, because it is a shared renderer composition root.
- Yellow: click-selection inventories/boundaries, architecture checker, package, verification metadata, core-runner test, active docs, and registry.
- Green: `RendererRuntimeContext`, public facade, state-write allowlist, dependencies, README, UI/CSS/scenario data, and parent archive/lessons WIP.

Validation evidence: pre-edit selector 19 files / 186 commands / 6 main-thread commands with only the new P1.8 phase record unmatched. Root-owned TDD RED exited 1 on the expected missing owner module assertion only; log `.runtime/tests/renderer-runtime-context-p1-remaining-20260709/p1-8-owner-red.log`. The implementation executor is static-only and claims no GREEN command.

Validation evidence: owner behavior 9/9; executable P54 inventory 10/10; Python boundary 6/6; metadata 13/13; core runner 8/8; architecture PASS; state-write allowlist PASS; test import graph PASS; supervisor contracts 12/12 plus routing 4/4; supervisor plan 15/15; `verify:core:list` 58 commands; actual selector 19 / 187 / 6 / zero unmatched; canonical Pages builder and `verify:pages-dist` exit 0; clean-head `verify:dist-drift` exit 0; focused scenario-chunk rerun exit 0 after updating its P1.8 dev-selection decision assertion; `git diff --check` exit 0; three independent reviews clear. `verify:core:main-thread`, browser, dev server, Playwright, perf, scenario-data, and heavy-geo remain explicit unrun lanes.

Recommended next action: verify the final remote hash, then clean the isolated worktree.

## Scenario Forge P1.7 Click-Selection Transaction Preflight 2026-07-09

Status: complete and `ready-for-integration`; functional Lore commit `5f78f3a545d1cfae2e311019718e25b5397bb218`; docs-only evidence checkpoint `f355546c281da1e51cbbbe651fb96a55801267cc`.

Worktree/branch/base: `C:\Users\raede\.codex\worktrees\mapcreator-p1-remaining-20260709`; `codex/renderer-runtime-context-p1-remaining-20260709`; P1.6 evidence checkpoint `1e14c944855225ec3913bd27bc942e86ede03202`; initial base `origin/main@a8f71822d705fcd3b26c32db1abd417b41264eb0`.

Scope: strengthen the existing P54 inventory with the complete pre-hit guard spine, composite sync/async plus read-only/effectful categories, strict local return counts, local ordering for HGO active/facility details/facility block-underlying/selected-facility clear/empty/special/water/land-clear/dev-selection/all three land-eraser subpaths/land fill/unchanged water fill, `applyWaterRegionFill`, and `applyVisualSubdivisionFill`; add the canonical Python composition-root boundary and closed future seam; register both commands in child-safe `renderer-owner` routing. Production/generated scope is empty.

Test files: `tests/renderer_click_selection_transaction_inventory_boundary.test.mjs`, new `tests/test_map_renderer_click_selection_transaction_boundary_contract.py`, `tests/verification_metadata_behavior.test.mjs`, and `tests/verify_core_runner_behavior.test.mjs`.

Package / metadata files: `package.json` and `tools/verification/verification_domains.mjs`.

Documentation files: `docs/active/renderer-click-selection-transaction-preflight-p1-7-20260709.md`, `docs/active/renderer-runtime-context-p1-remaining-20260709/{plan.md,context.md,task.md}`, and this registry.

Preserved files: `js/**`, `dist/**`, `tools/eslint-rules/state-writer-allowlist.json`, `docs/active/renderer-click-selection-transaction-preflight-20260702.md`, and `tools/check_architecture_boundaries.mjs` have zero diff; README retains its accepted contents.

Pre-edit evidence: the explicit-input dry-run exited 0 with 183 recommended commands. Its only unmatched path was the new P1.7 phase record; both new metadata entries now include that record in `sourceRefs`. Artifacts are `.runtime/reports/generated/p1-7-pre-edit-adaptive-selection.{json,md}` and `.runtime/tests/renderer-runtime-context-p1-remaining-20260709/p1-7-pre-edit-selector.log`.

Validation status: both historical review fixes and three final `APPROVE / CLEAR` verdicts are retained. `git interpret-trailers` parsed the functional commit; clean core passed 57/57 with report/log; committed selector reported 23 / 186 / 6 / zero unmatched with artifacts/log; worktree was clean before and after core; protected phase diff is empty.

Direct overlap risk: yellow for package/metadata/core-runner/registry; green for production renderer/runtime context, Pages mirrors, state allowlist, architecture checker, original P54 record, UI/CSS/scenario data, and parent archive/lessons WIP.

Recommended integration: preserve functional and evidence commits while P1.8 advances on the same worktree; integrate only after P1.8 and P1 Closeout acceptance.

## Scenario Forge P1.6 Hit/Hover Runtime Context Migration 2026-07-09

Status: `green`; complete and `ready-for-integration`.

Worktree/branch/base: `C:\Users\raede\.codex\worktrees\mapcreator-p1-remaining-20260709`; `codex/renderer-runtime-context-p1-remaining-20260709`; `origin/main@a8f71822d705fcd3b26c32db1abd417b41264eb0`.

Accepted checkpoint: functional Lore commit `02cbc3a5ee19716eb8be14a5ed52db877ad25eb4` over G001/G002 evidence commit `3d4c8d12b1a2516b22d45afc64bf11b396d23d5e`.

Scope: add the exact frozen `interaction.hitHover` read capsule, route hit-canvas scheduling and map-hover owner construction through it, and preserve all owner algorithms, effects, event resolution, scheduling, metrics, writes, DOM/UI access, timing reads, and public facade behavior at their existing root boundaries.

Core files: `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`.

Test files: `tests/renderer_runtime_context_hit_hover_behavior.test.mjs`, `tests/test_map_renderer_hit_hover_context_boundary_contract.py`, `tests/renderer_runtime_context_interaction_behavior.test.mjs`, `tests/hit_canvas_scheduling_owner_inventory.test.mjs`, `tests/map_hover_interaction_owner_inventory.test.mjs`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`.

Package / metadata / tooling files: `package.json`, `tools/verification/verification_domains.mjs`, `tools/check_architecture_boundaries.mjs`.

Pages artifacts: `dist/app/js/core/map_renderer.js`, `dist/app/js/core/map_renderer/renderer_runtime_context.js`, `dist/pages-dist-manifest.json`.

Documentation files: `docs/active/renderer-runtime-context-p1-remaining-20260709/plan.md`, `docs/active/renderer-runtime-context-p1-remaining-20260709/context.md`, `docs/active/renderer-runtime-context-p1-remaining-20260709/task.md`, `docs/active/renderer-runtime-context-hit-hover-p1-6-20260709.md`, and this registry.

Direct overlap risk: red for renderer/runtime-context source and mirrors, package/metadata/core-runner routing, and this registry; yellow for shared interaction/hit/hover inventory tests; green for public facade, owner algorithms, UI/CSS/scenario data, and the dirty parent archive/lessons work.

Validation status: the root-owned nine-group focused matrix passed, including named Node 4/4 and named Python 5/5. Architecture, state-write allowlist, test-import graph, supervisor contracts, and supervisor plan all exited 0. Three independent static lanes returned `APPROVE`. The pre-edit selector has no durable evidence and is recorded as a process deviation. The first clean full-core run passed 54/55 and exposed four stale manifest size fields through `verify:dist-drift`; the canonical manifest was amended into the same functional commit. The fresh clean-HEAD rerun passed 55/55 with exit 0; log `.runtime/tests/renderer-runtime-context-p1-remaining-20260709/p1-6-full-core-rerun.log`; report `.runtime/reports/generated/verify-core.json`. Final branch-history selector exit 0 has `unmatchedChangedFiles=[]`; artifacts `.runtime/reports/generated/p1-6-final-adaptive-selection.{json,md}` and `.runtime/tests/renderer-runtime-context-p1-remaining-20260709/p1-6-final-selector.log`. Source/dist Git blobs match for `map_renderer.js` at `b494fe2d97be761963a7b32493fec40ae4034de3` and runtime context at `f680c25e307a91b819ecdd35d8258ef610a7475a`; manifest blob is `075ac209a96c88e26d97effa406b3c546de50ebb`. `verify:core:main-thread`, browser, dev-server, Playwright, perf, scenario-data, and heavy-geo remain explicitly unrun.

Commit status: P1.6 is committed at `02cbc3a5ee19716eb8be14a5ed52db877ad25eb4`; the post-rerun worktree returned clean before these evidence documents were edited.

Recommended integration: retain `02cbc3a5ee19716eb8be14a5ed52db877ad25eb4` as the ready-for-integration checkpoint and advance linearly to P1.7.

## Scenario Forge P1.5 Interaction Read Model Migration 2026-07-09

Status: remote implementation accepted `green` by the continuation gate; fresh clean full core passed 53/53.

Base: `origin/main@db8bd6c118d158aaed4dd6734ecdd981fe80f326`.

Scope: add the read-only `RendererRuntimeContext.interaction` contract and route zoom lifecycle plus map event-binding construction reads through it. Timing, handlers, state writes, scheduling, DOM effects, click selection, and public facade remain at existing ownership boundaries.

Core files: `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`.

Focused coverage: new interaction context Node suite, receiver source contract, Python composition-root boundary, metadata routing, verify-core planning, and the existing zoom/event-binding owner behavior suites.

Focused validation passed: foundation 8/8, receiver 10/10, interaction 6/6, zoom lifecycle 7/7, event binding 6/6, Python boundary 4/4, metadata 11/11, runner 8/8, architecture boundaries, and state-write allowlist.

Route status: the new Node and Python commands are child-safe renderer-owner routes; zoom lifecycle now joins event binding in the default renderer-owner plan. The final selector reported 14 changed files, 183 recommended commands, 0 unmatched files, and 6 main-thread commands.

Fresh acceptance evidence: from clean `HEAD=a8f71822d705fcd3b26c32db1abd417b41264eb0`, root-owned `npm run verify:core` exited 0 with 53/53 commands, including `verify:pages-dist` and `verify:dist-drift`. The report is `.runtime/reports/generated/verify-core.json`; the complete log is `.runtime/tests/renderer-runtime-context-p1-remaining-20260709/p1-5-full-core-original.log`; the worktree returned clean after execution. Committed source/dist Git blobs match: `map_renderer.js` is `24d9718b816c1a4a7f912980d34755eab9620718`, and `renderer_runtime_context.js` is `27afb4005a05e8a8b8b6d7fab52096dad9e781e2`. Windows `core.autocrlf` checkout conversion explains raw-file SHA differences. Independent static audit returned `APPROVE`; effects, handlers, timing, scheduling, metrics, and click selection remain root-owned. Selector route gaps are zero.

Historical deterministic evidence: source/dist mirrors were byte-identical; base-environment `verify:pages-dist` passed with 4 optional geospatial builder skips; `verify:dist-drift`, selector schema, import graph, supervisor contracts, and supervisor plan passed. The earlier 53-command default `verify:core` execution passed its first 52 commands. Its final Pages command exposed four byte-comparison failures in unchanged Landing hero assets under the exact locked geospatial stack. Metadata differences are floating-point tails from `5.55e-17` to `2.27e-13`; blank and TNO SVGs were byte-identical, while the two HOI4 SVGs retained equal element counts with limited path ordering or floating-point geometry differences. Those assets, builders, and source data were outside and absent from the staged P1.5 patch. The fresh 53/53 acceptance result supersedes this historical disposition.

Not-tested lanes: `verify:core:main-thread`, browser, dev-server, and Playwright.

## Scenario Forge P1.4 Viewport Mutation Chain Context Migration 2026-07-09

Status: committed / verified after focused and main-thread validation.

Worktree: `C:\Users\raede\Desktop\dev\mapcreator`.

Task: migrate fit projection, viewport update, and resize lifecycle receiver construction paths to read viewport mutation dependencies through `RendererRuntimeContext.viewport`.

Base: `main...origin/main` at task start; current checkout also has unrelated unstaged WIP in `docs/archive/**` deletions and `lessons learned.md`.

Core files: `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`, `js/core/renderer/renderer_viewport_update_owner.js`.

Test files: `tests/renderer_runtime_context_projection_viewport_behavior.test.mjs`, `tests/renderer_runtime_context_receiver_behavior.test.mjs`, `tests/renderer_runtime_context_viewport_mutation_behavior.test.mjs`, `tests/renderer_viewport_update_owner_behavior.test.mjs`, `tests/test_map_renderer_viewport_mutation_context_boundary_contract.py`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`.

Docs files: `docs/active/renderer-runtime-context-viewport-mutation-chain-p1-4-20260709.md`, `docs/active/_worktree_registry.md`.

Metadata / package files: `tools/verification/verification_domains.mjs`, `package.json`.

Generated dist: builder generated and staged the checked-in dist mirror sync during `npm run verify:dist-drift`.

Shared hotspots: renderer runtime context, `map_renderer.js` owner wiring, viewport update owner, verification metadata, package scripts, and this registry.

Current diff summary: viewport context gains pure read accessors for map container, viewport group, global object, DPR, and land-feature presence; fit projection/update/resize owner wiring consumes those accessors through `getViewportReceiverContext()`; viewport update owner applies SVG transform through `getters.getViewportGroup`; focused Node/Python contracts and route metadata were added.

Commit status: P1.4 functional Lore commit `16e81581` exists locally; push to `origin/main` follows this closeout.

Potential overlap: red with other active edits to `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`, `js/core/renderer/renderer_viewport_update_owner.js`, runtime context tests, verification metadata, package scripts, or registry. Green with README, public facade, UI, CSS, scenario data, draw/pass/click-selection, and existing unrelated `docs/archive/**` / `lessons learned.md` WIP.

Validation passed: `node --check js/core/map_renderer.js`; `node --check js/core/map_renderer/renderer_runtime_context.js`; `node --check js/core/renderer/renderer_viewport_update_owner.js`; `node --check tests/renderer_runtime_context_viewport_mutation_behavior.test.mjs`; `node --check tests/renderer_runtime_context_receiver_behavior.test.mjs`; `node --check tests/renderer_viewport_update_owner_behavior.test.mjs`; `node --check tests/renderer_runtime_context_projection_viewport_behavior.test.mjs`; `node --check tests/verification_metadata_behavior.test.mjs`; `node --check tests/verify_core_runner_behavior.test.mjs`; `python -m py_compile tests/test_map_renderer_projection_viewport_context_boundary_contract.py tests/test_map_renderer_viewport_mutation_context_boundary_contract.py`; `npm run test:node:renderer-runtime-context-viewport-mutation` 4/4; `npm run test:python:map-renderer-viewport-mutation-context-boundary` 6/6; `npm run test:node:renderer-viewport-update-owner` 6/6; `npm run test:node:renderer-runtime-context-projection-viewport` 7/7; `npm run test:node:renderer-runtime-context-receiver` 9/9; `npm run test:node:viewport-resize-lifecycle-owner` 12/12; `npm run test:node:verification-metadata` 10/10; `npm run test:node:renderer-fit-projection-lifecycle` 23/23; `npm run test:python:map-renderer-projection-viewport-context-boundary` 4/4; `npm run test:node:verify-core-runner` 8/8; `npm run test:node:renderer-runtime-context-foundation` 8/8; `npm run test:node:renderer-runtime-context-render-cache` 9/9; `git diff --check`; `npm run verify:pages-dist` exit 0; `npm run verify:dist-drift` exit 0 after builder-generated dist mirror staging; `npm run verify:core` exit 0 with 50 commands and report `.runtime/reports/generated/verify-core.json`.

Route gaps: none. Initial selector dry-run wrote `.runtime/reports/generated/p1-4-adaptive-selection.json` and `.runtime/reports/generated/p1-4-adaptive-selection.md` with `Unmatched changed files: none`; final selector dry-run wrote `.runtime/reports/generated/p1-4-adaptive-selection-final.json` and `.runtime/reports/generated/p1-4-adaptive-selection-final.md` with `changed=22`, `recommended=181`, and `unmatched=[]`.

Not-tested lanes: `verify:core:main-thread`, browser, dev-server, and Playwright.

Recommended integration: push P1.4 commit `16e81581` and this closeout to `origin/main`. P1.5 can start from `16e81581`; keep `verify:core:main-thread`, browser, dev-server, and Playwright as explicit not-run lanes.

## Scenario Forge P1.3 RendererRuntimeContext Projection + Viewport Read Model 2026-07-09

Planned change:

1. Add real optional `projection` and `viewport` sections to `RendererRuntimeContext` with frozen constants/helpers/accessors and sanitized description output.
2. Make projection constants finite, viewport constants finite, and require `minZoomScale < maxZoomScale`.
3. Add `getProjectionReceiverContext()` and `getViewportReceiverContext()` as narrow read-model receivers.
4. Make `getRendererProjectionPathOwner()`, `getViewportReadModelOwner()`, and `getViewportCommandOwner()` consume context sections while keeping owner APIs and algorithms unchanged.
5. Add focused behavior/static tests, package scripts, verification metadata routes, and active docs.

Non-goals: no public facade change, no `drawCanvas()` migration, no pass drawing migration, no `renderPassToCache()` behavior change, no render-order change, no click selection migration, no water/special/dev selection migration, no scenario/UI/CSS/data change, no package dependency change, no dev server, no Playwright, and no browser lane.

Live owner: main thread owned deterministic P1.3 validation, Pages mirror sync, dist drift, and full `verify:core`. Browser/dev-server/Playwright and `verify:core:main-thread` were not run.

What changed:

1. `RendererRuntimeContext` now exposes optional `projection` and `viewport` read models with frozen wrapper objects and JSON-safe descriptions.
2. `getRendererRuntimeContext()` now assembles projection and viewport descriptors from existing constants, helpers, and `rendererSurfaceHost` read accessors.
3. `getRendererProjectionPathOwner()` now receives D3 and constants through `rendererContext.projection`, while setter access remains on `rendererContext.surface.host`.
4. `getViewportReadModelOwner()` and `getViewportCommandOwner()` now receive runtime state, constants, helpers, and surface read access through `rendererContext.viewport`.
5. P1.3 Node/Python tests and verification metadata routes now lock the context boundary.

Core files: `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`.

Test files: `tests/renderer_runtime_context_foundation_behavior.test.mjs`, `tests/renderer_runtime_context_receiver_behavior.test.mjs`, `tests/renderer_runtime_context_projection_viewport_behavior.test.mjs`, `tests/renderer_surface_host_inventory_boundary.test.mjs`, `tests/test_map_renderer_projection_viewport_context_boundary_contract.py`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`.

Docs files: `docs/active/renderer-runtime-context-projection-viewport-p1-3-20260709.md`, `docs/active/_worktree_registry.md`.

Metadata / package files: `tools/verification/verification_domains.mjs`, `package.json`.

Generated Pages mirror files: `dist/app/js/core/map_renderer.js`, `dist/app/js/core/map_renderer/renderer_runtime_context.js`, `dist/pages-dist-manifest.json`.

Temporary files: ignored reports under `.runtime/reports/generated/`, including `p1-3-adaptive-selection.json`, `p1-3-adaptive-selection.md`, and `verify-core.json`.

Diff summary: P1.3 extends the private runtime context contract, migrates three construction paths to context-derived reads, syncs checked-in Pages mirrors, and refreshes two stale inventory assertions. It does not modify owner modules, public facade, draw/pass functions, scenario/UI/CSS/data, or production dependencies.

Commit status: P1.3 functional commit `91ffdfa6` exists locally; this closeout update is preparing a follow-up docs commit, then push.

Base divergence: P1.3 started from `main@5d712c4b92ecb910a4aeb916ad11e195852d4e5f`; functional commit `91ffdfa6` is local and awaiting push with this closeout commit.

Potential conflicts: green for active worktree overlap because only the main worktree exists. Yellow with future edits to `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`, runtime context tests, inventory tests, Python boundary scans, verification metadata, this registry, and generated Pages mirrors. Green with public facade, UI, CSS, scenario data, click selection, pass drawing, and `renderPassToCache()` because P1.3 does not touch those surfaces.

Validation passed: syntax checks for changed JS/MJS; runtime context foundation, receiver, render-cache, and projection/viewport tests; projection path lifecycle, viewport read model, and viewport command owner tests; Python render-cache boundary plus P1.3 projection/viewport boundary; surface host inventory; verification metadata and verify-core runner; architecture boundaries, state-write allowlist, and test import graph; selector schema check; changed-file selector dry-run for 18 staged files wrote `.runtime/reports/generated/p1-3-adaptive-selection.json/.md` with `changedFiles=18`, `recommended=179`, `unmatched=0`, and `mainThreadSerial=6`; supervisor contracts and plan; `verify:pages-dist`; `verify:dist-drift`; full `npm run verify:core` passed 46 commands and wrote `.runtime/reports/generated/verify-core.json`.

Route gaps: none for P1.3. Routes were added for `test:node:renderer-runtime-context-projection-viewport` and `test:python:map-renderer-projection-viewport-context-boundary`; the complete staged patch selector dry-run covered 18 staged files and reported `changedFiles=18`, `recommended=179`, `unmatched=0`, and `mainThreadSerial=6` (6 main-thread serial checks).

Review follow-ups: `tests/renderer_projection_path_lifecycle_inventory_boundary.test.mjs` now expects viewport context getter tokens; `tests/renderer_render_phase_lifecycle_inventory.test.mjs` P43 dist untouched check now scopes to the P43 owner mirror because shared `dist/app/js/core/map_renderer.js` changes when source `map_renderer.js` changes.

Checks intentionally skipped: `verify:core:main-thread`, browser, dev-server, and Playwright lanes were not run. Deterministic core passed, and `verify:core` skipped explicit E2E main-thread commands by default.

Recommendation status: commit this closeout update, then push P1.3 functional commit `91ffdfa6` and the closeout commit.

## Scenario Forge P1.2 RendererRuntimeContext Render Cache Read Model 2026-07-09

Planned change:

1. Add a real `renderCache` section to `RendererRuntimeContext` with constants, helper descriptors, runtime/surface identity accessors, and a main context getter.
2. Add a clearer `surface` read model while preserving `surface.host`.
3. Make `getRenderCacheOwner()` consume runtime state, render cache constants, helper functions, and main context through the runtime context.
4. Keep `createRenderCacheOwner()` algorithm and API shape unchanged.
5. Add render cache context behavior coverage, receiver source-scan coverage, Python boundary coverage, package script, verification metadata, and route coverage.

Non-goals: no `drawCanvas()` migration, no pass drawing migration, no `renderPassToCache()` behavior change, no click selection migration, no public facade change, no scenario/UI/CSS/data change, no package dependency change.

Live owner: main Codex thread owns P1.2 implementation, dist drift, and final `verify:core`. Browser/dev-server E2E and `verify:core:main-thread` are reserved Not-tested lanes unless ownership changes explicitly.

Initial validation plan: syntax checks for changed JS/MJS; renderer runtime context foundation/receiver/render-cache tests; render cache owner and P51/P52 owner suites; Python render cache boundary contract; verification metadata; verify-core runner; architecture boundaries; state-write allowlist; test import graph; selector route check and changed-file dry-run; supervisor contracts/plan; `verify:dist-drift`; final `verify:core`.

What changed:

1. `RendererRuntimeContext` now exposes a `renderCache` read model with frozen constant/helper descriptors, live runtime/surface accessors, and a main context getter.
2. The `surface` section now has a `getMainContext()` read accessor while preserving `surface.host`.
3. `getRenderCacheOwner()` now receives runtime state, surface host, cache constants, helper functions, and main context through `RendererRuntimeContext`; `createRenderCacheOwner()` keeps its algorithm and API shape.
4. Render cache context behavior tests, receiver migration tests, Python boundary contract, package scripts, verification metadata, and route coverage now lock the P1.2 contract.
5. Checked-in Pages mirror files were regenerated only to satisfy `verify:dist-drift`.

Core files: `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`.

Test files: `tests/renderer_runtime_context_render_cache_behavior.test.mjs`, `tests/renderer_runtime_context_receiver_behavior.test.mjs`, `tests/test_map_renderer_render_cache_owner_boundary_contract.py`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`.

Docs files: `docs/active/renderer-runtime-context-render-cache-read-model-p1-2-20260709.md`, `docs/active/_worktree_registry.md`.

Metadata / package files: `tools/verification/verification_domains.mjs`, `package.json`.

Generated Pages mirror files: `dist/app/js/core/map_renderer.js`, `dist/app/js/core/map_renderer/renderer_runtime_context.js`, `dist/pages-dist-manifest.json`.

Temporary files: ignored reports under `.runtime/reports/generated/`, including `p1-2-adaptive-selection.json`, `p1-2-adaptive-selection.md`, `p1-2-adaptive-selection-with-dist.json`, `p1-2-adaptive-selection-with-dist.md`, `supervisor-plan.json`, and `verify-core.json`.

Diff summary: P1.2 extends the context contract and migrates only the render cache receiver wiring. It adds focused render cache context tests, strengthens source-scan and route coverage, and syncs the generated Pages mirror. It does not modify `drawCanvas()`, pass drawing, `renderPassToCache()` behavior, click selection, public facade, UI, CSS, or scenario data.

Commit status: functional Lore commit `b80b73ea` is validated and integrated; this registry closeout records integration truth.

Base divergence: base and pre-edit baseline are `main@0614f9ae2cde1538440f24764bd4c9ee07cc14c5` and `origin/main@0614f9ae2cde1538440f24764bd4c9ee07cc14c5`.

Potential conflicts: yellow with future edits to `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`, render cache receiver tests, Python boundary scans, verification metadata, this registry, and generated Pages mirrors. Green with public facade, UI, CSS, scenario data, click selection, pass drawing, and `renderPassToCache()` because P1.2 did not touch those surfaces.

Validation passed: syntax checks for changed JS/MJS; `test:node:renderer-runtime-context-foundation` 8/8; `test:node:renderer-runtime-context-receiver` 5/5; `test:node:renderer-runtime-context-render-cache` 9/9; `test:node:render-cache-owner` 6/6; P51/P52 owner suites 26/26 and 15/15; `test:python:map-renderer-render-cache-owner-boundary` 4/4; `test:node:verification-metadata` 8/8; `test:node:verify-core-runner` 8/8; `verify:architecture-boundaries`; `verify:state-write-allowlist`; `verify:test-import-graph`; changed-file selector dry-run with `unmatchedChangedFiles: []`; `verify:supervisor-contracts`; `verify:supervisor-plan`; `verify:dist-drift`; full `npm run verify:core` passed 44 commands and wrote `.runtime/reports/generated/verify-core.json`.

Route gaps: none for P1.2. The Python source-scan contract now has a real package script and independent verification route in addition to metadata `sourceRefs`.

Checks intentionally skipped: `verify:core:main-thread` remains an explicit browser/dev-server/Playwright lane outside this P1.2 delivery.

Recommendation status: integrated after push; start P1.3 from `origin/main@b80b73ea` or newer.

## Scenario Forge P1.1 RendererRuntimeContext First Receiver 2026-07-09

Planned change:

1. Add lazy `RendererRuntimeContext` creation in `js/core/map_renderer.js` through `getRendererRuntimeContext()`.
2. Use the context in render pass cache host / commit accounting owner dependency construction as the first receiver.
3. Keep `map_renderer.js` as the composition root and keep owner algorithms unchanged.
4. Add a receiver behavior/inventory test, package script, verification metadata, and route coverage.
5. Sync checked-in Pages mirror only if `verify:dist-drift` requires it.

Non-goals: no `drawCanvas()` migration, no pass drawing migration, no `renderPassToCache()` behavior change, no click selection migration, no public facade change, no scenario/UI/CSS/data change, no package dependency change.

Live owner: main Codex thread owns P1.1 implementation and deterministic verification. Browser/dev-server E2E and `verify:core:main-thread` are reserved Not-tested lanes unless ownership changes explicitly.

Initial validation plan: syntax checks for changed JS/MJS; `test:node:renderer-runtime-context-foundation`; new `test:node:renderer-runtime-context-receiver`; P51/P52 owner suites; verification metadata; verify-core runner; architecture boundaries; state-write allowlist; verify-core list; selector route check and changed-file dry-run; test import graph; supervisor contracts/plan; `verify:dist-drift` if renderer source mirror changed; final `verify:core`.

What changed:

1. `js/core/map_renderer.js` now lazily creates one private `RendererRuntimeContext` from `runtimeState`, `rendererSurfaceHost`, and owner tag `map-renderer`.
2. P51 `getRenderPassCacheHostOwner()` and P52 `getRenderPassCommitAccountingOwner()` now call `getRenderPassReceiverContext()` before existing owner construction.
3. P51/P52 owner constructor APIs, owner algorithms, `renderPassToCache()`, pass drawing, `drawCanvas()`, click selection, public facade, UI, CSS, and scenario data stay in their current locations and contracts.
4. `tests/renderer_runtime_context_receiver_behavior.test.mjs` locks lazy creation, first receiver ordering, private boundary, public-facade boundary, and P1.0 context module independence.
5. Verification metadata and `verify:core` default planning now include `test:node:renderer-runtime-context-receiver`; Pages dist mirror was regenerated for the `map_renderer.js` source change.

Core files: `js/core/map_renderer.js`.

Test files: `tests/renderer_runtime_context_receiver_behavior.test.mjs`, `tests/render_pass_cache_host_owner_inventory.test.mjs`, `tests/render_pass_commit_accounting_owner_inventory.test.mjs`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`.

Docs files: `docs/active/renderer-runtime-context-first-receiver-p1-1-20260709.md`, `docs/active/_worktree_registry.md`.

Metadata / package files: `tools/verification/verification_domains.mjs`, `package.json`.

Generated Pages mirror files: `dist/app/js/core/map_renderer.js`, `dist/pages-dist-manifest.json`.

Temporary files: ignored reports under `.runtime/reports/generated/`, including `p1-1-adaptive-selection.json`, `p1-1-adaptive-selection.md`, `p1-1-verify-core-plan.json`, `p1-1-verify-core-plan.md`, and `supervisor-plan.json`.

Diff summary: P1.1 adds a private lazy runtime context holder and first receiver assertion in `map_renderer.js`, one receiver behavior test, inventory assertions for P51/P52 receiver ordering, one package script, one renderer-runtime verification metadata route, verify-core plan expectations, active docs, and generated Pages mirror sync.

Commit status: P1.1 functional Lore commit `35bbcde6` pushed to `origin/main`; this registry closeout records integration truth.

Base divergence: base and pre-edit baseline are `main@d0386559c2c939a026e3863ea9c7515f7ee68450` and `origin/main@d0386559c2c939a026e3863ea9c7515f7ee68450`.

Potential conflicts: yellow with future edits to `js/core/map_renderer.js`, `package.json`, verification metadata/tests, this registry, generated Pages mirrors, and future renderer runtime context receiver work. Green with public facade, shared UI, CSS, scenario data, click selection, and pass drawing because P1.1 did not touch those surfaces.

Validation passed: `node --check js/core/map_renderer.js`; `node --check js/core/map_renderer/renderer_runtime_context.js`; `node --check tests/renderer_runtime_context_receiver_behavior.test.mjs`; `npm run test:node:renderer-runtime-context-receiver` 4/4; `npm run test:node:renderer-runtime-context-foundation` 8/8; `npm run test:node:render-pass-cache-host-owner-suite` 26/26; `npm run test:node:render-pass-commit-accounting-owner-suite` 15/15; `npm run test:node:verification-metadata` 7/7; `npm run test:node:verify-core-runner` 8/8; `npm run verify:state-write-allowlist`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph`; changed-file selector dry-run wrote `.runtime/reports/generated/p1-1-adaptive-selection.json` and `.runtime/reports/generated/p1-1-adaptive-selection.md` with `unmatchedChangedFiles: []`; `npm run verify:supervisor-contracts`; `npm run verify:supervisor-plan`; `npm run verify:core:list` wrote a 42-command plan with receiver/foundation/dist checks; `npm run verify:dist-drift` passed after staging generated Pages mirror files; code-reviewer reported no blocking findings and the one low test-format sensitivity comment was fixed; full `npm run verify:core` passed 42 commands and wrote `.runtime/reports/generated/verify-core.json`.

Route gaps: none. The P1.1 changed-file selector dry-run covered `js/core/map_renderer.js`, `js/core/map_renderer/renderer_runtime_context.js`, P51/P52 owner files, the new receiver test, P51/P52 inventory tests, verification metadata, verify-core runner, package script, active docs, and generated Pages mirror responsibilities.

Checks intentionally skipped: `verify:core:main-thread` remains an explicit browser/dev-server/Playwright lane outside this P1.1 delivery.

Recommendation status: integrated; start P1.2 from `origin/main@35bbcde6` or newer.

## Scenario Forge P1.0 Renderer Runtime Context Foundation 2026-07-09

1. Added `js/core/map_renderer/renderer_runtime_context.js` as a pure renderer runtime context contract module with schema version, section ids, create/assert/describe helpers, fail-fast validation, frozen context shell, and safe diagnostics snapshots.
2. Added `tests/renderer_runtime_context_foundation_behavior.test.mjs` to cover creation, missing dependency errors, shell freeze, mutable `runtimeState`, JSON-safe description, host snapshot diagnostics, reserved section ids, and no reverse imports.
3. Added `test:node:renderer-runtime-context-foundation` and verification metadata entry `verify-core:test:node:renderer-runtime-context-foundation` in the `renderer-owner` default group.
4. Added `docs/active/renderer-runtime-context-foundation-p1-0-20260709.md` with P1.0 scope, non-goals, reserved sections, risks, and verification commands.

Core files: `js/core/map_renderer/renderer_runtime_context.js`.

Test files: `tests/renderer_runtime_context_foundation_behavior.test.mjs`, `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`, `tests/renderer_render_phase_lifecycle_inventory.test.mjs`.

Docs files: `docs/active/renderer-runtime-context-foundation-p1-0-20260709.md`, `docs/active/_worktree_registry.md`.

Metadata / package files: `tools/verification/verification_domains.mjs`, `package.json`.

Generated Pages mirror files: `dist/app/js/core/map_renderer/renderer_runtime_context.js`, `dist/pages-dist-manifest.json`.

Temporary files: verification wrote ignored reports under `.runtime/reports/generated/`, including `p1-0-final-selector.json`, `p1-0-final-selector.md`, and `verify-core.json`.

Diff summary: P1.0 adds a context shell and its behavior test, adds one package script, adds one renderer-runtime metadata route into `verify:core`, records the preflight plan, and syncs the generated Pages mirror required by `verify:dist-drift`. It does not modify `js/core/map_renderer.js`, `js/core/map_renderer/public.js`, UI files, CSS, scenario data, or production renderer behavior.

Commit status: this delivery is validated for the main closeout commit.

Base divergence: base and pre-edit baseline are `main@6dc506f18632e104a39b4527b742392bc756a716` and `origin/main@6dc506f18632e104a39b4527b742392bc756a716`.

Potential conflicts: yellow with future edits to `package.json`, verification metadata/tests, this registry, generated Pages mirrors, and future renderer runtime context work. Green with appearance/transport shared UI files because P1.0 does not touch `index.html`, CSS, or `js/ui/toolbar.js`.

Validation passed: `node --check js/core/map_renderer/renderer_runtime_context.js`; `node --check tests/renderer_runtime_context_foundation_behavior.test.mjs`; `node --check dist/app/js/core/map_renderer/renderer_runtime_context.js`; source/dist `fc` showed no differences; `npm run test:node:renderer-runtime-context-foundation` 8/8; `npm run test:node:verification-metadata` 6/6; `npm run test:node:verify-core-runner` 8/8; `npm run verify:core:list` 41 commands; `node tools/select_verification_targets.mjs --check` 258 routes; `npm run verify:test-import-graph`; `npm run verify:architecture-boundaries`; `npm run verify:supervisor-contracts`; `npm run verify:supervisor-plan`; `npm run verify:state-write-allowlist`; `npm run verify:dist-drift`; `node --check tests/renderer_render_phase_lifecycle_inventory.test.mjs`; `npm run test:node:renderer-render-phase-lifecycle-inventory` 6/6; final changed-file selector dry-run wrote `.runtime/reports/generated/p1-0-final-selector.json` and `.runtime/reports/generated/p1-0-final-selector.md` with `unmatchedChangedFiles: []`; full `npm run verify:core` passed 41 commands and wrote `.runtime/reports/generated/verify-core.json`.

Route gaps: none. Final changed-file selector dry-run covered the source module, new behavior test, metadata, package script, active docs, registry, generated Pages mirror, and the narrowed P43 inventory test with `unmatchedChangedFiles: []`.

Recommendation: merge as the P1.0 main closeout, then proceed to P1.1 with a small runtime read-model or owner dependency wrap from the pushed main baseline.

## Local/Cloud Sync Cleanup 2026-07-08

1. Created recovery branch `codex/stale-main-wip-preserve-20260708` from stale local `main@16abfd5f` and split the old dirty checkout into three Lore commits:
   - `a48faea3` docs/archive cleanup recovery.
   - `4e5251aa` verification tooling recovery.
   - `199828a2` coordination notes recovery.
2. Pushed recovery branch to `origin/codex/stale-main-wip-preserve-20260708` for cloud backup.
3. Fast-forwarded local `main` from `16abfd5f` to `origin/main@7d7bd7a2`.
4. Removed clean synced worktree `C:\Users\raede\.codex\worktrees\mapcreator-p0-1-verify-core-integration-20260708` and pruned worktree metadata.
5. Deleted local branches already covered by `origin/main`: `codex/p0-1-verify-core-integration-20260708`, `codex/p7-public-demo-release-20260702`, `codex/phase6c-main-integration`, `codex/phase6c-sample-switcher`, `codex/phase6d-sample-export-proof`, `codex/phase6e-public-demo-qa-readiness`, and `codex/renderer-integration-sweep-p47-p50-20260701`.
6. Deleted merged remote branches already covered by `origin/main`: `origin/codex/comment-automation-20260701`, `origin/codex/p48-map-hover-interaction-owner-20260701`, `origin/codex/p49-renderer-transaction-reset-owner-20260701`, `origin/codex/p50-render-pass-cache-host-preflight-20260701`, `origin/codex/p7-public-demo-release-20260702`, `origin/codex/p8a-release-smoke-20260702`, `origin/codex/phase6c-sample-switcher`, `origin/codex/phase6e-public-demo-qa-readiness`, `origin/codex/renderer-hit-canvas-scheduling-p47`, `origin/codex/renderer-integration-sweep-p47-p50-20260701`, `origin/codex/sf-ats-wp1-5-route-coverage-20260702`, `origin/codex/sf-ats-wp1-clean-20260702`, and `origin/codex/sf-ats-wp2-dossier-plan-20260702`.

Retained local branches: `codex/hgo-preview-projection-base-replace@7a95e26c`, `codex/preserve-parent-wip-before-branch-cleanup-20260623@f64e3e62`, `codex/stale-main-wip-preserve-20260708@199828a2`, and `codex/wgi-post-push-truth-20260622@d802cfc8`.

Retained remote branches: `origin/codex/hgo-preview-projection-base-replace@7a95e26c`, `origin/codex/stale-main-wip-preserve-20260708@199828a2`, and `origin/codex/tno-toponym-zh-audit@17de2d57`.

Validation for the cleanup itself: `git fetch --all --prune`; `git worktree list --porcelain`; `git status --short --branch`; `git branch --merged origin/main`; `git branch -r --merged origin/main`. Product runtime tests were not run because this pass only changed git topology and registry truth after preserving stale WIP.

## Scenario Forge P0.1 Core Verification 2026-07-08

1. Added a unified `verify:core` runner that can list the core verification plan, execute child-safe default checks, and gate main-thread E2E behind `verify:core:main-thread`.
2. Added runner behavior tests for route classification, default/main-thread plan shape, command filtering, Windows command process resolution, report writing, and failure stop behavior.
3. Routed P0.1 tooling, docs, package script edits, and route registry edits into the `test-routing` domain so SF-ATS can audit future changes.
4. Documented the core verification baseline in `docs/testing/verify-core.md`.
5. Preserved current `origin/main` supervisor scripts and latest owner safety commit while replaying P0.1 from the dirty parent checkout into a clean integration worktree.

Core files: `tools/run_core_verification.mjs`, `package.json`, `tools/select_verification_targets.mjs`, `tools/test_route_registry.mjs`.

Test files: `tests/verify_core_runner_behavior.test.mjs`.

Docs files: `docs/testing/verify-core.md`, this registry.

Temporary files: `.runtime/reports/generated/verify-core.json` and `.runtime/reports/generated/supervisor-plan.json`, both ignored runtime evidence.

Diff summary: P0.1 adds 4 package scripts, one core verification runner, one focused Node test file, one testing guide, one explicit infrastructure route, route-domain classification for the runner, and selector coverage so package/runner/docs edits hit `test-routing`.

Commit status: functional P0.1 Lore commit `5465af2a1f81432152439a5711e9c813bcb94de7` and registry truth follow-up commits are pushed to `origin/main`. Base commit is `origin/main@5b027772f282aa8cc472e039367e9f653c2417e3`. Parent checkout `main@16abfd5f` remains dirty and untouched.

Base divergence: P0.1 started from `origin/main@b3210be2`, then fast-forwarded to `origin/main@5b027772` before validation. Current P0.1 worktree has no commit divergence from remote before the P0.1 commit.

Conflict summary: direct file overlap is yellow with future edits to `package.json`, `tools/select_verification_targets.mjs`, `tools/test_route_registry.mjs`, verification docs, or this registry. There is no overlap with renderer runtime, landing, Pages dist, browser tests, appearance, or transport product files.

Validation passed: `node --check tools/run_core_verification.mjs`; `node --check tests/verify_core_runner_behavior.test.mjs`; `node --check tools/test_route_registry.mjs`; `npm run test:node:verify-core-runner` 8/8; `node tools/select_verification_targets.mjs --check` with 254 routes; `npm run verify:core:list` listed 36 commands and wrote `.runtime/reports/generated/verify-core.json`; `npm run verify:supervisor-contracts` passed schema/domain/route contracts; `node tools/ai_test_supervisor/supervise_adaptive_verification.mjs --changed-file ...` wrote `.runtime/reports/generated/supervisor-plan.json` with `routeGaps: []`.

Checks skipped: full `npm run verify:core`, `verify:core:main-thread`, browser/dev-server E2E, and Pages dist. These are heavier or main-thread/live-process lanes; P0.1 locks the executable baseline through deterministic runner tests, route checks, list-mode artifact generation, and supervisor dry-run.

Recommendation: keep `C:\Users\raede\.codex\worktrees\mapcreator-p0-1-verify-core-integration-20260708` as the clean synced local audit copy while the parent checkout WIP is unresolved. Reconcile or split the parent checkout in a separate task.

## Scenario Forge P0.1.1 verify:core Post-acceptance Hardening 2026-07-08

1. Hardened the default `renderer-owner` group by adding the current P51/P52/P47 owner suite scripts: `test:node:render-pass-cache-host-owner-suite`, `test:node:render-pass-commit-accounting-owner-suite`, and `test:node:hit-canvas-scheduling-owner-suite`.
2. Kept the default `pages` group inside `verify:core` and clarified the lane as `non-browser deterministic core lane` with dist / runtime-output ownership semantics.
3. Updated runner behavior tests so missing package-script fixtures produce an explicit `omittedCommands` failure, while the key commandRef set is asserted directly.
4. Ran full `npm run verify:core`; the runner stopped at the first failing command in `python-quick`, after infra commands passed and before startup-node, renderer-owner, scenario, and pages groups executed.

Core files: `tools/run_core_verification.mjs`.

Test files: `tests/verify_core_runner_behavior.test.mjs`.

Docs files: `docs/testing/verify-core.md`, this registry.

Routing files: none.

Package files: none.

Temporary files: `.runtime/reports/generated/verify-core.json`, `.runtime/reports/generated/verify-core.md`, `.runtime/reports/generated/supervisor-plan.json`, and `tests/e2e/test-import-graph.json` regenerated by verification commands.

Diff summary: the runner now plans 39 default commands instead of 36 because three renderer-owner suite commandRefs were added; the report schema now records `lane`, `startsBrowserDevServerOrPlaywright`, and `requiresDistLaneOwner`; docs now call out `verify:core:list`, failing commandRef lookup, dist drift, route gaps, and P0.1.1 full-run acceptance evidence.

Commit status: this delivery package travels with the P0.1.1 Lore commit. Base commit and synchronized pre-edit baseline are `main@1c7a56ac` / `origin/main@1c7a56ac`.

Base divergence: none before P0.1.1 edits; `git worktree list --porcelain` lists only the main checkout.

Conflict summary: yellow with future edits to `tools/run_core_verification.mjs`, `tests/verify_core_runner_behavior.test.mjs`, `docs/testing/verify-core.md`, or this registry. There is no overlap with `js/main.js`, `js/core/map_renderer.js`, `index.html`, CSS, UI interaction files, scenario data, dist artifacts, transport, or appearance runtime files.

Validation passed: `node --check tools/run_core_verification.mjs`; `node --check tests/verify_core_runner_behavior.test.mjs`; `npm run test:node:verify-core-runner` 8/8; `npm run verify:core:list` wrote 39-command reports at `.runtime/reports/generated/verify-core.json` and `.runtime/reports/generated/verify-core.md`; `node tools/select_verification_targets.mjs --check` with 254 routes; `npm run verify:test-import-graph`; `npm run verify:supervisor-contracts` with 16/16 schema/domain/routing tests; `npm run verify:supervisor-plan` with 15/15 tests and supervisor plan report.

Full `verify:core` result: `npm run verify:core` exited 1. The first failing `commandRef` is `npm run python -- -m unittest tests.test_app_entry_resolver tests.test_main_deferred_detail_promotion_boundary_contract tests.test_scenario_chunk_refresh_contracts tests.test_scenario_renderer_bridge_boundary_contract tests.test_map_renderer_interaction_border_snapshot_orchestration_contract tests.test_perf_gate_contract tests.test_startup_shell -q`. Classification: existing verification contract drift in Python source-scan tests after owner extraction. Evidence: `tests.test_main_deferred_detail_promotion_boundary_contract` still checks the post-ready reconcile constant/function in `js/main.js`, while current implementation owns it in `js/bootstrap/startup_ready_handoff.js`; `tests.test_scenario_chunk_refresh_contracts` still checks `promotionWorkActive` in `js/core/map_renderer.js`, while current implementation owns it in `js/core/map_renderer/render_phase_lifecycle_owner.js`; `tests.test_map_renderer_interaction_border_snapshot_orchestration_contract` still expects old exact string shapes in `js/core/map_renderer.js`, while current behavior is split across render cache/interaction snapshot owners.

Checks not reached by `verify:core`: startup-node, renderer-owner, scenario-project-chunk, `verify:pages-dist`, and `verify:dist-drift`, because the runner stops on the first failing command. Dist drift status is therefore not evaluated by the full runner in this pass; no dist files are modified in the P0.1.1 diff.

Not-tested: `verify:core:main-thread` was not run because the default core lane already failed in `python-quick`; running browser/dev-server/Playwright E2E before the non-browser core lane is green would add live-process noise without resolving the blocker.

Route gaps: none. Final changed-file selector wrote `.runtime/reports/generated/p0-1-1-selector.json` and `.runtime/reports/generated/p0-1-1-selector.md`; it reported `unmatchedChangedFiles: []` and covered `test-routing` for `tools/run_core_verification.mjs`, `tests/verify_core_runner_behavior.test.mjs`, `docs/testing/verify-core.md`, and `docs/active/_worktree_registry.md`.

Recommendation: do not enter P0.2 yet. The next smallest unblock is a dedicated verification-contract repair for the stale Python source-scan tests, keeping product runtime unchanged and repointing each contract to the current owner files.

## Scenario Forge P0.2 Verification Metadata Single Source 2026-07-09

1. Repaired P0.1.1 acceptance blockers by repointing stale verification contracts to current owner files, with product runtime source unchanged.
2. Added `tools/verification/verification_domains.mjs` as the active verification metadata source for `verify:core` groups, main-thread E2E skips, resource locks, route registry projections, and CI profile ownership.
3. Added `tools/verification/verification_metadata_helpers.mjs` so `run_core_verification.mjs` and `test_route_registry.mjs` consume the same metadata instead of duplicating command ownership.
4. Added `test:node:verification-metadata` and `tests/verification_metadata_behavior.test.mjs` to lock metadata, route registry, selector, package scripts, and verify-core plan consistency.
5. Synchronized checked-in Pages dist mirrors for previously landed renderer owner comments that `verify:dist-drift` exposed while running full `verify:core`.

Core files: `tools/verification/verification_domains.mjs`, `tools/verification/verification_metadata_helpers.mjs`, `tools/run_core_verification.mjs`, `tools/test_route_registry.mjs`, `tools/select_verification_targets.mjs`, `package.json`.

Test files: `tests/verification_metadata_behavior.test.mjs`, `tests/verify_core_runner_behavior.test.mjs`, `tests/test_main_deferred_detail_promotion_boundary_contract.py`, `tests/test_scenario_chunk_refresh_contracts.py`, `tests/test_map_renderer_interaction_border_snapshot_orchestration_contract.py`, `tests/scenario_chunk_contracts.test.mjs`.

Docs files: `docs/testing/verification-metadata.md`, `docs/testing/verify-core.md`, `docs/active/_worktree_registry.md`, `lessons learned.md`.

Generated dist files: `dist/app/js/core/map_renderer/render_pass_cache_host_owner.js`, `dist/app/js/core/map_renderer/render_pass_commit_accounting_owner.js`, `dist/pages-dist-manifest.json`.

Temporary files: `.runtime/reports/generated/verify-core.json`, `.runtime/reports/generated/verify-core.md`, `.runtime/reports/generated/p0-2-selector.json`, `.runtime/reports/generated/p0-2-selector.md`, `.runtime/reports/generated/supervisor-plan.json`, and `tests/e2e/test-import-graph.json` regenerated by verification commands.

Diff summary: the runner no longer owns the hardcoded default command table; default `verify:core` expands to 40 commands because metadata self-check enters the infra group. The route registry now uses metadata-projected verification routes before dynamic E2E/Node/Python routes. Selector coverage now maps `tools/verification/**`, `docs/testing/verification-metadata.md`, and `lessons learned.md` into `test-routing`. P0.1.1 stale source-scan contracts now target `startup_ready_handoff.js`, `render_phase_lifecycle_owner.js`, `render_cache_owner.js`, `zoom_interaction_lifecycle_owner.js`, and `render_pass_commit_accounting_owner.js`.

Commit status: functional Lore commit `8dde86e47b85bdc6975e8caa4c9e91e86f374f53` exists on branch `codex/p0-2-verification-metadata-single-source`. Base was `main@2acdf0d104c5893a151999e376d9ab6275f02276`, matching `origin/main@2acdf0d104c5893a151999e376d9ab6275f02276` before edits.

Base divergence: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-p0-2-verification-metadata-20260709` diverges only by this P0.1.1 repair plus P0.2 metadata diff. Parent checkout remains untouched during this worktree pass.

Conflict summary: yellow with future verification-tooling edits to `package.json`, `tools/run_core_verification.mjs`, `tools/test_route_registry.mjs`, `tools/select_verification_targets.mjs`, `tools/verification/**`, verification docs, this registry, and checked-in Pages dist mirrors. Green for product runtime source behavior because no `js/` source file is modified in this diff.

Validation passed: syntax checks for changed JS/MJS test/tool files; `npm run test:node:verification-metadata` 5/5; `npm run test:node:verify-core-runner` 8/8; `npm run test:node:scenario-chunk-contracts` 57/57; three repaired Python contract modules individually passed; `node tools/select_verification_targets.mjs --check` with 256 routes; `npm run verify:supervisor-contracts` 16/16; `npm run verify:core:list` wrote 40-command reports; changed-file selector wrote `.runtime/reports/generated/p0-2-selector.json` and `.runtime/reports/generated/p0-2-selector.md` with `unmatchedChangedFiles: []`; `npm run verify:dist-drift` passed after staging the generated dist mirror sync; clean-HEAD `npm run verify:core` passed 40 commands and wrote `.runtime/reports/generated/verify-core.json` plus `.runtime/reports/generated/verify-core.md`.

Review result: independent code review returned APPROVE with no findings. Independent architecture review initially flagged dist-only mirror drift, then returned CLEAR after source/dist line evidence and `npm run verify:dist-drift` confirmed the dist changes were generated mirror sync.

Checks intentionally skipped before commit: `verify:core:main-thread` and browser/dev-server E2E. The default core lane covers the required non-browser deterministic acceptance; main-thread browser checks remain integration-owner lanes.

Recommendation: fast-forward merge `codex/p0-2-verification-metadata-single-source` into clean `main`, push, and remove the isolated worktree after push confirmation. Recovery remains through branch `codex/p0-2-verification-metadata-single-source` and commit `8dde86e47b85bdc6975e8caa4c9e91e86f374f53`.

## Comment Automation 2026-07-08

1. Added 11 Chinese maintenance comments across SF-ATS supervisor tooling, adaptive test runner, renderer pass host/commit owners, and the supervisor plan test.
2. Kept the implementation comment-only after reviewing the current structure: selector, dossier, lane summary, plan execution, host setup, and commit/accounting boundaries are already separated cleanly.
3. Avoided `index.html`, `css/style.css`, `js/ui/toolbar.js`, `dist/app/**`, README files, and the dirty parent checkout.
4. Updated this registry with a ready-for-integration delivery package from the isolated worktree.

Core files: `tools/ai_test_supervisor/command_lanes.mjs`, `tools/ai_test_supervisor/build_change_dossier.mjs`, `tools/ai_test_supervisor/supervise_adaptive_verification.mjs`, `tools/run_adaptive_tests.mjs`, `js/core/map_renderer/render_pass_cache_host_owner.js`, `js/core/map_renderer/render_pass_commit_accounting_owner.js`.

Test files: `tests/supervisor_plan_behavior.test.mjs`.

Docs files: this registry.

Temporary files: none.

Diff summary: 7 code/test files receive 11 explanatory Chinese comments, concentrated on route-gap hard stops, dry-run/execute plan safety, legacy selector compatibility, Git path discovery scope, recursive adaptive-run filtering, renderer host setup ownership, political fine-cache full-reference semantics, and the route-gap zero-execution contract. This registry receives the delivery package.

Commit status: uncommitted at delivery package creation; commit after final review with Lore protocol. Base commit is `origin/main@b3210be2cb0dfc4d93a3b0ebaeb9c7f62cab7072`. The parent checkout is behind current `origin/main` and has unrelated WIP, so it was preserved untouched.

Base divergence: the automation worktree is based on current `origin/main` and has only the comment/registry diff. Parent checkout `main@16abfd5f` remains stale and dirty.

Conflict summary: direct file overlap is yellow with future edits to SF-ATS tooling, `tests/supervisor_plan_behavior.test.mjs`, renderer pass owner files, or this registry. There is no overlap with shared appearance/transport hot files.

Validation passed: `node --check` for all 7 changed JS/MJS files; `node tools/select_verification_targets.mjs --check`; explicit selector dry-run for all changed files with `unmatchedChangedFiles: []`; `npm run -s test:node:supervisor-plan` 15/15; `npm run -s verify:supervisor-contracts` 16/16 across schema/domain/route tests; `npm run -s test:node:render-pass-cache-host-owner`; `npm run -s test:node:render-pass-cache-host-owner-suite`; `npm run -s test:node:render-pass-commit-accounting-owner`; `npm run -s test:node:render-pass-commit-accounting-owner-suite`; `NODE_PATH=C:\Users\raede\Desktop\dev\mapcreator\node_modules py -3 -m unittest tests.test_e2e_structural_tooling -q` 33/33; `git diff --check`.

Validation notes: plain `python -m unittest tests.test_e2e_structural_tooling -q` could not run because `python` is not on PATH in this shell. `py -3` without `NODE_PATH` failed because the isolated worktree could not resolve `@playwright/test`; rerunning with the parent checkout `node_modules` passed.

Checks skipped: `verify:pages-dist` was recommended by selector for renderer source mirror awareness, but it owns `dist` and `.runtime-output` locks and is disproportionate for comment-only source changes. Browser/dev-server tests were not started.

Recommendation: merge directly after final review, then remove `C:\Users\raede\Desktop\dev\mapcreator-comment-automation-20260708` and delete `codex/comment-automation-20260708` after ancestry confirmation.

## Audit Follow-up 2026-07-07

1. Audited `origin/main` after the 2026-07-05 audit baseline and found no newer functional commit cluster to repair.
2. Reviewed the stale parent checkout WIP. The direct-integration verdict is blocked because it is 48 commits behind and mixes unrelated governance, SF-ATS, renderer boundary, registry/archive, and landing edits.
3. Replayed only the safe landing accessibility/i18n fix onto a clean current-main worktree.
4. Aligned static `aria-label` fallback text with the existing `sampleProjectActionsLabel` / `Sample project actions` runtime translation key.
5. Regenerated the checked-in Pages mirror and manifest so `dist/index.html` and `dist/pages-dist-manifest.json` match the landing source.

Core files: none.

UI/Pages files: `landing/index.html`, `dist/index.html`, `dist/pages-dist-manifest.json`.

Docs files: this registry.

Diff summary: functional commit `0f499d7f` changes four static labels in the landing source and mirrored `dist/index.html`; the Pages manifest `index.html` byte count and total size decrease by 8 bytes. Product runtime JS, CSS, renderer, transport, supervisor, and package scripts are unchanged.

Commit status: functional commit `0f499d7f` is ready on branch `codex/audit-20260707-landing-i18n`; this registry closeout records integration truth before push. Parent checkout `main@16abfd5f` remains dirty and untouched.

Conflict summary: green with appearance/transport platformization, renderer runtime, supervisor, package, and CSS/JS hot files because none are touched. Yellow with future landing/source-dist work and this live registry. Parent checkout WIP remains red for direct integration.

Validation passed: `py -3 tools\i18n_audit.py` with `ui_missing=0`, `ui_english_fallback=0`, and `a11y_literals=0`; `node --test tests\landing_showcase_view_behavior.test.mjs` 18/18; `node --test tests\sample_project_contracts.test.mjs` 17/17; `py -3 tools\build_pages_dist.py` size `927.12 MiB`; `npm run -s verify:pages-dist` with 41 Python startup shell tests, landing 18/18, and sample contracts 17/17; `node tools\select_verification_targets.mjs --changed-file landing/index.html --changed-file dist/index.html --changed-file dist/pages-dist-manifest.json --json` with `unmatchedChangedFiles: []`; `git diff --check`; `git diff --cached --check`.

Checks skipped: browser/dev-server sample-guide E2E recommended by selector as main-thread/browser-owned. It was intentionally not run because this repair only changes static fallback label text and deterministic i18n/Pages/Node contracts covered the changed surface.

Recommendation: merge this follow-up directly into `origin/main`, then clean the temporary worktree. Keep parent checkout WIP as a separate split/rebase task.

## Audit Follow-up 2026-07-05

1. Fixed `tools/run_adaptive_tests.mjs --execute` so unmatched changed files write the adaptive report and exit 2 before any command runs.
2. Fixed `tools/ai_test_supervisor/supervise_adaptive_verification.mjs --execute` so route gaps leave execution results empty and exit through the existing strict route-gap path.
3. Kept the deployed Pages release-gate script discoverable through `package.json`/structural tests while avoiding duplicate selector recommendations for the same spec.
4. Narrowed Pages release-smoke retry phase tagging so only landing navigation/readiness/networkidle use `LANDING_PREFLIGHT`; sample card, HGO, href, and overflow assertions remain product assertions.
5. Preserved the parent checkout WIP and performed all edits in the clean audit follow-up worktree.

Core files: `tools/run_adaptive_tests.mjs`, `tools/ai_test_supervisor/supervise_adaptive_verification.mjs`, `tools/test_route_registry.mjs`, `tests/e2e/release/pages_public_release_gate.spec.js`.

Test files: `tests/supervisor_plan_behavior.test.mjs`, `tests/test_e2e_structural_tooling.py`.

Docs files: this registry.

Diff summary: the follow-up changes six code/test files plus this registry. It adds route-gap pre-execution blocking in both SF-ATS runners, updates release gate phase boundaries, and locks the behavior with Node/Python structural tests. Product runtime files are unchanged.

Commit status: ready for Lore commit on `codex/audit-20260705-uncommitted`, based on `origin/main@08d84f59ef92ea6a856b16a0e5c1dc27399fc1dd`. Parent checkout `main@16abfd5f` remains dirty and untouched.

Conflict summary: yellow with future edits to `tools/test_route_registry.mjs`, `tests/test_e2e_structural_tooling.py`, release-smoke tests, supervisor tests, or this registry. No overlap with `index.html`, `css/style.css`, `js/ui/toolbar.js`, renderer runtime, appearance, or transport product files.

Validation passed: `node --check` for changed JS/MJS files; `py -3 -m py_compile tests/test_e2e_structural_tooling.py`; targeted structural Python tests for release gate, route registry, selector, and adaptive unmatched execute behavior; `npm run -s test:node:release-smoke-helper`; `node tools/run_adaptive_tests.mjs --changed-file ...` with `unmatchedChangedFiles: []`; `node tools/select_verification_targets.mjs tests/e2e/release/pages_public_release_gate.spec.js --json` recommending only `test:e2e:pages-public-release-gate` as the main-thread release gate; `npm run -s verify:supervisor-contracts`; `npm run -s verify:supervisor-plan`; `npm run -s verify:test-import-graph`; `npm run -s verify:test:e2e-layers`; `node tools/select_verification_targets.mjs --check`; `git diff --check`.

Checks skipped: live browser Pages release gate, `verify:pages-dist`, and `verify:dist-drift`, because this follow-up changes verification tooling and release-gate test semantics without source/dist product runtime edits. The selector marks `test:e2e:pages-public-release-gate` as a main-thread command requiring reserved browser/dev-server ownership.

Recommendation: merge this follow-up directly into `origin/main`, then remove the temporary audit worktree. Refresh or clean the dirty parent checkout in a separate integration task.

## Audit Integration Closeout 2026-07-04

1. Integrated SF-ATS WP2 and P8A release-smoke work into `origin/main`, preserving parent checkout WIP.
2. Fixed review-found SF-ATS execution behavior so execute/strict modes fail on route gaps, added required package-script contract coverage, and corrected history/base discovery.
3. Fixed the P8A/SF-ATS overlap by adding the `release-smoke` supervisor domain and preserving both release-smoke and supervisor route classification.
4. Cleaned integrated P8A and SF-ATS worktrees after clean status and containment checks.

Core files: `AGENTS.md`, `package.json`, `tools/run_adaptive_tests.mjs`, `tools/test_route_registry.mjs`, `tools/ai_test_supervisor/**`, `tests/e2e/support/release-smoke.js`, `tests/e2e/release/pages_public_release_gate.spec.js`.

Test files: `tests/supervisor_*.test.mjs`, `tests/release_smoke_retry_behavior.node.test.mjs`, `tests/test_e2e_structural_tooling.py`, `tests/e2e/test-import-graph.json`.

Docs files: `docs/testing/sf-ats-overview.md`, `docs/archive/sf-ats-wp2-supervisor-plan-20260702/**`, `docs/active/p8a-release-smoke-propagation-guard-20260702/**`, `docs/active/p8-public-demo-followups-20260702.md`, this registry, and `lessons learned.md`.

Diff summary: `origin/main@defc90ba..874709cc` adds 33 files/paths with 5,043 insertions and 163 deletions, concentrated in supervisor tooling/tests/docs and release-smoke helper/tests.

Commit status: functional integration is pushed as `2cc626df2b37db7cfb6409334e210f728ce1ab25` and `874709ccb33c45e47edfa8d7a0e7537e0b53da5c`; this registry closeout is a follow-up docs commit.

Base status: the audit branch started from `origin/main@defc90ba2f0768ff1948d4cfbf79af943080225b`; parent checkout `main@16abfd5f` remains locally dirty and behind remote.

Conflict summary: text conflicts occurred in this registry and `tools/test_route_registry.mjs`; route registry was resolved by preserving both release-smoke and supervisor classifiers. Registry was resolved to current integration truth.

Validation passed: `node --check` for release-smoke helper/spec/test and route registry; `npm run -s test:node:release-smoke-helper`; `npm run -s verify:test-import-graph`; `npm run -s verify:supervisor-contracts`; `npm run -s verify:supervisor-plan`; `py -3 -m unittest tests.test_e2e_structural_tooling -q` with `NODE_PATH=C:\Users\raede\Desktop\dev\mapcreator\node_modules`; `node tools/run_adaptive_tests.mjs --dry-run --include-branch-history`; route-gap execute negative check returned exit 2; `git diff --cached --check`; `git diff --check`; final read-only code-reviewer returned READY.

Checks skipped: live browser Pages release gate, `verify:pages-dist`, and `verify:dist-drift`, because this audit changed supervisor/release test infrastructure and the live release gate owns browser/deployed URL validation.

Recommendation: keep `origin/main@874709cc` plus this closeout as the integration baseline; handle parent checkout cleanup in a separate task.

## Integrated Worktree Cleanup 2026-07-03

- Removed already-integrated recovery worktrees `C:\Users\raede\Desktop\dev\mapcreator-p51-render-pass-cache-host-owner`, `C:\Users\raede\Desktop\dev\mapcreator-p52-render-pass-commit-accounting-owner`, `C:\Users\raede\Desktop\dev\mapcreator-p53-draw-canvas-orchestration-preflight`, and `C:\Users\raede\Desktop\dev\mapcreator-p54-click-selection-preflight` after clean status and `merge-base --is-ancestor HEAD origin/main` checks passed.
- Deleted local branches `codex/p51-render-pass-cache-host-owner-20260702`, `codex/p52-render-pass-commit-accounting-owner-20260702`, `codex/p53-draw-canvas-orchestration-preflight-20260702`, and `codex/p54-click-selection-transaction-preflight-20260702`. Recovery remains through the landed commits listed above and the delivery packages below.
- Preserved `mapcreator-p8a-release-smoke`, `mapcreator-sfats-wp1`, `mapcreator-sfats-wp15`, and `mapcreator-sfats-wp2` because they remain separate, clean, unmerged lanes with package/registry/test-route overlap.

### Recent Renderer Audit Dist Sync 2026-07-03

1. Synchronized checked-in Pages dist mirrors for the P51/P52 renderer pass owner extraction.
2. Added missing `dist/app/js/core/map_renderer/render_pass_cache_host_owner.js` and `dist/app/js/core/map_renderer/render_pass_commit_accounting_owner.js`.
3. Updated `dist/app/js/core/map_renderer.js` and `dist/pages-dist-manifest.json` from the Pages dist builder.
4. Routed `js/core/map_renderer.js` and `js/core/map_renderer/**` source changes to the main-thread `verify:pages-dist` selector gate.
5. Cleaned integrated P51-P54 recovery worktrees and left P8A/SF-ATS lanes untouched.

Files: core generated dist `dist/app/js/core/map_renderer.js`, `dist/app/js/core/map_renderer/render_pass_cache_host_owner.js`, `dist/app/js/core/map_renderer/render_pass_commit_accounting_owner.js`, `dist/pages-dist-manifest.json`; tooling `tools/select_verification_targets.mjs`, `tools/test_route_registry.mjs`; tests `tests/test_e2e_structural_tooling.py`; docs this registry.

Diff summary: production source is unchanged in this audit patch. The checked-in Pages mirror now matches `js/core/map_renderer.js` and the two new P51/P52 owner modules. The verification selector now treats renderer source mirror files as Pages dist sources, so future renderer module edits select `verify:pages-dist` even before dist files are touched.

Commit status: ready for Lore commit on `codex/audit-20260703-recent-render`, based on `origin/main@9fe96d54`. Parent checkout `main@16abfd5f` remains dirty and untouched.

Potential conflicts: red with P8A/SF-ATS edits to `package.json`, `tools/test_route_registry.mjs`, `tests/test_e2e_structural_tooling.py`, or this registry; green with renderer runtime source because this audit patch only changes generated dist mirrors and test routing.

Validation passed before commit: `npm run -s verify:pages-dist`; changed-file `node --check` for source/dist JS and tooling; selector JSON dry-run showed `verify:pages-dist` for `js/core/map_renderer.js`, `js/core/map_renderer/render_pass_cache_host_owner.js`, `dist/app/js/core/map_renderer/render_pass_cache_host_owner.js`, and `dist/pages-dist-manifest.json` with `unmatchedChangedFiles: []`; `npm run -s verify:architecture-boundaries`. `py -3 -m unittest tests.test_e2e_structural_tooling -q` initially failed in the isolated worktree because `@playwright/test` was only installed in the parent checkout; rerun with `NODE_PATH` pointing at the parent dependency tree is part of final validation.

Review results: code-reviewer returned REQUEST CHANGES for stale Pages dist mirror and selector routing; both findings are fixed here. Architect returned WATCH for future string-anchor maintenance cost in `tools/check_architecture_boundaries.mjs` and inventory tests; this audit records the risk without changing P51-P54 architecture boundaries.

## Branch Sync and Cleanup 2026-06-30

- Synced local remote metadata with `git fetch --all --prune`; local `main` and `origin/main` both point at `88a9f5a3`.
- Deleted 41 local merged branches. The lone local `-d` refusal, `codex/tno-coverage-chain-audit@ffab42b8`, was deleted with `-D` only after `merge-base --is-ancestor codex/tno-coverage-chain-audit main` proved it is contained in `main`.
- Deleted 64 remote branches whose refs were ancestors of `origin/main`. Recovery for those deleted branch refs is through `origin/main@88a9f5a3` history and the historical delivery notes below.
- Retained local branches: `codex/hgo-preview-projection-base-replace@7a95e26c`, `codex/wgi-post-push-truth-20260622@d802cfc8`, and recovery snapshot `codex/preserve-parent-wip-before-branch-cleanup-20260623@f64e3e62`.
- Retained remote branches: `origin/codex/hgo-preview-projection-base-replace@7a95e26c` and `origin/codex/tno-toponym-zh-audit@17de2d57`.
- Merge decision: no retained branch was merged during this cleanup. `git diff main..branch --stat` showed stale, very broad rollback surfaces for HGO/WGI/recovery branches, while TNO toponym work still has independent data commits that need a separate data-locale verification lane before integration.

## Integrated Worktree Closeout 2026-06-30

## Ready Delivery Packages

### Renderer Draw Canvas Orchestration Preflight P53 2026-07-02

1. Added a P53 active preflight doc that inventories `drawCanvas()` orchestration after P51/P52 landed.
2. Added a P53 Node inventory boundary test that keeps `drawCanvas()` and `renderPassToCache(` in `map_renderer.js`, locks P51/P52 owner boundaries, and keeps pipeline catalog, hit canvas, exact scheduler, scenario refresh, strategic overlay, public facade, state-write allowlist, and `dist/**` stable.
3. Added the named package script `test:node:renderer-draw-canvas-orchestration-inventory`.
4. Updated `tools/check_architecture_boundaries.mjs` with P53 doc/test/package/source-boundary gates and a semantic detector for accidental drawCanvas orchestration owner files during preflight.
5. Kept production runtime files unchanged.

Files: docs `docs/archive/renderer-frame-orchestration-p2-20260710/renderer-draw-canvas-orchestration-preflight-20260702.md` and this registry; tests `tests/renderer_draw_canvas_orchestration_inventory_boundary.test.mjs`; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary files none.

Diff summary: P53 is docs/tests/tooling-only. It adds one active preflight doc, one static inventory test, one package script, and architecture-boundary checks. `js/core/map_renderer.js`, `js/core/map_renderer/public.js`, `tools/eslint-rules/state-writer-allowlist.json`, and `dist/**` have no P53 diff. P51/P52 production owners remain unchanged.

Commit status: P53 Lore commit `24ab1264a9bb89e0c3f4b8b6cb26700592257edd` was pushed to `origin/main`.

Base status: P53 starts from P52 `origin/main@c60fd9239f8352b1916686b6dac8ee16eee8f017`. Parent checkout `main@16abfd5f` remains dirty and untouched. At P53 closeout time, the P54 click-selection worktree was paused on a stale pre-P51/P52 baseline; it has since been fast-forwarded to the P53 registry baseline and landed as P54.

Potential conflicts: red with future edits to `package.json`, `tools/check_architecture_boundaries.mjs`, `tests/renderer_draw_canvas_orchestration_inventory_boundary.test.mjs`, or this registry; yellow with future renderer draw orchestration implementation lanes; green with production runtime, public facade, state-write allowlist, and `dist/**`.

Validation passed: `node --check tests/renderer_draw_canvas_orchestration_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:renderer-draw-canvas-orchestration-inventory` `6/6`; `npm run test:node:render-pass-cache-host-owner-suite` `26/26`; `npm run test:node:render-pass-commit-accounting-owner` `10/10`; `npm run test:node:renderer-render-lifecycle-inventory` `8/8`; `npm run test:node:render-pipeline-catalog` `3/3`; `npm run test:node:render-transaction-diagnostics` `21/21`; `npm run test:node:exact-after-settle-refresh-plans` `9/9`; `npm run test:node:scenario-refresh-plans` `24/24`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:state-write-allowlist` with 115 tracked files; adaptive dry-run planned 162 commands; `git diff --check`.

Not run: browser/dev-server E2E, Playwright, Pages dist generation, and `verify:pages-dist`. P53 changes docs/tests/tooling only and explicitly leaves production runtime and checked-in dist unchanged.

Recommendation: land P53, then start the smallest implementation boundary: a drawCanvas orchestration owner that only selects pass groups and delegates existing helpers. Do not start by moving individual pass drawing functions.

### Renderer Click Selection Transaction Preflight P54 2026-07-02

1. Added a P54 active preflight doc that inventories current map click, selection, fill, water, special region, and dev workspace interaction transactions.
2. Added a P54 Node inventory boundary test that locks the click handler in `map_renderer.js`, injected event binding, pure hit candidates, hover-only P48 owner, current land/water/special/dev branches, history/dirty/render refresh paths, public facade stability, state-write allowlist stability, and absence of a click-selection owner.
3. Added the named package script `test:node:renderer-click-selection-transaction-inventory`.
4. Updated `tools/check_architecture_boundaries.mjs` with P54 doc/test/package/source-boundary gates and a detector for accidental click-selection transaction owner/helper/controller/adapter files during preflight.
5. Kept production runtime files unchanged.

Files: docs `docs/active/renderer-click-selection-transaction-preflight-20260702.md` and this registry; tests `tests/renderer_click_selection_transaction_inventory_boundary.test.mjs`; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary files none.

Diff summary: P54 is docs/tests/tooling-only. It adds one active preflight doc, one static inventory test, one package script, and architecture-boundary checks. `js/core/map_renderer.js`, `js/core/map_renderer/public.js`, `tools/eslint-rules/state-writer-allowlist.json`, and `dist/**` have no P54 diff.

Commit status: P54 Lore commit `e747930dfb06dd080c325dc7ac44f17d998e3e32` was pushed to `origin/main`.

Base status: P54 was fast-forwarded from stale branch base `2354adb1` to P53 registry closeout baseline `origin/main@7e503bb3b9596b5066f3e845f28c539c810fd735` before edits. Parent checkout `main@16abfd5f` remains dirty and untouched. Current P54 worktree is a clean recovery worktree after the main commit.

Potential conflicts: red with future edits to `package.json`, `tools/check_architecture_boundaries.mjs`, `tests/renderer_click_selection_transaction_inventory_boundary.test.mjs`, or this registry; yellow with future click/selection owner implementation lanes; green with production runtime, public facade, state-write allowlist, and `dist/**`.

Validation passed: `node --check tests/renderer_click_selection_transaction_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:renderer-click-selection-transaction-inventory` `7/7`; `npm run test:node:map-hover-interaction` `19/19`; `npm run test:node:map-interaction-event-binding-owner` `6/6`; `npm run test:node:interaction-hit-candidates` `5/5`; `npm run test:node:dev-workspace-selection-ownership` `2/2`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:state-write-allowlist` with 115 tracked files; adaptive dry-run planned 163 commands; production runtime/public/state/dist diff check returned no files; `git diff --check`.

Not run: browser/dev-server E2E, Playwright, Pages dist generation, and `verify:pages-dist`. P54 changes docs/tests/tooling only and explicitly leaves production runtime and checked-in dist unchanged.

Recommendation: start P55/P56 with water/special selection clearing or the dev-selection click transaction. Keep land fill, sovereignty fill, water fill, special region selection, and dev selection split across small owner moves.

### Recent Platform Audit Selector Fix 2026-07-02

1. Audited recent related functional commits after the last audit baseline `c20cc67f`, covering P47 hit canvas scheduling, Phase 6 sample export/public demo work, P48 map hover interaction, P49 renderer transaction reset, and P50 render pass cache host preflight.
2. Confirmed the renderer owner ordering, sample import/export state flow, and Pages release wiring have no production-code blocker in this audit scope.
3. Fixed the concrete selector bug where checked-in Pages dist mirrors under `dist/app/**` could be reported as `unmatchedChangedFiles` instead of routing to the Pages dist gate.
4. Added a structural tooling regression that locks `dist/app/**` and `dist/pages-dist-manifest.json` to `verify:pages-dist` with main-thread `dist` and `.runtime-output` locks.
5. Left the SF-ATS worktree as a separate ready-for-integration lane and recorded P51 render pass cache host production extraction as future owner work.

Files: core tooling `tools/select_verification_targets.mjs`; tests `tests/test_e2e_structural_tooling.py`; docs `docs/active/_worktree_registry.md` and `lessons learned.md`; temporary files none.

Diff summary: selector-only functional fix. `isCheckedInPagesDistFile()` now maps checked-in Pages dist payload files to `infra:pages-dist` before direct source matching; the regression proves the route is matched, has no unmatched files, and remains main-thread serial with the expected resource locks. Runtime source, generated dist, package scripts, and sample payloads are unchanged.

Commit status: this delivery package travels with the audit fix commit on branch `codex/audit-20260702-recent-platform`, based on `origin/main@19d96a1507e46382c9a9d16be5d80530bc4b5024`. Parent `main@16abfd5f` remains dirty and was preserved untouched. The branch is intended to fast-forward `origin/main`, then the audit worktree and the already integrated P47-P50 renderer worktrees can be removed with recovery through their branches and commits listed in the historical sections below.

Potential conflicts: green for runtime, sample payload, generated dist, and package scripts; yellow with parent/SF-ATS registry or lessons edits; red only for future selector/test-routing edits.

Validation passed: `node --check tools/select_verification_targets.mjs`; targeted new unittest; selector dry-run for recent renderer/sample/dist files with `unmatchedChangedFiles: []`; selector dry-run for the changed selector/test files; `node tools/select_verification_targets.mjs --check`; `py -3 -m unittest tests.test_e2e_structural_tooling -q` with `NODE_PATH` pointing at the parent checkout dependency tree; `npm run -s verify:test-import-graph`; `npm run -s verify:test:e2e-layers`; `npm run -s test:node:sample-project-contracts`; `npm run -s test:node:hit-canvas-scheduling-owner-suite`; `npm run -s test:node:map-hover-interaction-owner`; `npm run -s test:node:renderer-transaction-reset-owner`; `npm run -s test:node:map-hover-interaction`; `npm run -s test:node:renderer-transaction-reset`; `npm run -s test:node:renderer-render-pass-cache-host-inventory`; `npm run -s test:node:post-ready-scheduler`; `npm run -s verify:architecture-boundaries`; `git diff --check`.

Not run: browser smoke, Playwright page inspection, `verify:pages-dist`, and `verify:dist-drift`; this patch changes selector/test docs only and does not modify checked-in dist outputs. The selector dry-run proves any future `dist/app/**` diff now selects `verify:pages-dist`.

Review results: code-reviewer found one medium selector route gap and no high/critical blocker; the route gap is fixed here. Architect returned WATCH for the known future P51 render pass cache host production extraction; it is outside this selector fix and remains a next owner lane.

Recommended next step: push this audit fix to `origin/main`, remove the audit worktree after remote confirmation, remove the already integrated local P47/P48/P49/P50/renderer-hit-canvas worktrees, and keep `C:\Users\raede\Desktop\dev\mapcreator-sfats-wp1` for a dedicated SF-ATS integration pass.

### P7 And Phase 6E Worktree Cleanup 2026-07-02

- Removed local worktree registrations for `C:\Users\raede\.codex\worktrees\mapcreator-p7-public-demo-release-20260702` and `C:\Users\raede\.codex\worktrees\mapcreator-phase6e-public-demo-qa-readiness` after P7 closeout commit `2c72e17ce76d9333673bc1ff65d8104630a511da` was pushed.
- Removed the residual `node_modules` junction-only directories after verifying both junctions targeted `C:\Users\raede\Desktop\dev\mapcreator\node_modules` and the target still existed afterward.
- Recovery for P7 remains branch `codex/p7-public-demo-release-20260702`, closeout commit `2c72e17ce76d9333673bc1ff65d8104630a511da`, release package commit `9f8f4be2d50a315554f97c785fcf1c174ad1a76d`, and tag `v0.1-public-demo`.
- Recovery for Phase 6E remains branch `codex/phase6e-public-demo-qa-readiness`, functional commit `72eb36d35148927ceb40156366cac3407cac4b80`, and closeout commit `bcc00b62a1beac38d3a0d7978ddf282897f97273`.
- Current `git worktree list` no longer includes either removed path.

### P7 v0.1 Public Demo Release Packaging 2026-07-02

1. Finalized the public demo release facts, release notes, deployed Pages evidence, and P8 follow-up list.
2. Archived the completed Phase 6E validation docs and the completed P7 task docs as historical release evidence.
3. Kept runtime, sample payloads, generated dist, README files, and production UI code unchanged during P7.
4. Published the v0.1 public demo as tag `v0.1-public-demo` and GitHub pre-release https://github.com/raederhans/scenario-forge/releases/tag/v0.1-public-demo.
5. Preserved parent checkout WIP untouched.

Files: release docs `docs/releases/v0.1-public-demo.md`, `docs/releases/v0.1-public-demo-release-notes.md`, `docs/releases/v0.1-public-demo-draft.md`; archived docs `docs/archive/p7-public-demo-release-20260702/**`, `docs/archive/phase6e-public-demo-qa-readiness-20260701/**`; active follow-up `docs/active/p8-public-demo-followups-20260702.md`; registry `docs/active/_worktree_registry.md`; lessons `lessons learned.md`.

Diff summary: release documentation and registry-only closeout. P7 added canonical release facts, release notes, a P8 follow-up backlog, and archived completed active docs. Product runtime, checked-in sample data, and Pages dist outputs have no P7 functional diff.

Commit status: release package commit `9f8f4be2d50a315554f97c785fcf1c174ad1a76d` is pushed to `origin/main` and `origin/codex/p7-public-demo-release-20260702`; this closeout commit records post-release evidence. Base `origin/main@bcc00b62a1beac38d3a0d7978ddf282897f97273` was current when P7 started; P7 release package is now tagged as `v0.1-public-demo`.

Potential conflicts: red with future registry or release-doc edits; yellow with P8 release-smoke/deploy-workflow guard work and parent README/package WIP; green with renderer/runtime/product code.

Validation passed: `npm run verify:pages-dist`; `npm run verify:dist-drift`; `py -3 tools\i18n_audit.py`; `npm run test:e2e:sample-guide`; local `/dist/` release gate; `npm run verify:test-import-graph`; `git diff --check`; independent code-review and architecture review; GitHub Pages workflow `28561116483` attempt 2 deployed smoke `1 passed`.

Remaining risk: Pages smoke can hit a short propagation window immediately after deployment. P8 tracks a propagation-aware guard.

Recommended next step: run P8 as a separate workflow/deploy hardening task when release-smoke propagation handling becomes the next priority.

### Renderer Integration Sweep After P47-P50 2026-07-01

1. Audited the integrated renderer owner chain from P47 hit canvas scheduling through P50 render pass cache host preflight without starting a new owner extraction.
2. Confirmed package scripts, architecture checker file inventory/line budgets, state-write allowlist, public facade, test import graph, and dist policy are aligned with current `origin/main`.
3. Corrected stale registry rows for already pushed P48/P50 wording and the current P47/P49/P50/Phase6E worktree facts.
4. Kept renderer runtime, package scripts, architecture checker, state-write allowlist, public facade, test import graph, and `dist/**` unchanged in this sweep.

Files: docs only, `docs/active/_worktree_registry.md`; temporary validation log `.runtime/tests/renderer-integration-sweep-p47-p50-20260701.final2.log`.

Diff summary: registry-only sync from P50 closeout baseline `b76b782b76c2a45bd7fd5f4ecaf6e37bd4e90aba`. P47/P48/P49/P50 production/runtime owner chain, package scripts, checker gates, state-write allowlist, public facade, test import graph, and `dist/**` were audited and left unchanged.

Validation passed: `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:state-write-allowlist` with 115 tracked files; `npm run test:node:renderer-set-map-data-transaction`; `npm run test:node:renderer-render-request-boundary`; `npm run test:node:renderer-render-phase-lifecycle`; `npm run test:node:visible-frame-diagnostics`; `npm run test:node:hit-canvas-scheduling-owner-suite`; `npm run test:node:render-transaction-diagnostics`; `npm run test:node:scenario-refresh-plans`; `npm run test:node:exact-after-settle-refresh-plans`; `git diff --check`; `git diff --cached --check`. `dist/**` has no P47-P50 diff, so `verify:pages-dist` and `verify:dist-drift` were not required for this sweep.

### Renderer Render Pass Cache Host Preflight P50 2026-07-01

1. Added a P50 preflight doc that inventories the current `renderPassToCache(` host surface and locks the next-step guardrails.
2. Added a P50 inventory boundary test that keeps `renderPassToCache(` and `drawCanvas()` in `map_renderer.js`, locks current host/cache/timing/draw-callback tokens, and keeps P41/P42/P43/P47 owners out of render pass host ownership.
3. Added `test:node:renderer-render-pass-cache-host-inventory` and integrated the same checks into `verify:architecture-boundaries`.
4. Kept all production runtime files, public facade, state-write allowlist, and `dist/**` untouched.
5. Confirmed P51 can proceed as a narrow render pass cache host adapter owner focused on pass canvas/context acquisition and target context preparation while delegating existing `drawFn(k)` behavior.

Files: docs `docs/active/renderer-render-pass-cache-host-preflight-20260701.md` and this registry; tests `tests/renderer_render_pass_cache_host_inventory_boundary.test.mjs`; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary log `.runtime/tests/p50-validation-20260701.log`.

Diff summary: P50 is docs/tests/tooling-only. It adds one preflight doc, one static Node inventory test, one package script, and P50 architecture-boundary assertions. `js/core/map_renderer.js`, `js/core/map_renderer/public.js`, `js/core/renderer/render_cache_owner.js`, `js/core/renderer/render_pipeline_passes.js`, `js/core/renderer/render_transform_reuse_policy_owner.js`, `js/core/map_renderer/render_request_boundary_owner.js`, `js/core/map_renderer/render_phase_lifecycle_owner.js`, `js/core/renderer/visible_frame_diagnostics_owner.js`, `js/core/map_renderer/hit_canvas_scheduling_owner.js`, `js/core/map_renderer/scenario_refresh_runtime.js`, `js/core/map_renderer/exact_after_settle_scheduler.js`, `tools/eslint-rules/state-writer-allowlist.json`, and `dist/**` have no P50 diff.

Commit status: P50 Lore commit `3e19bd0f614ff91b3dc8c8955989ce69461dbe89` was pushed to `origin/codex/p50-render-pass-cache-host-preflight-20260701` and fast-forwarded to `origin/main`; base and pre-integration `origin/main` were both `b91c92a9266d0aa4f88af244517250bea88d4244`. Potential conflicts: red with future renderer lanes touching `package.json`, `tools/check_architecture_boundaries.mjs`, P50 inventory test, or this registry; yellow with P51 render pass cache host adapter implementation; green with P50 forbidden production/runtime/public/state/dist paths because the diff and untracked scan returned only allowed P50 files.

Validation passed: `node --check tests/renderer_render_pass_cache_host_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:renderer-render-pass-cache-host-inventory` `8/8`; `npm run test:node:renderer-render-lifecycle-inventory` `8/8`; `npm run test:node:renderer-render-request-boundary` `13/13`; `npm run test:node:renderer-render-phase-lifecycle` `17/17`; `npm run test:node:visible-frame-diagnostics` `14/14`; `npm run test:node:hit-canvas-scheduling-owner-suite` `32/32`; `npm run test:node:render-cache-owner` `6/6`; `npm run test:node:render-transform-reuse-policy-owner` `7/7`; `npm run test:node:render-pipeline-catalog` `3/3`; `npm run test:node:render-transaction-diagnostics` `21/21`; `npm run test:node:exact-after-settle-refresh-plans` `9/9`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:state-write-allowlist` with 115 tracked files; `git diff --check`. Log: `.runtime/tests/p50-validation-20260701.log`.

Review closeout: self-review found the simpler boundary is the P51 host adapter, not another broad lifecycle preflight. P51 should manage pass canvas/context acquisition and target context preparation around the existing draw callback; it should not move `drawCanvas()`, render pass drawing functions, hit canvas build, scenario refresh runtime, exact scheduler, strategic runtime/render, public facade, state-write allowlist, or `dist/**`. No P50 integration action remains after this registry closeout is pushed.

### Renderer Transaction Reset Owner P49 2026-07-01

1. Added `createRendererTransactionResetOwner(...)` as the only production owner for reset sequencing shared by `resetRendererTransactionState`, `resetRendererRefreshTransactionState`, and `markRendererTopologyChanged`.
2. Kept all three wrapper names in `map_renderer.js` and kept runtime/module-local writes there as injected effects.
3. Preserved refresh-before-topology order, cancel flags, `renderer-refresh-reset` reason, P47 hit canvas cancellation routing, optional dirty hit canvas behavior, topology revision bump, and hit canvas topology revision reset.
4. Kept setMapData, startup, scenario refresh, exact scheduler, render/draw/pass/hit-build/strategic/public/state-write/dist boundaries stable.
5. Added P49 behavior/inventory coverage, package scripts, architecture-boundary gates, and P47 inventory alignment for the moved refresh-reset cancellation path.

Files: core `js/core/map_renderer.js`, `js/core/map_renderer/renderer_transaction_reset_owner.js`; tests `tests/renderer_transaction_reset_owner_behavior.test.mjs`, `tests/renderer_transaction_reset_hardening_inventory_boundary.test.mjs`, `tests/hit_canvas_scheduling_owner_inventory.test.mjs`, `tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs`; docs `docs/active/renderer-transaction-reset-owner-p49-20260701.md` and this registry; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary logs under `.runtime/tests/`.

Diff summary: P49 extracts only reset sequencing into a 183-line owner and leaves the wrappers as composition-root delegates. `map_renderer.js` injects effects for timer/UI refresh clearing, phase reset, diagnostics/staged-task clearing, exact-after-settle cancellation, P47 hit canvas cancellation, optional secondary spatial cancellation, defer flags, layer resolver/dev state resets, physical land clip reset, exact optimization reset, visible internal border signature reset, topology revision bump, hit canvas dirty flag, and hit canvas topology revision reset. The 228-line behavior test locks order, options, frozen summaries, missing dependency fail-fast, and forbidden tokens.

Commit status: functional Lore commit `9c9996ccebda7ac5e8ffab71753da0f7a762eb78` was pushed to `origin/codex/p49-renderer-transaction-reset-owner-20260701` and fast-forwarded to `origin/main`; base and pre-integration `origin/main` were both `1fc19e97bd102083f4739d8053d306504ad39d13`. Potential conflicts: red with future renderer owner lanes touching `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, P49 tests, P47 inventory tests, or this registry; yellow with Phase6E registry WIP; green with `js/core/map_renderer/public.js`, `js/core/map_renderer/set_map_data_transaction_owner.js`, `js/core/renderer/renderer_startup_transaction_owner.js`, `js/core/map_renderer/scenario_refresh_runtime.js`, `js/core/map_renderer/exact_after_settle_scheduler.js`, `js/core/map_renderer/hit_canvas_scheduling_owner.js`, `tools/eslint-rules/state-writer-allowlist.json`, and `dist/**` because forbidden-path diff scan returned empty.

Validation passed: `node --check js/core/map_renderer/renderer_transaction_reset_owner.js`; `node --check js/core/map_renderer.js`; `node --check tests/renderer_transaction_reset_owner_behavior.test.mjs`; `node --check tests/renderer_transaction_reset_hardening_inventory_boundary.test.mjs`; `node --check tests/hit_canvas_scheduling_owner_inventory.test.mjs`; `node --check tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:renderer-transaction-reset-owner` `10/10`; `npm run test:node:renderer-transaction-reset-hardening-inventory` `9/9`; `npm run test:node:renderer-transaction-reset` `19/19`; `npm run test:node:renderer-set-map-data-transaction` `18/18`; `npm run test:node:renderer-startup-transaction-owner` `8/8`; `npm run test:node:renderer-startup-transaction-inventory` `9/9`; `npm run test:node:hit-canvas-scheduling-owner-suite` `32/32`; `npm run test:node:renderer-render-phase-lifecycle` `17/17`; `npm run test:node:render-transaction-diagnostics` `21/21`; `npm run test:node:scenario-refresh-plans` `24/24`; `npm run test:node:exact-after-settle-refresh-plans` `9/9`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:state-write-allowlist` with 115 tracked files; `git diff --check`. Log: `.runtime/tests/p49-validation-20260701.log`.

Review closeout: self-review confirmed the reset owner has no `runtimeState`, render/draw/pass/hit-build/scenario/exact/strategic tokens; `scenario_refresh_runtime.js` still receives reset by injection; exact scheduler and startup/setMapData owners remain independent; no public facade, state-write allowlist, or `dist/**` migration occurred. Not run: browser/dev-server E2E and Pages dist generation because P49 is a Node/static reset-sequencing extraction and explicitly leaves `dist/**` unchanged. No P49 integration action remains after this registry closeout is pushed; recovery remains through branch `codex/p49-renderer-transaction-reset-owner-20260701` and functional commit `9c9996ccebda7ac5e8ffab71753da0f7a762eb78`.

### Renderer Map Hover Interaction Owner P48 2026-07-01

1. Added `createMapHoverInteractionOwner(...)` as the only production owner for `handleMouseMove(event)` hover, tooltip, cursor, and hover-overlay orchestration.
2. Kept `function handleMouseMove(event)` in `map_renderer.js` as the stable wrapper and kept all runtime writes in `map_renderer.js` through injected effects.
3. Preserved HGO runtime hover, special-zone editor clearing, reduced-hover clearing, land/water/special hover id transitions, facility/city/feature tooltip priority, cursor updates, and hover overlay scheduling order.
4. Updated old HGO/facility Python contract methods to follow the moved hover tokens while keeping click/facility card behavior anchored in `map_renderer.js`.
5. Kept click/double-click, selection/fill, brush/physical-intensity, hit canvas, render/draw/pass, scenario refresh, exact scheduler, strategic runtime, public facade, state-write allowlist, and `dist/**` out of scope.

Files: core `js/core/map_renderer.js`, `js/core/map_renderer/map_hover_interaction_owner.js`; tests `tests/map_hover_interaction_owner_behavior.test.mjs`, `tests/map_hover_interaction_owner_inventory.test.mjs`, `tests/test_runtime_hooks_boundary_contract.py`, `tests/test_transport_facility_interactions_contract.py`; docs `docs/active/renderer-map-hover-interaction-owner-p48-20260701.md` and this registry; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary files ignored under `.runtime/tests/`.

Diff summary: functional commit `f9b0a2691058cc7e3855cf49fade5c798c23530e` changes `js/core/map_renderer.js` (+73/-133), `package.json` (+3), `tests/test_runtime_hooks_boundary_contract.py` (+8/-4), `tests/test_transport_facility_interactions_contract.py` (+17/-4), and `tools/check_architecture_boundaries.mjs` (+165), plus new P48 owner/doc/behavior/inventory files. Owner file is 255 diff lines and passes the 260-line architecture budget. Commit status: functional commit was pushed to `origin/codex/p48-map-hover-interaction-owner-20260701` and fast-forwarded to `origin/main`; base and pre-integration `origin/main` were both `0d6667f58b111d23d728c03733c553370b253d07`.

Potential conflicts: red with future edits to `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, P48 tests, and this registry; yellow with Phase6E registry WIP; green with `js/core/map_renderer/public.js`, `tools/eslint-rules/state-writer-allowlist.json`, `dist/**`, `js/core/map_renderer/hit_canvas_scheduling_owner.js`, `js/core/map_renderer/scenario_refresh_runtime.js`, `js/core/map_renderer/exact_after_settle_scheduler.js`, `js/core/map_renderer/interaction_hit_candidates.js`, and `js/core/renderer/map_interaction_event_binding_owner.js` because forbidden-path diff scan returned empty.

Validation passed: `node --check js/core/map_renderer/map_hover_interaction_owner.js`; `node --check js/core/map_renderer.js`; `node --check tests/map_hover_interaction_owner_behavior.test.mjs`; `node --check tests/map_hover_interaction_owner_inventory.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:map-hover-interaction-owner` `12/12`; `npm run test:node:map-hover-interaction-inventory` `7/7`; `npm run test:node:map-hover-interaction` `19/19`; `npm run test:node:map-interaction-event-binding-owner` `6/6`; `npm run test:node:interaction-hit-candidates` `5/5`; `npm run test:node:hit-canvas-scheduling-owner-suite` `32/32`; `npm run test:node:renderer-render-lifecycle-inventory` `8/8`; `npm run test:node:renderer-render-request-boundary` `13/13`; `npm run test:node:renderer-render-phase-lifecycle` `17/17`; `npm run test:node:visible-frame-diagnostics` `14/14`; `npm run test:node:scenario-chunk-contracts` `57/57`; `npm run test:node:scenario-refresh-plans` `24/24`; `npm run test:node:exact-after-settle-refresh-plans` `9/9`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:state-write-allowlist` with 115 tracked files; `git diff --check`. Additional focused Python contracts passed for HGO hover hook migration and facility hover/card wiring. Log: `.runtime/tests/p48-validation-final-20260701.log`.

Review closeout: Noether returned WATCH with the same owner boundary recommendation and flagged old HGO/facility contract tests; the contracts were updated and their touched methods passed. Self-review checked behavioral parity against `origin/main:js/core/map_renderer.js`, confirmed P48 keeps writes in injected effects, confirmed forbidden paths have no diff, and found no simpler lower-risk implementation than the current owner plus wrapper.

Not run: optional browser/dev-server E2E and Pages dist generation. P48 explicitly leaves `dist/**` unchanged and the required Node/static/scenario contract suite covers the changed mousemove hover path. No P48 integration action remains; recovery remains through branch `codex/p48-map-hover-interaction-owner-20260701`, functional commit `f9b0a2691058cc7e3855cf49fade5c798c23530e`, registry closeout commit `22066cb859bc6687086cf1687dc9c872d22c3acb`, and final wording commit `1fc19e97bd102083f4739d8053d306504ad39d13`.

### Renderer Hit Canvas Scheduling Owner P47 2026-07-01

1. Added `createHitCanvasSchedulingOwner(...)` as the only production hit canvas scheduling owner. It owns deferred schedule gates, duplicate-handle guard, scheduled-handle write/clear lifecycle, scheduled callback dispatch, and cancellation.
2. Kept `scheduleHitCanvasBuildIfNeeded({ reason = "idle-render" } = {})` in `map_renderer.js` as the stable wrapper returning `false`, and routed strict validation/reset cancellation through the owner.
3. Kept hit canvas draw/build/point-probe/dirty-source/topology writes in `map_renderer.js`; kept scenario refresh runtime on injected scheduling; kept exact scheduler, spatial owner, interaction owner, public facade, state-write allowlist, and `dist/**` unchanged.
4. Added owner behavior/inventory tests, flipped the existing hit canvas scheduling inventory to P47 owner state, added package scripts, and updated `tools/check_architecture_boundaries.mjs`.
5. Removed the review-flagged unused clear-only public API and added behavior coverage for an empty scheduled-handle return from `scheduleDeferredWork`.

Files: core `js/core/map_renderer.js`, `js/core/map_renderer/hit_canvas_scheduling_owner.js`; tests `tests/hit_canvas_scheduling_owner_behavior.test.mjs`, `tests/hit_canvas_scheduling_owner_inventory.test.mjs`, `tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs`; docs `docs/active/renderer-hit-canvas-scheduling-owner-p47-20260701.md` and this registry; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary files none.

Diff summary: tracked P47 diff from merge-base `c20cc67f481a3b635dd00d52a64a70c51db021fb` changes `js/core/map_renderer.js` (+48/-23), `package.json` (+3), `tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs` (+165/-281 after review fix), and `tools/check_architecture_boundaries.mjs` (+157/-104), plus new P47 owner/doc/tests files. Owner file is 155 lines, under the 220-line architecture budget. Commit status: source branch commit `715db7bbd96527f05b7f69ed601c4a79b2162faa` is integrated into `origin/main` by merge commit `a2131698a353aed4a7688c14aa63b80a92d651b5` on top of `bb75c0cc5fc9c38ddb076674151a3f17135c5ec7`; closeout commit `acf55f8425b332ef618efe56ef4c701be922a075` recorded final validation truth and was confirmed on `origin/main`.

Potential conflicts: red with future edits to `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, `tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs`, and this registry; yellow with future renderer owner extraction lanes and scenario refresh scheduling semantics; green with `js/core/map_renderer/public.js`, `tools/eslint-rules/state-writer-allowlist.json`, `dist/**`, `js/core/map_renderer/scenario_refresh_runtime.js`, `js/core/map_renderer/exact_after_settle_scheduler.js`, `js/core/renderer/spatial_index_runtime_owner.js`, `js/core/map_renderer/interaction_hit_candidates.js`, and `js/core/renderer/map_interaction_event_binding_owner.js` because forbidden-path diff scan returned empty.

Validation passed after integration: `node --check js/core/map_renderer/hit_canvas_scheduling_owner.js`; `node --check js/core/map_renderer.js`; `node --check tests/hit_canvas_scheduling_owner_behavior.test.mjs`; `node --check tests/hit_canvas_scheduling_owner_inventory.test.mjs`; `node --check tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:hit-canvas-scheduling-owner` `13/13`; `npm run test:node:hit-canvas-scheduling-owner-inventory` `9/9`; `npm run test:node:renderer-hit-canvas-scheduling-inventory` `10/10`; `npm run test:node:hit-canvas-scheduling-owner-suite` `32/32`; `npm run test:node:renderer-render-lifecycle-inventory` `8/8`; `npm run test:node:renderer-render-request-boundary` `13/13`; `npm run test:node:renderer-render-phase-lifecycle` `17/17`; `npm run test:node:visible-frame-diagnostics` `14/14`; `npm run test:node:interaction-hit-candidates` `5/5`; `npm run test:node:scenario-chunk-contracts` `57/57`; `npm run test:node:scenario-refresh-plans` `24/24`; `npm run test:node:exact-after-settle-refresh-plans` `9/9`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:state-write-allowlist` with 115 tracked files; `git diff --check`. Log: `.runtime/tests/p47-integration-validation-20260701T174436.log`.

Review closeout: Ralplan architect returned WATCH and the plan was hardened; second critic approved implementation. Code-reviewer returned COMMENT with one LOW simplification, fixed by removing `clearScheduledHitCanvasBuildHandle()`. Architect returned CLEAR. Verifier returned PARTIAL and requested empty-handle coverage plus an execution-level wrapper proof; empty-handle coverage is added, while the wrapper remains an internal `map_renderer.js` function with a closed public facade, so coverage is supplied by owner behavior tests, map-renderer source-boundary inventory, architecture checker gates, and scenario/refresh contract tests.

Not run: optional browser/dev-server E2E smoke and Pages dist generation. P47 explicitly leaves `dist/**` unchanged and the required Node/static/scenario contract suite covers the changed scheduling path. Recommended next step: after this post-push registry truth commit lands, remove the temporary P47 integration worktree and optionally remove the now-integrated P47 branch worktree, keeping branch/commit recovery references above.

### Phase6E Public Demo QA Readiness 2026-07-01

1. Added public demo QA hardening for sample Guide choices, Export sample context, Project banner focus, mobile Guide actions, landing sample cards, and landing proof layout.
2. Preserved user-visible sample deeplinks through post-ready chunk backlog by allowing the startup sample task to run while infra chunk work is queued.
3. Extended existing coverage for default Guide, TNO sample focus/modal/dirty-switch paths, Chinese UI, 375/768/desktop app widths, 375px landing cards, and the Pages release gate.
4. Regenerated checked-in Pages dist and recorded the Phase 6E size snapshot as `972144112` bytes / `927.11 MiB`.
5. Kept the parent checkout WIP untouched; local runtime artifacts stayed under ignored `.runtime/` and the ignored `node_modules` junction.

Files: core `index.html`, `css/style.css`, `js/bootstrap/post_ready_scheduler.js`, `js/bootstrap/startup_sample_project_deeplink.js`, `js/ui/toolbar.js`, `js/ui/toolbar/sample_project_banner_controller.js`, `landing/index.html`, `landing/styles.css`; tests `tests/sample_project_contracts.test.mjs`, `tests/post_ready_scheduler_behavior.test.mjs`, `tests/e2e/sample_guide_deeplink.spec.js`, `tests/e2e/release/pages_public_release_gate.spec.js`; docs `docs/releases/v0.1-public-demo-draft.md`, archived `docs/archive/phase6e-public-demo-qa-readiness-20260701/*`, `lessons learned.md`, and this registry; generated mirrors `dist/**`, including P47/P48/P49 renderer owner mirrors; temporary files ignored under `.runtime/**` and `node_modules/`.

Diff summary: UI/accessibility changes add explicit grouped/pressed/status semantics, focus restoration after Guide-triggered Export and sample switching, responsive wrapping for sample/export/banner actions, and a scheduler option for user-visible post-ready tasks. Tests extend existing sample/release gates without adding a new test system. Commit status: Lore commit on branch `codex/phase6e-public-demo-qa-readiness`, rebased onto `origin/main@58e3935b`; parent checkout remains at `main@16abfd5f` with unrelated WIP. Potential conflicts: red with future sample Guide/export/landing/release-smoke edits; yellow with registry/lessons; green with P50 production runtime files.

Validation passed: changed-file `node --check`; `npm run test:node:post-ready-scheduler` `11/11`; `npm run test:node:sample-project-contracts` `17/17`; `py -3 tools\i18n_audit.py` with `ui_missing=0` and `ui_english_fallback=0`; pre-rebase local dist release gate at `http://127.0.0.1:8920/` `1/1`; pre-rebase `npm run verify:test-import-graph` with 51 specs; pre-rebase `npm run verify:test:e2e-layers` with 47 specs. Post-P49 validation passed: `npm run verify:pages-dist` with startup shell `41/41`, landing showcase `18/18`, sample contracts `17/17`, and size `972144112` bytes / `927.11 MiB`; final `npm run test:e2e:sample-guide` `5/5`; final local dist release gate at `http://127.0.0.1:8924/` `1/1` with sample status `success`, scenario `tno_1962`, and HGO disabled; `npm run verify:test-import-graph` with 51 specs; `npm run verify:test:e2e-layers` with 47 specs. Post-b76 validation passed: changed-file `node --check`; `npm run test:node:post-ready-scheduler` `11/11`; `npm run test:node:sample-project-contracts` `17/17`; `npm run verify:dist-drift` with size `927.11 MiB`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:test:e2e-layers` with 47 specs; `npm run verify:architecture-boundaries`. Post-58e3935b validation passed: changed-file `node --check`; `npm run test:node:post-ready-scheduler` `11/11`; `npm run test:node:sample-project-contracts` `17/17`; `py -3 tools\i18n_audit.py` with `ui_missing=0` and `ui_english_fallback=0`; `npm run verify:dist-drift` with size `927.11 MiB`; `npm run verify:test-import-graph` with 51 specs; `npm run verify:test:e2e-layers` with 47 specs; `npm run verify:architecture-boundaries`; `git diff --check`; `npm run test:e2e:sample-guide` `5/5`. The Pages dist synchronized `dist/app/js/core/map_renderer.js`, `dist/app/js/core/map_renderer/hit_canvas_scheduling_owner.js`, `dist/app/js/core/map_renderer/map_hover_interaction_owner.js`, and `dist/app/js/core/map_renderer/renderer_transaction_reset_owner.js` with the latest renderer baseline. Remaining integration gate: remote deployed Pages smoke after this branch lands. Recommended next step: push this branch, fast-forward it to `origin/main`, then start P7 release packaging from the updated baseline.

### Audit Automation 2026-07-01

1. Fixed the Guide sample card so a public sample-list load failure remains visible even when no sample choices loaded yet.
2. Fixed `resetRenderPhaseState` so resetting the renderer phase to idle also clears the coupled `isInteracting` flag through the lifecycle owner.
3. Registered `sample_guide_deeplink.spec.js` in the E2E layer manifest, regenerated layer lists, and added sample runtime selector routing to recommend both node contracts and the sample Guide E2E.
4. Regenerated Pages dist mirrors after rebasing over the comment-maintenance commit.
5. Left parent checkout WIP untouched and kept local runtime artifacts under ignored `.runtime/` / `node_modules/`.

Files: core `js/ui/toolbar/sample_project_banner_controller.js`, `js/core/map_renderer/render_phase_lifecycle_owner.js`; generated mirrors `dist/app/js/bootstrap/startup_sample_project_deeplink.js`, `dist/app/js/core/sample_project_import_workflow.js`, `dist/app/js/core/map_renderer/render_phase_lifecycle_owner.js`, `dist/app/js/ui/toolbar/sample_project_banner_controller.js`, `dist/pages-dist-manifest.json`; tests `tests/sample_project_contracts.test.mjs`, `tests/renderer_render_phase_lifecycle_owner_behavior.test.mjs`, `tests/e2e/test-layer-manifest.json`, `tests/e2e/test-lists/{all.txt,feature.txt}`, `tests/test_e2e_structural_tooling.py`; tooling `tools/e2e_layering.mjs`, `tools/select_verification_targets.mjs`, `tools/test-timeout-guardrail-allowlist.json`; docs this registry; temporary files ignored under `.runtime/` and `node_modules/`.

Diff summary: one functional commit `edee9a9f` is ahead of `origin/main@b06d4624`, with 16 tracked files changed and 207 insertions / 58 deletions. Commit status: ready to push from `codex/audit-20260701-recent-samples`; registry closeout follows in this docs commit. Base divergence: branch rebased cleanly over `origin/main@b06d4624`. Potential conflicts: yellow with future sample Guide/import workflow and E2E routing edits; yellow with renderer phase owner follow-ups; green with parent checkout WIP because no parent files were modified.

Validation passed: code-review subagent returned CLEAR; architect subagent returned CLEAR; test-engineer found the missing E2E layer registration, which is fixed. Commands passed: `node --test tests/sample_project_contracts.test.mjs tests/renderer_render_phase_lifecycle_owner_behavior.test.mjs` `27/27`; changed-file `node --check`; `npm run verify:test:e2e-layers` with 47 manifest specs; `npm run verify:test-import-graph` with 51 specs; `node tools/select_verification_targets.mjs --check` with 229 routes; `node tools/check_test_timeout_guardrails.mjs`; `npm run verify:architecture-boundaries`; `npm run verify:pages-dist` after rebase; `npm run test:e2e:sample-guide` `2/2`; `git diff --check`; `git show --check`. Not run: full E2E layer suite and unrelated scenario/browser suites. Recommended next step: push `edee9a9f` plus this registry closeout to `origin/main`, confirm remote contains both commits, then remove the audit worktree and local branch.

### Comment Annotation Automation 2026-07-01

1. Added Chinese maintenance comments to the sample-project startup deeplink, shared import workflow, Guide sample card controller, and sample contract tests.
2. Explained the post-ready sample import gate, failed-switch previous-sample snapshot, shared runtime refresh hook, Guide selected-sample model, and in-flight sample switcher lock.
3. Kept runtime behavior unchanged and did not refactor, because the current source boundaries are already clear and helper extraction would broaden this maintenance pass.
4. Avoided `index.html`, `css/style.css`, `js/ui/toolbar.js`, `dist/app/**`, README files, and parent dirty files.
5. Refreshed this registry's live worktree truth for the automation handoff.

Files: core `js/bootstrap/startup_sample_project_deeplink.js`, `js/core/sample_project_import_workflow.js`, `js/ui/toolbar/sample_project_banner_controller.js`; tests `tests/sample_project_contracts.test.mjs`; docs `docs/active/_worktree_registry.md`; temporary files none.

Diff summary: comment-only source/test changes add 7 Chinese notes around sample deeplink scheduling, sample import state transitions, shared UI refresh, Guide card state derivation, switcher busy locking, and focused contract tests. Registry changes record the current parent checkout, the clean audit scratch worktree, this automation worktree, and the delivery package. Commit status: ready for Lore commit from branch `codex/comment-automation-20260701`. Base divergence: branch started from `origin/main@a6a8105c`; parent checkout remains at `main@16abfd5f` with unrelated WIP and is not used for integration. Potential conflicts: yellow with sample Guide/import workflow and registry edits; green with appearance/transport shared hot files, generated dist, and browser/runtime paths.

Validation passed: `node --check js/bootstrap/startup_sample_project_deeplink.js js/core/sample_project_import_workflow.js js/ui/toolbar/sample_project_banner_controller.js tests/sample_project_contracts.test.mjs`; `npm run test:node:sample-project-contracts` `15/15`; `git diff` review confirmed code/test edits are comment-only. Not run: browser/dev-server/E2E, Pages dist, i18n audit, or architecture boundaries because this pass only adds comments to existing JS/MJS files and registry documentation. Recommended next step: commit and fast-forward push this isolated branch to `origin/main`, then remove the temporary worktree after remote confirmation.

### Phase6C Sample Switcher 2026-07-01

1. Extracted startup and in-app sample loading into `js/core/sample_project_import_workflow.js`, so both paths reuse the same manifest/project fetch, FileManager import funnel, state writes, and public error handling.
2. Added public sample-list resolution to `js/core/sample_project_registry.js`, keeping checked-in manifest validation separate from public display filtering and excluding developer-preview samples from the switcher.
3. Added the Guide sample switcher, selected/loading/error UI states, dirty confirmation before replacement, success-only URL update, pending-state switcher lock, and a dialog stacking fix so confirmations appear above the Guide.
4. Updated focused Node/E2E/release-smoke tests, i18n copy, README files, release notes, import graph, active task docs, and generated Pages dist.

Files: core `js/core/sample_project_import_workflow.js`, `js/core/sample_project_registry.js`, `js/bootstrap/startup_sample_project_deeplink.js`, `js/ui/toolbar.js`, `js/ui/toolbar/sample_project_banner_controller.js`, `js/ui/ui_contract.js`, `js/ui/ui_surface_url_state.js`, `index.html`, `css/style.css`, `js/core/i18n_catalog.js`, `data/locales.json`; tests `tests/sample_project_contracts.test.mjs`, `tests/e2e/sample_guide_deeplink.spec.js`, `tests/e2e/release/pages_public_release_gate.spec.js`, `tests/e2e/test-import-graph.json`; docs `README.md`, `README.zh-CN.md`, `docs/releases/v0.1-public-demo-draft.md`, `lessons learned.md`, `docs/archive/phase6c-sample-switcher-20260701/*`, this registry; generated Pages mirror `dist/app/**`, `dist/pages-dist-manifest.json`; temporary runtime files under `.runtime/**` only and local ignored `node_modules` junction.

Diff summary: current functional diff removes the duplicated startup sample import implementation and adds a shared workflow, public list resolver, Guide switcher rendering/orchestration, dirty-confirm path, URL sample sync, i18n strings, pending in-flight lock, E2E/release assertions, docs, import graph, and Pages mirror. Current tracked stat before final staging is about 1,379 insertions and 337 deletions across 28 tracked files, plus new workflow/source docs and dist workflow mirror files.

Commit status: functional Lore commit `395238d8` is pushed to `origin/codex/phase6c-sample-switcher`; integration archive closeout follows. Base divergence: functional branch was one Phase 6C commit ahead of `origin/main@16abfd5f`; parent `main` remains untouched. Potential conflicts: red with future sample import, Guide card, toolbar orchestration, `index.html`, `css/style.css`, release smoke, sample contracts, and `dist/app/**`; yellow with docs/i18n/URL helper changes and parent `lessons learned.md` WIP; green with renderer-only production lanes.

Validation passed: changed-file `node --check`; `data/locales.json` JSON parse; `npm run test:node:sample-project-contracts` `15/15`; `py -3 tools/i18n_audit.py` with `ui_missing=0` and `ui_english_fallback=0`; `npm run test:e2e:sample-guide` `2/2`; `npm run verify:pages-dist` with size `927.07 MiB`, Pages startup shell `41/41`, landing showcase `18/18`, sample contracts `15/15`; local public mirror release gates `PLAYWRIGHT_TEST_BASE_URL=http://127.0.0.1:8810/dist/ npm run test:e2e:pages-public-release-gate` and post-review `http://127.0.0.1:8811/dist/`, both `1/1`; `npm run verify:architecture-boundaries`; `node tools/build_test_import_graph.mjs && npm run verify:test-import-graph` for 51 specs; code-review P2 fixed by adding `pending` to in-flight switcher states and rerunning sample contracts plus Pages dist; staged `npm run verify:dist-drift`; `git diff --check`; `git diff --cached --check`. Current validation gap: post-archive closeout gates and remote deployed Pages smoke after push.

Recommended next step: commit archive closeout, run post-archive gates, push integration branch to `origin/main`, then remove Phase6C worktrees after confirming remote state.

### Renderer Hit Canvas Scheduling Preflight 2026-06-30

1. Added the hit canvas scheduling preflight doc with explicit docs/tests/tooling-only guardrails and P45/P47 first-move recommendation.
2. Added `tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs` to lock current hit canvas build, scheduling, dirty/topology, cancellation, scenario-refresh injection, spatial-owner, interaction-candidate, public-facade, state-write, and forbidden-owner boundaries.
3. Added `test:node:renderer-hit-canvas-scheduling-inventory` and architecture-boundary coverage for the new preflight line, including broad `hit_canvas` production-module detection.
4. Updated `tests/scenario_chunk_contracts.test.mjs` so the existing exact-after-settle promotion-wait contract follows the P43 owner location in `render_phase_lifecycle_owner.js`.
5. Kept production runtime, hit canvas owner files, spatial index runtime owner, interaction funnel/event binding owner, scenario refresh runtime, exact scheduler, public facade, state-write allowlist, and `dist/**` unchanged.

Files: core production files none; tests `tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/sample_project_contracts.test.mjs`; docs `docs/active/renderer-hit-canvas-scheduling-preflight-20260630.md` and this registry; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary files none. Diff summary: new inventory test and checker additions lock `buildHitCanvasAfterStartup`, `scheduleHitCanvasBuildIfNeeded`, hit canvas dirty/topology/scheduled-handle tokens, reset cancellation, scenario-refresh injected scheduling, the current scenario-refresh topology reset, spatial and interaction owner boundaries, no production `hit_canvas` module, no broad render lifecycle owner, and closed public/state-write boundaries. `scenario_chunk_contracts` now reads the P43 render phase lifecycle owner for the promotion-active wait contract. `sample_project_contracts` renames a local test model so state-write scanning stays focused on real `runtimeState` writers. Commit status: ready for Lore commit from this validated diff. Base divergence: no divergence after rebase; dirty diff is local docs/tests/tooling only. Potential conflicts: yellow with future package/checker/scenario-contract/sample-contract/registry edits; green with production renderer runtime, public facade, generated dist, spatial owner, scenario refresh runtime, exact scheduler, and interaction owner paths.

Validation passed: `node --check tests/renderer_hit_canvas_scheduling_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:renderer-hit-canvas-scheduling-inventory` `9/9`; `npm run test:node:renderer-render-lifecycle-inventory` `8/8`; `npm run test:node:renderer-render-request-boundary` `13/13`; `npm run test:node:visible-frame-diagnostics` `14/14`; `npm run test:node:interaction-hit-candidates` `5/5`; `node --check tests/scenario_chunk_contracts.test.mjs`; `npm run test:node:scenario-chunk-contracts` `57/57`; `npm run test:node:sample-project-contracts` `11/11`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` `51` specs; `npm run verify:state-write-allowlist` `115` tracked files; `git diff --check` with Windows LF-to-CRLF warnings only; forbidden production/public/dist/state-write diff scan returned empty. Not run: browser/dev-server/E2E smoke, Pages dist generation, or production runtime smoke because this preflight only changes docs/tests/tooling and explicitly excludes production runtime/dist. Recommended next step: implement a narrow hit canvas scheduling owner first, limited to scheduled-handle lifecycle, duplicate scheduling guard, idle/defer gating, and cancellation; leave draw/build/point-probe/dirty-source/topology writes in `map_renderer.js`.

### Phase6B Sample Guide Export 2026-06-30

1. Added a compact sample-aware card at the top of the Guide quick path for loaded or failed sample project deeplinks.
2. Added `resolveSampleProjectGuideContext(...)` and `createSampleProjectGuideCardController(...)` in the existing sample banner controller module, sharing sample state and public error copy.
3. Wired Guide actions to the existing export workbench, original sample JSON download, and default quick-guide continuation.
4. Added English and Simplified Chinese strings, release docs, focused Node tests, a focused Playwright sample Guide spec, and release-gate assertions.
5. Regenerated Pages dist and updated `tests/e2e/test-import-graph.json` to include the new E2E spec.

Files: core `js/ui/toolbar/sample_project_banner_controller.js`, `js/ui/toolbar.js`, `index.html`, `css/style.css`, `js/core/i18n_catalog.js`, `data/locales.json`; tests `tests/sample_project_contracts.test.mjs`, `tests/e2e/sample_guide_deeplink.spec.js`, `tests/e2e/release/pages_public_release_gate.spec.js`, `tests/e2e/test-import-graph.json`; docs `README.md`, `README.zh-CN.md`, `docs/releases/v0.1-public-demo-draft.md`, `docs/archive/phase6b-sample-guide-export-20260630/*`, this registry; Phase6B generated Pages mirror `dist/app/index.html`, `dist/app/css/style.css`, `dist/app/js/core/i18n_catalog.js`, `dist/app/js/ui/toolbar.js`, `dist/app/js/ui/toolbar/sample_project_banner_controller.js`, `dist/pages-dist-manifest.json`; P43 generated Pages mirror synchronized after rebase `dist/app/js/core/map_renderer.js`, `dist/app/js/core/map_renderer/render_phase_lifecycle_owner.js`; temporary runtime files under `.runtime/dev/` only.

Diff summary: the Guide quick path now derives a card from `runtimeState.sampleProjectDeeplink`. Success shows sample title, export, and original JSON download. Error shows public error copy and keeps the default Guide path available. The Project sample banner remains on the same refresh hook, and release smoke checks the Guide card before the Project banner.

Commit status: functional Lore commit `81f2f30e`, rebased cleanly on P43 commits `952af9d2` and `473cd389`; registry/archive closeout commit `f8ad8aaa`; final generated-dist closeout follows after drift exposed the P43 mirror gap. Base divergence: Phase6B started from `main@fc59d527` aligned with `origin/main@fc59d527`, then rebase integrated P43 from `origin/main@473cd389`. Potential conflicts: resolved with P43 on `package.json`; yellow for future sample helper, Guide UI, release smoke, and Pages dist edits.

Validation passed: changed-file `node --check`; `npm run test:node:sample-project-contracts` `11/11`; `py -3 tools/i18n_audit.py` with `ui_missing=0` and `ui_english_fallback=0`; `npm run test:e2e:sample-guide` `2/2`; `npm run verify:pages-dist` with size `927.04 MiB`; local public mirror release gate `PLAYWRIGHT_TEST_BASE_URL=http://127.0.0.1:8810/dist/ npm run test:e2e:pages-public-release-gate` `1/1`; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` after regenerating 51 specs; `git diff --check`; `git diff --cached --check`; final code-reviewer CLEAR. Final closeout reran `npm run verify:dist-drift`, found missing P43 generated dist mirror, synchronized it, and reran the drift gate. Not run: remote deployed Pages smoke. Recommended next step: commit final dist/registry closeout and push `main`.

### Renderer Render Phase Lifecycle Owner P43 2026-06-30

1. Added `createRenderPhaseLifecycleOwner({ state, effects, getters })` to own render phase value writes, phase-enter timestamp writes, phase timer clearing, adaptive idle scheduling, and reset phase state.
2. Delegated `clearRenderPhaseTimer()`, `setRenderPhase()`, and `scheduleRenderPhaseIdle()` from `map_renderer.js` while keeping the wrapper names stable.
3. Preserved old idle callback behavior for chunk refresh flush, promotion-active wait, exact fast path, render, and exact-after-settle scheduling through injected effects.
4. Added P43 behavior/inventory tests plus architecture-boundary coverage for owner narrowness, full DI wiring, P41/P42 boundary preservation, and `dist/app/**` no-change proof.
5. Kept `drawCanvas()`, `renderPassToCache()`, hit canvas, scenario refresh runtime, exact scheduler, strategic runtime, public facade, state-write allowlist, and `dist/app/**` unchanged.

Files: core `js/core/map_renderer.js`, `js/core/map_renderer/render_phase_lifecycle_owner.js`; tests `tests/renderer_render_phase_lifecycle_owner_behavior.test.mjs`, `tests/renderer_render_phase_lifecycle_inventory.test.mjs`, `tests/renderer_render_lifecycle_inventory_boundary.test.mjs`, `tests/renderer_startup_transaction_inventory_boundary.test.mjs`; tooling/docs `package.json`, `tools/check_architecture_boundaries.mjs`, `docs/active/renderer-render-phase-lifecycle-owner-p43-20260630.md`, this registry, and `lessons learned.md`; temporary files none.

Diff summary: `map_renderer.js` remains the composition root and injects all runtime writes/effects into the new owner. The owner keeps the phase/timer lifecycle order and returns frozen summaries. Startup/reset and render-lifecycle inventory tests now point to the owner delegation contract. The architecture checker registers the P43 owner/doc/tests/scripts, rejects broad lifecycle-owner creation, locks full DI wiring, and keeps public/state/scenario/exact/strategic/dist boundaries closed.

Commit status: functional Lore commit `952af9d2`; fast-forward merged into clean integration branch `codex/p43-main-integration` because the parent checkout had unrelated Phase6B/sample-guide WIP. Recovery remains through branch `codex/p43-render-phase-lifecycle-owner` and commit `952af9d2`. Base divergence: branch started at `origin/main@fc59d527`, and `origin/main` was still `fc59d527` at integration time. Potential conflicts: yellow with future changes to `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, renderer inventory tests, or this registry; green with `public.js`, state-write allowlist, `dist/app/**`, scenario refresh runtime, exact scheduler, setMapData owner, request owner, and visible-frame diagnostics owner.

Validation passed: `node --check` for owner, renderer, P43 tests, and architecture checker; package JSON parse; P43 behavior `11/11`; P43 inventory `6/6`; P41 request boundary `13/13`; P42 visible-frame diagnostics `14/14`; P40 render lifecycle inventory `8/8`; P38 setMapData transaction `18/18`; P39 reset-hardening inventory `8/8`; render transaction diagnostics `21/21`; exact-after-settle refresh plans `9/9`; scenario refresh plans `24/24`; architecture boundaries; test import graph `50` specs; state-write allowlist `115` tracked files; `git diff --check` with Windows LF-to-CRLF warnings only; forbidden-path diff scan returned empty for `dist/app`, `public.js`, scenario/exact/setMapData/request/visible-frame owner files, and state-write allowlist.

Review fixes: reset now clears an active phase timer inside `resetRenderPhaseState()`; P43 tests and checker now lock the complete DI effect/getter set; P43 inventory now guards `dist/app/**` as untouched because the P43 attachment explicitly forbids dist changes. Phase6B closeout later synchronized the generated Pages mirror after rebasing onto P43. Not run: optional browser/E2E smoke for the original P43 lane, because targeted Node/static gates covered that narrow renderer phase lifecycle extraction. Recommended next step: no P43 action remains; P43 implementation and integration worktrees were removed before Phase6B closeout. Future renderer work should refresh from `origin/main` after Phase6B lands.

### Renderer Visible Frame Diagnostics Owner P42 2026-06-30

1. Added `createVisibleFrameDiagnosticsOwner({ effects, getters })` to own visible-frame diagnostic payload, counter, and metric ordering.
2. Delegated `recordVisibleFrameTransactionMetric()`, `noteFirstVisibleFrameBlocked()`, `markFirstVisibleFramePainted()`, and `resetFirstVisibleFramePainted()` from `map_renderer.js`.
3. Preserved metric names, status buckets, reason/blockReason/paintSource fields, committed identity payload, commit-key signature, first-visible hook payload, and reset/blocked semantics.
4. Added P42 behavior/inventory tests and architecture-boundary gates, including cache `lastAction` reason fallback and explicit detail-field override coverage.
5. Kept `drawCanvas()`, `renderPassToCache()`, hit canvas build, scenario refresh runtime, exact scheduler, strategic runtime, public facade, and state-write allowlist unchanged; review follow-up synchronized the generated `dist/app/**` mirror.

Files: core `js/core/renderer/visible_frame_diagnostics_owner.js`, `js/core/map_renderer.js`; generated Pages mirror `dist/app/js/core/map_renderer.js`, `dist/app/js/core/renderer/visible_frame_diagnostics_owner.js`, `dist/pages-dist-manifest.json`; tests `tests/visible_frame_diagnostics_owner_behavior.test.mjs`, `tests/visible_frame_diagnostics_owner_inventory.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; docs `docs/active/renderer-visible-frame-diagnostics-owner-p42-20260630.md` and this registry; temporary files none.

Diff summary: `map_renderer.js` now keeps stable wrappers and effect/getter injection while the new owner builds visible-frame diagnostics and metrics. `tests/scenario_chunk_contracts.test.mjs` follows the current P38/P41/P42 owner shape instead of old inline anchors. `tools/check_architecture_boundaries.mjs` registers P42 files/scripts and scopes migrated inline-token checks to the wrapper slice. Review follow-up regenerates the checked-in Pages mirror and strengthens the blocked first-visible test assertion.

Commit status: functional Lore commit `ab4d90f9` pushed to `origin/main`; temporary worktree `C:\Users\raede\.codex\worktrees\scenario-forge-p42-visible-frame-diagnostics-owner` removed after parent `main` fast-forwarded. Recovery remains through branch `codex/p42-visible-frame-diagnostics-owner` and commit `ab4d90f9`. Base divergence: started from `origin/main@419c6ba0`, rebased cleanly onto `origin/main@74bc91ff`, and final validation ran on the latest baseline. Potential conflicts: yellow with future edits to `js/core/map_renderer.js`, `tools/check_architecture_boundaries.mjs`, `package.json`, `tests/scenario_chunk_contracts.test.mjs`, or this registry; green against integrated Phase6A/public-sample files and generated dist.

Validation passed: `node --check` for owner, renderer, new tests, scenario contract, and architecture checker; package JSON parse; P42 owner behavior `9/9`; P42 inventory `5/5`; scenario chunk contracts `57/57`; P40 lifecycle inventory `8/8`; render transaction diagnostics `21/21`; P38 setMapData transaction `18/18`; P39 reset-hardening inventory `8/8`; exact-after-settle refresh plans `9/9`; scenario refresh plans `24/24`; architecture boundaries; test import graph `50` specs; state-write allowlist `115` tracked files; `git diff --check` and `git diff --cached --check`. Review follow-up validation passed: `npm run verify:pages-dist` startup shell `41/41`, landing showcase `18/18`, sample contracts `9/9`; `npm run test:node:visible-frame-diagnostics` owner behavior `9/9` and inventory `5/5`; `npm run verify:architecture-boundaries`; `git diff --check`; `git diff --cached --check`; post-commit `npm run verify:dist-drift`. Review fixes: all new files were staged with the functional commit; the P42 architecture checker no longer full-file scans ordinary visible-frame tokens; payload tests cover reason fallback, explicit details overrides, and blocked first-visible paths that leave the painted flag unset; Pages dist mirror drift is fixed in review follow-up commit `3e2ffe25`. Not run: browser/dev-server/E2E smoke because targeted Node/static and Pages dist gates cover this review follow-up. Recommended next step: commit and push this registry closeout; no P42 functional integration action remains.

### Phase6A Public Sample Experience Polish 2026-06-30

1. Added a Project-sidebar sample banner that confirms loaded sample projects, surfaces bad sample links as visible nonfatal app UI, opens the existing export workbench, and links to the original checked-in JSON.
2. Kept the Phase5B import architecture unchanged: sample registry allowlist, startup sample state, FileManager text import normalization, and interaction funnel remain the import path.
3. Updated landing sample CTA wording to `Open editable sample`, `Download JSON`, and `View recipe`, with a short Guide-panel note.
4. Extended existing sample, landing, runtime-hook, and release-gate tests instead of adding a new test system.
5. Regenerated `data/locales.json`, `data/manifest.json`, and Pages `dist/**` mirrors for the Phase6A copy and app files; this also synced already-committed P41 renderer source into `dist/app/**`.

Files: core `js/ui/toolbar/sample_project_banner_controller.js`, `js/bootstrap/startup_sample_project_deeplink.js`, `js/core/state/config.js`, `js/ui/i18n.js`, `js/ui/toolbar.js`, `index.html`, `css/style.css`, `js/core/i18n_catalog.js`, `data/locales.json`, `data/manifest.json`; landing/docs `landing/index.html`, `landing/app.js`, `landing/styles.css`, `README.md`, `README.zh-CN.md`, `docs/releases/v0.1-public-demo-draft.md`; tests `tests/sample_project_contracts.test.mjs`, `tests/landing_showcase_view_behavior.test.mjs`, `tests/e2e/release/pages_public_release_gate.spec.js`, `tests/test_runtime_hooks_boundary_contract.py`; generated `dist/**`; task docs `docs/archive/phase6a-public-sample-experience-20260630/*` and this registry. Diff summary: sample deep-link state now refreshes a narrow toolbar banner, the Project tab exposes success/error actions, bad-link toast copy goes through `ui.t`, landing text clarifies editable sample entry, and release smoke asserts the visible banner plus export path. Commit status: functional Lore commit `690b1997`; archive closeout commit follows. Base divergence: started from `main@28a743c5` aligned with `origin/main@28a743c5`; P41 integration closeout is aligned at `origin/main@419c6ba0`; local main is ahead with Phase6A. Potential conflicts: green against integrated P41 source; yellow only for this registry/archive closeout until pushed. Validation passed: changed-file `node --check`; sample contracts `9/9`; runtime hooks boundary `6/6`; annotation-productization `63/63`; i18n audit with `ui_missing=0`; data manifest `14/14`; `verify:pages-dist` startup shell `41/41`, landing `18/18`, sample contracts `9/9`; local Pages release gate `1/1` at `http://127.0.0.1:4173/` with `sampleProjectDeeplink.status=success` for `tno-1962-atlantropa-briefing`; `git diff --check` passed with Windows LF-to-CRLF warnings only; final QA verifier returned WATCH/ready-for-integration; post-functional `verify:dist-drift` passed. Remaining validation: final-head `verify:dist-drift` after this docs-only closeout commit. Recommended next step: commit archive closeout, rerun final-head dist drift, then push main.

### Renderer Render Request Boundary Owner P41 2026-06-30

1. Added `createRenderRequestBoundaryOwner({ effects, getters })` to own only render request/flush/fallback ordering.
2. Delegated `requestRendererRender()`, `requestInteractionRender()`, and `flushInteractionRender()` from `map_renderer.js` while preserving existing boolean return semantics.
3. Added behavior coverage for frozen owner summaries, request/flush/fallback ordering, interaction fallback, and dependency fail-fast.
4. Added inventory and architecture-boundary gates for the P41 narrow owner.
5. Kept public facade, state-write allowlist, `dist/app/**`, render/draw/pass/hit/scenario/exact/strategic ownership, `setRenderPhase()`, and `scheduleRenderPhaseIdle()` unchanged.

Files: core `js/core/map_renderer.js`, `js/core/map_renderer/render_request_boundary_owner.js`; tests `tests/renderer_render_request_boundary_owner_behavior.test.mjs`, `tests/renderer_render_request_boundary_inventory.test.mjs`; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; docs `docs/active/renderer-render-request-boundary-owner-p41-20260630.md`, this registry, and `lessons learned.md`; temporary files none. Diff summary: the request wrappers now call a DI owner that records frozen diagnostics summaries, while the public internal wrapper behavior remains boolean-compatible and external fallback callbacks keep their old no-argument surface. Commit status: functional Lore commit `0fa68761` pushed to `origin/main`; this closeout records post-push truth. Base divergence: `main@28a743c5` and `origin/main@28a743c5` were aligned at task start; local `main@0fa68761` and `origin/main@0fa68761` are aligned after push. Potential conflicts: yellow with future renderer request wrapper/package/checker edits; green by path against current public-sample/dist/UI WIP except shared registry. Validation passed: `node --check` for owner, `map_renderer.js`, new tests, P40 inventory test, and architecture checker; package JSON parse; P41 owner `9/9`; P41 inventory `4/4`; P41 combined `13/13`; P40 lifecycle inventory `8/8`; P38 setMapData transaction `18/18`; P39 reset-hardening inventory `8/8`; render transaction diagnostics `21/21`; render cache owner `6/6`; transform reuse policy owner `7/7`; exact-after-settle refresh plans `9/9`; scenario refresh plans `24/24`; architecture boundaries; test import graph `50` specs; state-write allowlist `115` tracked files; `git diff --check` with Windows LF-to-CRLF warnings only. Review fix: narrowed external fallback callback surface and corrected registry dirty-truth summary. Optional browser/E2E not run because P41 is Node/static renderer request boundary wiring. Recommended next step: no P41 integration action remains; continue Phase6A/public-sample WIP separately.

### Renderer Render Lifecycle Preflight P40 2026-06-30

1. Added a P40 render lifecycle preflight doc that inventories the current render facade, `drawCanvas`, `renderPassToCache`, hit canvas, cache/pass catalog, exact scheduler, scenario refresh, strategic overlay, and public facade boundaries.
2. Added a P40 inventory boundary test that proves no production render lifecycle owner/helper/controller exists and locks current render lifecycle anchors in `js/core/map_renderer.js`.
3. Added `test:node:renderer-render-lifecycle-inventory` to `package.json`.
4. Extended `tools/check_architecture_boundaries.mjs` so the global architecture gate fails on missing P40 doc/test/script, missing render lifecycle anchors, production render lifecycle owner creation, or state-write allowlist entries for a render lifecycle owner.
5. Kept production renderer/runtime/dist/state-write allowlist files unchanged.

Files: core files none; tests `tests/renderer_render_lifecycle_inventory_boundary.test.mjs`; docs `docs/active/renderer-render-lifecycle-preflight-20260630.md` and this registry; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary files none. Diff summary: docs/test/tooling only, with a new P40 inventory gate and architecture checker coverage over existing P38/P39 render lifecycle boundaries plus renamed render lifecycle owner/helper/controller variants. Commit status: functional P40 integration merge commit `14b2a7a3`; lessons-learned closeout commit `8a055fe4`; isolated P40 worktree removed after push. Recovery remains through branch `codex/p40-render-lifecycle-preflight@0cb924ea` and main merge commit `14b2a7a3`. Potential conflicts: yellow with future P41 package/checker edits; green for production renderer code because P40 touches no production runtime file. Validation passed before merge: `node --check` for the P40 test and architecture checker; package JSON parse; P40 inventory `8/8`; `verify:architecture-boundaries`; `verify:test-import-graph` `50` specs; `verify:state-write-allowlist` `115` tracked files; `git diff --check`; forbidden-file diff scan returned no changes. Earlier full renderer-adjacent validation also passed: P38 combined `18/18`; P39 inventory `8/8`; startup owner `8/8`; startup inventory `9/9`; render cache `6/6`; transform reuse `7/7`; render pipeline catalog `3/3`; render transaction diagnostics `21/21`; exact-after-settle refresh plans `9/9`; scenario refresh plans `24/24`. Post-merge validation on main passed P40 inventory `8/8`, architecture boundaries, test import graph `50` specs, state-write allowlist `115` tracked files, and final forbidden runtime/dist/state-write diff scans. Recommended next step: start P41 from the P40 guardrails.

### Phase5B Sample Deep Links 2026-06-30

1. Added checked-in public sample deep links from the landing page to `/app/?sample=<id>&view=guide`.
2. Routed sample imports through the public landing manifest, checked-in sample project JSON, shared FileManager normalization, and the existing interaction funnel.
3. Added `sampleProjectDeeplink` boot-state visibility and startup scheduling after app ready state.
4. Extended sample, FileManager, landing, Pages static, release smoke, and state-split tests.
5. Regenerated Pages dist and archived the Phase5B task docs under `docs/archive/phase5b-sample-deeplink-20260630/`.

Files: core `js/core/file_manager.js`, `js/core/interaction_funnel.js`, `js/core/sample_project_registry.js`, `js/core/state/boot_state.js`, `js/bootstrap/startup_sample_project_deeplink.js`, and `js/main.js`; landing/source docs `landing/**`, `README.md`, `README.zh-CN.md`, and `docs/releases/v0.1-public-demo-draft.md`; tests `tests/sample_project_contracts.test.mjs`, `tests/file_manager_project_roundtrip_behavior.test.mjs`, `tests/landing_showcase_view_behavior.test.mjs`, `tests/test_pages_dist_startup_shell.py`, `tests/test_state_split_boundary_contract.py`, and `tests/e2e/release/pages_public_release_gate.spec.js`; generated dist `dist/**`; archived docs `docs/archive/phase5b-sample-deeplink-20260630/*`. Diff summary: public sample links now open checked-in projects in the app through a manifest allowlist, project import text uses shared normalization, startup state records deep-link import status, landing sample actions and manifests align, and Pages mirrors include the new modules plus P38 owner dist sync. Commit status: functional Lore commit `dc787796` was pushed to `origin/main`; this docs-only closeout archives the active task docs and records cleanup guidance. Base commit was `6c0400c5`; final rebase base was `origin/main@4375923d`. Potential conflicts: green against integrated P39 production runtime because P39 touched docs/tests/tooling only; yellow only for future landing/sample manifest/FileManager/import funnel/Page dist work. Validation passed after final rebase: `node --check` changed JS; sample contracts `6/6`; FileManager roundtrip `37/37`; state split boundary `14/14`; `npm run verify:architecture-boundaries`; `npm run verify:pages-dist` with startup shell `41/41`, landing `18/18`, sample contracts `6/6`, and total size `927.00 MiB`; `npm run verify:dist-drift`; `git diff --check`; final local dist release smoke with `sampleProjectDeeplink.status=success`, sample `tno-1962-atlantropa-briefing`, and scenario `tno_1962`. Review results: code-reviewer APPROVE and architect CLEAR after metadata and boot-state fixes. Recommended next step: push this closeout, fast-forward parent main, remove `C:\Users\raede\.codex\worktrees\scenario-forge-phase5b-sample-deeplink`, and keep recovery through branch `codex/phase5b-sample-deeplink` plus commits `dc787796` and this closeout.

### Renderer Transaction Reset Hardening P39 2026-06-30

1. Added a P39 preflight doc that inventories current reset ownership for startup, setMapData, scenario refresh, exact-after-settle, `resetRendererTransactionState`, `resetRendererRefreshTransactionState`, and `markRendererTopologyChanged`.
2. Added a P39 inventory boundary test that proves no production reset helper or render lifecycle owner exists and locks the current reset tokens/order.
3. Added `test:node:renderer-transaction-reset-hardening-inventory` to `package.json`.
4. Extended `tools/check_architecture_boundaries.mjs` so the global architecture gate fails on missing P39 doc/test/script, missing reset inventory tokens, production helper creation, or boundary imports crossing into setMapData/startup/scenario/exact owners.
5. Kept production renderer/runtime/dist/state-write allowlist files unchanged.

Files: core files none; tests `tests/renderer_transaction_reset_hardening_inventory_boundary.test.mjs`; docs `docs/active/renderer-transaction-reset-hardening-preflight-20260630.md` and this registry; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; temporary files none. Diff summary: docs/test/tooling only, with a new P39 inventory gate and architecture checker coverage over existing P38 reset boundaries plus renamed helper/controller and import-form variants. Commit status: functional Lore commit `33056a21` fast-forwarded into local main; P39 worktree `C:\Users\raede\.codex\worktrees\scenario-forge-p39-reset-hardening` was removed after post-merge verification; this registry closeout records final local integration truth before push. Base commit is `origin/main@b6a777f`; local main is ahead pending this closeout and push. Potential conflicts: yellow with Phase5B generated dist mirrors around the P38 setMapData owner; yellow with future P40/P41 package/checker edits; green for production renderer code because P39 touches no production runtime file. Validation passed before merge: `node --check` for the P39 test and architecture checker; package JSON parse; P39 inventory `8/8`; P38 combined `18/18`; startup owner `8/8`; startup inventory `9/9`; renderer runtime state `10/10`; render transaction diagnostics `21/21`; scenario refresh plans `24/24`; `verify:architecture-boundaries`; `verify:test-import-graph` `50` specs; `verify:state-write-allowlist` `115` tracked files; `git diff --check` with Windows LF-to-CRLF warnings only. Post-merge validation passed on main: P39 inventory `8/8`, architecture boundaries, test import graph `50` specs, state-write allowlist `115` tracked files, and `git diff --check`. Review/deslop: architect P1 and code-review P2 findings were fixed by the widened reset-helper classifier, side-effect `map_renderer.js` import detection, and registry status/timestamp cleanup; architect re-review CLEAR and code review CLEAR; deslop pass found no masking fallback and kept the explicit token-gate style. Recommended next step: push main, then rebase/regenerate Phase5B over P39 before integration.

### Renderer setMapData Transaction Owner P38 2026-06-30

1. Added `createSetMapDataTransactionOwner({ state = {}, getters = {}, effects = {} } = {})` to own the `setMapData` transaction order and return a frozen diagnostic summary.
2. Kept `function setMapData({ refitProjection = true, resetZoom = true, suppressRender = false, interactionLevel = "full", deferInteractionInfrastructure = false } = {})` in `js/core/map_renderer.js` as the stable public wrapper.
3. Kept concrete runtime/module-local writes in `map_renderer.js` injected effects, including composite coverage diagnostics, color sanitation, sovereignty/cache resets, deferred hit-canvas state, and hit-canvas dirty state.
4. Added owner behavior coverage and upgraded the P37 inventory/checker gates to P38 implementation-state gates.
5. Left public facade, state-write allowlist, scenario refresh runtime, exact-after-settle scheduler, render/drawCanvas/renderPassToCache/hit canvas, and strategic overlay runtime unchanged.

Files: core `js/core/map_renderer.js`, `js/core/map_renderer/set_map_data_transaction_owner.js`; tests `tests/renderer_set_map_data_transaction_owner_behavior.test.mjs`, `tests/renderer_set_map_data_transaction_inventory_boundary.test.mjs`; tooling/package `tools/check_architecture_boundaries.mjs`, `package.json`; docs `docs/active/renderer-set-map-data-transaction-owner-p38-20260630.md` and this registry. Diff summary: `setMapData` wrapper now delegates to an injected owner while the owner preserves the original transaction ordering through required getters/effects; behavior and boundary gates now require the owner and keep render/hit/scenario/exact/strategic anchors outside it. Commit status: functional Lore commit `2e348cc0` fast-forwarded into local main; this registry closeout records integrated-local truth before push. Base divergence: P38 started from `main@6c0400c5` and fast-forwarded cleanly. Potential conflicts: yellow with Phase5B for this registry; green for production paths based on current Phase5B planned files; yellow with any future renderer extraction touching `map_renderer.js`, package scripts, or architecture checker. Validation passed before merge: `node --check` for owner, renderer, owner test, inventory test, and checker; package JSON parse; P38 owner `7/7`; P38 inventory `11/11`; P38 combined `18/18`; startup owner `8/8`; startup inventory `9/9`; fitProjection lifecycle `23/23`; viewport update owner `5/5`; runtime state `10/10`; render transaction diagnostics `21/21`; scenario refresh plans `24/24`; architecture boundaries; test import graph `50` specs; state-write allowlist `115` tracked files; `git diff --check` with Windows LF-to-CRLF warnings only. Post-merge validation passed on main: P38 combined `18/18`, architecture boundaries, scenario refresh plans `24/24`. Review/QA: code review returned COMMENT/CLEAR with no findings and a staging reminder for new files; QA returned PASS/CLEAR with no required follow-ups. Not run: optional browser/E2E smoke, because this is a renderer owner extraction covered by targeted Node/static gates. Recommended next step: push main, then rebase Phase5B over P38 if Phase5B continues.

### Phase4B Output Gallery

- Worktree `C:\Users\raede\.codex\worktrees\scenario-forge-phase4b-output-gallery` was fast-forwarded into main at commit `36dbcb1a`.
- Integration registry closeout commit `865d58ac` was pushed to `origin/main`.
- Post-rebase validation before merge passed `npm run verify:pages-dist`, `npm run test:node:landing-showcase-view`, `npm run verify:dist-drift`, and `git diff --check`.
- Post-merge main validation passed `npm run test:node:landing-showcase-view` 18/18.
- The Phase4B worktree was removed after remote confirmation; recovery remains through commits `36dbcb1a` and `865d58ac`.

### Phase5A Sample Projects

- Worktree `C:\Users\raede\.codex\worktrees\scenario-forge-phase5a-sample-projects` was fast-forwarded into main at functional commit `78f101b2`.
- Branch `codex/phase5a-sample-projects` is pushed to `origin/codex/phase5a-sample-projects@78f101b2`.
- Integration closeout commit `40229af8` was pushed to `origin/main`.
- Pre-merge validation passed `npm run verify:pages-dist`, `npm run verify:dist-drift`, `npm run verify:test-import-graph`, `npm run verify:architecture-boundaries`, `git diff --check`, and `git diff --cached --check`.
- Follow-up code review returned APPROVE after the five-project public-download coverage fix; follow-up architecture review returned CLEAR.
- Post-merge main validation passed `npm run test:node:sample-project-contracts` 3/3, `npm run test:node:landing-showcase-view` 18/18, and `npm run verify:dist-drift`.
- The parent main docs WIP was preserved in stash `preserve main docs WIP before phase5a integration 2026-06-30`.
- Task docs are archived under `docs/archive/phase5a-sample-projects-20260630/`.
- The Phase5A worktree was removed after remote confirmation; recovery remains through functional commit `78f101b2`, closeout commit `40229af8`, and remote branch `origin/codex/phase5a-sample-projects`.

## Local/Remote Sync Closeout 2026-06-30

1. Preserved the dirty parent checkout in stash `preserve main dirty docs cleanup before remote sync 2026-06-29`.
2. Fast-forwarded local `main` from `383a626a` to `origin/main@f65bf8e0`; local and remote now match.
3. Removed integrated-clean worktrees `mapcreator-p28-renderer-projection-path-owner`, `mapcreator-p29-renderer-svg-surface-lifecycle-preflight`, `mapcreator-p30-renderer-svg-surface-lifecycle-owner-current`, `mapcreator-p31-fit-projection-lifecycle-preflight`, and `scenario-forge-phase3a-public-narrative`.
4. Deleted their local branches after confirming each HEAD was already an ancestor of `origin/main`; recovery commits remain `0ab4385e`, `f0000344`, `6cc83150`, `7fb3ade5`, and `13457c54`.
5. At that sync point, `git worktree list --porcelain` returned only the main checkout; later Phase4B and audit worktrees are tracked in the current rows above.

Files: registry only for this closeout; pre-sync WIP remains in stash. Diff summary: docs-only registry truth update after fast-forward and integrated-worktree cleanup. Commit status: docs-only registry closeout follows the `origin/main@f65bf8e0` sync baseline. Base commit was local `main@383a626a`; synced baseline was `origin/main@f65bf8e0`. Potential conflicts: yellow only for future registry edits or replaying the preserved docs cleanup stash. Validation passed: `git status --short --branch`, `git rev-list --left-right --count HEAD...origin/main`, `git worktree list --porcelain`, `git stash list --date=local -n 5`, `git diff --check`, `node --check js/ui/toolbar.js`, `npm run -s verify:test-import-graph`, and `npm run -s verify:architecture-boundaries`.

## Ready Delivery Packages

### Renderer Startup Transaction Owner P36 2026-06-30

1. Added `createRendererStartupTransactionOwner({ state = {}, getters = {}, effects = {} } = {})` in `js/core/renderer/renderer_startup_transaction_owner.js` to own only the post-projection `initMap` reset transaction order.
2. Wired `js/core/map_renderer.js` so `initializeProjectionPaths()` still runs first, then `runInitMapResetTransaction({ debugMode })`, then pointer/touch setup and later startup lifecycle branches.
3. Added owner behavior coverage for exact effect order, topology/hit revisions, bridge position, cancel/reset order, diagnostic summary, and missing required effects/getters.
4. Updated P35 inventory, architecture boundary checks, and package scripts for the P36 owner boundary.
5. Regenerated Pages dist mirrors and archived the P36 closeout docs under `docs/archive/renderer-startup-transaction-owner-p36-20260630/`.

Files: core `js/core/renderer/renderer_startup_transaction_owner.js` and `js/core/map_renderer.js`; tests `tests/renderer_startup_transaction_owner_behavior.test.mjs` and `tests/renderer_startup_transaction_inventory_boundary.test.mjs`; tooling/package `tools/check_architecture_boundaries.mjs` and `package.json`; generated dist `dist/app/js/core/map_renderer.js`, `dist/app/js/core/renderer/renderer_startup_transaction_owner.js`, and `dist/pages-dist-manifest.json`; docs `docs/archive/renderer-startup-transaction-owner-p36-20260630/*` and this registry. Diff summary: startup reset ordering moved into an effects-only owner while concrete runtime state writes stay injected from `map_renderer.js`; render, hit-canvas, scenario, exact scheduler, strategic overlay, zoom, binding, and public facade semantics stay outside the owner. Commit status: functional Lore commit `084f8fd8` is pushed to `origin/main` and `origin/codex/p36-renderer-startup-transaction-owner`; this registry closeout records final integration truth. Base commit is `origin/main@12890fc6`. Current main divergence: parent checkout remains dirty with unrelated docs and is behind the pushed remote, so P36 integration used this isolated worktree. Potential conflicts: yellow with Phase4B for `docs/active/_worktree_registry.md` and `dist/pages-dist-manifest.json`; yellow with future renderer extraction lanes touching `map_renderer.js`, architecture boundaries, package scripts, startup transaction tests, or generated dist; green against landing/gallery production files. Validation passed: `node --check` for owner, renderer, tests, and architecture checker; owner test `8/8`; inventory test `9/9`; surface lifecycle `13/13`; projection/path lifecycle `14/14`; SVG lifecycle `12/12`; fitProjection lifecycle `23/23`; viewport update owner `5/5`; runtime state `10/10`; render transaction diagnostics `21/21`; architecture boundaries; state-write allowlist `115` tracked files; test import graph `50` specs; `git diff --check`; `verify:pages-dist` with startup shell `41/41`, landing showcase `13/13`, and Pages size `926.94 MiB`; `verify:dist-drift`; TNO ready-state `5/5`; smoke `4/4`. Review results: the original P36 code review returned only the stale-registry closeout finding, which was fixed in the P36 closeout; architecture review returned WATCH for legacy P33 bridge-order test naming/string-anchor drift, while P36 behavior and architecture gates now own the startup reset order. The 2026-06-30 audit rechecked the P36 production path and found no production-code blocker; it fixed stale Current Worktrees registry truth and tightened the startup owner factory-scope assertions in the inventory test plus architecture boundary checker. This audit closeout removes the stale integrated P36 worktree row and leaves Phase4B as the only active secondary worktree. Remaining risk: the older P33 inventory test name still reflects a pre-P36 bridge-order boundary. Recommendation: push this audit closeout, remove the temporary audit worktree after remote confirmation, and let Phase4B refresh its registry and Pages manifest over the latest main.

### Renderer Startup Transaction Preflight P35 2026-06-29

1. Added `docs/active/renderer-startup-transaction-preflight-20260629.md` documenting the current `initMap` startup transaction and P36 guardrails.
2. Added `tests/renderer_startup_transaction_inventory_boundary.test.mjs` to lock that P35 adds no startup transaction owner yet, `initMap` still owns reset ordering, and render/chunk/exact/strategic anchors remain outside P36 first move.
3. Added package script `test:node:renderer-startup-transaction-inventory`.
4. Extended `tools/check_architecture_boundaries.mjs` with P35 doc/test existence and guardrail checks.
5. Preserved production runtime, generated dist, and state-write allowlist unchanged.

Files: docs `docs/active/renderer-startup-transaction-preflight-20260629.md` and this registry; tests `tests/renderer_startup_transaction_inventory_boundary.test.mjs`; tooling `tools/check_architecture_boundaries.mjs`; package `package.json`. Diff summary: preflight-only guardrails for P36 startup transaction extraction, no production runtime behavior changes. Commit status: ready to push from branch `codex/p35-renderer-startup-transaction-preflight` to `origin/main`; cleanup removes the temporary P35 worktree after push while retaining the branch and commit as recovery handles. Base commit is `origin/main@1c3a9766`. Potential conflicts: yellow with future renderer extraction lanes touching startup transaction inventory docs/tests/tooling or package scripts; green for `js/core/map_renderer.js`, `dist/app/**`, and `tools/eslint-rules/state-writer-allowlist.json` because P35 does not modify them. Validation passed: `node --check tests/renderer_startup_transaction_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; package JSON parse; `npm run test:node:renderer-startup-transaction-inventory` 7/7; `npm run verify:architecture-boundaries`; `npm run verify:test-import-graph` 50 specs; `git diff --check`; forbidden-file diff check for `js/core/map_renderer.js`, `dist/app/**`, and `tools/eslint-rules/state-writer-allowlist.json` returned no changes. Recommendation: push fast-forward to `origin/main`, then clean the P35 worktree; P36 can start from the active preflight doc and should move only the reset transaction block first.

### Phase 4A Landing Product Story 2026-06-29

1. Added an interactive source-backed story section after Selected works, covering baseline, scenario switch, transport/context, evidence inspection, and export.
2. Added `initProductStory()` as the single landing owner for story state, keyboard navigation, comparison switching, dynamic map image/alt syncing, and evidence highlighting.
3. Added responsive sticky-stage styling with reduced-motion-safe behavior and fixed story title font sizing.
4. Extended landing Node tests and Pages startup-shell tests for story evidence markers, click/keyboard controls, reduced-motion behavior, source/dist contracts, fixed title sizing, and the 950 MiB preferred Pages dist warning gate.
5. Regenerated Pages dist mirrors and manifest; post-rebase dist total is `971962986` bytes (`926.94 MiB`), with hard cap `within_limit` and preferred warning gate `within_warning`.

Files: landing source `landing/index.html`, `landing/app.js`, `landing/styles.css`; generated dist `dist/index.html`, `dist/app.js`, `dist/styles.css`, `dist/pages-dist-manifest.json`; tooling `tools/build_pages_dist.py`; tests `tests/landing_showcase_view_behavior.test.mjs`, `tests/test_pages_dist_startup_shell.py`; docs `docs/archive/landing-product-story-phase4a-20260629/` and this registry. Diff summary: landing-only product story UI, one `initProductStory()` owner, i18n, sticky/responsive/reduced-motion CSS, Pages warning gate, source/dist tests, and regenerated dist mirrors. Commit status: functional Lore commit `a9ca60dd` is rebased over `origin/main@c5f87aed`; this closeout archives docs and records final cleanup guidance. Base commit was `origin/main@13457c54`; rebase base is `origin/main@c5f87aed`. Potential conflicts: yellow for future landing/Pages dist/startup-shell/registry edits; green for renderer/runtime/data schema/backend code. Validation passed before rebase: `node --check landing/app.js`; `py -3 -m py_compile tools\build_pages_dist.py tests\test_pages_dist_startup_shell.py`; `npm run verify:pages-dist` with Python 41/41 and Node 13/13; `npm run test:node:landing-showcase-view` 13/13; `py -3 -m unittest tests.test_pages_dist_startup_shell -q` 41/41; targeted CSS guard 2/2; `git diff --check`; `git diff --cached --check`; `npm run verify:dist-drift`. Post-rebase validation passed: `npm run verify:pages-dist` with Python 41/41, landing Node 13/13, and dist total `971962986` bytes (`926.94 MiB`); final `npm run verify:dist-drift`; `git diff --check origin/main..HEAD`. Rebase conflict classification: text conflicts in this registry and generated manifest; no semantic product conflict found because P34 touched renderer/runtime integration while Phase 4A touches landing/Pages contracts. Independent architecture review returned CLEAR; independent code review reported the manifest deletion finding resolved and docs checklist addressed. Recommendation: push functional commit and closeout to `origin/main`, then remove this worktree after confirming remote state.

### Renderer Viewport Update Owner P34 2026-06-29

1. Added `createRendererViewportUpdateOwner({ effects = {}, getters = {} } = {})` as an effects-only viewport update owner.
2. Kept `map_renderer.js` as the composition root and preserved `function updateMap(transform)` as the public wrapper.
3. Preserved update order: zoom transform, hit canvas dirty, zoom UI, viewport transform attr, physical brush preview, unit counter scales, special zone pattern transform, and final draw.
4. Added behavior/inventory tests, package script wiring, and architecture boundary gates for owner isolation and wrapper delegation.
5. Fixed the architect BLOCK finding by making `zoom_interaction_lifecycle_owner.js` require `effects.updateMap` and reject optional `effects.updateMap?.(` paths.

Files: core `js/core/renderer/renderer_viewport_update_owner.js`, `js/core/map_renderer.js`, and `js/core/renderer/zoom_interaction_lifecycle_owner.js`; tests `tests/renderer_viewport_update_owner_behavior.test.mjs` and `tests/zoom_interaction_lifecycle_owner_behavior.test.mjs`; tooling `tools/check_architecture_boundaries.mjs` and `package.json`; generated dist `dist/app/js/core/map_renderer.js`, `dist/app/js/core/renderer/renderer_viewport_update_owner.js`, `dist/app/js/core/renderer/zoom_interaction_lifecycle_owner.js`, and `dist/pages-dist-manifest.json`; docs `docs/archive/renderer-viewport-update-owner-p34-20260629/*`, `lessons learned.md`, and this registry. Diff summary: core/test/tooling changes ownerize the viewport update path, strengthen zoom `updateMap` as a required effect, add tests/gates, sync Pages dist, archive docs, and record the E2E metadata lesson. Commit status: functional commit `d0a9fd67`; this closeout commit records final integration truth. Base commit was `origin/main@fef1d059`; final rebase base is `origin/main@13457c54`. Potential conflicts: yellow for future edits to `js/core/map_renderer.js`, `tools/check_architecture_boundaries.mjs`, `package.json`, generated dist, registry, or renderer viewport/zoom owner tests; green against Phase 3A code because Phase 3A is already in `origin/main@13457c54`. Validation passed after rebase: syntax checks for changed JS/test/tool files; `test:node:renderer-viewport-update-owner` 5/5; `test:node:zoom-interaction-lifecycle-owner` 7/7; viewport command 8/8; viewport resize 12/12; fitProjection lifecycle 23/23; renderer runtime state 10/10; strategic overlay runtime 16/16; architecture boundary; state-write allowlist 115 tracked files; test import graph 50 specs; `git diff --check origin/main..HEAD`; `git diff --check`; `verify:pages-dist` with startup shell 41/41 and landing showcase 9/9; `verify:dist-drift`; TNO ready-state 5/5; smoke 4/4 with explicit local base URL. Scenario chunk runtime passed 8/8 before rebase, then full post-rebase reruns returned 7/8, 6/8, and 7/8 around the existing TNO post-edit `FR_ARR_18002` / `pendingInfraPromotion` path; a focused rerun of the failing test passed 1/1, and P34 has no diff in the failing E2E file. Recommendation: push this closeout fast-forward to `origin/main`, remove the local P34 worktree after confirming the push, and keep recovery through functional commit `d0a9fd67`, branch `codex/p34-renderer-viewport-update-owner`, and archived docs.

### Phase 3A Public Product Narrative 2026-06-29

1. Added a top-level public-demo success path to `README.md` and `README.zh-CN.md`, including Live Demo, editor entry, `/app/?view=guide`, TNO 1962 default path, and Project / Export snapshot download.
2. Added matching public-surface tables that separate online baselines, HGO developer/local preview, transport workbench maturity, export workbench, local backend preview, and data provenance records.
3. Added `docs/releases/v0.1-public-demo-draft.md` with demo URL, five public baselines, HGO local preview policy, Pages size status, deployed release smoke coverage, local-only systems, data/source families, known limitations, and GitHub About/topics recommendation.
4. Preserved runtime, map rendering, Pages build policy, data payloads, scenario manifests, and `dist/**`.

Files: core docs `README.md`, `README.zh-CN.md`, `docs/releases/v0.1-public-demo-draft.md`; coordination docs `docs/active/_worktree_registry.md`. Diff summary: docs-only README opening flow, public demo path, surface tables, release draft, and registry row/package. Commit status: Phase 3A docs commit `c9fc044e` and closeout commit `13457c54` are pushed to `origin/main`. Base commit was `origin/main@fef1d059`; latest Phase 3A closeout commit is `origin/main@13457c54`. Potential conflicts: yellow for future README/release/registry edits; green for runtime/render/data/Pages policy. Validation passed: no README/markdown lint script exists in `package.json`; `git diff --check`; `git diff --cached --check`; `py -3 -m unittest tests.test_app_entry_resolver -q` 4/4; docs fact matrix verified headings, five public baselines, TNO default path, HGO local boundary, local backend boundary, Pages size status, release smoke facts, GitHub About recommendation, and changed-file scope. GitHub Actions passed for `c9fc044e`: Build and Deploy Scenario Forge `28406526858`, Scenario Contract Matrix `28406526236`, perf-pr-gate `28406526223`, and Transport Contract Required `28406526216`; the deploy job's `Deploy to GitHub Pages` and `Smoke deployed Pages URL` steps passed. Not run locally: broad app tests, browser smoke, Pages dist rebuild, and deployment smoke because this is a docs-only narrative pass with no runtime/build/data changes and the GitHub deploy smoke covered the public URL. Recommendation: keep this commit and closeout; leave local integrated-worktree cleanup to the broader cleanup pass.

### Audit Final CI Gate Closeout 2026-06-29

1. Audited the related CI-gate cluster through `4a5c1e34`, covering scenario-contract, transport-contract, perf-gate, runtime diagnostics, and Pages deploy drift.
2. Fixed scenario-contract push coverage, removed push-time PR-only expressions, and kept YAML scalar syntax valid across skip/classifier paths.
3. Made scenario contract hashes stable for JSON/GeoJSON text across LF/CRLF while keeping gzipped sidecars byte-exact.
4. Aligned perf-gate CI with Python 3.12 locked dependencies, setup-python interpreter ownership, dev-server reachability probes, runtime diagnostic polling, and richer failure artifacts.
5. Synchronized the generated Pages dist mirror after runtime diagnostics gained boot-state fields.

Files: workflows `.github/workflows/scenario-contract-matrix.yml`, `.github/workflows/transport-contract-required.yml`, and `.github/workflows/perf-pr-gate.yml`; core/tools `tools/check_scenario_contracts.py`, `tools/perf/run_baseline.mjs`, `js/bootstrap/main_runtime_diagnostics.js`, and `.gitattributes`; generated/dist `dist/app/js/bootstrap/main_runtime_diagnostics.js`, `dist/pages-dist-manifest.json`, and TNO scenario contract metadata; tests `tests/test_scenario_contracts.py`, `tests/test_perf_gate_contract.py`, `tests/main_runtime_diagnostics_behavior.test.mjs`, `tests/test_e2e_structural_tooling.py`, and `tests/test_transport_workbench_manifest_runtime_contract.py`; docs `lessons learned.md` and this registry. Diff summary for `918302ce..4a5c1e34`: 18 files changed, 461 insertions, 66 deletions. Commit status: functional commits `e413627e`, `5f23efdf`, `5c0f9dd2`, `719809ae`, `1368c656`, `86517f1d`, `0078fd15`, `2f78a9cf`, `5af7b67a`, and `4a5c1e34` are pushed to `origin/main`; P33 subsequently advanced main to `65bae7ed`. Base commit was `918302ce`; current `origin/main` is `65bae7ed` plus this closeout once pushed. Potential conflicts: yellow for future perf runner/workflow, scenario contract checker, runtime diagnostics, transport contract, P33 registry cleanup, and Pages dist work. Validation passed locally before the P33 advance: `node --check tools/perf/run_baseline.mjs`; `py -3 -m unittest tests.test_perf_gate_contract -q` 22/22; `node --test tests/main_runtime_diagnostics_behavior.test.mjs` 8/8; `py -3 tools/build_pages_dist.py`; `npm run -s verify:pages-dist` with 41 Python tests and 9 landing Node tests; `git diff --check`. GitHub Actions passed for `4a5c1e34`: Scenario Contract Matrix `28404798915`, perf-pr-gate `28404798907`, Transport Contract Required `28404798898`, and Build and Deploy Scenario Forge `28404799047` including deployed Pages smoke. Current main `65bae7ed` also passed Scenario Contract Matrix `28405572346`, perf-pr-gate `28405572365`, Transport Contract Required `28405572360`, and Build and Deploy Scenario Forge `28405572524`. Remaining risk: local integrated worktree cleanup only. Recommendation: push this documentation closeout with skip-ci, remove `C:\Users\raede\.codex\worktrees\mapcreator-audit-final-closeout-20260629`, and keep recovery through pushed `origin/main`.

### Renderer Surface Runtime Bridge P33 2026-06-29

Files: core `js/core/state/renderer_runtime_state.js`, `js/core/map_renderer.js`, matching dist mirrors, `dist/pages-dist-manifest.json`; tests `tests/renderer_surface_runtime_bridge_state_behavior.test.mjs`, `tests/renderer_surface_lifecycle_inventory_boundary.test.mjs`, `tests/test_renderer_runtime_state_boundary_contract.py`; tooling/docs `tools/check_architecture_boundaries.mjs`, `package.json`, archived docs `docs/archive/renderer-surface-runtime-bridge-p33-20260629/`. Diff summary: source/tests/tools update the surface bridge into one runtime state op and add contracts; dist mirrors regenerate from Pages builder. Commit status: functional commit `882fff6c` and closeout commit `65bae7ed` pushed to `origin/main`. Base commit was `2f78a9cffda64ea69378b789a735f7aa1ae8c426`; final rebase base was `origin/main@4a5c1e34`. Potential conflicts: yellow for future edits to `map_renderer.js`, `renderer_runtime_state.js`, architecture boundary, lifecycle inventory, package scripts, and dist mirrors. Validation passed before rebase: syntax checks for changed JS/test/tool files; `test:node:renderer-surface-runtime-bridge-state` 4/4; lifecycle inventory 7/7; lifecycle suite 13/13; fitProjection lifecycle 23/23; runtime state 10/10; Python runtime boundary 5/5; Python spatial owner boundary 1/1; architecture boundary; state-write allowlist; test import graph 50 specs; `git diff --check`; `verify:pages-dist` with startup shell 41/41 and landing showcase 9/9; `verify:dist-drift` after staging generated dist. Rebase note: generated `dist/pages-dist-manifest.json` was the only conflict and was resolved by rerunning Pages builder. Final validation after rebase passed syntax checks, bridge behavior 4/4, lifecycle inventory 7/7, architecture boundary, state-write allowlist, test import graph 50 specs, `git diff --check origin/main..HEAD`, `verify:pages-dist` with startup shell 41/41 and landing showcase 9/9, and `verify:dist-drift`. GitHub Actions for `65bae7ed` passed Scenario Contract Matrix `28405572346`, perf-pr-gate `28405572365`, Transport Contract Required `28405572360`, and Build and Deploy Scenario Forge `28405572524`. Independent code-review reported no findings; independent architecture review returned CLEAR/approve. Remaining risk: local P33 worktree removal only. Recommendation: remove the local P33 worktree after this registry cleanup push; recovery remains functional commit `882fff6c`, closeout commit `65bae7ed`, branch `codex/p33-renderer-surface-runtime-bridge`, and archived docs.

### Pages Release Gate Audit 2026-06-29

1. Audited the latest related Pages release/reproducibility commits through `d7ca503a`, fixed the concrete release-gate target ambiguity in `e42eaaf2`, and fixed the follow-up CI webServer startup failure in `1f067fe5`.
2. Changed `test:e2e:pages-public-release-gate` so candidate runs require `SCENARIO_FORGE_PAGES_URL` or `PLAYWRIGHT_TEST_BASE_URL`; the `:deployed` entry remains the explicit default-public-site check.
3. Wired GitHub Pages deploy to run the release smoke after `actions/deploy-pages`, using `steps.deployment.outputs.page_url` as both the release-gate target and Playwright base URL.
4. Moved TNO coverage ledger/report path constants into lightweight `tools/scenario_contract_paths.py`, keeping deploy-minimal startup-shell tests free of shapely-backed contract imports.
5. Added structural tests and refreshed `tests/e2e/test-import-graph.json` so the release gate, deploy workflow wiring, webServer skip rule, and heavy startup entry stay named and discoverable.

Files: core/workflow `.github/workflows/deploy.yml`, `tools/scenario_contract_paths.py`, `tools/check_scenario_contracts.py`, `tests/e2e/release/pages_public_release_gate.spec.js`, `tests/e2e/support/playwright-web-server.js`, and `package.json`; tests `tests/test_e2e_structural_tooling.py`, `tests/test_pages_dist_startup_shell.py`, `tests/e2e/test-import-graph.json`; temporary worktrees `C:\Users\raede\.codex\worktrees\mapcreator-audit-pages-release-20260629`, `C:\Users\raede\.codex\worktrees\mapcreator-audit-registry-closeout-20260629`, and `C:\Users\raede\.codex\worktrees\mapcreator-audit-pages-release-ci-fix-20260629` removed after their pushes; older integrated worktree `C:\Users\raede\.codex\worktrees\scenario-forge-phase2b-pages-release-gate` removed after run `28398790964` succeeded. Diff summary: `e42eaaf2` changed 8 files with 157 insertions and 37 deletions; `1f067fe5` changed 3 files with 6 insertions and 1 deletion. Commit status: functional commits `e42eaaf2` and `1f067fe5` pushed to `origin/main`; registry closeout follows without recording its own hash in this delivery package. Base commit was `d7ca503a`; current `origin/main` contains `1f067fe5` plus registry closeout. Potential conflicts: yellow with future Pages deploy workflow, public release smoke, package scripts, E2E import graph, Playwright webServer helper, and TNO coverage report path changes. Validation passed: `node --check tests/e2e/release/pages_public_release_gate.spec.js`; package JSON parse; Python `py_compile` for changed Python; new structural unittest 2/2; `tests.test_pages_dist_startup_shell` 41/41; TNO coverage ledger contract 1/1; `npm run -s verify:test-import-graph`; `npm run -s verify:architecture-boundaries`; `npm run -s verify:test-timeout-guardrails`; `npm run -s test:py:pages-dist-startup-shell-heavy` 2/2; `npm run verify:pages-dist` with builder, startup shell 41/41, and landing showcase 9/9; CI fix checks: `node --check tests/e2e/support/playwright-web-server.js`, `py_compile tests/test_e2e_structural_tooling.py`, direct `getWebServerConfig()` assertion with `SCENARIO_FORGE_PAGES_URL`, targeted structural unittest 2/2, and `git diff --check`; GitHub Actions run `28398790964` passed `verify`, `build`, `deploy`, and `Smoke deployed Pages URL`; code-reviewer returned `OK TO LAND` with P0/P1/P2 = 0. Remaining risk: GitHub Actions reported a Node.js 20 deprecation warning for pinned upstream actions forced onto Node 24. Recommendation: keep the functional commits and recovery hashes retained; address the Node action deprecation warning in a separate workflow-maintenance pass.


### Phase 2A Pages Payload Slimming

1. Changed Pages publishing so local-only HGO runtime preview payload, HGO scenario runtime, Japan industrial preview GeoJSON, full city aliases, unpublished catalog entries, unpublished runtime registry assets, and unpublished data manifest outputs stay out of Pages while source data remains intact.
2. Updated HGO preview UI gating so the Pages picker hides HGO Preview when runtime assets are absent, while local developer builds can still expose it.
3. Rewrote Pages `city_aliases.json` as a priority-based subset and updated `dist/app/data/manifest.json` size/hash/count metadata to match the shipped file.
4. Added publish-contract tests for Pages size gate, scenario policy, catalog/registry/data-manifest references, HGO local preview exclusion, Japan industrial preview pruning, and city alias manifest hash alignment.
5. Synced source/dist JS mirrors and localized the new unavailable scenario copy.

Files: core `tools/build_pages_dist.py`, `js/core/hgo_runtime_asset_loader.js`, `js/core/runtime_asset_registry.js`, `js/core/i18n_catalog.js`, `js/ui/scenario_controls.js`, and `dist/app/**` mirrors; tests `tests/test_pages_dist_startup_shell.py`, `tests/test_toolbar_split_boundary_contract.py`; docs archived under `docs/archive/pages-payload-slimming-phase2a-20260629/`, plus this registry. Functional commit `efa2a632` is pushed to `origin/main`. File overlap: yellow only for future Pages dist generator/manifest edits, public scenario policy/runtime registry metadata, and HGO preview behavior. Validation passed: `npm run verify:pages-dist` with builder + 42 Python + 9 Node, `py -3 tools/i18n_audit.py`, `npm run verify:toolbar-split-boundary`, `npm run verify:architecture-boundaries`, `npm run verify:test-import-graph`, HGO runtime Node tests, scenario/data/transport Python contracts, syntax checks, manifest probe, and `git diff --check`. Code-review and architect both returned CLEAR. Remaining risk: browser smoke was skipped because this is a build/publish contract change covered by builder, manifest, registry, Node preview, and Python contract checks. Recommendation: push this closeout commit, verify remote contains it, then clean the temporary worktree.

## Recent Integrated Branches

- `C:\Users\raede\.codex\worktrees\scenario-forge-p42-visible-frame-diagnostics-owner` was removed after functional commit `ab4d90f9` was pushed to `origin/main` and parent `main` fast-forwarded. Recovery remains commit `ab4d90f9` and branch `codex/p42-visible-frame-diagnostics-owner`.
- `C:\Users\raede\.codex\worktrees\mapcreator-p34-renderer-viewport-update-owner` is scheduled for filesystem removal after this closeout push. Recovery remains functional commit `d0a9fd67`, branch `codex/p34-renderer-viewport-update-owner`, and archived docs `docs/archive/renderer-viewport-update-owner-p34-20260629/`.
- `C:\Users\raede\.codex\worktrees\scenario-forge-phase2b-pages-release-gate` was removed after GitHub Actions run `28398790964` passed the deployed Pages smoke. Recovery remains branch `codex/phase2b-pages-release-gate`, original commits `46f25dc1`, `6c39e169`, `e58a5725`, and audit fix commits `e42eaaf2`, `1f067fe5`.
- `C:\Users\raede\.codex\worktrees\mapcreator-p33-renderer-surface-runtime-bridge` is no longer in the Current Worktrees table and is scheduled for filesystem removal after this registry cleanup push. Recovery remains functional commit `882fff6c`, branch `codex/p33-renderer-surface-runtime-bridge`, and archived docs `docs/archive/renderer-surface-runtime-bridge-p33-20260629/`.
- `C:\Users\raede\.codex\worktrees\mapcreator-p32-fit-projection-owner` is no longer in the Current Worktrees table and is scheduled for filesystem removal after this registry cleanup push. Recovery remains functional commit `59924ec7`, closeout commit `dc4e4aff`, branch `codex/p32-fit-projection-owner`, and archived docs `docs/archive/renderer-fit-projection-owner-p32-20260629/`.
- `C:\Users\raede\.codex\worktrees\scenario-forge-phase1` is no longer returned by `git worktree list`; recovery remains commit `a77cb051` and branch `codex/phase1-public-trust-sync`.
- `C:\Users\raede\.codex\worktrees\mapcreator-p30-renderer-svg-surface-lifecycle-owner` is no longer returned by `git worktree list`; recovery remains functional commit `f2fe6435` and branch `codex/p30-renderer-svg-surface-lifecycle-owner`.

These rows are historical integration summaries retained for recovery and sequencing context. They are not current filesystem worktrees.

| Worktree | Branch / HEAD | Base | Status | Dirty / hot files | Evidence | Overlap risk | Integration action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `C:\Users\raede\Desktop\dev\mapcreator-audit-20260628-renderer-projection` | branch `audit/recent-renderer-projection-20260628`; functional commit `5a305f66` | `origin/main@24bac435`, with P27 post-review hardening current at audit start | integrated after push to `origin/main`; this closeout commit records registry truth and local cleanup follows | Core resolved: `js/core/renderer/viewport_resize_lifecycle_owner.js`; tests resolved: `tests/viewport_resize_lifecycle_owner_behavior.test.mjs`; docs/guidance resolved: `lessons learned.md`, registry; generated mirrors resolved: `dist/app/js/core/renderer/viewport_resize_lifecycle_owner.js`, `dist/pages-dist-manifest.json`. Shared hotspots: viewport resize lifecycle owner, owner behavior test, Pages dist manifest. Untouched: `js/core/map_renderer.js`, surface/projection owners, package scripts, architecture tool, and state-write allowlist. | Delivery: audited recent renderer owner/projection cluster P20-P27 and found a browser zoom bug where DPR change and `visualViewport` resize in the same frame could lose forced DPR invalidation; also found DPR media queries clamped below-1 DPR to `1dppx`. Fix keeps a separate pending DPR invalidation flag, applies it through one `handleResize` entry, preserves DPR-only overlay/render behavior, and keeps full visual viewport resize projection/zoom recalculation. Diff summary before registry: 5 files, 108 insertions, 54 deletions. Commit status: functional commit `5a305f66` exists. Base divergence: `HEAD...origin/main` was `0 0` before functional commit, so the audit branch was based on current remote main. Potential conflicts by file path: red with future edits to viewport resize owner/test/Pages dist manifest; yellow with viewport/projection/zoom lifecycle semantics; green against parent docs/archive WIP. Validation passed: node syntax checks for source/dist/test; `npm run -s test:node:viewport-resize-lifecycle-owner` 12/12; `npm run -s verify:architecture-boundaries`; `npm run -s verify:test-import-graph`; `npm run -s verify:pages-dist` with startup shell 39/39 and landing showcase 8/8; source/dist mirror compare; `git diff --check`; surface host 12/12; surface lifecycle 13/13; projection/path inventory 8/8; map interaction 6/6; zoom lifecycle 6/6; viewport command 8/8; viewport read-model 12/12; code-reviewer CLEAR; architect CLEAR. Not run: live browser smoke, because the change is a focused renderer input path covered by owner, boundary, and Pages dist gates. | Green for surface/projection host work and parent WIP; yellow for future viewport resize/browser zoom work; red only for direct edits to the same owner/test/dist files. | Push functional plus closeout commits to `origin/main`, verify remote contains them, then remove the local audit worktree and branch. Recovery via commit `5a305f66` and remote main. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p27-postreview-fix` | branch `codex/p27-postreview-fix`; functional commit `2cea19f5` | `origin/main@fd0af0fa`, with P27 preflight and closeout integrated | integrated after post-review fix push to `origin/main`; this closeout commit records registry truth | Tests/tooling/registry only: `tests/renderer_projection_path_lifecycle_inventory_boundary.test.mjs`, `tools/check_architecture_boundaries.mjs`, and registry. Untouched: production renderer runtime files, `dist/app/**`, package scripts, P27 preflight doc, and state-write allowlist. | Delivery: post-review fixed a weak P27 inventory assertion by slicing `map_renderer.js` around `getProjectedGeometryBoundsOwner()` and `getViewportReadModelOwner()` before checking projection/path getter injection. Architecture boundary now requires those scoped assertions so future edits keep owner-factory-specific guards. Validation passed: `node --check` for the inventory test and architecture tool; projection/path lifecycle inventory 8/8; architecture boundaries; state-write allowlist 115 tracked files; test import graph 49 specs; `git diff --check`; `git diff --cached --check`; `git ls-files --eol` confirmed touched files as `i/lf w/lf`. | Green for production runtime and dist; yellow only for future P28 touching `js/core/map_renderer.js`, architecture boundary, and renderer projection/path inventory tests. | No active integration action after closeout push. Remove the local P27 post-review worktree after push; recovery via functional commit `2cea19f5`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p27-renderer-projection-path-preflight` | branch `codex/p27-renderer-projection-path-preflight`; functional commit `58c88c04` | `origin/main@47cd14e5`, with P26 surface lifecycle owner functional commit `fc87a218` integrated | integrated after functional push to `origin/main`; this closeout commit records registry truth | Docs/test/tooling/package only: `docs/active/renderer-projection-path-lifecycle-preflight-20260627.md`, `tests/renderer_projection_path_lifecycle_inventory_boundary.test.mjs`, `tools/check_architecture_boundaries.mjs`, `package.json`, and registry. Untouched: production renderer runtime files, `dist/app/**`, and `tools/eslint-rules/state-writer-allowlist.json`. | Delivery: P27 documents the current P26 projection/path lifecycle map, locks `map_renderer.js` ownership of projection/path creation and `fitProjection`, keeps the surface lifecycle owner mechanical-only, reserves `renderer_projection_path_owner.js` for P28, and limits P28 to projection/path handle creation plus registration. Validation passed: syntax checks for the new test and architecture tool; projection/path lifecycle inventory 8/8; surface lifecycle 13/13; surface host 12/12; renderer host inventory 7/7; projected geometry bounds 12/12; viewport read model 12/12; architecture boundaries; state-write allowlist 115 tracked files; test import graph 49 specs; `git diff --check`; `git diff --cached --check`; optional Node confidence checks for runtime state 10/10, canvas layer 4/4, viewport command 8/8, viewport resize 12/12, and zoom lifecycle 6/6. Browser E2E was not run in this clean worktree because `node_modules/@playwright/test` is absent and P27 has no runtime diff. | Green for production runtime and dist; yellow only for future P28 touching `js/core/map_renderer.js`, `package.json`, architecture boundary, and renderer projection/path inventory tests. | No active integration action after closeout push. Remove the local P27 worktree after push; recovery via functional commit `58c88c04` and active guidance doc `docs/active/renderer-projection-path-lifecycle-preflight-20260627.md`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p26-renderer-surface-lifecycle-owner` | branch `codex/p26-renderer-surface-lifecycle-owner`; functional commit `fc87a218` | `origin/main@651017e3`, with P25 lifecycle preflight merged | integrated after functional push to `origin/main`; this closeout commit records registry truth | Core resolved: `js/core/renderer/renderer_surface_lifecycle_owner.js`, `js/core/map_renderer.js`, `dist/app/js/core/renderer/renderer_surface_lifecycle_owner.js`, `dist/app/js/core/map_renderer.js`; tests/tooling/docs resolved: `tests/renderer_surface_lifecycle_owner_behavior.test.mjs`, `tests/renderer_surface_lifecycle_inventory_boundary.test.mjs`, `tests/renderer_surface_host_inventory_boundary.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tools/check_architecture_boundaries.mjs`, `package.json`, `dist/pages-dist-manifest.json`, `docs/active/renderer-surface-lifecycle-owner-p26-20260627.md`, and registry. | Delivery: P26 adds a dependency-injected mechanical surface lifecycle owner for DOM lookup, named canvas layer registration, hit canvas handle creation, and 2D context acquisition; `map_renderer.js` remains the composition root and retains projection/path creation, runtimeState bridge writes, setCanvasSize, fitProjection, initZoom, bindEvents, render/update/hit/selection/scenario/exact/strategic semantics. Validation passed: syntax checks; lifecycle owner 6/6; lifecycle inventory 7/7; combined lifecycle 13/13; surface host combined 12/12; surface host inventory 6/6; renderer host inventory 7/7; canvas layer 4/4; viewport resize 12/12; zoom lifecycle 6/6; runtime state 10/10; render diagnostics 21/21; scenario refresh 24/24; exact-after-settle 9/9; scenario chunk 57/57; architecture; state-write allowlist 115 tracked files; test import graph 49 specs; pages-dist 39/39 plus landing showcase 8/8; dist-drift after staging; TNO ready-state 5/5; smoke 4/4; scenario chunk runtime 8/8; `git diff --check`; `git diff --cached --check`; final self-review found only stale static scenario chunk anchors, fixed in tests. | Green after integration; yellow only for future renderer surface lifecycle, shared `map_renderer.js`, package script, architecture boundary, surface/lifecycle inventory tests, scenario chunk static contracts, and dist mirror changes. | No active integration action after closeout push. Remove the local P26 worktree after push; recovery via functional commit `fc87a218`, remote `origin/main`, and active guidance doc `docs/active/renderer-surface-lifecycle-owner-p26-20260627.md`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p25-renderer-surface-lifecycle-preflight` | branch `p25-renderer-surface-lifecycle-preflight`; functional commit `c603c654` | `origin/main@4f3fa895`, with P24 surface host functional commit `284809a6` confirmed as ancestor | integrated after functional push to `origin/main`; this closeout commit records registry truth | Core runtime files untouched: `js/core/map_renderer.js`, `js/core/renderer/renderer_surface_host.js`, `dist/app/**`, and `tools/eslint-rules/state-writer-allowlist.json`. Docs/test/tooling resolved: `docs/active/renderer-surface-lifecycle-preflight-20260626.md`, `tests/renderer_surface_lifecycle_inventory_boundary.test.mjs`, `tools/check_architecture_boundaries.mjs`, `package.json`, and registry. | Delivery: P25 documents the current P24 surface lifecycle map; adds a lifecycle inventory test that keeps `renderer_surface_host.js` registry-only, keeps `map_renderer.js` as the only production importer, locks current `initMap` lifecycle anchors and runtimeState bridge anchors, forbids `renderer_render_lifecycle_owner.js`, and keeps P26 limited to a DOM/canvas/SVG surface lifecycle wrapper. Validation passed: `node --check` on the new test and architecture tool; lifecycle inventory 6/6; surface host combined 12/12; surface host inventory 6/6; renderer host inventory 7/7; architecture boundaries; state-write allowlist with 115 tracked files; test import graph 49 specs; runtime state 10/10; canvas layer 4/4; viewport resize lifecycle 12/12; zoom interaction lifecycle 6/6; `git diff --check`; static code-review CLEAR. Optional browser E2E not run because this clean worktree has no local Playwright dependency and P25 has no runtime diff. | Green for production renderer behavior; yellow only for future P26 touching `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, and P25/P24 inventory tests. | No active integration action after closeout push. Remove the local P25 worktree after push; recovery via functional commit `c603c654` and active guidance doc `docs/active/renderer-surface-lifecycle-preflight-20260626.md`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p24-renderer-surface-host-impl` | branch `codex/p24-renderer-surface-host-impl`; functional commit `284809a6` | `origin/main@56f22e7380416429d6cc5e2ce58fd8472cae8542` | integrated after push to `origin/main`; this closeout commit archives docs and records registry truth | core resolved: `js/core/renderer/renderer_surface_host.js`, `js/core/map_renderer.js`; tests/tooling/docs: `tests/renderer_surface_host_behavior.test.mjs`, `tests/renderer_surface_host_inventory_boundary.test.mjs`, `tools/check_architecture_boundaries.mjs`, `package.json`, `dist/app/**`, archived P24 docs, registry. Untouched: `js/main.js`, `js/core/map_renderer/public.js`, and state-write allowlist. | Delivery: P24 adds a getter-first surface handle registry with `reset`, `setMany`, metadata-only `snapshot`, and individual getters/setters; `map_renderer.js` routes former module-scope DOM/canvas/SVG/projection/path/zoom handles through `rendererSurfaceHost`; canvas manager, DOM/SVG setup, projection/path lifecycle, zoom lifecycle, render/update/hit/selection/scenario/strategic overlay behavior remain in `map_renderer.js`. Validation passed: host behavior 6/6; surface host combined 12/12; inventory 6/6; runtime state 10/10; diagnostics 21/21; scenario refresh 24/24; exact-after-settle 9/9; canvas layer 4/4; physical 6/6; river 8/8; border draw 4/4; border mesh 4/4; architecture; state-write; import graph; pages-dist with startup shell 39/39 and landing showcase 8/8; dist-drift after staging; TNO ready-state 5/5; smoke 4/4; UI rework mainline 5/5; scenario chunk runtime 8/8; diff checks; final code-review found only staging risk, fixed before commit. | Green after integration; yellow only for future surface lifecycle extraction touching `js/core/map_renderer.js`, `package.json`, architecture boundary, or surface host tests. | No active integration action after closeout push. Recovery via functional commit `284809a6`, remote branch `origin/codex/p24-renderer-surface-host-impl`, and archive docs `docs/archive/renderer-surface-host-phase24-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p23-review-fix` | branch `codex/p23-review-fix`; functional commit `00651fb8` | `origin/main@d31bffe6bc6b0e73859ad547b4af9c96f314a9ef` | integrated after post-P23 review fix; closeout commit records registry truth | docs/test/registry resolved: `docs/active/renderer-surface-host-preflight-20260626.md`, `tests/renderer_surface_host_inventory_boundary.test.mjs`, and `docs/active/_worktree_registry.md`. Untouched: production renderer runtime files, `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, and `js/core/map_renderer/public.js`. | Delivery: code-review APPROVE and architect WATCH items resolved by locking the package script entry, changing the P24 candidate API from concrete method names to responsibility boundaries, updating stale P23 test evidence from 5/5 to 6/6, and refreshing the renderer baseline pointer. Validation passed: `node --check tests/renderer_surface_host_inventory_boundary.test.mjs`; `npm run test:node:renderer-surface-host-inventory` 6/6; `npm run test:node:renderer-host-inventory` 7/7; `npm run verify:architecture-boundaries`; `npm run verify:state-write-allowlist`; `npm run verify:test-import-graph`; `git diff --check`. | Green for production behavior because no runtime source changed; yellow only for future P24 surface host doc/test sequencing. | No active integration action after main push. Recovery via commit `00651fb8` and branch `origin/codex/p23-review-fix`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p23-renderer-surface-host-preflight` | branch `codex/p23-renderer-surface-host-preflight`; functional commit `cd0f52e2` | `origin/main@a49f294cdbd4ae60279f65ac3e7b312eb2760fdc` | integrated after preflight commit; closeout commit records registry truth | docs/test/tooling resolved: `docs/active/renderer-surface-host-preflight-20260626.md`, `tests/renderer_surface_host_inventory_boundary.test.mjs`, `package.json`, `tools/check_architecture_boundaries.mjs`, registry. Untouched: production renderer runtime files, `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, and `js/core/map_renderer/public.js`. | Delivery: P23 documents the surface handle inventory, owner dependency map, P24 candidate API, allowed first move, deferred high-risk items, and P24 acceptance standards. The boundary test locks current `map_renderer.js` ownership for DOM/canvas/SVG/projection/path/zoom handles, current owner getter dependency groups, canvas layer manager bridge tokens, the package script entry, and the reserved production surface host module; architecture boundary also reserves `js/core/renderer/renderer_surface_host.js` for P24. Validation passed: `node --check tests/renderer_surface_host_inventory_boundary.test.mjs`; `node --check tools/check_architecture_boundaries.mjs`; `npm run test:node:renderer-surface-host-inventory` 6/6; `npm run test:node:renderer-host-inventory` 7/7; `npm run verify:architecture-boundaries`; `npm run verify:state-write-allowlist`; `npm run verify:test-import-graph`; `git diff --check`. | Green for production behavior because no runtime source changed; yellow only for future P24 surface host, shared `package.json`, architecture boundary, and this active preflight doc. | No active integration action after main push. Recovery via functional commit `cd0f52e2`, branch `origin/codex/p23-renderer-surface-host-preflight`, and active guidance doc `docs/active/renderer-surface-host-preflight-20260626.md`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p22-map-interaction-event-binding-owner` | branch `codex/p22-map-interaction-event-binding-owner`; functional commit `3e9f57df` | `origin/main@cb6f551a0d02e67433947bf4cc1553b52666b45f` | integrated after functional commit; closeout commit records archive/registry truth | core resolved: `js/core/renderer/map_interaction_event_binding_owner.js`, `js/core/map_renderer.js`; tests/tooling/docs: `tests/map_interaction_event_binding_owner_behavior.test.mjs`, `tools/check_architecture_boundaries.mjs`, `package.json`, archived P22 docs, registry. Untouched: `dist/app/**`, state-write allowlist, and public renderer facade. | Delivery: event registration mechanics moved behind `createMapInteractionEventBindingOwner`; host keeps click/fill/selection/brush/tooltip behavior, mouseleave state cleanup, and renderer state writes. Validation passed syntax checks; new owner 6/6; zoom owner 6/6; viewport command 8/8; viewport resize lifecycle 12/12; viewport read model 12/12; host inventory 7/7; architecture; state-write; import graph; diff check; TNO ready-state 5/5; smoke 4/4; interaction funnel 3/3; strategic overlay smoke 1/1; final code-review CLEAR. | Green after integration; yellow only for future map interaction event binding owner, renderer host wrapper, package script, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `3e9f57df`, branch `origin/codex/p22-map-interaction-event-binding-owner`, and archive docs `docs/archive/map-interaction-event-binding-owner-p22-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p21-zoom-interaction-lifecycle-owner` | branch `codex/p21-zoom-interaction-lifecycle-owner`; functional commit `5c60a2e5` | `origin/main@efd0f840cd1b302dce4b628f4f94be415e1516be` | integrated after functional commit; closeout commit records archive/registry truth | core resolved: `js/core/renderer/zoom_interaction_lifecycle_owner.js`, `js/core/map_renderer.js`; tests/tooling/docs: `tests/zoom_interaction_lifecycle_owner_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/e2e/interaction_funnel_contract.spec.js`, `tools/check_architecture_boundaries.mjs`, `package.json`, archived P21 docs, registry. Untouched: `dist/app/**`, state-write allowlist, and public renderer facade. | Delivery: D3 zoom setup plus start/zoom/end lifecycle moved behind `createZoomInteractionLifecycleOwner`; host effects retain state writes and renderer orchestration. Validation passed syntax checks; zoom owner 6/6; related owner node suites; scenario chunk 57/57; architecture; state-write; import graph; diff check; TNO ready-state 5/5; smoke 4/4; interaction funnel 3/3; final code-review no blocking findings. | Green after integration; yellow only for future zoom interaction owner, renderer host wiring, package script, interaction funnel, scenario chunk static contract, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `5c60a2e5`, remote branch `origin/codex/p21-zoom-interaction-lifecycle-owner`, and archive docs `docs/archive/zoom-interaction-lifecycle-owner-p21-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p20-viewport-resize-lifecycle-owner` | branch `codex/viewport-resize-lifecycle-owner-p20-20260626`; functional commit `ac5a9ee0` | `origin/main@d2171b5fec0f78557736bf456416c1691925e204` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout | core resolved: `js/core/renderer/viewport_resize_lifecycle_owner.js`, `js/core/map_renderer.js`; tests/tooling/docs: `tests/viewport_resize_lifecycle_owner_behavior.test.mjs`, `tests/test_frontend_render_boundary_contract.py`, `tools/check_architecture_boundaries.mjs`, `tools/test_route_registry.mjs`, `package.json`, archived P20 docs, registry. Untouched: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, and `js/core/map_renderer/public.js`. | Delivery and validation details are retained in Active Notes below; `origin/main@ac5a9ee0` contains the functional change. | Green after integration; yellow only for future viewport resize lifecycle owner, shared `map_renderer.js` wrappers, package script, route registry, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `ac5a9ee0`, branch `origin/codex/viewport-resize-lifecycle-owner-p20-20260626`, and archive docs `docs/archive/viewport-resize-lifecycle-owner-p20-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-p19-viewport-command-owner` | branch `codex/viewport-command-owner-p19-20260626`; functional commit `837b48c9` | `origin/main@7e72eddf811a064347c15ec0b98f70744f41ba72` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout | core resolved: `js/core/renderer/viewport_command_owner.js`, `js/core/map_renderer.js`; tests/tooling/docs: `tests/viewport_command_owner_behavior.test.mjs`, `tools/check_architecture_boundaries.mjs`, `package.json`, archived P19 docs, registry, `lessons learned.md`. Untouched: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, and `js/core/map_renderer/public.js`. | Delivery and validation details are retained in Active Notes below; `origin/main@837b48c9` contains the functional change. | Green after integration; yellow only for future viewport command owner, shared `map_renderer.js` wrappers, package script, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `837b48c9`, branch `origin/codex/viewport-command-owner-p19-20260626`, and archive docs `docs/archive/viewport-command-owner-p19-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-scenario-water-cache-policy-owner-p18-20260626` | branch `codex/scenario-water-cache-policy-owner-p18-20260626`; functional commit `7c05f633`; closeout commit `07bafc24` | `origin/main@100c6a2c6a8b7621e301ad986e182685dec1adda` | integrated and cleaned locally before P19 | core resolved: `js/core/renderer/scenario_water_cache_policy_owner.js`, `js/core/map_renderer.js`; tests/tooling/docs: `tests/scenario_water_cache_policy_owner_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tools/check_architecture_boundaries.mjs`, `package.json`, registry. Untouched: `dist/app/**`, state-write allowlist, and `js/core/map_renderer/public.js`. | Delivery and validation details are retained in Active Notes below; `origin/main@07bafc24` contains the closeout. | Green after integration; yellow only for future scenario water cache policy and shared renderer/tooling paths. | No active integration action. Recovery via functional commit `7c05f633`, closeout commit `07bafc24`, and branch `origin/codex/scenario-water-cache-policy-owner-p18-20260626`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-viewport-read-model-owner-p17-20260626` | branch `codex/viewport-read-model-owner-p17-20260626`; functional commit `2eb79186` | `origin/main@25df0b12905594fe4e6fd285cd3c3062c473796a` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout commit | core resolved: `js/core/renderer/viewport_read_model_owner.js`, `js/core/map_renderer.js`; tests resolved: `tests/viewport_read_model_owner_behavior.test.mjs`, `tests/test_map_renderer_render_pipeline_passes_boundary_contract.py`; tooling/package/docs: `tools/check_architecture_boundaries.mjs`, `package.json`, archived P17 docs, registry. Untouched by diff: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, `js/core/map_renderer/public.js`, DOM/canvas/SVG/projection/zoom lifecycle, `updateMap`, `resetZoomToFit`, `zoomByStep`, `setZoomPercent`, `enforceZoomConstraints`, `fitProjection`, `handleResize`, `initZoom`, `drawCanvas`, and `renderPassToCache`. | Delivery: viewport/projection signatures, geo viewport bounds, pan extent calculation, renderable content bounds, centered fit transform, and zoom percent moved into `viewport_read_model_owner.js`; host wrappers and side effects stay in `map_renderer.js`. Validation passed syntax checks; viewport owner 12/12; projected geometry bounds 12/12; transform reuse 7/7; render cache 6/6; pipeline catalog 3/3; exact pass catalog 6/6; host inventory 7/7; runtime state 10/10; diagnostics; scenario refresh 23/23; scenario chunk 57/57; canvas layer 4/4; architecture; state-write allowlist; import graph; Python boundary 5/5; TNO ready-state 5/5; smoke 4/4; `git diff --check`; owner side-effect scan clear. | Green after integration; yellow only for future viewport read-model, zoom wrapper dependencies, package script, Python boundary, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `2eb79186`, remote branch `origin/codex/viewport-read-model-owner-p17-20260626`, and archive docs `docs/archive/viewport-read-model-owner-p17-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-projected-geometry-bounds-owner-p16-20260626` | branch `codex/projected-geometry-bounds-owner-p16-20260626`; functional commit `97e385ca` | `origin/main@760f08291cd6425870ed63b327f6709092e13601` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout commit | core resolved: `js/core/renderer/projected_geometry_bounds_owner.js`, `js/core/map_renderer.js`; tests resolved: `tests/projected_geometry_bounds_owner_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`; tooling/package/docs: `tools/check_architecture_boundaries.mjs`, `package.json`, archived P16 docs, registry. Untouched by diff: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, `js/core/map_renderer/public.js`, DOM/canvas/SVG/projection/zoom lifecycle, `drawCanvas`, `renderPassToCache`, render pipeline execution, exact scheduler state machine, and scenario refresh runtime. | Delivery: projected bounds, spherical diagnostics, safe water hit geometry, D3-unsafe water sanitization, and owner-local caches moved into `projected_geometry_bounds_owner.js`; host wrappers and `createSpatialIndexRuntimeOwner` helper names preserved; `runtimeState` diagnostics write remains in host callback; `scenarioWaterPartPathCache` and `scenarioWaterFeaturePathCache` remain host-owned. Validation passed syntax checks; owner 12/12; transform reuse 7/7; render cache 6/6; pipeline catalog 3/3; exact pass catalog 6/6; host inventory 7/7; runtime state 10/10; diagnostics 21/21; scenario refresh 23/23; scenario chunk 57/57; canvas layer 4/4; architecture; state-write allowlist; import graph; TNO ready-state 5/5; smoke 4/4; `git diff --check`; code-review CLEAR. | Green after integration; yellow only for future projected bounds, spherical diagnostics, water geometry safety, package script, scenario chunk contract, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `97e385ca`, remote branch `origin/codex/projected-geometry-bounds-owner-p16-20260626`, and archive docs `docs/archive/projected-geometry-bounds-owner-p16-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-transform-reuse-policy-p15-20260626` | branch `codex/render-transform-reuse-policy-p15-20260626`; functional commit `3f995014` | `origin/main@f00ae918cda4f8f4f813a7f2a1f3c183aa126b07` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout commit | core resolved: `js/core/renderer/render_transform_reuse_policy_owner.js`, `js/core/map_renderer.js`; tests resolved: `tests/render_transform_reuse_policy_owner_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`; tooling/package/docs: `tools/check_architecture_boundaries.mjs`, `package.json`, archived P15 docs, registry. Untouched by diff: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, `js/core/map_renderer/public.js`, `js/core/map_renderer/exact_after_settle_scheduler.js`, `js/core/map_renderer/scenario_refresh_runtime.js`, `js/core/renderer/render_pipeline_passes.js`. | Delivery: transform reuse constants, contextBase/contextScenario reuse decisions, and exact-after-settle fast-path readiness moved to `render_transform_reuse_policy_owner.js`; `map_renderer.js` keeps wrapper names and delegates to the owner; fast-path required list is locked to background, physicalBase, political, contextBase, contextScenario, effects, lineEffects, contextMarkers, dayNight, textureLabels. Validation passed syntax checks; new owner 7/7; render cache owner 6/6; render pipeline catalog 3/3; exact pass catalog 6/6; exact refresh plans 9/9; render pass catalog 6/6; render invalidation catalog 6/6; renderer host inventory 7/7; runtime state 10/10; diagnostics 21/21; scenario refresh 23/23; scenario chunk 57/57; architecture; state-write allowlist; import graph; TNO ready-state 5/5; smoke 4/4; `git diff --check`; code review CLEAR. Smoke retained known local auth 401 and D3 unsafe water geometry warnings. | Green after integration; yellow only for future transform reuse policy, renderer host wrapper, package script, scenario chunk contract, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `3f995014`, remote branch `origin/codex/render-transform-reuse-policy-p15-20260626`, and archive docs `docs/archive/render-transform-reuse-policy-p15-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-exact-after-settle-pass-policy-p14-20260626` | branch `codex/exact-after-settle-pass-policy-p14-20260626`; functional commit `d9e941d5` | `origin/main@df3f54670ae9afb11dbc6455d6fe5e19e727b5a5` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout commit | core resolved: `js/core/renderer/exact_after_settle_pass_catalog.js`, `js/core/map_renderer/exact_after_settle_refresh_plans.js`, `js/core/renderer/render_pipeline_passes.js`, `js/core/map_renderer.js`; tests resolved: `tests/exact_after_settle_pass_catalog_behavior.test.mjs`, `tests/exact_after_settle_refresh_plans_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/test_map_renderer_render_pipeline_passes_boundary_contract.py`; tooling/package/docs: `tools/check_architecture_boundaries.mjs`, `package.json`, archived P14 docs, registry. Untouched by diff: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, `js/core/map_renderer/public.js`. | Delivery: `exact_after_settle_pass_catalog.js` now owns deferred pass names, always-target pass names, and DPR restore pass selection; refresh plans keep compatibility re-exports; render pipeline passes use the catalog default while preserving the constants override; `map_renderer.js` no longer imports or bridges the deferred pass set. Validation passed syntax checks; exact pass catalog 6/6; exact refresh plans 9/9; render pass catalog 6/6; render invalidation catalog 6/6; render pipeline catalog 3/3; render cache owner 6/6; renderer host inventory 7/7; runtime state 10/10; diagnostics 21/21; scenario refresh 23/23; scenario chunk 57/57; architecture; state-write allowlist; import graph; Python render pipeline boundary 5/5; TNO ready-state 5/5; smoke 4/4; `git diff --check`; code review CLEAR. E2E smoke recorded known local auth 401 and D3 unsafe water geometry warnings. | Green after integration; yellow only for future exact-after-settle pass policy, render pipeline owner, package script, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `d9e941d5`, remote branch `origin/codex/exact-after-settle-pass-policy-p14-20260626`, and archive docs `docs/archive/exact-after-settle-pass-policy-p14-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-pipeline-catalog-p13-20260626` | branch `codex/render-pipeline-catalog-p13-20260626`; functional commit `b533493f` | `origin/main@0f07d9943dfec2b1245320160306e4d6dd3519f7` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout commit | core resolved: `js/core/renderer/render_pipeline_catalog.js`, `js/core/renderer/render_pipeline_passes.js`; tests resolved: `tests/render_pipeline_catalog_behavior.test.mjs`, `tests/test_map_renderer_render_pipeline_passes_boundary_contract.py`; tooling/package/docs: `tools/check_architecture_boundaries.mjs`, `package.json`, archived P13 docs, registry. Untouched by diff: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, `js/core/map_renderer/public.js`, `js/core/map_renderer.js`. | Delivery: `IDLE_RENDER_PASS_DEFINITIONS` now owns idle render pass order and draw-key mapping, while `render_pipeline_passes.js` keeps prepare/render behavior and maps catalog entries to injected draw callbacks with noop fallback. Validation passed four `node --check` commands; render pass catalog 6/6; invalidation catalog 6/6; cache owner 6/6; pipeline catalog 3/3; host inventory 7/7; runtime state 10/10; diagnostics 21/21; scenario refresh 23/23; exact-after-settle 8/8; canvas layer 4/4; scenario chunk 57/57; architecture; state-write allowlist; import graph; Python render pipeline boundary 5/5; TNO ready-state 5/5; smoke 4/4; `git diff --check`; code review CLEAR. E2E smoke recorded known local auth 401 and D3 unsafe water geometry warnings. Architect dist/name suggestions were resolved against explicit P13 constraints. | Green after integration; yellow only for future renderer pipeline owner, package script, and architecture boundary changes. | No active integration action after closeout push. Recovery via functional commit `b533493f`, remote branch `origin/codex/render-pipeline-catalog-p13-20260626`, and archive docs `docs/archive/render-pipeline-catalog-p13-20260626/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-cache-p12-20260625` | branch `codex/render-cache-invalidation-owner-p12-20260625`; functional commit `ebfb86f3` | `origin/main@456b05130b37c2a7f8364c2a5a6a8430957388e8` | integrated after push to `origin/main` on 2026-06-26; local cleanup follows this closeout commit | core resolved: `js/core/renderer/render_cache_owner.js`, `js/core/map_renderer.js`; tests resolved: `tests/render_cache_owner_invalidation_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/test_map_renderer_render_cache_owner_boundary_contract.py`; tooling/package/docs: `tools/check_architecture_boundaries.mjs`, `package.json`, archived P12 docs, registry. Untouched by diff: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, `js/core/map_renderer/public.js`. | Delivery: render cache owner now owns render-pass dirty/reason invalidation, reference-transform clearing, last-good-frame invalidation/clear, and interaction-composite invalidation, returning versioned summaries with requested/normalized/dropped pass names and host follow-up flags. Host wrappers retain diagnostics, continuity metrics, political path cache invalidation, interaction border snapshot invalidation, and render orchestration. Validation passed syntax, owner 6/6, render pass catalog 6/6, invalidation catalog 6/6, host inventory 7/7, runtime state 10/10, diagnostics 21/21, scenario refresh 23/23, exact-after-settle 8/8, canvas layer 4/4, scenario chunk 57/57, Python boundary 4/4, architecture, state-write allowlist, import graph, TNO ready-state 5/5, smoke 4/4, `git diff --check`, architect WATCH handled, and code-review finding fixed by staging new files. | Green after integration; yellow only for future renderer cache invalidation, package script, and architecture boundary changes. | No active integration action after closeout push. Recovery via commit `ebfb86f3`, remote branch `origin/codex/render-cache-invalidation-owner-p12-20260625`, and archive docs `docs/archive/render-cache-invalidation-p12-20260625/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-invalidation-p11-20260625` | branch `codex/render-invalidation-catalog-p11-20260625`; functional commit `13b331f1` | `origin/main@498b34a40ac338b46fc1ecc81f42a25eec1f46ac` | integrated after push to `origin/main` on 2026-06-25; local cleanup follows this closeout commit | core resolved: `js/core/map_renderer/render_invalidation_catalog.js`, `scenario_refresh_plans.js`, `scenario_visual_invalidation_executor.js`, `tools/check_architecture_boundaries.mjs`; tests resolved: `tests/render_invalidation_catalog_behavior.test.mjs`, `tests/scenario_refresh_plans_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/test_scenario_chunk_refresh_contracts.py`; package: `package.json`; docs: registry. Untouched by diff: `dist/app/**`, `tools/eslint-rules/state-writer-allowlist.json`, `js/core/map_renderer/public.js`, `js/core/map_renderer.js`. | Delivery: extracts pass-resource/resource-pass maps, default invalidation pass list, first-frame resource allowlists, unsupported pass input keys, and target resource helper APIs into a pure catalog while preserving the `scenario_refresh_plans.js` export facade and visual executor side-effect order. Validation passed syntax checks, Python py_compile, render pass catalog 6/6, render invalidation catalog 6/6, scenario refresh plans 23/23, architecture boundaries, state-write allowlist, renderer host inventory 7/7, renderer runtime state 10/10, render transaction diagnostics 21/21, exact-after-settle 8/8, canvas layer manager 4/4, scenario chunk contracts 57/57, import graph 49 specs, focused Python bridge contract 1/1, TNO ready-state 5/5, smoke 4/4, `node_modules` junction cleanup, and diff checks. | Green after integration; yellow only for future renderer invalidation, package script, and architecture boundary changes. | No active integration action after closeout push. Recovery via commit `13b331f1` and remote branch `origin/codex/render-invalidation-catalog-p11-20260625`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-renderer-host-p9-p10-20260625` | branch `codex/renderer-host-p9-p10-20260625`; functional commit `5e839bc4`; closeout commit `498b34a4` | `origin/main@383a626acd4f15002a80ff20e235a4103581ab43` | integrated after push, cleaned locally before P11 closeout | hot files resolved: `js/core/map_renderer.js`, `js/core/map_renderer/render_pass_catalog.js`, `tools/check_architecture_boundaries.mjs`, `package.json`, `tests/renderer_host_inventory_boundary.test.mjs`, `tests/render_pass_catalog_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/test_map_renderer_render_pipeline_passes_boundary_contract.py`, `docs/active/renderer-host-audit-20260625.md`, registry; dist and state-write allowlist intentionally untouched. | Validation passed on 2026-06-25: syntax checks for modified JS/test/tool files, P9 inventory 7/7, P10 catalog 6/6, architecture boundaries, state-write allowlist, renderer runtime state 10/10, render transaction diagnostics 21/21, scenario refresh 22/22, exact-after-settle 8/8, canvas layer manager 4/4, import graph 49 specs, renderer splits 51/51, scenario chunk contracts 57/57, Python render pipeline boundary 5/5, TNO ready-state 5/5, smoke 4/4, and `git diff --check` with only LF/CRLF warnings. | Green after integration; yellow only for future renderer core/package/architecture boundary changes. | No active integration action. Recovery via commit `498b34a4` and remote branch `origin/codex/renderer-host-p9-p10-20260625`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-startup-ready-handoff-phase8-20260625` | branch `codex/startup-ready-handoff-phase8-20260625`; functional commit `f1943b0b` | `origin/main@3826e4f1c383d9c2a22999ad8dbc7639381d7e18` | integrated after push, archived, and cleaned locally on 2026-06-25 | hot files resolved: `js/main.js`, `js/bootstrap/startup_ready_handoff.js`, `package.json`, startup ready handoff behavior/boundary tests, updated phase7/post-ready static contracts, generated `dist/app/js/main.js`, generated `dist/app/js/bootstrap/startup_ready_handoff.js`, `dist/pages-dist-manifest.json`, archived task docs, and registry | Delivery: extracted `createStartupReadyHandoffOwner(...)`, preserved ready handoff order, scheduler keys, timing, first-ready chunk flush, hydration, context warmup, visual warmup, political reconcile, and full interaction infrastructure warmup, added `npm run test:node:startup-ready-handoff`, rebuilt Pages dist, and kept parent `docs/archive/**` WIP untouched. Validation passed syntax, startup ready handoff 16/16, main bootstrap wiring 8/8, deferred bootstrap 21/21, UI shell boot 11/11, startup failure recovery 14/14, render runtime binding, main runtime diagnostics, post-ready scheduler 10/10, startup hydration 12/12, scenario refresh plans 22/22, exact-after-settle 8/8, render transaction diagnostics 21/21, state-write allowlist, architecture boundaries, Pages dist 39+8, dist drift, TNO ready-state 5/5, smoke 4/4, UI rework mainline 5/5, scenario chunk runtime 8/8, and diff checks. | Yellow only for future startup/bootstrap, package script, phase boundary tests, and generated dist work; green against parent docs/archive deletion WIP. | No active integration action after push. Recovery via functional commit `f1943b0b`, branch `origin/codex/startup-ready-handoff-phase8-20260625`, and archive docs `docs/archive/startup-ready-handoff-phase8-20260625/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-bootstrap-wiring-phase7-20260625` | branch `codex/bootstrap-wiring-phase7-20260625`; functional commit `3f71eaf20ca5fb39254be14d82c0e525638771cc` | `origin/main@a86f6a841539ec0b3622142fd999f07b18ce2578` | integrated after push, archived, ready for cleanup | hot files resolved: `js/main.js`, `package.json`, `tests/main_bootstrap_wiring_boundary.test.mjs`, generated `dist/app/js/main.js`, generated `dist/pages-dist-manifest.json`, archived task docs, and registry | Delivery: removed the proven-unused `normalizeBatchFillScopes` import from `main.js`, kept the deferred UI bootstrap alias stable for UI shell helper injection, added `npm run test:node:main-bootstrap-wiring`, locked phase1-6 owner imports and phase7 ready handoff retention, rebuilt Pages dist, and kept parent `docs/archive/**` WIP untouched. Validation passed syntax, main bootstrap wiring 8/8, deferred bootstrap 21/21, UI shell boot 11/11, startup failure recovery 14/14, render runtime binding 14/14, diagnostics 12/12, post-ready scheduler 10/10, startup hydration 12/12, state-write allowlist, architecture boundaries, Pages dist 39+8, dist drift, TNO ready-state 5/5, smoke 4/4, UI rework mainline 5/5, and diff check. | Yellow only for future startup/bootstrap, package script, and generated dist work; green against parent docs/archive deletion WIP. | No active integration action after push. Recovery via functional commit `3f71eaf20ca5fb39254be14d82c0e525638771cc`, branch `origin/codex/bootstrap-wiring-phase7-20260625`, and archive docs `docs/archive/bootstrap-wiring-phase7-20260625/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-deferred-bootstrap-phase6-20260625` | branch `codex/deferred-bootstrap-phase6-20260625`; functional commit `d1d5c04750cc545f8a2fb60231c425442ab16bbc` | `origin/main@edf9e4dc282a694d33dc931209274d6b558b6808` | integrated after push, archived, ready for cleanup | hot files resolved: `js/main.js`, `js/bootstrap/deferred_vendor_loader.js`, `js/bootstrap/deferred_ui_bootstrap.js`, `package.json`, deferred bootstrap behavior/boundary tests, updated startup/static boundary contracts, generated `dist/app/js/main.js`, generated `dist/app/js/bootstrap/deferred_*.js`, `dist/pages-dist-manifest.json`, archived task docs, and registry | Delivery: extracted deferred milsymbol vendor loading and deferred UI dynamic import/yield/init ordering into two bootstrap owners, preserved cached promise semantics and UI shell helper injection, added `npm run test:node:deferred-bootstrap`, rebuilt Pages dist, and kept parent `docs/archive/**` WIP untouched. Validation passed syntax, deferred bootstrap 21/21, UI shell boot 11/11, startup failure recovery 14/14, render runtime binding 14/14, diagnostics 12/12, post-ready scheduler 10/10, startup hydration 12/12, Python startup contracts 8/8, scenario chunk contracts 57/57, UI mainline static contracts 18/18, state-write allowlist, architecture boundaries, Pages dist 39+8, dist drift, TNO ready-state 5/5, smoke 4/4, UI rework mainline 5/5, and diff check. | Yellow only for future startup/bootstrap and generated dist work; green against parent docs/archive deletion WIP. | No active integration action after push. Recovery via functional commit `d1d5c04750cc545f8a2fb60231c425442ab16bbc`, branch `origin/codex/deferred-bootstrap-phase6-20260625`, and archive docs `docs/archive/deferred-bootstrap-phase6-20260625/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-ui-shell-debug-boot-phase5-20260625` | branch `codex/ui-shell-debug-boot-phase5-20260625@60342f08` | `origin/main@c30486e4285bf780d5893cf908137255d023a672` | integrated after push, archived, ready for cleanup | hot files resolved: `js/main.js`, `js/bootstrap/ui_shell_boot.js`, `package.json`, UI shell boot tests, generated `dist/app/js/main.js`, generated `dist/app/js/bootstrap/ui_shell_boot.js`, `dist/pages-dist-manifest.json`, lessons learned, archived task docs, and registry | Delivery: extracted `runUiShellDebugBoot()` as the UI shell debug boot owner, preserved query semantics, kept dispatcher and UI bootstrap promise visible to failure recovery through hooks before await, rebuilt Pages dist, and added behavior plus boundary tests. Validation passed syntax, UI shell boot 11/11, startup failure recovery 14/14, render runtime binding 14/14, diagnostics 12/12, post-ready scheduler 10/10, startup hydration 12/12, state-write allowlist, architecture boundaries, Pages dist 39+8, dist drift, TNO ready-state 5/5, smoke 4/4, UI rework mainline 5/5, and diff check. | Yellow only for future startup/bootstrap and generated dist work; green against parent docs/archive deletion WIP. | No active integration action after push. Recovery via functional commit `60342f08` and archive docs `docs/archive/ui-shell-debug-boot-phase5-20260625/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-ui-rework-mainline-phase45-20260625` | branch `codex/ui-rework-mainline-phase45-20260625@90b207e2` | `origin/main@9fce96593cc4ff8fa0e8616f86187a0580cb3cfc` | integrated, pushed, archived, and cleaned on 2026-06-25 | hot files resolved: `css/style.css`, `dist/app/css/style.css`, `dist/pages-dist-manifest.json`, `tests/e2e/ui_rework_mainline_shell_sidebar.spec.js`, archived task docs, and registry | Delivery: restored `test:e2e:ui-rework-mainline` from 2/5 to 5/5 by fixing the 1024px scenario/zoom top-rail overlap, accepting the exact 96px single-row bottom dock rail, binding visual-adjustments inspector height to the 66vh CSS contract, and stabilizing Special Regions content assertions around visible list rows or scenario/relief controls. Validation passed ui mainline 5/5, UI mainline static contracts 18/18, TNO ready-state 5/5, smoke 4/4, render runtime binding 14/14, startup failure recovery 14/14, Pages dist 39+8, dist drift, and final `git diff --check`. | Yellow only for future UI shell/sidebar layout and generated dist work; green against startup/bootstrap phase1-4 owner files and parent docs/archive deletion WIP. | No active integration action. Recovery via commit `90b207e2573e37ddbbb72088c6f2b81588ad6c97` and archive docs `docs/archive/ui-rework-mainline-phase45-20260625/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-startup-failure-recovery-phase4-20260625` | branch `codex/startup-failure-recovery-phase4-20260625@8b0d8dde`; functional commit `898b2e1e` | original clean base `origin/main@c8fbe1241eca7bba7900464da67698868dd98f73`, rebased and integrated over `origin/main@2aab955c5bc98694ca6109e5660ed613585557f2` | integrated, pushed, archived, and cleaned on 2026-06-25 | hot files resolved: `js/main.js`, `js/bootstrap/startup_failure_recovery.js`, `package.json`, startup failure recovery tests, generated `dist/app/js/main.js`, generated `dist/app/js/bootstrap/startup_failure_recovery.js`, `dist/pages-dist-manifest.json`, archived task docs, and registry | Delivery: extracted `handleStartupFailure(...)` as the startup failure recovery owner, kept the original deferred UI bootstrap wait plus continue-handler order, added node behavior and boundary coverage, rebuilt Pages dist, archived the delivery package, and cleared the LF-only `dist/app/css/style.css` worktree noise before final closeout. Validation passed syntax, startup failure recovery 14/14, render runtime binding 14/14, main runtime diagnostics 12/12, post-ready scheduler 10/10, startup hydration 12/12, exact-after-settle 8/8, render diagnostics 21/21, state-write allowlist, architecture boundaries, Pages dist 39+8, dist drift, ready-state browser smoke 5/5, e2e smoke 4/4, and final `git diff --check`. | Yellow only for future startup/bootstrap and generated dist work; green against parent docs/archive deletion WIP. | No active integration action. Recovery via closeout commit `8b0d8ddef43667c753d6f530aa792fd499894e4e`, functional commit `898b2e1e418c437a049389de9865ca751cd33ea9`, and archive docs `docs/archive/startup-failure-recovery-phase4-20260625/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-main-runtime-diagnostics-phase2-20260625` | branch `codex/main-runtime-diagnostics-phase2-20260625@5b88fb6d`; functional commit `feca3a19` | `origin/main@949801b1` | integrated into `origin/main`; local worktree and local branch cleaned on 2026-06-25 | hot files resolved: `js/main.js`, `js/bootstrap/main_runtime_diagnostics.js`, `package.json`, diagnostics tests, Python boundary contracts, `dist/app/js/main.js`, `dist/app/js/bootstrap/main_runtime_diagnostics.js`, `dist/pages-dist-manifest.json`, active docs and registry | Prior delivery validated diagnostics 12/12, post-ready scheduler 10/10, startup hydration 12/12, exact-after-settle 8/8, render diagnostics 21/21, state-write allowlist, architecture boundaries, Python contracts, Pages dist 39+8, dist drift, and diff check. Audit cleanup confirmed branch HEAD is contained in `origin/main`. | Green after cleanup; yellow only for future startup/bootstrap edits. | No active integration action. Recovery via remote branch `origin/codex/main-runtime-diagnostics-phase2-20260625` and commit `feca3a19`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-post-ready-scheduler-phase1-20260625` | branch `codex/post-ready-scheduler-phase1-20260625@7beeb4a7`; integrated through `bcfa9eb6` | `origin/main@c4a5632f` | integrated into `origin/main`; local worktree and local branch cleaned on 2026-06-25 | hot files resolved: `js/main.js`, `js/bootstrap/post_ready_scheduler.js`, `package.json`, scheduler tests, startup/scenario chunk/physical contracts, active docs and registry | Prior delivery validated scheduler 10/10, startup hydration 12/12, exact-after-settle 8/8, render diagnostics 21/21, state-write allowlist, architecture boundaries, physical contracts, focused Python contracts, Pages dist, and diff check. Audit cleanup confirmed branch HEAD is contained in `origin/main`. | Green after cleanup; yellow only for future startup scheduling changes. | No active integration action. Recovery via remote branch `origin/codex/post-ready-scheduler-phase1-20260625` and commits `7beeb4a7`, `bcfa9eb6`. |
| `C:\Users\raede\Desktop\dev\scenario-forge-integration` | branch `integrate/post-ready-scheduler-phase1-20260625@949801b1`; integrated head `bcfa9eb6` | `origin/main@c4a5632f` | integrated and pushed; local worktree and local branch cleaned on 2026-06-25 | hot files resolved: post-ready scheduler source/dist, Pages dist manifest, package script, tests, active docs and registry | Prior integration validated scheduler 10/10, startup hydration 12/12, exact-after-settle 8/8, render diagnostics 21/21, state-write allowlist, architecture boundaries, Pages dist 39+8, dist drift, smoke 4/4, physical 2/2, and Python contracts 41/41. Audit cleanup confirmed branch HEAD is contained in `origin/main`. | Green after cleanup. | No active integration action. Recovery via remote branch `origin/integrate/post-ready-scheduler-phase1-20260625` and commit `949801b1`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-runtime-binding-phase3-20260625` | branch `codex/render-runtime-binding-phase3-20260625@912a86d3`; pushed to `origin/codex/render-runtime-binding-phase3-20260625` and `origin/main` | `origin/main@5b88fb6d94e320bad6bb52a58c96ef7ce60f264c` | integrated; local cleanup completed after this registry closeout | hot files resolved: `js/main.js`, `js/bootstrap/render_runtime_binding.js`, `package.json`, render runtime binding tests, `dist/app/js/main.js`, `dist/app/js/bootstrap/render_runtime_binding.js`, `dist/pages-dist-manifest.json`, active docs, registry, lessons learned | Delivery: extracted duplicate startup render dispatcher/boundary/renderNow/ensureDetailTopology/toast/preset binding into `createStartupRenderRuntimeBinding`, kept explicit UI shell and legacy flush reasons, preserved global render hooks, and added behavior plus boundary tests. Validation passed syntax, render runtime binding 14/14, phase1/phase2 startup regressions, renderer diagnostics/state tests, state-write allowlist, architecture boundaries, Pages dist 39+8, dist drift after generated dist updates, browser ready-state 5/5, and e2e smoke 4/4. Optional UI mainline e2e failed 2/5 here and on clean phase2 baseline with matching layout assertions. | Yellow for historical startup/bootstrap and Pages dist overlap; green against parent docs/archive deletion WIP. | No active integration action. Recovery via commit `912a86d35a4de0a15285bf540f30516be5e504b0` and remote branch `origin/codex/render-runtime-binding-phase3-20260625`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-audit-recent-ui-startup-20260624` | `codex/audit-recent-ui-startup-20260624@3a8c1559` plus registry cleanup | `origin/main@36c0ea8f` | integrated after push, local cleanup completed | hot files resolved: special zones workbench source/dist, toolbar boundary contract, special zones behavior test, Pages dist manifest, registry, lessons learned | Delivery: fixes stale special-zone scenario layer UI continuations and loaded/failed caches by binding them to `activeScenarioId` plus `currentScenarioApplyRequestId`; adds stale scenario switch and same-scenario request-cache regressions; syncs Pages dist. Validation passed special zones behavior 15/15, toolbar boundary 53/53, renderer splits 51/51, UI mainline 18/18, architecture boundaries, import graph, Pages dist 39+8, source/dist syntax checks, normalized source/dist parity, `git diff --check`, code-review CLEAR, and architect CLEAR. | Green against parent WIP for production code; yellow only for historical registry/dist overlap. | Pushed to `origin/main` and cleaned locally after remote confirmation. Recovery via commit `3a8c1559` and main history. |
| `C:\Users\raede\.codex\worktrees\mapcreator-ui-palette-startup-integration-20260624` | `codex/ui-palette-startup-integration-20260624@180886f7` plus dist/docs closeout | `origin/main@15941a7dbe54655fbc79c28932159e9d8c47723c` | integrated after push, archived, local cleanup completed | hot files resolved: UI/palette/startup source, focused tests, regenerated dist, registry and archive docs; renderer core source unchanged | Delivery: parent WIP inventory completed read-only with 27 tracked files, 4 untracked files, and no staged changes. Source commit `1197b0d6` migrates UI shell, palette, startup, styled selects, and special-zone workbench timing. Test/support commit `180886f7` refreshes focused UI/startup contracts, import graph, state-write allowlist, and current-baseline architecture budgets. Dist/docs closeout regenerates `dist/app/**`, archives task docs under `docs/archive/ui-palette-startup-integration-20260624/`, and records `git diff --check` as passed. Validation passed UI/palette Node tests, startup Python contracts 45/45, UI support E2E 14/14, import graph, state-write allowlist, architecture boundaries, `verify:pages-dist` 39+8, scenario chunk runtime 8/8, non-1962 1936/1939 Red Sea targeted checks 2/2, and requested renderer Node sentinels. | Green for renderer baseline sentinels and parent checkout preservation; yellow for `css/style.css`, `js/main.js`, UI toolbar/sidebar tests, startup contracts, and generated dist because those are the intentional migrated WIP surface. | Local worktree and local branch were cleaned on 2026-06-24 after confirming the branch is contained in `origin/main`. Recovery via remote branch `origin/codex/ui-palette-startup-integration-20260624` and archive docs `docs/archive/ui-palette-startup-integration-20260624/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-rc-render-chain-stabilization-20260623` | `codex/rc-render-chain-stabilization-20260623@ed08b08a` plus registry closeout | `origin/main@6196e737dbbe211f5dbdb63b7a8a0f9749b30403` | integrated after push, archived, ready for cleanup | hot files resolved: `tests/e2e/non_1962_runtime_matrix.spec.js`, `tests/test_scenario_renderer_bridge_boundary_contract.py`, RC archive docs, registry | Delivery: cleanup commit `ed08b08a435807e35186485dcf6169ac4fb72f92` preserves production renderer behavior and updates stale test contracts only. Full scenario chunk runtime passed 8/8, including Phase 2C `FR_ARR_18002`; full non-1962 matrix covered `blank_base`, `hoi4_1936`, `hoi4_1939`, and `modern_world` and passed 4/4 after test-only optional startup localization sidecar cleanup; 1936/1939 Red Sea checks passed; requested Node suites passed; requested Python boundary suites passed 61/61 after stale bridge import contract cleanup; `verify:pages-dist` passed 39+8; final diff check passed. | Yellow for renderer/test contracts; green for Thematic/Appearance/Map Content UI and parent checkout WIP. | Use this pushed main as the renderer-stable baseline for UI/palette/startup WIP integration. Recovery via branch `codex/rc-render-chain-stabilization-20260623` and archive docs `docs/archive/render-chain-rc-stabilization-20260623/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-phase2b-red-sea-water-political-20260623` | `codex/phase2b-red-sea-water-political-20260623` | `origin/main@bad30a8d7b32ec7f91963538b318b73e3f14d621` | integrated, pushed, archived, ready for cleanup | hot files resolved: `js/core/scenario_runtime_queries.js`, `js/core/scenario/pure_helpers.js`, `js/core/renderer/color_resolution_strategy.js`, `js/core/map_renderer.js`, matching `dist/app/js/core/**` mirrors, `dist/pages-dist-manifest.json`, 1936/1939 runtime matrix E2E, scenario helper/chunk contracts, archived Phase 2B docs, registry | Delivery: B0 classified the Red Sea issue as water-political boundary and hit-gate behavior; Red Sea water runtime/index/canvas paint were present and blue, while hidden water overlay state blocked open-ocean selection. SOV Far East backfill now admits only land candidates, preserving ordinary RU land and shell-fallback land. Generic water-like helpers resolve to ocean fill while Atlantropa owner/base-color surfaces keep owner semantics. Validation passed syntax checks, helper Node tests, scenario chunk contracts 57/57, targeted 1936/1939 runtime E2E 2/2, Phase 1.5/2A Node tests, TNO Phase 2C targeted grep 1/1, full scenario chunk runtime 8/8, Python boundary tests, `verify:pages-dist` 39+8, and final `git diff --check`. | Yellow for renderer/scenario/color shared flow; green for Thematic/Appearance/Map Content UI and parent checkout WIP. Phase 2C was regression-verified, not mixed into this change. | Recovery via branch `codex/phase2b-red-sea-water-political-20260623` and archive docs `docs/archive/phase2b-red-sea-water-political-boundary-20260623/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-audit-admin-metric-dup-closeout-20260623` | `codex/audit-admin-metric-dup-closeout-20260623@40b07f43` plus registry closeout | `origin/main@4b3728c6` | ready to push into `origin/main`, then clean | hot files: `js/core/thematic_admin_metrics_loader.js`, `dist/app/js/core/thematic_admin_metrics_loader.js`, `map_builder/thematic_layer_contracts.py`, `tests/thematic_admin_metrics_loader_behavior.test.mjs`, `tests/test_thematic_layer_contracts.py`, `dist/pages-dist-manifest.json`; docs: registry delivery package | Delivery: duplicate admin metric ids now fail fast in manifest and payload paths; Python contracts cover duplicate manifest and payload `metric_ids`; source and dist loader copies are mirrored; diff summary is 6 files, 117 insertions, 11 deletions before registry. Validation passed `npm run -s test:node:thematic-admin-metrics-loader` 22/22, JS source/dist `node --check`, Python `py_compile`, Python contract unittest 13/13, and `git diff --check`. Functional commit exists as `40b07f43db31e1a82ed17233445ab7c38d8ae673`; main had not diverged from origin at base. | Red versus parent checkout WIP by overlapping admin metric files; green for remote integration because branch starts from current `origin/main`. | Push this branch HEAD to `origin/main`, confirm `main...origin/main` is `0 0`, then remove the temporary worktree. |
| `C:\Users\raede\.codex\worktrees\mapcreator-tno-owner-color-coverage-20260623` | `codex/tno-owner-color-coverage-20260623@7abadf1c` | `origin/main@123e36ec` | integrated, pushed, archived, and ready for cleanup | hot files resolved: `js/core/palette_runtime_bridge.js`, `js/core/scenario_apply_pipeline.js`, lifecycle/palette tests, `lessons learned.md`, Pages dist mirrors, archived task docs | Phase 1.5 stability signal passed; Phase 2A owner/base-color gaps fixed generically; Node/Python/Pages/syntax/diff checks passed. Full scenario chunk e2e passed Phase 1.5 and Phase 2A color coverage, with residual post-edit FR_ARR_18002 pixel probe failure recorded for a later lane. | Green vs parent dirty `manual_ui.json`; yellow only for future scenario apply/color shared-flow work. Phase 2B Red Sea remains separate. | No active integration action after closeout push. Recovery via commit `7abadf1cdc2c04197796a5b1f1163e9e0ebbfcad` and docs `docs/archive/tno-owner-base-color-coverage-phase2a-20260623/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-thematic-admin-metrics-loader-20260622` | `codex/thematic-admin-metrics-loader-20260622@f75e32f4` plus main follow-up quality polish | rebased onto `main@353645b7` | integrated, verified, archived, and cleaned | hot files resolved: `js/core/data_service.js`, `js/core/thematic_admin_metrics_loader.js`, `tests/thematic_admin_metrics_loader_behavior.test.mjs`, `package.json`, `lessons learned.md`, archived task docs, registry | Adds a read-only runtime loader/query API for thematic admin metrics, catalog metadata role/readMode checks, schema version and feature join-key validation, null/source-gap preservation, WGI uncertainty/source field preservation, and focused Node coverage. Validation passed syntax, admin metrics loader 19/19, explicit node test 19/19, thematic catalog 5/5, layer panel 6/6, layer diagnostics 6/6, architecture boundaries, test import graph, `git diff --check`, code-review CLEAR, architect CLEAR, post-merge admin loader 19/19 plus architecture/import/diff checks, and final quality polish with repeat admin loader 20/20 plus architecture/import/diff checks. | Green after integration; direct registry conflict with Legend/Safety was resolved during rebase by preserving main truth and moving admin loader to historical integrated status. | No active integration action; recovery via branch `codex/thematic-admin-metrics-loader-20260622`, commit `f75e32f4`, main follow-up commits, and archive docs `docs/archive/thematic-admin-metrics-runtime-loader-v1/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-thematic-legend-safety-semantics-20260622` | `codex/thematic-legend-safety-semantics-20260622@ecdbd88a` | `origin/main@159870ed0752d5e03ef550c2ac51e2af87125f24` | integrated, pushed, and cleaned | docs only: registry/archive truth and `docs/active/thematic-legend-safety-semantics-20260622/` | Added the active Legend/Safety planning surface, recorded WGI post-push truth, registered admin metrics loader overlap risk, pushed `ecdbd88a` to main, synced main, and removed the temporary worktree. Verification passed `git diff --check`, stale WGI action `rg` check, thematic catalog Node 5/5, architect CLEAR, and code-reviewer APPROVE. | Green after cleanup for production code/data/dist; red registry overlap remains only for the separate admin metrics loader worktree. | No active integration action; recovery via commit `ecdbd88adceaf47d2eee0a0a498f825f6855fc72` and branch `codex/thematic-legend-safety-semantics-20260622`. |
| `C:\Users\raede\Desktop\dev\mapcreator-wgi-real-source-qa-fix-20260622` | `codex/wgi-real-source-qa-fix-20260622@6ac22158`; functional commit `d7e361f4` | `origin/main@902c83fd5aff6bffb8ea1f29ceec36800e6a6882` | integrated, pushed, archived, and cleaned | hot files resolved: WGI ingest/contracts/data assets, thematic runtime catalog diagnostics, Pages dist allowlist/manifest, WGI and Pages startup tests, lessons learned, archived task docs | Preserves WGI source uncertainty fields, keeps composite uncertainty `not_computed`, labels UI metadata as source-derived, publishes thematic registry URLs in Pages dist, and adds runtime-registry URL contracts. Validation passed py_compile, WGI/thematic/data unittest 52, data catalog check 658, data health, thematic catalog 5, layer panel 6, diagnostics 6, toolbar boundary 53, architecture boundaries, state-write allowlist 112, import graph 49, Pages dist build with 39 startup shell tests plus 8 landing tests, runtime registry probe missing_count 0, `git diff --check`, follow-up code review CLEAR, and later cleanup/registry truth commits `551347f4` and `159870ed`. | Green for the completed code/data/dist path; future thematic semantics should start from current main. | No active integration action; recovery via branch `codex/wgi-real-source-qa-fix-20260622@6ac22158` and archive docs. |
| `C:\Users\raede\Desktop\dev\mapcreator-thematic-real-source-wgi-v1-20260622` | `codex/thematic-real-source-wgi-v1-20260622@7336c055` | rebased through current preview closeout main | integrated, verified, archived, pushed, and cleaned | hot files resolved: WGI data assets, thematic manifests/catalog/runtime registry, WGI ingest owner, thematic schemas/contracts, builder, package route, structural route, Pages dist, archived task docs | Adds source-cache-only World Bank WGI 2024 admin0 state-capacity metrics for government effectiveness, rule of law, and a two-input composite. Validation passed py_compile, WGI/thematic 18, structural tooling 28, real/default thematic builds, data catalog build, data manifest/catalog 32, data catalog check 658, architecture boundaries, import graph, Pages dist 38+8, dist drift, `git diff --check`, code-review APPROVE, architect WATCH handled, and quality review GOOD. | Green after integration; yellow only for future thematic UI/rendering phases. | Recovery via commit `7336c05583fa546dcb783970a972ffe3868a855f`, closeout commit `16e9e4ef`, and archived docs `docs/archive/thematic-real-source-wgi-v1/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-thematic-runtime-discovery-readonly-preview-20260622` | `codex/thematic-runtime-discovery-readonly-preview-20260622@a5d99022` | `origin/main@ad4b6b8659d2d56a2e8f01b9f4cbd2428462782f` | integrated, verified, archived, and cleaned | hot files resolved: `css/style.css`, `index.html`, `js/core/runtime_asset_registry.js`, thematic catalog/preview controllers, layer panel/status diagnostics, `package.json`, focused tests, Pages dist mirrors | Adds registry-driven fixture-only thematic catalog discovery and a read-only Map Content preview panel. Validation passed thematic/data builders, 37 Python data contracts, data catalog check, thematic catalog/preview 5/5, layer panel 6/6, layer diagnostics 6/6, route 7/7, structural tooling 28/28, toolbar split 53/53, architecture boundaries, state-write allowlist, import graph, Pages dist 38+8, `git diff --check`, code-reviewer APPROVE, architect CLEAR, post-merge thematic catalog/preview 5/5, toolbar split 53/53, and Pages dist 38+8. | Green after integration; WGI must refresh because it overlaps generated thematic data, route/package scripts, and `dist/**`. | Recovery via commit `a5d99022f2589f0ca071b5165d4f90f233c0b1ae` and docs `docs/archive/thematic-runtime-discovery-readonly-preview-20260622/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-thematic-layer-foundation-v1` | `codex/thematic-layer-foundation-v1-20260622@b81a83ff` | branch created from `main@a023e4a3`, rebased to `origin/main@447b972d`, fast-forwarded into main | integrated, verified, archived, pushed, and cleaned | hot files resolved: `.gitattributes`, `data/thematic_layers/**`, `data/CATALOG.json`, `data/CATALOG.md`, `data/manifest.json`, `data/runtime_asset_registry.json`, `landing/app.js`, `landing/index.html`, `dist/app.js`, `dist/index.html`, `dist/pages-dist-manifest.json`, `map_builder/contracts.py`, `map_builder/runtime_asset_registry.py`, thematic schemas/contracts, thematic tests, `tools/build_thematic_layers.py`, archived task docs | Adds fixture-only political, social, and population thematic layer contracts plus catalog/runtime registry discovery. Validation passed `py -3 -m py_compile map_builder/thematic_layer_contracts.py tools/build_thematic_layers.py`, `py -3 tools/build_thematic_layers.py`, `py -3 tools/build_data_catalog.py`, 37 unittest contract tests, `tools/check_data_catalog.py`, `tools/data_health.py`, architecture boundary, import graph, `git diff --check`, and `verify:pages-dist` with 38 Pages shell tests plus 8 landing node tests. | Green by direct file overlap after Stage C; yellow for future layer-panel/thematic runtime UI work. | No active integration action; recovery via commit `b81a83ffb3ae926b54877869322c38feea712c5f` and docs `docs/archive/thematic-layer-foundation-v1/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-layer-panel-contract-foundation` | `codex/layer-panel-contract-foundation-20260621@79e214b5`; integrated through `codex/stage-c-layer-panel-contract-integration-20260621@c6142749` | `origin/main@a023e4a3a764ef30143598ef3f761deea43f515c` | integrated, verified, archived, pushed, and cleaned | hot files resolved: `js/ui/toolbar/layer_panel_contracts.js`, `js/ui/toolbar/layer_status_diagnostics.js`, `js/ui/toolbar/appearance_controls_controller.js`, `package.json`, toolbar boundary test, Pages dist mirrors, archived task docs | Adds a read-only layer panel contract owner for foundation layer ids, status anchors, transport capability support, and workbench-only unsupported reasons. Validation passed contract test 5/5, layer status diagnostics 5/5, toolbar split boundary 53/53, toolbar scheduler 7/7, Appearance owner suites, scenario chunk contracts 55/55, architecture boundary, state-write allowlist, import graph, Pages dist startup shell 38/38, landing showcase 8/8, post-cleaner verification, independent code-review APPROVE, architect CLEAR, and post-push spot checks. | Green vs thematic by file path; yellow for future Appearance shell and layer contract follow-ups. | No active integration action; recovery via remote branch `origin/codex/layer-panel-contract-foundation-20260621@79e214b5` and `origin/codex/stage-c-layer-panel-contract-integration-20260621@c6142749`. |
| `C:\Users\raede\Desktop\dev\mapcreator-baseline-architecture-repair` / `mapcreator-state-write-allowlist-repair` / `mapcreator-stage-a-integration` | `609f8280` and `a023e4a3`; integrated through `codex/stage-a-baseline-gate-integration-20260621` | `origin/main@1a52603de0be04d798a9e71d50788b9ff5e3c2e2` | integrated, verified, archived, pushed, and cleaned | hot files resolved: renderer boundary budget, canvas color helper ownership, state writer allowlist, baseline task docs, Pages dist mirrors | Stage A restored enforceable baseline gates before Stage C. Validation passed architecture boundary, state-write allowlist, import graph, scenario chunk contracts, renderer runtime state, render transaction diagnostics, layer diagnostics, toolbar scheduler, Pages dist, and post-push spot checks. | Green for current Stage C; yellow only for future renderer-boundary or state-writer allowlist work. | No active integration action; recovery via commits `609f8280` and `a023e4a3`. |
| `C:\Users\raede\Desktop\dev\mapcreator-layer-observability` | `codex/layer-observability-stability-20260621@5f4b78ab`; merged after `origin/main@c8f4f24f` | `origin/main@8e79ea0cebb3a44d89247dc6094baca9f25b22c9` | integrated, verified, archived, and ready for cleanup | hot files resolved: `js/ui/toolbar/appearance_controls_controller.js`, `js/ui/toolbar/toolbar_render_scheduler.js`, `js/ui/toolbar/layer_status_diagnostics.js`, `js/ui/toolbar/appearance_texture_owner.js`, `js/ui/toolbar/ocean_lake_controls_controller.js`, `package.json`, Pages dist mirrors, archived task docs | Adds read-only layer status summaries, workbench-only support text, and high-frequency render coalescing after Stage 5. Validation passed layer diagnostics 5/5, toolbar scheduler 7/7, texture owner 12/12, presets 10/10, toolbar split boundary 52/52, transport 3/3, ocean depth 3/3, ocean render 6/6, Appearance owner suites, import graph, and Pages dist in the feature branch; final integration reran scenario chunk contracts 55/55, import graph, syntax checks, diff checks, and Pages dist with 38 startup shell tests plus 8 landing showcase tests after conflict resolution. Baseline `origin/main@c8f4f24f` reproduces the architecture line-budget and state-write allowlist failures. | Green after merge; only future toolbar diagnostics/scheduling changes overlap this code path. | No active integration action after push. Recovery via feature commit `5f4b78ab` and archive docs `docs/archive/layer-observability-interaction-stability-20260621/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-stage5-visual-acceptance` | `codex/stage5-visual-acceptance@138de67f` plus closeout archive commit | `origin/main@8e79ea0cebb3a44d89247dc6094baca9f25b22c9` | integrated, verified, archived, and ready for cleanup | hot files resolved: `js/core/scenario_chunk_manager.js`, `dist/app/js/core/scenario_chunk_manager.js`, `dist/pages-dist-manifest.json`, `package.json`, Stage 5 dev E2E harness, import graph, state-writer allowlist, lessons learned, archived task docs | Stage 5 summary `pass=true`, 9/9 cases after review fixes; diagnostics latest stable prohibited warnings are zero for every case; stable-kind prohibited warnings since test start are zero; TNO startup/Atlantropa/water/relief/fill-before-zoom report water 141, `scenario_atlantropa` 897, relief 25 as runtime-state required layers; queued scenario switch records `hoi4_1939` then `modern_world`; code-review follow-up APPROVE; architect WATCH handled; `verify:pages-dist` passed. | Green after Layer Observability merge resolved the shared generated files. | No active integration action after push. Recovery via branch `codex/stage5-visual-acceptance`, functional commit `138de67f`, and archive docs `docs/archive/stage5-visual-acceptance-20260621/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-stage3-political-color-readiness` | former `stage3-political-color-readiness@ee9ad5c8`; worktree removed | `origin/main@b2f3a97ef073bf5cc4c7743ede3ea079f0530471` | integrated, pushed, verified, and cleaned | hot files resolved: render diagnostics, map renderer color readiness, exact-after-settle scheduler, scenario chunk contracts, render diagnostics tests, exact-after-settle E2E, Pages dist mirrors, active task docs | Runtime sampling saved `.runtime/output/render-diagnostics/stage3-political-color-readiness.json`: stable Stage 3 core warning count 0, stable deferred Stage 4 semantic warning count 2 for relief. Validation passed render diagnostics 16/16, scenario apply ownership 3/3, scenario runtime 6/6, renderer runtime 10/10, scenario chunk contracts 55/55, scenario apply concurrency E2E, scenario chunk runtime E2E, Pages dist, and final review gates. | Green after cleanup; yellow only for future semantic layer coverage, renderer diagnostics, and Pages dist mirror work. | No active integration action; recovery via commit `ee9ad5c8`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-stage2-main-integration` | former `codex/scenario-apply-stage2-main-integration@8ae91ae6`; worktree removed | `origin/main@29c008f73348752ced55ebd56f916d734b86e37e` | integrated, verified, pushed, and cleaned | hot files resolved: scenario manager, apply pipeline, post-apply effects, chunk runtime, scenario resources, focused tests, Pages dist mirrors | Stage 2 ownership/fence implementation complete. Validation passed syntax checks, scenario apply ownership, render diagnostics, runtime state, lifecycle, chunk contracts, optional layers, Python boundary contracts, E2E concurrency, runtime diagnostics sample, Pages dist, diff check, code-review follow-up CLEAR, post-merge spot checks, and push to `origin/main`. Delivery package archived under `docs/archive/scenario-apply-transaction-stage2-20260621/`. | Green after cleanup; yellow only for future scenario lifecycle/chunk/runtime work. | No active integration action; recovery via branch `codex/scenario-apply-transaction-stage2`, functional commit `c98c65da`, and closeout commit `8ae91ae6`. |
| `C:\Users\raede\Desktop\dev\mapcreator` | former `codex/render-transaction-diagnostics-20260621@21c49be3` | `main@967d9f58` / `origin/main@967d9f58` | integrated, pushed, verified, and cleaned | hot files resolved: scenario apply, scenario post apply, chunk runtime, scenario resources, map renderer, focused Node test, Pages dist mirror, archived docs, lessons learned | Functional commit `21c49be3`; delivery package archived under `docs/archive/render-transaction-diagnostics-20260621/`; validation passed syntax checks, render transaction diagnostics 14/14, scenario chunk contracts 54/54, renderer runtime state 10/10, scenario runtime state 6/6, scenario lifecycle 12/12, optional layers 6/6, architecture boundaries, Pages dist startup shell 38/38, landing showcase 8/8, diff check, and main push verification. | Green after integration; yellow only for future renderer/chunk/scenario diagnostics paths. | No active integration action; recovery via former branch `codex/render-transaction-diagnostics-20260621` and functional commit `21c49be3`. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-chain-slimming-v6` | `codex/render-chain-slimming-v6@4f930787`; worktree removed | `origin/main@a3e4f8a0` | integrated, verified, archived, and cleaned | hot files resolved: `scenario_visual_invalidation_executor.js`, focused visual invalidation contracts, architecture boundary checker, Pages dist mirror and manifest | Executor top-level and execution-plan `targetPasses`/`legacyTargetPasses` now fail fast; final review finding fixed. Pre-merge validation passed scenario refresh 20/20, promotion helpers 9/9, scenario chunk contracts 54/54, exact plans 8/8, runtime state 10/10, Python contracts 46/46, architecture boundaries, import graph, Pages dist builder, landing showcase 8/8, diff check, and source/dist mirror compare. Full `verify:pages-dist` startup shell failed on a base-reproduced landing builder `ArrayMemoryError`; clean main reproduced the same error. Post-merge main passed scenario refresh 20/20, architecture boundaries, import graph, source/dist mirror compare, and diff check. Docs archived under `docs/archive/render-chain-slimming-v6-20260620/`. | Green after worktree cleanup; yellow only for future visual executor, renderer refresh-chain, or landing builder memory gate work. | No active integration action; recovery via branch `codex/render-chain-slimming-v6` and commit `4f930787`. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-chain-slimming-v5` | `codex/render-chain-slimming-v5@39092a82`; worktree removed | `main@f7501edf` | integrated, verified, archived, and cleaned | hot files resolved: `scenario_refresh_plans.js`, `scenario_refresh_runtime.js`, `scenario_visual_invalidation_executor.js`, focused scenario refresh/chunk contracts, architecture boundary tool, Pages dist mirrors and manifest | FrameGraph bridge-to-executor contract now exposes `targetResources`, `invalidationTargetPasses`, and `hasExplicitTargetResources`; retired `executionPlan.targetPasses` fails fast; renderer `targetPasses` remains at renderer refresh-plan and legacy direct-call boundaries. Pre-merge validation passed scenario refresh 17/17, promotion helpers 9/9, scenario chunk contracts 54/54, exact plans 8/8, runtime state 10/10, Python contracts 46, architecture boundaries, import graph, Pages dist, diff check, source/dist mirrors, and final review no findings. Post-merge main passed scenario refresh 17/17, scenario chunk contracts 54/54, architecture boundaries, import graph, Pages dist startup shell 38/38, landing showcase 8/8, diff check, and source/dist mirrors. Docs archived under `docs/archive/render-chain-slimming-v5-20260620/`. | Green after worktree cleanup; yellow only for future renderer refresh-chain or Pages dist work. | No active integration action; recovery via branch `codex/render-chain-slimming-v5` and functional commit `39092a82`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-chain-slimming-v4` | `codex/render-chain-slimming-v4@2b4be437`; worktree removed | `main@861e79e5` | integrated, verified, archived, and cleaned | hot files resolved: `scenario_refresh_plans.js`, scenario refresh behavior/static contracts, architecture boundary tool, Pages dist mirrors | FrameGraph invalidation factory is resource-first; pass-shaped descriptor inputs now fail fast; pass fallback remains in `resolveFrameGraphInvalidationExecutionPlan(...)`; dead promotion pass local removed. Pre-merge validation passed scenario refresh 16/16, promotion helpers 9/9, scenario chunk contracts 54/54, exact plans 8/8, runtime state 10/10, Python contracts 46, architecture boundaries, import graph, Pages dist, diff check, source/dist mirror check, and final review no findings. Post-merge main passed architecture boundaries, scenario refresh plans 16/16, scenario chunk contracts 54/54, and import graph. Docs archived under `docs/archive/render-chain-slimming-v4-20260620/`. | Green after worktree cleanup; yellow only for future renderer refresh-chain or Pages dist work. | No active integration action; recovery via branch `codex/render-chain-slimming-v4`, functional commit `7f15cf43`, and integration package commit `2b4be437`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-chain-slimming-v3` | `codex/render-chain-slimming-v3@3eefa80e`; worktree removed | `main@7c8b375f2dfe0e8a159be00b9e1de626cd8b6c75` | integrated, verified, archived, and cleaned | hot files resolved: `scenario_refresh_plans.js`, `scenario_refresh_runtime.js`, `scenario_chunk_promotion_helpers.js`, behavior/static contracts, architecture boundary tool, Pages dist mirrors | Retired `legacyTargetPasses` and descriptor-level `targetPasses` from new FrameGraph invalidation descriptors; kept bridge output pass fields as resolved execution data; removed promotion delta `resources.legacyTargetPasses`; renamed runtime metric to `targetPassCount`. Pre-merge validation passed targeted Node tests, Python contracts, architecture boundaries, import graph, Pages dist, repeated Pages dist after rebase conflicts, and diff check. Post-merge main passed architecture boundaries, scenario refresh plans 16/16, scenario chunk contracts 54/54, exact-after-settle refresh plans 8/8, renderer runtime state 10/10, import graph, and diff check. Docs archived under `docs/archive/render-chain-slimming-v3-20260620/`. | Green after worktree cleanup; yellow only for future renderer refresh-chain, legacy pass unification, or Pages dist work. | No active integration action; recovery via branch `codex/render-chain-slimming-v3`, commits `35f81376` and `3eefa80e`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-chain-slimming-v2` | `codex/render-chain-slimming-v2@b4df20eb`; worktree removed | `origin/main@5351c25d2a0dd951fb69da91be4a10263844febd` | integrated, verified, archived, and cleaned | hot files resolved: `scenario_refresh_plans.js`, `scenario_refresh_runtime.js`, new `scenario_visual_invalidation_executor.js`, exact-after-settle refresh helper/scheduler, scenario chunk promotion helper, behavior/static contracts, architecture boundary tool, Pages dist mirrors | FrameGraph invalidation execution reads now go through `resolveFrameGraphInvalidationExecutionPlan(...)`; visual invalidation side effects now live in the executor; explicit empty resources skip pass invalidation; exact-after-settle pass filtering uses a local helper. Pre-integration validation passed targeted Node tests, Python contracts, architecture boundaries, import graph, Pages dist, and diff check. Post-merge validation passed architecture boundaries, scenario refresh plans 16/16, scenario chunk contracts 54/54, import graph, and diff check. Docs archived under `docs/archive/render-chain-slimming-v2-20260620/`. | Green after worktree cleanup; yellow only for future renderer refresh-chain, exact contract unification, or Pages dist work. | No active integration action; recovery via branch `codex/render-chain-slimming-v2`, functional commit `b4df20eb`, and closeout commit `0c15bf55`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-resource-authority` | `codex/render-resource-authority@bbf0e1ca`; worktree removed | `origin/main@7bbebfb4e9ec12c077aebdeb0ba883df719fe9a5` | integrated, verified, archived, and cleaned | hot files resolved: `scenario_refresh_plans.js`, `scenario_refresh_runtime.js`, `scenario_chunk_promotion_helpers.js`, `chunk_runtime.js`, `scenario_renderer_bridge.js`, Pages dist mirrors, TNO coverage ledgers, focused tests, `.gitattributes` derived JSON LF rule | Resource authority, pure delta, runtime descriptor, first-frame allowlist, and TNO ledger byte-hash sync are merged. Verification passed scenario refresh plans 11/11, promotion helpers 9/9, runtime state 10/10, scenario chunk contracts 54/54, exact plans 7/7, startup hydration 12/12, HGO preview 21/21, canvas layer 4/4, transport preview 27/27, data service 8/8, Python refresh/render contracts 40/40, TNO relief 3/3, strict TNO safe repair, architecture boundaries, import graph, Pages dist startup shell 38/38, landing showcase 8/8, post-merge architecture boundaries, post-merge scenario refresh plans 11/11, post-merge scenario chunk contracts 54/54, and post-merge Pages dist startup shell 38/38 plus landing showcase 8/8 after derived JSON LF stabilization. | Green after worktree cleanup; yellow only for future renderer refresh-chain, HGO/TNO refresh contracts, or byte-exact scenario ledger work. | No active integration action; recovery via branch `codex/render-resource-authority`, commit `bbf0e1ca`, and archive docs `docs/archive/render-resource-authority-20260620/`. |
| `C:\Users\raede\Desktop\dev\mapcreator-tno-coverage-chain-audit` | `codex/parent-wip-docs-cleanup@e36f3016` plus closeout registry/archive commit; worktree scheduled for removal | `origin/main@73e64166` | integrated cleanup branch; ready for push and parent sync | docs-only files: stale `docs/archive/*` deletions, `lessons learned.md`, registry, archived cleanup docs | 16 archive directory names had zero references; `data-foundation-audit` had no path-level references and only historical worktree-name mentions. `git diff --check -- "lessons learned.md" docs` passed with line-ending warnings only. | Green for production code; yellow only for shared docs file history. | Push `origin/codex/parent-wip-docs-cleanup` and fast-forward `origin/main`; keep recovery via branch and commits before removing the local worktree. |
| `C:\Users\raede\Desktop\dev\mapcreator-audit-20260620-render-frame` | `audit/recent-render-frame-20260620@853678ba`; worktree and local branch removed | `origin/main@27ace561` | integrated, pushed, verified, and cleaned | hot files resolved: `js/core/map_renderer.js`, `dist/app/js/core/map_renderer.js`, `dist/pages-dist-manifest.json`, `tests/scenario_chunk_contracts.test.mjs`, `tests/scenario_refresh_plans_behavior.test.mjs` | Fixed political frame readiness to count real `runtimeState.colors` with color-revision caching, made deferred full-cache slices reject stale transform signatures, and added a runtime execution test for default FrameGraph chunk-promotion invalidation. Verification passed JS syntax checks, scenario chunk contracts 54/54, scenario refresh plans/runtime 8/8, architecture boundaries, import graph, Pages dist startup shell 37/37, landing showcase 8/8, source/dist mirror compare, diff check, cloud SHA proof, and final code review CLEAR. | Green after cleanup; TNO owner still needs a registry rebase because this file moved. | No active integration action; recovery via commits `853678ba` and `84e2d068`. |
| `C:\Users\raede\Desktop\dev\mapcreator-frame-transaction` | `frame-transaction-convergence@be9acb74`; worktree and local branch removed | `main@07eecef3` | integrated, pushed, archived, and cleaned | hot files resolved: `js/core/map_renderer.js`, `scenario_refresh_plans.js`, `scenario_refresh_runtime.js`, scenario chunk runtime/resources/manager, draw subset helper, renderer runtime state, focused tests, Pages dist mirrors, architecture budget | Functional commit `be9acb74` adds stable committed frame identity for visible transactions and last-good reuse/reject, required semantic layer resolution for TNO Atlantropa/water, FrameGraphInvalidation descriptors consumed by runtime invalidation, and DrawSubsetIndex helper contracts. Verification passed scenario refresh plans 7/7, chunk promotion helpers 7/7, renderer runtime state 10/10, scenario chunk contracts 54/54, worker packet 5/5, Python boundary 14/14, Python scenario refresh/bridge 39/39, architecture boundaries, clean-worktree dist drift, diff check, and final review CLEAR. | Green after integration; yellow for future renderer/chunk-runtime/Pages dist work. | No active integration action; recovery via commit `be9acb74`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-transaction-stability` | `codex/render-transaction-stability@4dae0a16`; pushed to `origin/main` | `origin/main@d3671ca5e8117e0bbc3f8503444072b8359ea091` | integrated, pushed, verified, archived, and ready for cleanup | hot files resolved: `js/workers/political_raster.worker.js`, `js/core/scenario/chunk_runtime.js`, `js/core/scenario_resources.js`, `js/core/map_renderer/scenario_refresh_plans.js`, matching `dist/app/js` mirrors, worker/scenario/refresh-plan tests, archived docs | Functional commit `516a64eb` preserves scene/data generation identity in political raster worker replies, routes visible optional chunk promotion through `isScenarioOptionalLayerRequestedForVisibility`, adds strategic values refresh pass mapping, and keeps hidden optional chunks data-only. Closeout commit `4dae0a16` records delivery evidence. Verification passed worker packet 3/3, scenario chunk contracts 51/51, Python refresh/cache/runtime hooks 44/44, Python scenario resources 53/53, scenario refresh plans 5/5, chunk promotion helpers 3/3, `verify:pages-dist`, JS syntax checks, diff check, architect CLEAR, code-simplifier PASS, and code-reviewer no code findings. | Green after fast-forward integration; yellow only for future scenario chunk runtime, optional-layer visibility, refresh-plan, or worker-protocol work. | Remove local feature worktree after final status confirmation; recovery via branch `origin/codex/render-transaction-stability` and commits `516a64eb`, `4dae0a16`. |
| `C:\Users\raede\Desktop\dev\mapcreator` | `codex/render-scene-snapshot-stability@0c13e8e0`, fast-forwarded into `main` | `origin/main@8c13c395b9704cb8c380d4aa8c0f05e302326074` | integrated, pushed, verified, and archived | hot files resolved: renderer pass cache, runtime state, worker bitmap client, scenario chunk runtime, scenario rollback, matching dist mirrors, rollback/render contracts | Added scene/data generation identity across visible frames, worker bitmaps, pass cache metadata, deferred full-cache work, interaction composites, partial repaint, and rollback restore. Political feature override writes now use one transaction; water-region overrides stay in their own visual domain. Verification passed JS syntax checks, Node 87/87, Python 70/70, scenario chunk 47/47, Pages dist startup shell 37/37 and landing showcase 8/8, diff check, code-review CLEAR, and post-merge main checks 22/22 Node plus 7/7 Python. Docs archived under `docs/archive/render-scene-snapshot-stability-20260619/`. | Green after fast-forward integration; yellow only for future renderer/rollback hot-path work. | No active integration action; keep feature branch for recovery until routine branch cleanup. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-fluidity-audit` | `codex/render-fluidity-post-audit@a2577335`; cleanup after closeout | `origin/main@da191163` | integrated, pushed, archived, and ready for cleanup | hot files resolved: `js/core/map_renderer.js`, `dist/app/js/core/map_renderer.js`, `tests/scenario_chunk_contracts.test.mjs`, archived docs | Found accepted worker bitmap frames were consumed after main-thread political background fill; commit `a2577335` moves ready bitmap consumption before `drawPoliticalBackgroundFills` and locks the order in scenario chunk contracts. Verification passed syntax, canvas layer 4/4, worker packet 2/2, scenario chunk contracts 47/47, perf probe 5/5, perf gate 22/22, import graph, Pages dist startup shell 37/37, landing showcase 8/8, diff check, and final review CLEAR. | Green after fast-forward integration; yellow only for future renderer/perf work. | No active integration action after closeout push; recovery via commit `a2577335` and branch `origin/codex/render-fluidity-post-audit`. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-fluidity-p4-p5` | `codex/render-fluidity-p4-p5@da191163`; worktree removed | `origin/main@dcd7c9d8` | integrated, pushed, archived, and cleaned | hot files resolved: `js/core/map_renderer.js`, `js/core/political_raster_worker_client.js`, `js/workers/political_raster.worker.js`, `js/core/map_renderer/*packet/layer*`, renderer/worker tests, perf runner, Pages dist mirror | Functional commit `4f79fc03` pushed to `origin/codex/render-fluidity-p4-p5` and fast-forwarded to `origin/main`; closeout commit `da191163` recorded delivery and cleanup. Archived docs under `docs/archive/render-fluidity-p4-p5-20260619/`. | Green after integration; yellow only for future renderer or worker-protocol work. | No active integration action; recovery via commits `4f79fc03` and `da191163`. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-fluidity-p1-p3` | `codex/render-fluidity-p1-p3@15008502`; worktree removed | `origin/main@d1faea1f` | integrated, pushed, and cleaned | hot files resolved: `js/core/map_renderer.js`, scenario refresh runtime, scenario chunk promotion helpers, runtime state, perf gate, tests, Pages dist mirror, registry | Functional commit `15008502` pushed to `origin/codex/render-fluidity-p1-p3` and fast-forwarded into `origin/main`; registry closeout preserved recovery evidence. | Green after integration; yellow only for future renderer/perf metric changes. | No active integration action; recovery via commit `15008502`. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-fluidity-integration` | `codex/render-fluidity-main-integration@dcd7c9d8`; worktree removed | `origin/main@d1faea1f` | integrated, pushed, archived, and cleaned | archived task docs and registry only after functional push | Clean integration branch fast-forwarded feature commits, ran post-merge validation, pushed to `origin/main`, and was removed after closeout. | Green; historical recovery row only. | No active integration action; recovery via commits `15008502` and `dcd7c9d8`. |
| `C:\Users\raede\Desktop\dev\mapcreator-hgo-render-continuity` | `codex/hgo-render-continuity@312821df`, fast-forwarded through `codex/hgo-render-continuity-main-integration` | `main@bf76965d` | integrated, verified, archived, and ready for cleanup | hot files resolved: HGO preview render owner, new HGO frame committer, `js/core/hgo_runtime_preview.js`, `js/core/map_renderer.js`, exact-after-settle policy, data/Page dist manifests, HGO/runtime/render contract tests, archived task docs | Functional commit `123d0b6f` implemented staged HGO frame commits, preserved the runtime hook `targetCanvas` payload, allowed a complete low-ratio first projected HGO frame, skipped cache clean/signature updates on `committed:false`, and kept inactive dirty `hgoPreview` out of idle refresh plans. Branch closeout `312821df` recorded the delivery package. Pre-integration verification passed `verify:hgo-runtime-poc`, `verify:pages-dist`, focused political progressive recovery E2E 3/3 on port 8812, scenario chunk contracts 44/44, architecture boundaries, import graph, Python render/cache/public/runtime hook contracts, node syntax checks, diff check, and independent review after fixes. Post-merge integration validation passed HGO runtime preview 21/21 and scenario chunk contracts 44/44. Docs archived under `docs/archive/render-performance-hgo-continuity-20260619/`. | Green after fast-forward integration; yellow only for future renderer pass lifecycle changes. | Push the clean integration branch to `origin/main`, then remove local integration and feature worktrees after final status confirmation. |
| `C:\Users\raede\.codex\worktrees\mapcreator-module-boundary-continuation-v2` | `codex/module-boundary-continuation-v2@a1c0d6e2`, fast-forwarded into main; closeout pushed at `c664efd5`; local branch/worktree removed | `main@0ca9e3df` | integrated, pushed, verified, archived, and cleaned | hot files resolved: `js/core/map_renderer.js`, three private map_renderer owners, matching dist mirrors, `dist/pages-dist-manifest.json`, `package.json`, architecture/spike tools, renderer/scenario/HGO/physical/Python boundary tests, archived docs | Extracted scenario refresh runtime, exact-after-settle scheduler, and HGO preview render owner. Added `verify:architecture-boundaries` and `spike:hgo-runtime-lod`. Pre-merge passed syntax, scenario refresh 4/4, exact plan 5/5, HGO runtime preview 20/20, physical layer 2/2, scenario chunk 44/44, Python owner/boundary 77/77, import graph, Pages dist, diff check, ai-slop scan, and read-only code/architecture reviews. Post-merge main passed architecture boundary, scenario chunk 44/44, import graph, Pages dist startup shell 37/37, landing showcase 8/8, and diff check. Docs archived under `docs/archive/module-boundary-continuation-v2-20260619/`. | Green after merge; yellow only for future renderer-boundary work that touches `map_renderer.js` and Pages dist. | No active integration action; recovery via commits `a1c0d6e2` and `c664efd5`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-module-boundary-phase-d-guardrails` | `codex/module-boundary-phase-d-guardrails@23a6c645`, fast-forwarded into main | `main@e4b04c66` | integrated, pushed, verified, and worktree removed | hot files resolved: `tests/test_toolbar_split_boundary_contract.py`, active docs, registry | Adds toolbar shell and scenario context owner source-line budgets to the existing toolbar split boundary contract. Passed pre-merge toolbar boundary 51 tests, import graph 48 specs, diff check, and ai-slop scan. Post-merge main passed toolbar boundary 51 tests, import graph 48 specs, and diff check. | Green for final QA; yellow only for future toolbar shell growth because the budget is intentionally strict. | No active integration action; use branch `codex/module-boundary-phase-d-guardrails` for recovery. |
| `C:\Users\raede\.codex\worktrees\mapcreator-module-boundary-phase-b-ui-shell` | `codex/module-boundary-phase-b-ui-shell@838b08fa`, fast-forwarded into main | `main@84b9ef5b` | integrated, pushed, verified, and worktree removed | hot files resolved: `js/ui/toolbar.js`, `js/ui/toolbar/scenario_context_bar_controller.js`, matching dist mirror, controller behavior test, toolbar/UI boundary tests, `package.json`, Pages manifest, active docs | New scenario context bar controller owns labels, selection chip, safe-width layout, collapse binding, resize refresh, and guide highlight timer. Pre-merge passed syntax, controller behavior 6 tests, toolbar boundary 50 tests, UI foundation 6 tests, HGO preview 19 tests, import graph, Pages dist startup shell 37 tests, landing showcase 8 tests, ai-slop scan, Curie code review, Archimedes docs review, and diff check. Post-merge main passed controller 6 tests, toolbar boundary 50 tests, HGO preview 19 tests, import graph, Pages dist, and diff check. | Yellow only for future `scenario_controls.js` selector-shell work; green for Phase D guardrails. | No active integration action; use branch `codex/module-boundary-phase-b-ui-shell` for recovery. |
| `C:\Users\raede\Desktop\dev\mapcreator` | `codex/hgo-vector-scene-terrain-suppression@deba2034`, fast-forwarded into `main@deba2034` | `main@db2e40d1` | integrated and pushed | hot files resolved: `js/core/renderer/border_draw_owner.js`, `js/core/renderer/ocean_render_owner.js`, `js/core/renderer/river_layer_render_owner.js`, matching dist mirrors, owner behavior tests, Pages manifest | HGO vector scene terrain suppression skips canonical coastline/accent/base-river strokes for HGO vector scenes. Passed owner behavior 18 tests, HGO preview 19 tests, HGO strict contract, `verify:pages-dist`, import graph, and diff check. | Green for future UI files; yellow only for future renderer-owner work. | No active integration action. |
| `C:\Users\raede\.codex\worktrees\mapcreator-hgo-runtime-preview-fix` | `codex/hgo-runtime-preview-fix@db2e40d1` | `main@ea9fd52a` | integrated, pushed, Git worktree removed; locked `.runtime/dev` residue remains | hot files resolved: `js/core/map_renderer.js`, `js/ui/scenario_controls.js`, matching dist mirrors, HGO preview tests, renderer/toolbar/Pages contracts, `package.json`, Pages manifest | Functional commit `db2e40d1` keeps HGO projected preview inside the renderer lifecycle, carries current projection/zoom options through render and inspect, updates toolbar restore/reset behavior, and moves npm Python scripts to `tools/run_python.mjs`. Passed HGO node 19 tests, HGO strict scenario contract, import graph, `verify:pages-dist`, Python renderer/toolbar/Pages contracts 92 tests after manifest refresh, package JSON parse, and diff check. Feature branch pushed. | Green for Phase B after merge; red only if the preserved border/ocean follow-up stash is later replayed over Pages manifest or renderer owner files. | No active integration action. Recovery follow-ups are stashed as `preserve-hgo-worktree-followups-before-cleanup-20260618T1935` with patch backup `.runtime/cleanup-backups/hgo-worktree-followups-before-cleanup-20260618T1935/`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-module-boundary-phase-c-backend-shell` | `codex/module-boundary-phase-c-backend-shell@89642668` | `main@5fc3dc3d` | integrated, pushed, and worktree removed | hot files resolved: `backend/app.js`, `backend/backend_console_helpers.js`, `tests/backend_console_helpers.test.mjs`, `tools/run_python.mjs`, `package.json`, active docs, registry | New backend console helper owner and Python wrapper added; helper tests and backend verification entries updated. Passed wrapper version check, helper node 6 tests, backend preview Python 25 + Node 13 + syntax checks, backend cloud-support Node 36 tests, diff check, ai-slop scan, Ptolemy boundary review, and Helmholtz coverage review. Post-merge clean main validation passed `verify:backend-preview`, backend cloud-support 36 tests, and diff check. Duplicate parent HGO WIP preserved in stash `preserve-main-hgo-runtime-preview-duplicate-wip-before-phase-c-integration-20260618T182321` plus patch backup. | Green vs active HGO by path; package-script changes are now on main. | No active integration action; push main after closeout docs commit. |
| `C:\Users\raede\.codex\worktrees\mapcreator-module-boundary-phase-a-renderer-shell` | `codex/module-boundary-phase-a-renderer-shell@9139c073` | `main@4ea5252e` | integrated, pushed, and worktree removed | hot files resolved: `js/core/map_renderer.js`, `js/core/map_renderer/exact_after_settle_refresh_plans.js`, `dist/app/js/core/map_renderer.js`, exact-after-settle tests/contracts, `package.json`, active docs | New exact-after-settle plan owner added; Pages dist rebuilt; syntax, exact-after-settle 5 tests, scenario refresh 4 tests, hit candidates 5 tests, render pipeline Python contract 5 tests, physical layer 2 tests, import graph, Pages startup shell 37 tests, landing showcase 8 tests, diff check, ai-slop scan, ponytail CLEAR, and behavior review PASS completed. Clean post-merge detached worktree validation passed exact-after-settle 5 tests, scenario refresh 4 tests, hit candidates 5 tests, physical layer 2 tests, renderer boundary 5 tests, import graph, `verify:pages-dist`, and diff check. `scenario_chunk_contracts` remains 43/44 with existing `hoverFacilityAndCityProbeMetricsRemainNamed` failure. | Yellow vs active HGO runtime preview worktree; Phase A itself is now on main. | No active integration action; branch and main pushed; worktree removed. |
| `C:\Users\raede\.codex\worktrees\mapcreator-module-boundary-slimming` | `codex/module-boundary-slimming@4db72f12`; main closeout commit follows | `origin/main@5494431c` | integrated and pushed | hot files resolved: `js/core/map_renderer.js`, `js/core/scenario/scenario_renderer_bridge.js`, `js/core/i18n.js`, `js/core/i18n_catalog.js`, `js/core/map_renderer/scenario_refresh_plans.js`, `js/core/map_renderer/interaction_hit_candidates.js`, `js/main.js`, `dist/app/js`, `dist/pages-dist-manifest.json`, i18n/render/scenario tests | Phase 1 moved pure i18n/catalog/tooltip logic to core, kept UI DOM i18n in `js/ui/i18n.js`, routed core toast calls through `showToastFn`, moved startup audit environment reads into bootstrap, rebuilt import graph and Pages dist. Phase 2 moved pure scenario refresh plans and chunk-promotion target-pass policy into `scenario_refresh_plans.js`; scenario bridge remains a thin wrapper. Phase 3 moved pure hit result, grid candidate collection, ranking, first-containing search, hit-result mapping, and water-hit preference into `interaction_hit_candidates.js`; renderer wrappers inject runtime state, metrics, d3, country-code, and water policies. Targeted Phase 1 suite 104/104 passed; Phase 2/3 node behavior tests 4/4 and 5/5 passed; targeted Python renderer/bridge/spatial/public/import/runtime-hook suites passed; support node contracts passed; `verify:test-import-graph` passed; Pages dist builder passed; pages dist startup shell 37/37 passed; landing showcase 8/8 passed; `scenario-chunk-contracts` remains 43/44 with existing `hoverFacilityAndCityProbeMetricsRemainNamed`; ai-slop scan clean; code-reviewer stale contract finding fixed then APPROVE; architect CLEAR. Post-merge validation on main passed: syntax checks, 4/4 refresh plans, 5/5 hit candidates, 21 Python contract tests, import graph, Pages dist builder, startup shell 37/37, landing showcase 8/8, and diff check. Docs archived under `docs/archive/module-boundary-slimming-20260618/`. | Red vs parent renderer WIP remains for future work: parent still touches `map_renderer.js`, matching dist, Pages manifest, package/test surfaces. | Cleanup module worktrees after confirming recovery commit and branch refs are available. |
| `C:\Users\raede\.codex\worktrees\mapcreator-tno-hgo-integration` | `codex/tno-hgo-integration@a714cad3` plus archive closeout | `origin/main@1206eb43` | integrated and pushed | hot files resolved: `js/core/map_renderer.js`, `dist/app/js/core/map_renderer.js`, render pipeline, HGO projection, scenario apply pipeline, targeted tests, Pages manifest | TNO fix confirmed in base; HGO cherry-picked; final review reported NO P0/P1; HGO node 39/39, runtime state 9/9, HGO boundary 5/5, TNO E2E 3/3, Pages dist builder + 37 unittest + 8 landing tests passed; `git diff --check` passed. Scenario chunk contract is 43/44 with existing `hoverFacilityAndCityProbeMetricsRemainNamed` failure. Docs archived under `docs/archive/tno-hgo-integration-20260618/`. | Red overlap resolved by keeping HGO early returns ahead of TNO pending-edit normal path. | Main fast-forward/push complete; keep recovery via commit hashes and archive docs. |
| `C:\Users\raede\.codex\worktrees\mapcreator-hgo-preview-projection-base-replace` | `codex/hgo-preview-projection-base-replace@7a95e26c` | `origin/main@1206eb43` | integrated into `codex/tno-hgo-integration` | hot files changed: `js/core/hgo_projection_model.js`, `js/core/map_renderer.js`, `js/core/renderer/render_pipeline_passes.js`, `js/core/scenario_apply_pipeline.js`, HGO/scenario/renderer/Pages dist tests, `.gitattributes`, `tools/build_pages_dist.py`, `dist/app`, Pages dist manifest | Targeted HGO node 39/39, renderer boundary 5/5, Pages dist unittest 37/37, landing node 8/8, syntax/source-dist/diff checks passed; independent static re-review APPROVE; task docs archived under `docs/archive/hgo-preview-projection-base-replace-20260618/`. | Red vs parent TNO WIP resolved in integration branch. | Cleanup allowed after final closeout records recovery commit `7a95e26c`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-landing-work-maps-integration` | `main@0538481a`; former branch `codex/landing-work-maps-integration` at `a48eec68` | `origin/main@6874731f` | integrated and pushed | hot files resolved: `landing/index.html`, `landing/app.js`, `landing/assets/work-*.{svg,webp,json}`, `dist/index.html`, `dist/assets/work-*.{svg,webp,json}`, `tools/build_landing_work_maps.py`, `tools/rasterize_landing_assets.py`, landing and Pages dist tests, archived task docs | Commit `a48eec68`; work-map builder, rasterizer, Pages dist builder, startup shell unittest 37/37, landing node behavior 8/8, diff check, browser card smoke, and independent reviewer APPROVE passed. Closeout commit `0538481a` archived docs under `docs/archive/landing-local-work-maps-20260617/` and updated this registry. | Green vs renderer WIP: landing/static asset files only; parent renderer checkout preserved as separate WIP. | Clean up this integrated worktree after final status confirmation. |
| `C:\Users\raede\Desktop\dev\mapcreator` | `main` / former `codex/geometry-simplification-benchmark-spike` | `main@b984a59e` | integrated | hot files resolved: `tools/perf/polyline_simplification_benchmark.mjs`, `tests/fixtures/polyline_simplification_benchmark_fixtures.mjs`, `tests/polyline_simplification_benchmark_contract.test.mjs`, `package.json` scripts, archived task docs | Commit `97626a26`; red-first contract failed before implementation with `ERR_MODULE_NOT_FOUND`; helper + benchmark tests passed; benchmark script and syntax checks passed; dev-only `simplify-js@1.2.4` comparison ran from `.runtime`; forbidden dependency scan found no package/package-lock matches; independent code-reviewer APPROVE and architect CLEAR; post-merge focused validation passed; `verify:pages-dist` skipped because app source, dist files, and Pages manifests were untouched. Delivery package archived under `docs/archive/geometry-simplification-benchmark-and-dependency-spike-20260617/`. | Green | Push `origin/main`, then delete merged local feature branch |
| `C:\Users\raede\Desktop\dev\mapcreator` | `main` / former `codex/geometry-simplification-helper-contracts` | `main@06604075` | integrated | hot files resolved: `js/core/map_renderer.js`, `js/core/renderer/polyline_simplification_helpers.js`, border mesh boundary tests, `dist/app`, Pages dist manifest, archived task docs | Commit `b59fb749`; helper behavior tests, border mesh boundary contract, node syntax checks, owner behavior, renderer splits, Pages dist verification, diff check, dependency scan, source/dist helper byte compare, architect CLEAR, code-review APPROVE, closeout QA PASS, and post-merge short validation passed. Delivery package archived under `docs/archive/geometry-simplification-helper-contracts-20260617/`. | Green | Push `origin/main`, then delete merged local feature branch |
| `C:\Users\raede\Desktop\dev\mapcreator` | `main` / former `codex/strategic-overlay-editor-followups` | `main@ee42ba98` | integrated and pushed | hot files resolved: `js/core/map_renderer.js`, `js/core/renderer/strategic_overlay_runtime_owner.js`, `js/core/renderer/strategic_overlay_runtime/operation_graphics_runtime_domain.js`, strategic overlay tests, `dist/app`, Pages dist manifest, dependency Spike docs | Commit `5780fa16`; runtime/render owner tests, Python boundary contracts, renderer split suite, Pages dist verification, `node --check`, `git diff --check`, residual transaction scan, post-merge short validation, and main push passed. Docs archived under `docs/archive/strategic-overlay-editor-followups-20260617/`. | Green | No active integration action |
| `C:\Users\raede\Desktop\dev\mapcreator` | `main` / former `codex/strategic-overlay-cleanup-followups` | `origin/main@07e98d17` | integrated and pushed | hot files resolved: `js/core/map_renderer.js`, `js/core/renderer/strategic_overlay_render_owner.js`, `js/core/renderer/strategic_overlay_runtime/operation_graphics_runtime_domain.js`, strategic overlay tests, `dist/app`, Pages dist manifest | Commit `0a8b351e`; runtime/render owner node tests, Python boundary contracts, renderer split suite, Pages dist verification, `git diff --check`, ai-slop-cleaner scan, independent review, and post-merge validation passed. Docs archived under `docs/archive/strategic-overlay-cleanup-followups-20260617/`. | Green | No active integration action |
| `C:\Users\raede\Desktop\dev\mapcreator` | `main` | `origin/main@0095aed6` | integrated and pushed | hot files resolved: `js/core/map_renderer.js`, strategic overlay runtime/render owner files, strategic overlay tests, `package.json`, `dist/app`, Pages dist manifest | Commit `e2473cbb`; runtime/render owner node tests, Python boundary contracts, renderer split suite, Pages dist verification, `git diff --check`, architecture review, and code review passed after fixing drag session snapshot pollution; post-merge validation passed on `main` | Green | No active integration action |
| `C:\Users\raede\Desktop\dev\mapcreator` | `main` | `origin/main` closeout | integrated and pushed | hot files resolved: `js/core/map_renderer.js`, `js/core/renderer/city_points_render_owner.js`, city owner behavior test, urban/city boundary test, city e2e support fixture, `dist/app`, Pages dist manifest | Commit `79dfc678`; explicit d3 helper injection, node syntax, owner behavior, boundary, appearance city-points, Pages dist, city e2e 8/8, layer smoke 4/4, post-merge short validation, archive closeout, and main push passed. Review fixed a shared city zoom settle timeout clamp. | Green | No active integration action |

## Current Overlap Matrix

| Pair | Risk | Reason |
| --- | --- | --- |
| Phase C backend shell vs integrated HGO runtime preview fix | Green | Both are now on main; Phase C backend files and HGO renderer/UI files have no active file overlap. |
| Integrated HGO runtime preview fix vs Phase B UI shell | Green | Both are now on main; Phase B preserved HGO preview wiring and kept `scenario_controls.js` untouched. |
| Integrated HGO vector terrain suppression vs Phase B | Green | Both are now on main; renderer terrain suppression and toolbar chrome ownership have no active file overlap. |
| Phase B toolbar context-bar owner vs future scenario selector shell | Yellow | Phase B now owns toolbar chrome in `scenario_context_bar_controller.js`; Hypatia recommended a later `scenario_controls.js` selector shell split that should start from current main. |
| Phase A exact-after-settle extraction vs integrated HGO runtime preview fix | Green | Both are on main; exact-after-settle policy remains in its pure owner and HGO uses adjacent renderer lifecycle hooks. |
| Parent TNO dirty checkout vs current main | Green for behavior coverage; red for staging | `origin/main` already contains TNO recovery behavior and module-boundary owners; staging parent renderer/dist/test files would revert those owners. |
| Module boundary slimming Phase 1-3 vs future localization/i18n work | Yellow | Runtime catalog source moved to `js/core/i18n_catalog.js` while `js/ui/i18n_catalog.js` remains a compatibility re-export; future translation tooling should use the core source of truth. |
| Module boundary slimming Phase 2-3 vs future renderer interaction work | Yellow | New pure owners `scenario_refresh_plans.js` and `interaction_hit_candidates.js` now hold refresh and hit-candidate policies; future renderer refactors should extend these owners before adding shell logic. |
| TNO scoped recovery vs HGO preview fix | Green after `origin/main@e1f4eb20` | Main contains both TNO pending edit behavior and HGO integration records; no active code integration remains. |
| TNO scoped recovery vs current `origin/main` landing work maps | Green | Scoped TNO files are renderer/runtime/test files; landing work-map files already live on `origin/main` and are not imported from the dirty parent checkout. |
| HGO preview projection/base replacement vs parent TNO renderer WIP | Green after `origin/main@e1f4eb20` | HGO and TNO code paths are already integrated in main; parent dirty files are recovery material. |
| Landing work maps vs parent renderer WIP | Green | Clean branch touches homepage/static asset generation and landing behavior tests; parent renderer WIP touches app renderer/runtime files. The only filename overlap is `package.json`, and this branch carries only the landing build script. |
| Landing work maps vs future Pages dist work | Yellow | This branch regenerates `dist/index.html`, `dist/assets/work-*`, and `dist/pages-dist-manifest.json`; future Pages dist branches should start from the pushed main. |
| Historical integrated worktrees vs current main | Green | Rows below are recovery records; no active local integration action remains for them. |

## Recovery Records

These rows are branch or commit recovery indexes. They are historical references, not current worktrees.

| Former worktree | Recovery ref | HEAD commit | Removed at | Archived docs | Reopen condition |
| --- | --- | --- | --- | --- | --- |
| HGO runtime preview follow-up recovery backups | stashes `preserve-hgo-worktree-followups-before-cleanup-20260618T1935` and `preserve-main-border-ocean-river-followup-before-phase-b-20260618T191148`; patch backups `.runtime/cleanup-backups/hgo-worktree-followups-before-cleanup-20260618T1935/` and `.runtime/cleanup-backups/main-border-ocean-river-followup-before-phase-b-20260618T191148/` | represented by commit `deba20346b4d90cc9649136bbf9374095511c8ce` | 2026-06-18 Git worktree unregistered; locked zero-byte `.runtime/dev/hgo-fix-server.*.log` residue remains | active docs `docs/active/module-boundary-continuation-20260618/` | Recovery only; current behavior is now in a normal commit. |
| Parent checkout duplicate HGO runtime preview WIP before Phase C integration | stash `preserve-main-hgo-runtime-preview-duplicate-wip-before-phase-c-integration-20260618T182321`; patch backup `.runtime/cleanup-backups/phase-c-main-hgo-duplicate-preserve-20260618T182321/` | based on `main@89642668` merge window | not a removed worktree; parent checkout was cleaned by stashing duplicate WIP | active docs `docs/active/module-boundary-continuation-20260618/` | Use only if HGO runtime preview work is missing from `C:\Users\raede\.codex\worktrees\mapcreator-hgo-runtime-preview-fix`; prefer that active worktree as the source of truth. |
| Parent checkout duplicate HGO runtime preview WIP during Phase A integration | stashes `preserve-main-dirty-map-renderer-before-phase-a-integration-20260618T2158Z`, `preserve-main-hgo-runtime-preview-wip-before-phase-a-integration-20260618T2202Z`, `preserve-main-hgo-runtime-preview-wip-after-phase-a-pages-verify-20260618T2208Z`, `preserve-main-hgo-runtime-preview-duplicate-wip-after-phase-a-push-20260618T2218Z`; patch backups under `.runtime/cleanup-backups/phase-a-main-dirty-preserve-*` | based on `main@9139c073` after Phase A merge | not a removed worktree; parent checkout was cleaned by stashing duplicate WIP | active docs `docs/active/module-boundary-continuation-20260618/` | Use only if HGO runtime preview work is missing from `C:\Users\raede\.codex\worktrees\mapcreator-hgo-runtime-preview-fix`; prefer that active worktree as the source of truth. |
| `C:\Users\raede\Desktop\dev\mapcreator` parent dirty checkout | branch `codex/tno-political-color-recovery@a4957713`; stash `3a251933f031959dbe303700ed0cebd6e77486d3`; backup `.runtime/cleanup-backups/parent-dirty-cleanup-20260618T194327Z/`; branch `origin/codex/tno-political-recovery-integration@fcd63a32` | `a4957713` plus stash `3a251933f031959dbe303700ed0cebd6e77486d3` | 2026-06-18 after backup, stash, and switch to clean `main@8f289606` | `docs/archive/parent-dirty-checkout-cleanup-20260618/`; related `docs/archive/tno-political-edit-progressive-recovery-20260618/`, `docs/archive/landing-local-work-maps-20260617/`, and `docs/archive/module-boundary-slimming-20260618/` | Inspect this stash or backup only when recovering old dirty parent WIP; continue active development from clean `main`. |
| `C:\Users\raede\.codex\worktrees\mapcreator-tno-political-recovery-integration` | branch `codex/tno-political-recovery-integration@fcd63a32`; main registry closeout `da94091d` | `fcd63a32` plus `da94091d` | 2026-06-18 after main push | `docs/archive/tno-political-edit-progressive-recovery-20260618/` | Inspect this branch/docs when auditing the covered TNO political color recovery, parent dirty checkout boundaries, or the 2026-06-18 validation evidence. |
| `C:\Users\raede\.codex\worktrees\mapcreator-render-chain-cleanup-city-lights` | branch `codex/render-chain-cleanup-city-lights`; main closeout branch `codex/render-main-integration-20260616` | `31c7be26` plus registry closeout commit | cleanup after main push | `docs/archive/render-chain-cleanup-city-lights-20260616/` | Inspect this branch/docs when auditing City Lights owner extraction, historical 1930 night lights, or layer smoke stale-contract repairs. |
| `C:\Users\raede\.codex\worktrees\mapcreator-city-lights-baseline` | detached `origin/main@bb40fe02` | `bb40fe02` | cleanup after push | none; comparison-only worktree | Used only for same-machine perf/layer-smoke baseline checks; remove after final closeout. |
| `C:\Users\raede\.codex\worktrees\mapcreator-renderer-review-fixes` | `origin/main` after renderer owner postmerge review fix / former branch `codex/renderer-review-fixes` | `60f13301` | 2026-06-16 | `docs/archive/renderer-owner-postmerge-review-20260616/` | Inspect this commit/docs when auditing renderer owner intensity channel tolerance or modern city lights render-cache contract location. |
| `C:\Users\raede\.codex\worktrees\mapcreator-transport-render-data-closeout` | `origin/main` after transport closeout push / former branch `refactor/transport-render-data-closeout` | `106c83c2` | 2026-06-16 | `docs/archive/transport-render-data-closeout-20260616/` | Inspect this commit and archive docs when auditing preview splits, dist drift guards, smoke profile schema, or OSM-GPKG builder registry changes. |
| `C:\Users\raede\Desktop\dev\mapcreator` | `origin/main` after renderer owner integration / branch `codex/renderer-modern-lights-owner` | `ace6bc34` | 2026-06-16 | `docs/archive/renderer-modern-lights-owner-20260616/` | Inspect this commit and archive docs when auditing map renderer owner splits for Modern City Lights, Rivers, Ocean, Physical, or Scenario relief overlays. |
| `C:\Users\raede\Desktop\dev\mapcreator-localization-governance-20260615` | `main` / former branch `codex/localization-governance-20260615` | `4711b0dd` | 2026-06-15 | `docs/archive/localization-governance-20260615/` | Inspect `4711b0dd` and archived docs when auditing localization governance ownership changes. |
| `C:\Users\raede\Desktop\dev\mapcreator-a11y-home-app-fix-20260615` | commit `55f143de` | `55f143de` | 2026-06-15 | `docs/archive/a11y-home-app-fix-20260615/` | Inspect commit/docs when auditing homepage and app-page accessibility fixes. |
| `C:\Users\raede\Desktop\dev\mapcreator-audit-20260615-registry-closeout` | `origin/main` | `691c933f` | 2026-06-15 | `docs/archive/housekeeping-review-fix-20260615/` | Inspect `691c933f` when auditing this automation's registry correction. |
| `C:\Users\raede\Desktop\dev\mapcreator-housekeeping-review-fix-20260615` | commit `64ae29be` | `64ae29be` | 2026-06-15 | `docs/archive/housekeeping-review-fix-20260615/` | Inspect commit/docs when auditing this registry review fix. |
| `C:\Users\raede\Desktop\dev\mapcreator-worktree-housekeeping-20260615` | commit `c076d5e5` | `c076d5e5` | 2026-06-15 | `docs/archive/worktree-housekeeping-20260615/` | Inspect commit/docs when auditing cleanup history. |
| `C:\Users\raede\Desktop\dev\mapcreator-e2e-route-contract-repair-20260615` | commit `f4d16b20` | `f4d16b20` | 2026-06-15 | `docs/archive/e2e-route-contract-repair-20260615/` | Inspect commit/docs when auditing E2E route contract repairs. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-data-chain-split-20260615` | commit `b8727d42` | `b8727d42` | 2026-06-15 | `docs/archive/render-data-chain-split-20260615/` | Inspect commit/docs when auditing render/data split work. |
| `C:\Users\raede\Desktop\dev\mapcreator-data-chain-phase2-3-deepening-2026-06-14` | commit `b1db07b1` | `b1db07b1` | earlier cleanup | prior active/docs history | Inspect commit when recovering phase 2/3 transport deepening details. |
| `C:\Users\raede\Desktop\dev\mapcreator-data-chain-integration-2026-06-14` | commit `77d18776` | `77d18776` | earlier cleanup | prior active/docs history | Inspect commit when recovering data-chain integration decisions. |
| `C:\Users\raede\Desktop\dev\mapcreator-tooling-simplification-phase1` | commit `d7125716` | `d7125716` | 2026-06-16 | `docs/archive/tooling-simplification-phase1/` | Main already covered this branch during cherry-pick; inspect commit/docs only when recovering old tooling validation details. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-chain-cleanup-phases` | commit `85621443` | `85621443` | 2026-06-16 | prior active/docs history | Covered by later render-chain phase 4/5 and current main runtime helpers; inspect commit only for old line preview helper history. |
| `C:\Users\raede\Desktop\dev\mapcreator-render-chain-cleanup` | commit `8e89262e` | `8e89262e` | 2026-06-16 | `docs/archive/render-chain-cleanup-phase4-5/` | Missing spatial/chunk helper work was integrated; inspect commit/docs when auditing render helper extraction. |
| `C:\Users\raede\Desktop\dev\mapcreator-data-quality-repair-2026-06-14` | commit `b856ceca` | `b856ceca` | 2026-06-16 | `docs/archive/data-quality-repair-2026-06-14/` | Main already contains the nested water geometry coordinate guard; inspect commit/docs only for old data repair context. |
| `C:\Users\raede\Desktop\dev\mapcreator-data-chain-phases-2-4` | commit `d858d276` | `d858d276` | 2026-06-16 | `docs/archive/data-chain-phases-2-4/` | Main already contains the helper boundaries with newer runtime updates; inspect commit/docs only for old phase 2-4 context. |
| `C:\Users\raede\Desktop\dev\mapcreator-audit-20260612-appearance-transport` | commit `01811500` | `01811500` | 2026-06-16 | prior active/docs history | Main already contains the appearance preset intensity revision repair; inspect commit only for old audit context. |
| `C:\Users\raede\Desktop\dev\mapcreator-audit-20260610-20260610-113750` | commit `cf2a57a1` | `cf2a57a1` | 2026-06-15 | prior active/docs history | Inspect commit when recovering old audit preservation details. |
| `C:\Users\raede\Desktop\dev\mapcreator-hoi4-strategic-values` | commit `979b20de` | `979b20de` | 2026-06-15 | prior active/docs history | Inspect commit when recovering HOI4 strategic values details. |
| `C:\Users\raede\Desktop\dev\mapcreator-tooling-simplification-phase2` | commit `9a5febfe` | `9a5febfe` | earlier cleanup | prior active/docs history | Inspect commit when recovering tooling phase 2 details. |
| `C:\Users\raede\Desktop\dev\mapcreator-tooling-simplification-phase3` | commit `e296e660` | `e296e660` | earlier cleanup | prior active/docs history | Inspect commit when recovering tooling phase 3 details. |
| `C:\Users\raede\Desktop\dev\mapcreator-tooling-simplification-phase4a` | commit `16360a6f` | `16360a6f` | earlier cleanup | prior active/docs history | Inspect commit when recovering tooling phase 4A details. |

## Active Notes

- 2026-07-01 Phase 6D sample export proof delivery package: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-phase6d-sample-export-proof` was created from `origin/main@c20cc67f` because the parent checkout has unrelated docs/archive/landing/lessons WIP. What changed: public samples now carry validated `recommended_export` metadata; `sample_export_recommendation` resolves committed sample recommendation context; Guide shows `Recommended export: 2x PNG briefing map`; Export Workbench shows `Exporting sample: <title>` and `Recommended: PNG · 2x · Composite image`; failed sample switches preserve `previousRecommendedExport` so attempted-sample metadata cannot drift onto the previous committed sample. File groups: core `js/core/sample_export_recommendation.js`, sample registry/import workflow, Guide card, Export Workbench, toolbar wiring, sample manifest, i18n; tests `tests/sample_project_contracts.test.mjs`, `tests/e2e/sample_guide_deeplink.spec.js`, `tests/e2e/release/pages_public_release_gate.spec.js`; docs README/release draft, archived task docs `docs/archive/phase6d-sample-export-proof-20260701/`, and `lessons learned.md`; generated `dist/**`. Functional commit is `959d65dc`; this closeout commit records archive and integration truth. Potential conflicts: yellow with future sample Guide/export UI and Pages dist work; green with active P47 product files. Validation passed: `npm run test:node:sample-project-contracts`, `npm run test:e2e:sample-guide`, `npm run verify:test:e2e-layers`, `npm run verify:test-import-graph`, `npm run verify:pages-dist` with Pages size `927.08 MiB`, local dist release gate at `http://127.0.0.1:8917/`, `git diff --check`, `git diff --cached --check`, `py -3 tools/i18n_audit.py`, and staged `npm run verify:dist-drift`. Review found one failed-switch recommendation binding bug; fixed and re-reviewed clear. Recommended next step: push this closeout to `origin/main`, remove the temporary worktree after remote confirmation, and keep parent checkout WIP untouched.
- 2026-06-26 P20 viewport resize lifecycle owner delivery package: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-p20-viewport-resize-lifecycle-owner` was created from P19 closeout `origin/main@d2171b5f` because the parent checkout has unrelated `docs/archive/**` deletion WIP and `lessons learned.md` WIP. What changed: new `createViewportResizeLifecycleOwner(...)` owns resize observer/frame/timer state, pending resize reason, DPR media listener rebinding, visualViewport resize listener, and deferred spatial refresh handle; `map_renderer.js` keeps wrapper names and host-only effects for render phase, canvas size, projection fit, zoom reset/enforce, overlay dirtying, render, spatial index, and hit canvas dirtiness; architecture boundaries and selector routing now track the owner; package script adds `test:node:viewport-resize-lifecycle-owner`. File groups: core `js/core/renderer/viewport_resize_lifecycle_owner.js`, `js/core/map_renderer.js`; tests `tests/viewport_resize_lifecycle_owner_behavior.test.mjs`, `tests/test_frontend_render_boundary_contract.py`; docs/tooling `docs/archive/viewport-resize-lifecycle-owner-p20-20260626/`, `docs/active/_worktree_registry.md`, `tools/check_architecture_boundaries.mjs`, `tools/test_route_registry.mjs`, `package.json`; temporary ignored `node_modules` junction for E2E dependency access. Functional commit is `ac5a9ee0`; this closeout commit records archive and integration truth. Potential conflicts by path: yellow with future renderer extraction lanes touching `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, `tools/test_route_registry.mjs`, or viewport resize owner tests; green against parent docs/archive WIP and `dist/app/**`. Validation passed: `node --check` on owner, renderer, owner test, architecture tool, and route registry; owner 12/12; viewport command 8/8; viewport read-model 12/12; scenario water cache 7/7; projected bounds 12/12; transform reuse 7/7; render cache 6/6; renderer host inventory 7/7; renderer runtime state 10/10; render transaction diagnostics 21/21; scenario refresh 24/24; scenario chunk contracts 57/57; architecture boundaries; state-write allowlist 115 tracked files; test import graph 49 specs; frontend boundary 5/5; e2e structural 28/28; selector schema 189 routes; selector explain recommends owner and architecture gates; TNO ready-state 5/5; smoke 4/4; forbidden path scan; owner token scan; and `git diff --check`. Known smoke issues observed exactly: `/api/backend/auth/me` 401 and D3 unsafe water geometry warnings for `marine_arctic_ocean, marine_southern_ocean`. Remaining risk: `dist/app/**` remains at the previous generated bundle by explicit P20 constraint, so Pages dist regeneration is a downstream gate. Recommended next step: after this closeout push, remove the temporary worktree and keep parent checkout WIP untouched.
- 2026-06-26 P19 viewport command owner delivery package: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-p19-viewport-command-owner` was created from P18 closeout `origin/main@07bafc24` because the parent checkout has unrelated `docs/archive/**` deletion WIP and `lessons learned.md` WIP, then rebased over audit closeout `7e72eddf811a064347c15ec0b98f70744f41ba72`. What changed: new `createViewportCommandOwner(...)` owns `updateZoomTranslateExtent`, `resetZoomToFit`, `zoomByStep`, `setZoomPercent`, and `enforceZoomConstraints`; `map_renderer.js` keeps wrapper names and host-only `runtimeState.zoomTransform` writes through an injected effect; architecture boundaries track the owner and forbid old command snippets from returning to the renderer host; package script adds `test:node:viewport-command-owner`. File groups: core `js/core/renderer/viewport_command_owner.js`, `js/core/map_renderer.js`; tests `tests/viewport_command_owner_behavior.test.mjs`; docs/tooling `docs/archive/viewport-command-owner-p19-20260626/`, `docs/active/_worktree_registry.md`, `tools/check_architecture_boundaries.mjs`, `package.json`, `lessons learned.md`; temporary ignored `node_modules` junction for E2E dependency access. Functional commit is `837b48c9`; this closeout commit records archive and integration truth. Potential conflicts by path: yellow with future renderer extraction lanes touching `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, or viewport owner tests; green against parent docs/archive WIP and `dist/app/**`. Validation passed after rebase: `node --check` on owner, renderer, owner test, and architecture tool; `npm run test:node:viewport-command-owner` 8/8; viewport read-model 12/12; scenario water cache policy 7/7; projected bounds 12/12; transform reuse 7/7; render cache 6/6; renderer host inventory 7/7; renderer runtime state 10/10; render transaction diagnostics 21/21; scenario refresh 24/24; scenario chunk contracts 57/57; architecture boundaries; state-write allowlist 115 tracked files; test import graph 49 specs; TNO ready-state 5/5; smoke 4/4; forbidden path scan; owner token scan; renderer command token scan; host lifecycle presence scan; and `git diff --check`. Logs: `.runtime/tests/p19-non-e2e-post-rebase-20260626T152712Z.log`, `.runtime/tests/p19-e2e-ready-state-post-rebase-20260626T152732Z.log`, `.runtime/tests/p19-e2e-smoke-post-rebase-20260626T152905Z.log`. Known smoke issues observed exactly: `/api/backend/auth/me` 401 and D3 unsafe water geometry warnings for `marine_arctic_ocean, marine_southern_ocean`. Remaining risk: future zoom command changes must update owner tests and architecture forbidden tokens together. Recommended next step: after this closeout push, remove the temporary worktree and keep parent checkout WIP untouched.
- 2026-06-26 recent renderer platform audit delivery package: isolated worktree `C:\Users\raede\Desktop\dev\mapcreator-audit-20260626-render-platform` was created from `origin/main@07bafc24` because parent `C:\Users\raede\Desktop\dev\mapcreator` has unrelated `docs/archive/**` deletion WIP and `lessons learned.md` WIP. Audit window covered related renderer/platform commits `5e839bc4`, `13b331f1`, `ebfb86f3`, `b533493f`, `d9e941d5`, `3f995014`, `97e385ca`, `2eb79186`, and `7c05f633`. Review found one concrete bug: startup hydration could carry optional `changedLayerKeys` such as `water` and `cities`, then `refreshMapDataForScenarioChunkPromotion` narrowed the full scenario plan to `getRendererRefreshPlan(refreshPlan)` and dropped those keys before chunk-promotion resource fan-out. What changed: `scenario_renderer_bridge.js` now forwards `changedLayerKeys: options.changedLayerKeys ?? refreshPlan.changedLayerKeys`; `scenario_refresh_plans_behavior.test.mjs` locks startup hydration optional-layer fan-out into `contextScenarioBuffer` and `dayNightBuffer`; `test_scenario_renderer_bridge_boundary_contract.py` locks the bridge boundary; `test_scenario_chunk_refresh_contracts.py` was repointed from stale `main.js` assertions to `startup_ready_handoff.js`. File groups: core `js/core/scenario/scenario_renderer_bridge.js`; tests `tests/scenario_refresh_plans_behavior.test.mjs`, `tests/test_scenario_renderer_bridge_boundary_contract.py`, `tests/test_scenario_chunk_refresh_contracts.py`; docs `docs/active/_worktree_registry.md`; temporary files none. Diff summary before registry: 4 files, 55 insertions, 17 deletions. Functional commit is `5052d102`; registry closeout commit records integration truth. Base `origin/main@07bafc24` was current when the audit worktree was created; parent local `main@383a626a` remains behind and dirty. Potential conflicts by path: P19 registry overlap is historical after `origin/main@837b48c9`; green for production code and test paths; green against parent `docs/archive/**` WIP. Validation passed: `npm run -s test:node:startup-hydration-behavior` 12/12; `npm run -s test:node:scenario-refresh-plans` 24/24; `npm run -s test:node:scenario-chunk-contracts`; `npm run -s test:node:render-invalidation-catalog` 6/6; `npm run -s test:node:renderer-host-inventory` 7/7; `npm run -s test:node:render-cache-owner` 6/6; `npm run -s test:node:render-pipeline-catalog` 3/3; `npm run -s test:node:render-transform-reuse-policy-owner` 7/7; `npm run -s test:node:projected-geometry-bounds-owner` 12/12; `npm run -s test:node:viewport-read-model-owner` 12/12; `npm run -s test:node:scenario-water-cache-policy-owner` 7/7; `npm run -s test:node:exact-after-settle-pass-catalog` 6/6; `npm run -s verify:architecture-boundaries`; `npm run -s verify:test-import-graph`; Python boundary contracts 49/49; `node --check` on changed JS/MJS; and `git diff --check`. Not run: browser smoke, because this fix is a renderer refresh metadata bridge covered by targeted Node/Python/static gates. Recommended next step: audit worktree cleanup is complete in the latest registry truth; keep parent checkout WIP untouched.
- 2026-06-26 P18 scenario water cache policy owner delivery package: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-scenario-water-cache-policy-owner-p18-20260626` was created from `origin/main@100c6a2c` because the parent checkout has unrelated `docs/archive/**` deletion WIP and `lessons learned.md` WIP. What changed: new `createScenarioWaterCachePolicyOwner(...)` owns water cache mode, coverage algo, legacy/grid coverage ratio, complexity signals, and direct draw threshold policy; `map_renderer.js` keeps wrapper names and delegates; scenario chunk static water coverage contract now checks the owner; architecture boundary tracks the owner and forbids renderer lifecycle tokens; package script adds `test:node:scenario-water-cache-policy-owner`. File groups: core `js/core/renderer/scenario_water_cache_policy_owner.js`, `js/core/map_renderer.js`; tests `tests/scenario_water_cache_policy_owner_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`; docs/tooling `docs/active/_worktree_registry.md`, `tools/check_architecture_boundaries.mjs`, `package.json`; temporary ignored `node_modules` junction for E2E dependency access. Diff summary before registry was roughly `map_renderer.js` -204/+32, new owner 250 lines, new owner test, architecture +52, package +1, scenario chunk contract +6/-2. Functional commit is `7c05f633`; this closeout commit records integration truth. Base `origin/main@100c6a2c` was current when created; parent local `main@383a626a` is behind remote and remains untouched. Potential conflicts by path: yellow with future renderer extraction lanes touching `js/core/map_renderer.js`, `package.json`, `tools/check_architecture_boundaries.mjs`, or `tests/scenario_chunk_contracts.test.mjs`; green against parent docs/archive WIP and `dist/app/**`. Validation passed: `node --check` on owner, renderer, owner test, architecture tool, and scenario chunk contract; `npm run test:node:scenario-water-cache-policy-owner` 7/7; projected bounds 12/12; viewport read-model 12/12; transform reuse 7/7; render cache 6/6; renderer host inventory 7/7; renderer runtime state 10/10; render transaction diagnostics; scenario refresh 23/23; scenario chunk contracts 57/57; canvas layer 4/4; architecture boundaries; state-write allowlist; test import graph; TNO ready-state 5/5; smoke 4/4; focused canal E2E 1/1; full water-rendering second run 12/12; final diff checks. The first water-rendering run passed 11/12 with one canal `waitForChunkIdle` timeout on `pendingInfraPromotion`, then the focused rerun and full second run both passed. Known smoke issues observed exactly: `/api/backend/auth/me` 401 and D3 unsafe water geometry warnings for `marine_arctic_ocean, marine_southern_ocean`, plus one preload warning in TNO smoke. Remaining risk: no product-code risk found in the final review. Recommended next step: after remote confirmation, remove the temporary worktree and keep parent checkout WIP untouched.
- 2026-06-24 recent UI startup audit delivery package: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-audit-recent-ui-startup-20260624` was created from `origin/main` because the parent checkout has unrelated UI/locale WIP. Audit window covered the related UI shell / startup / styled select / special-zone commits through `66e10c8b`, then rebased over comment-only `36c0ea8f`. Fix scope is narrow: core files `js/ui/toolbar/special_zones_workbench_controller.js`, `dist/app/js/ui/toolbar/special_zones_workbench_controller.js`; test files `tests/special_zones_workbench_controller_behavior.test.mjs` and `tests/test_toolbar_split_boundary_contract.py`; docs `lessons learned.md` and this registry; generated dist mirrors/manifest from `verify:pages-dist`. Diff summary: bind new special-zone scenario layer creation, cache hits, cache failures, render-triggered loads, and first-save loads to captured `activeScenarioId` plus `currentScenarioApplyRequestId` after awaiting optional scenario assets; add stale-load and same-scenario request-cache regressions; sync Pages dist mirrors; and record the async UI lesson. Functional commit `3a8c1559` was pushed to `origin/main`, then this registry cleanup moved the audit worktree to historical integrated status. File overlap risk is green against remaining parent WIP for production code, yellow only for historical generated dist and registry overlap. Validation passed `node --test tests/special_zones_workbench_controller_behavior.test.mjs` 15/15, `py -3 -m unittest tests.test_toolbar_split_boundary_contract -q` 53/53, `npm run -s test:node:renderer-splits` 51/51, `npm run -s verify:ui-rework-mainline` 18/18, `npm run -s verify:architecture-boundaries`, `npm run -s verify:test-import-graph`, `npm run -s verify:pages-dist` 39+8, `node --check` on source and dist controller, `git diff --check`, code-review CLEAR, and architect CLEAR.
- 2026-06-24 注释添加自动化交付包：隔离 worktree `C:\Users\raede\.codex\worktrees\mapcreator-comment-automation-20260624` 从 `origin/main@66e10c8b` 创建，父 checkout 的 UI/locale WIP 保持原样。改动只补中文维护注释：核心文件 `js/bootstrap/ui_shell_debug_seed.js`、`js/ui/styled_selects.js`、`js/core/renderer/color_resolution_strategy.js`、`js/core/scenario_runtime_queries.js`；测试文件、文档外的新 task/context 文件和临时文件均未新增。diff 摘要为 4 个 JS 文件 12 行注释，解释 UI shell 调试种子最小 runtime 合同、releasable 父子索引、inspector 双结构补水、styled select 原生 select 真相源、viewport 菜单定位、整表 option 同步、Atlantropa/water-like 颜色边界和 water-like 渲染/命中共同合同。base commit 为 `66e10c8b`，当前 branch `codex/comment-automation-20260624`；相对当前 `origin/main` 尚未分叉。路径重叠风险：绿色相对已归档 UI integration worktree；黄色相对父 checkout 的 `docs/active/_worktree_registry.md` 本地脏改，仅注册表留档重叠，代码文件在隔离 worktree 内从最新 main 修改。已验证 `node --check` 4 个改动 JS 文件、`python -m unittest tests.test_startup_shell tests.test_ui_rework_plan02_mainline_contract -q` 21/21、`node --test tests/scenario_pure_helpers.node.test.mjs tests/scenario_chunk_contracts.test.mjs` 60/60、`git diff --check` 通过。结构复核结论：本轮候选简化点会扩大 comment-maintenance scope，保留 comment-only。推荐整合：提交后直接 fast-forward 推送 `origin/main`，再清理该临时 worktree；恢复线索为 branch `codex/comment-automation-20260624`。
- 2026-06-23 branch/WIP cleanup pass, refreshed 2026-06-30: main includes `a1b1a8c3` for the query-gated UI shell debug entry and `0ff43122` for conservative archive pruning. The 2026-06-30 cleanup deleted all local branches proven merged into `main`, including `codex/tno-coverage-chain-audit@ffab42b8` after an explicit ancestor check. Local branches kept: `codex/hgo-preview-projection-base-replace@7a95e26c` and `codex/wgi-post-push-truth-20260622@d802cfc8` still contain independent unmerged content; `codex/preserve-parent-wip-before-branch-cleanup-20260623@f64e3e62` remains the recovery snapshot for the dirty parent checkout before cleanup. Remote merged branch refs were deleted after proving they were ancestors of `origin/main@88a9f5a3`; unmerged remotes remain `origin/codex/hgo-preview-projection-base-replace@7a95e26c` and `origin/codex/tno-toponym-zh-audit@17de2d57`.
- 2026-06-19 HGO render continuity: functional commit `123d0b6f` and branch closeout `312821df` were fast-forwarded through clean integration branch `codex/hgo-render-continuity-main-integration`. The fix stages HGO runtime preview frames before pass-canvas commit, preserves previous HGO pixels on rejected frames, keeps runtime hook payload shape, and removes inactive `hgoPreview` from normal vector refresh paths. Task docs are archived under `docs/archive/render-performance-hgo-continuity-20260619/`. Post-merge validation passed HGO runtime preview 21/21 and scenario chunk contracts 44/44.
- 2026-06-18 HGO1936 base-line residue fix delivery: committed as `deba2034` and recorded by closeout commit `fb044598`. The change adds HGO vector scene guards to `js/core/renderer/border_draw_owner.js`, `js/core/renderer/ocean_render_owner.js`, and `js/core/renderer/river_layer_render_owner.js`, mirrors them to `dist/app/js/core/renderer/*`, refreshes `dist/pages-dist-manifest.json`, and extends existing owner behavior tests. Diff summary: suppress canonical coastlines/coastal accent/base rivers when `scenario_contract_profile=hgo_vector` or `hgo_vector_scene_default=true`; river owner also recognizes `activeScenarioId=hgo_1936` as a runtime guard. Verification passed: owner behavior 18 tests, HGO preview 19 tests, HGO strict contract, `verify:pages-dist`, import graph, and diff check. Visual trace evidence from the earlier diagnostic remains `.runtime/browser/hgo1936-base-line-residue-fix-menu-dom-redraw.png`. Phase B has since started from `main@84b9ef5b`; keep this note as HGO follow-up evidence.
- 2026-06-18 module boundary slimming Phase 1-3: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-module-boundary-slimming` was created from `origin/main@5494431c` because the parent checkout is dirty on `codex/tno-political-color-recovery`. Phase 1 moved pure i18n/catalog/tooltip logic to core and routed core toast through `showToastFn`. Phase 2 moved scenario refresh plans into `js/core/map_renderer/scenario_refresh_plans.js`. Phase 3 moved interaction hit candidates into `js/core/map_renderer/interaction_hit_candidates.js`. Final ai-slop scan passed; code-reviewer stale contract finding was fixed; architect returned CLEAR; main post-merge validation passed. Docs archived under `docs/archive/module-boundary-slimming-20260618/`.
- 2026-06-18 TNO political recovery coverage closeout: branch `codex/tno-political-recovery-integration@fcd63a32` confirmed the parent `codex/tno-political-color-recovery@a4957713` branch and effective TNO recovery are already covered by `origin/main@e1f4eb20`. Active TNO docs moved to `docs/archive/tno-political-edit-progressive-recovery-20260618/`; parent dirty files remain untouched as recovery evidence. Validation passed for targeted syntax checks, renderer runtime-state behavior, final TNO E2E 3/3 in `tno-political-progressive-e2e-junction.log`, import graph, Pages dist builder, Pages startup shell 37 tests, landing showcase 8/8, and diff check; `scenario-chunk-contracts` remains the registered 43/44 in `scenario-chunk-contracts-rerun.log`.
- 2026-06-18 parent dirty checkout cleanup: the dirty `codex/tno-political-color-recovery@a4957713` parent checkout was backed up to `.runtime/cleanup-backups/parent-dirty-cleanup-20260618T194327Z/`, stashed as `3a251933f031959dbe303700ed0cebd6e77486d3`, and switched to clean `main@8f289606`. The backed-up dirty set was classified as covered or stale residue after the TNO recovery, landing work maps, module-boundary split, generated dist, package, test, and docs audits.
- Parent main checkout had unrelated local WIP in `docs/archive/data-architecture-hardening-v2/context.md`; renderer integration used a clean temporary worktree to preserve that local file.
- Main Codex agent owns validation commands; child agents are read-only static reviewers.
- Historical delivery packages live in their archived task/context docs; active registry keeps current worktree rows and recovery indexes.
- 2026-06-19 module-boundary continuation v2: integrated, pushed, archived, and cleaned. Stage 1 extracted scenario refresh runtime state into `js/core/map_renderer/scenario_refresh_runtime.js`; Stage 2 extracted exact-after-settle/deferred exact context scheduling into `js/core/map_renderer/exact_after_settle_scheduler.js`; Stage 3 extracted HGO preview render/inspect/pass/bounds glue into `js/core/map_renderer/hgo_runtime_preview_render_owner.js`; Stage 4 added `verify:architecture-boundaries`; Stage 5 added repeatable HGO runtime LOD spike evidence through `tools/spike_hgo_runtime_lod_assets.mjs`; Stage 6 repaired stale owner source-scan contracts and completed final validation. Recovery commits are `a1c0d6e2` and `c664efd5`; docs are archived under `docs/archive/module-boundary-continuation-v2-20260619/`. Final evidence: scenario refresh 4/4, exact plan tests 5/5, HGO runtime preview 20/20, physical layer contract 2/2, scenario chunk contracts 44/44, Python owner/boundary contracts 77/77, architecture boundary gate passed, import graph passed, `verify:pages-dist` passed with startup shell 37/37 and landing showcase 8/8, diff check passed, and HGO LOD spike measured 58,449,790 bytes of Pages-published HGO static assets.
- 2026-06-18 TNO+HGO integration: branch `codex/tno-hgo-integration` was created from clean `origin/main@1206eb43`, confirmed the TNO political color recovery fix was already in base, cherry-picked HGO as `9494ca52`, validated combined behavior, and archived docs under `docs/archive/tno-hgo-integration-20260618/`.
- 2026-06-18 HGO preview projection/base replacement: source branch `codex/hgo-preview-projection-base-replace@7a95e26c` was integrated through `codex/tno-hgo-integration`; docs archived under `docs/archive/hgo-preview-projection-base-replace-20260618/`.
- 2026-06-17 landing work maps: branch `codex/landing-work-maps-integration` was created from clean `origin/main@6874731f`, functional commit `a48eec68` was fast-forward merged into `main`, closeout commit `0538481a` archived the task docs, and `origin/main` was pushed. The parent `codex/tno-political-color-recovery` checkout remains mixed renderer WIP and was not staged wholesale.
- 2026-06-17 strategic overlay render owner: branch `codex/strategic-overlay-render-owner` was created from clean `origin/main@0095aed6`, fast-forward merged into `main` at `e2473cbb`, validated with runtime/render owner node tests, Python boundary contracts, renderer split suite, and Pages dist. Docs archived under `docs/archive/strategic-overlay-render-owner-20260617/`; main push includes the closeout state.
- 2026-06-17 strategic overlay cleanup followups: branch `codex/strategic-overlay-cleanup-followups` was created from clean `origin/main@07e98d17`, fast-forward merged into `main` at `0a8b351e`, validated with runtime/render owner node tests, Python boundary contracts, renderer split suite, and Pages dist. Docs archived under `docs/archive/strategic-overlay-cleanup-followups-20260617/`; main push includes the closeout state.
- 2026-06-17 strategic overlay editor followups: branch `codex/strategic-overlay-editor-followups` was created from clean `main@ee42ba98`, pushed, fast-forward merged into `main` at `5780fa16`, and pushed to `origin/main`; main Codex agent owned all live validation. Operation Graphic midpoint insertion and Special Zone membership click/drag transactions moved into runtime/domain owners; dependency Spike kept production dependencies unchanged. Post-merge short validation passed and docs are archived under `docs/archive/strategic-overlay-editor-followups-20260617/`.
- 2026-06-17 city points render owner: branch `codex/city-points-render-owner` was created from clean `origin/main@38d7835a`; main Codex agent owned all live validation, while child agents stayed in static inspection/review lanes. Commit `6b50e479` was fast-forward merged into `main`; post-merge short validation passed and `main` was pushed.
- 2026-06-17 city points explicit d3 injection: branch `codex/city-points-explicit-d3-injection` was created from clean `origin/main@a37a934d`; main Codex agent owned live validation. Commit `79dfc678` removes direct `globalThis.d3` reads from `city_points_render_owner.js`, fixes the shared city e2e zoom settle timeout clamp found during audit, and was fast-forward merged into `main`; docs are archived under `docs/archive/city-points-render-owner-20260617/`; `main` was pushed.
- 2026-06-15: `codex/housekeeping-review-fix-20260615` was merged, archived, and cleaned locally. Commit `64ae29be` remains as the recovery record.
- 2026-06-15 localization governance: `4711b0dd` was fast-forward merged into main, validated on main, archived, pushed through the registry closeout, and cleaned from the active worktree list.
- 2026-06-15 a11y home/app fix: `55f143de` was rebased onto current main, validated with i18n, behavior, Pages dist, and a11y scan gates, fast-forwarded into main, and archived for cleanup.
- 2026-06-16 worktree cleanup: removed two unregistered local directory remnants with no `.git`: `C:\Users\raede\Desktop\dev\mapcreator-hgo-review-fix` and `C:\Users\raede\Desktop\dev\mapcreator-ocean-scotia-source-refinement-2026-06-02`. Recovery docs remain under `docs/archive/`.
- 2026-06-16 branch cleanup: deleted all local branches merged into `main` and all remote `origin/codex`, `origin/audit`, and `origin/backup` branches merged into `main`; recovery rows now use commit hashes for deleted branch refs.
- 2026-06-16 residual branch cleanup: deleted stale residual branches, backup branches, and open-PR head branches requested for cleanup. GitHub API close attempts for PRs #45, #73, #85, and #86 returned 401, then deleting the head branches removed them from the open PR list.
- 2026-06-16 remaining local branch pass: `codex/tooling-simplification-phase1` was covered by current main; `codex/render-chain-cleanup-phase4-5` contributed only the missing spatial query and scenario chunk promotion helper work; `codex/render-chain-cleanup-phases` was its covered ancestor.
- 2026-06-16 remaining local branch pass: `codex/data-chain-phases-2-4`, `codex/data-quality-repair-2026-06-14`, and `codex/audit-20260612-appearance-transport` were classified as covered or stale after direct helper/test checks; their branch refs can be recreated from the commit hashes above.
- 2026-06-16 transport render/data closeout: isolated worktree `C:\Users\raede\.codex\worktrees\mapcreator-transport-render-data-closeout` was used because the parent main checkout had unrelated dirty localization WIP. Main Codex agent owned live builds/tests; subagents stayed in static/review lanes.
- 2026-06-16 transport render/data closeout: WS3 dist drift guard, WS2 jsonschema smoke profile validator, and WS1 OSM-GPKG family driver were implemented, audited, verified, archived, and pushed to `origin/main` at `106c83c2`.
- 2026-06-16 pre-renderer-owner note: before switching this checkout to `codex/renderer-modern-lights-owner`, the same path had local `main` dirt and unrelated WIP; preserve that context during integration, but keep Current Worktrees limited to the actual branch checked out at this path.
- 2026-06-16 renderer owner integration: `codex/renderer-modern-lights-owner` was fast-forward merged in `C:\Users\raede\.codex\worktrees\mapcreator-renderer-main-integration`, validated with Pages dist and renderer targeted gates, archived under `docs/archive/renderer-modern-lights-owner-20260616/`, and prepared for `origin/main` push.
- 2026-06-16 renderer owner postmerge review: `C:\Users\raede\.codex\worktrees\mapcreator-renderer-review-fixes` reviewed `origin/main@6e818068`, fixed missing intensity channel tolerance in modern city lights and physical owners, refreshed `dist/app`, repointed the render-cache contract to the owner module, and archived under `docs/archive/renderer-owner-postmerge-review-20260616/`.
- 2026-06-16 render branch sync: `codex/renderer-modern-lights-owner` is already covered by `origin/main`; `codex/render-chain-cleanup-city-lights` was fast-forwarded into `codex/render-main-integration-20260616`, validated, and prepared for `main` push.
## Renderer frame orchestration P2 current admission note

State: P2.1 admitted under scoped governance waiver.

Governance basis: primary contemporary A/B readiness PASS, workload/runner identity PASS, B/A startup thresholds PASS, B/A render thresholds PASS. HOI4 promotion short-path A/B counts 3/3 and long-path A/B counts 6/6, all stratified deltas inside declared deadband. Old block-drift data is preserved as historical evidence only. April baseline/threshold frozen. Control SHA: `b14165c0e693a87872361b87ac78dc31cd7a0155`. P2 starting HEAD: `1c58d5dbe1a42794074beb792b64b5e8ab26e153`.

Waiver scope: authorizes P2.1 implementation entry only. P2.1 acceptance A/B, P2.2 entry A/B, and P2 closeout A/B remain required.

Formal supplement: reports `.runtime/reports/generated/p2-perf-hoi4-isolated-abba-20260710.{json,md}` stay `inconclusive/blocked` for attribution; attempt01/02 invalid due validator `[double]::IsFinite` flaw after A3 exit 0; attempt03 invalid due stale task-owned Chromium; attempt04 A3/control 5 valid partial samples and B3/P2 quiet-window failed 3 bounded attempts, with B4/A4 not run; attempt05 discarded. Supplement cannot establish regression or no-regression and cannot overturn phase admission.

Cleanup: runner restored; junction removed; control worktree removed/pruned; ports 8000/8892 clear; task-owned runner/server/Chromium 0; parent untouched; release residue removed/no longer registered on 2026-07-11 with recovery evidence at commit `b14165c0e693a87872361b87ac78dc31cd7a0155`; artifact `.runtime/tmp/p2-supplement-control-cleanup.json`.
