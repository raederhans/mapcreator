# Phase 2B Task Ledger

## Objective

Validate that current `main` is deployable to GitHub Pages and that the public demo surface works after Phase 2A payload slimming and P32 renderer work.

## Checklist

- [x] Create isolated worktree from current `origin/main`.
- [x] Register active worktree.
- [x] Run current-main Pages verification.
- [x] Record manifest total bytes and size gate.
- [ ] Verify or trigger GitHub Pages deployment.
- [x] Run local minimal Pages smoke against generated `dist/`.
- [ ] Run deployed public URL smoke after workflow success or document a precise blocker.
- [ ] Archive release-gate evidence.
- [ ] Review and QA the final evidence.
- [ ] Commit and push.

## Delivery Notes

- Commit status: not committed yet.
- Integration status: in progress.
- Base commit: `origin/main@456aea2f4e94267f347f72dde634aa88e1d344ad`.
- Current branch: `codex/phase2b-pages-release-gate`.

## Initial Evidence

- Initial checked-in `dist/pages-dist-manifest.json`: `total_bytes=972534038`, `size_gate.status=within_limit`, `max_allowed_bytes=1073741824`.
- Deployment workflow: `.github/workflows/deploy.yml` has `push` on `main` and `workflow_dispatch`.

## Local Gate Evidence

- `npm run verify:pages-dist`: pass.
- `tools/build_pages_dist.py`: pass, `927.48 MiB`.
- `tests.test_pages_dist_startup_shell`: pass, `40` minimal-dependency tests.
- `tests.test_pages_dist_startup_shell_heavy`: pass, `2` Shapely-backed landing builder tests.
- Shapely-blocked import smoke for `tests.test_pages_dist_startup_shell`: pass.
- `tests/landing_showcase_view_behavior.test.mjs`: pass, `9` tests.
- `npm run verify:test:e2e-layers`: pass.
- Manifest after final rerun: `total_bytes=972534076`, `size_gate.status=within_limit`, `max_allowed_bytes=1073741824`.
- Existing deploy workflow run for `main@456aea2f4e94267f347f72dde634aa88e1d344ad`: failed in the minimal CI dependency guardrail because two heavy-import tests were missing from `tests/heavy_dependency_groups.json`.
- Fix staged for next push: split Shapely-backed landing builder coverage to `tests/test_pages_dist_startup_shell_heavy.py`; add that heavy module and `tests/test_transport_country_source_contracts.py` to `tests/heavy_dependency_groups.json`.
- Public URL smoke blocker found before redeploy: export workbench preview logged `Cannot read properties of undefined (reading 'includes')`. Local dist reproduced it.
- Export workbench fix: pass `RENDER_PASS_NAMES` into both direct `resolveExportPassSequence(...)` calls in `js/ui/toolbar.js`; `dist/app/js/ui/toolbar.js` regenerated by `verify:pages-dist`.
- Local generated `dist/` release gate: pass against `http://127.0.0.1:8766/`.

## Remaining Risks

- GitHub workflow or Pages environment access may be blocked by credentials or repository settings.
- Public URL smoke depends on deployed workflow success and available browser tooling.
