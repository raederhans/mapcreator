# Renderer Viewport Update Owner P34 Task

## Objective
Ownerize `updateMap(transform)` into `renderer_viewport_update_owner.js` with behavior preserved exactly and no public facade change.

## Target Files
- Core: `js/core/renderer/renderer_viewport_update_owner.js`, `js/core/map_renderer.js`
- Tests: `tests/renderer_viewport_update_owner_behavior.test.mjs`
- Gates: `tools/check_architecture_boundaries.mjs`, `package.json`
- Dist: `dist/app/**` generated by project tooling
- Docs: this task folder and `docs/active/_worktree_registry.md`

## Verification
Passed before closeout:
- Syntax: `node --check` for changed owner, renderer, tests, and architecture tool.
- Node behavior: viewport update owner 5/5, zoom interaction lifecycle 7/7, viewport command 8/8, viewport resize 12/12, fitProjection lifecycle 23/23, renderer runtime state 10/10, strategic overlay runtime 16/16.
- Static gates: architecture boundaries, state-write allowlist, test import graph, and `git diff --check`.
- Dist gates: `verify:pages-dist` and `verify:dist-drift`.
- Browser confidence: TNO ready-state 5/5, smoke 4/4, scenario chunk runtime 8/8 through the default project webServer path.

## Delivery Package
1. Changed `updateMap(transform)` into a thin wrapper that delegates to `createRendererViewportUpdateOwner`.
2. Added an effects-only viewport update owner preserving the exact old effect order and keeping `drawCanvas()` last.
3. Added owner behavior/inventory coverage plus package and architecture boundary gates.
4. Strengthened zoom interaction lifecycle so `updateMap` is a required effect, fixing the architect BLOCK finding.
5. Synced generated Pages dist mirrors.

### Files
- Core: `js/core/renderer/renderer_viewport_update_owner.js`, `js/core/map_renderer.js`, `js/core/renderer/zoom_interaction_lifecycle_owner.js`
- Tests: `tests/renderer_viewport_update_owner_behavior.test.mjs`, `tests/zoom_interaction_lifecycle_owner_behavior.test.mjs`
- Tooling: `tools/check_architecture_boundaries.mjs`, `package.json`
- Dist: `dist/app/js/core/map_renderer.js`, `dist/app/js/core/renderer/renderer_viewport_update_owner.js`, `dist/pages-dist-manifest.json`
- Docs: `docs/active/renderer-viewport-update-owner-p34-20260629/*`, `docs/active/_worktree_registry.md`

### Integration Status
- Base commit: `origin/main@fef1d059`; current `origin/main` is `13457c54`, so P34 needs rebase before merge.
- Commit status: pending until final review, rebase, and verification complete.
- Conflict risk: red for `js/core/map_renderer.js`, `tools/check_architecture_boundaries.mjs`, `package.json`, generated dist, and `docs/active/_worktree_registry.md`; green against `codex/phase3a-public-narrative` code because that clean worktree already matches `origin/main@13457c54`.
- Recommended action: rebase P34 onto `origin/main@13457c54`, rerun required validation, push fast-forward to `origin/main`, then archive this task folder and clean the worktree.
- Remaining risk: the manual-base scenario chunk runtime run saw `pendingInfraPromotion`; the official default `npm run test:e2e:dev:scenario-chunk-runtime` path passed 8/8.
