# v0.1 Public Demo Release Candidate

## Release Status

- Release package: v0.1 public demo.
- Phase 6E runtime baseline: `bcc00b62a1beac38d3a0d7978ddf282897f97273`.
- Release package commit and tag target: `9f8f4be2d50a315554f97c785fcf1c174ad1a76d`.
- Release tag: `v0.1-public-demo`; annotated tag dereferences to `9f8f4be2d50a315554f97c785fcf1c174ad1a76d`.
- GitHub release: https://github.com/raederhans/scenario-forge/releases/tag/v0.1-public-demo
- Release type: pre-release for v0.1.
- GitHub Pages deploy workflow: https://github.com/raederhans/scenario-forge/actions/runs/28561116483, attempt 2, success.
- Canonical P7 release facts source: this file. Earlier active Phase 6E docs are historical validation snapshots.
- Public demo URL: https://raederhans.github.io/scenario-forge/
- Guide URL: https://raederhans.github.io/scenario-forge/app/?view=guide
- Sample deep link format: `https://raederhans.github.io/scenario-forge/app/?sample=<sample-id>&view=guide`

## Public Baselines

- Blank Map
- Modern World
- HOI4 1936
- HOI4 1939
- TNO 1962

TNO 1962 is the default public guided scenario path.

## Public Sample Projects

The public landing page exposes five checked-in editable project JSON files, one for each public baseline. Editor links open `/app/?sample=<sample-id>&view=guide`, show a sample-aware Guide card with export and original JSON actions, list the same five public starter samples for in-app switching, and show a Project-tab sample banner with the same sample state.

| Sample | Deep link |
| --- | --- |
| Blank Map starter | `/app/?sample=blank-base-starter&view=guide` |
| Modern World Japan corridor | `/app/?sample=modern-world-japan-corridor&view=guide` |
| HOI4 1936 Europe briefing | `/app/?sample=hoi4-1936-europe-briefing&view=guide` |
| HOI4 1939 Europe switch | `/app/?sample=hoi4-1939-europe-switch&view=guide` |
| TNO 1962 Atlantropa briefing | `/app/?sample=tno-1962-atlantropa-briefing&view=guide` |

The sample manifest records `developer_preview_exclusions: ["hgo_1936"]`, so HGO 1936 stays outside the public sample set.

## Sample Switcher And Export Path

- The Guide lists the five public starter samples.
- Switching samples inside the Guide reuses the checked-in manifest.
- Unsaved work triggers confirmation before replacement.
- The URL updates only after the selected sample imports successfully.
- Recommended export metadata lives in `landing/assets/sample-runs.json`.
- Guide shows the user-facing recommendation label.
- Export Workbench repeats the loaded sample plus format, scale, and target summary.

## Pages Size Status

The checked-in Pages distribution manifest reports `972144323` bytes, or `927.11 MiB`.

- Hard cap: 1 GiB.
- Preferred warning target: 950 MiB.
- `size_gate.status`: `within_limit`.
- `size_gate.warning_status`: `within_warning`.

## Public Release Smoke

The deploy workflow runs the public release smoke after `actions/deploy-pages`, using `steps.deployment.outputs.page_url`.

The release smoke covers:

- landing root loads;
- landing sample download cards remain readable at 375px width;
- landing exposes five public sample CTAs;
- editor links stay on `/app/?sample=<sample-id>&view=guide`;
- `/app/?sample=tno-1962-atlantropa-briefing&view=guide` loads and auto-imports the checked-in TNO sample project;
- Guide shows the sample-aware card, recommended export label, five starter samples, HGO exclusion, focus return, dirty-switch confirmation, Project-tab banner, and original JSON download link;
- TNO 1962 reaches the interactive public scenario path;
- HGO remains hidden or unavailable on Pages;
- Export Workbench opens and shows the TNO loaded-sample context with its recommended PNG 2x composite output.

Deployed evidence:

- GitHub Pages deploy workflow `28561116483` attempt 2 completed successfully.
- Deploy job `84679882111` ran `npm run test:e2e:pages-public-release-gate` against `https://raederhans.github.io/scenario-forge/`.
- Smoke result: `1 passed`; `activeScenarioId=tno_1962`; TNO sample deeplink import `success`; `hgoPreviewEnabled=false`; `hasHgoRuntimeAssets=false`.
- The first deploy smoke attempt timed out while waiting for shell readiness immediately after Pages deployment. Live URL probes, a local remote smoke rerun, and the workflow rerun passed; P8 tracks a propagation-aware release smoke guard.

Local Phase 6E coverage also checked the sample Guide and Export context at 375px, 768px, and desktop widths, plus Simplified Chinese visible text on the TNO sample path.

## Local Release Validation

- `npm run verify:pages-dist`: passed with Pages size `927.11 MiB`, startup shell `41/41`, landing showcase `18/18`, and sample contracts `17/17`.
- `npm run verify:dist-drift`: passed with no dist drift.
- `py -3 tools\i18n_audit.py`: passed with `ui_missing=0` and `ui_english_fallback=0`.
- `npm run test:e2e:sample-guide`: passed `5/5`.
- `PLAYWRIGHT_TEST_BASE_URL=http://127.0.0.1:8930/dist/ npm run test:e2e:pages-public-release-gate`: passed `1/1`.
- `npm run verify:test-import-graph`: passed with 51 specs.
- `git diff --check`: passed.

The local release gate recorded `activeScenarioId=tno_1962`, sample deeplink `success`, `hgoPreviewEnabled=false`, and `hasHgoRuntimeAssets=false`.

## Known Local-Only Systems

- HGO 1936 runtime preview.
- Cloud Saves and community flows through the local backend preview.
- Local backend state under `.runtime/backend/`.
- Large local data preparation and developer validation flows.

## Main Data And Source Families

- Natural Earth for base geography, countries, coastlines, and small-scale reference layers.
- geoBoundaries for administrative boundary reference data.
- GeoNames for place names and settlement reference data.
- NOAA ETOPO 2022 for relief, bathymetry, and terrain context.
- NASA Black Marble for night lights and city-light texture context.
- OpenStreetMap and Geofabrik for roads, rail, facilities, and transport/context extracts.
- Japanese MLIT road data (N06) for Japan road hardening and transport preview reference data.

Detailed records live in `data/source_ledger.json`, `.provenance.json` files under `data/`, source recipes under `data/transport_layers/`, and generated asset source records.

## Known Limitations

- The public demo exposes five public baselines; HGO 1936 remains a developer/local preview.
- Roads and rail are the strongest public transport paths.
- Airports and ports provide overview context.
- Mineral resources, energy, industry, and logistics remain preview/workbench families.
- Cloud Saves, public community posts, downloads, comments, reports, and admin moderation require the local backend preview.
- Some large data preparation and validation flows require a local Windows/Python setup.

## GitHub About Recommendation

Suggested About text:

> Scenario-first political map workbench for alternate-history, strategy modding, and geopolitical storytelling.

Suggested topics:

`maps`, `cartography`, `alternate-history`, `hoi4`, `tno`, `geopolitical`, `map-editor`, `github-pages`
