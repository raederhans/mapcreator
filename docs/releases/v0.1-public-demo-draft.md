# v0.1 Public Demo Draft

## Demo URL

- https://raederhans.github.io/scenario-forge/
- Guide path: https://raederhans.github.io/scenario-forge/app/?view=guide
- Sample deep link format: https://raederhans.github.io/scenario-forge/app/?sample=<sample-id>&view=guide

## Public Baselines

- Blank Map
- Modern World
- HOI4 1936
- HOI4 1939
- TNO 1962

TNO 1962 is the default public guided scenario path.

## HGO Local Preview Policy

HGO 1936 remains a developer/local preview. It is separate from the five public baselines and is used for HOI4-style country identity, palette, flag, and raster-render validation.

## Downloadable Sample Projects

The public landing page exposes five checked-in editable project JSON files, one for each public baseline. Editor links open `/app/?sample=<sample-id>&view=guide`, show a sample-aware Guide card with export and original JSON actions, list the same five public starter samples for in-app switching, and surface a Project-tab sample banner with the same sample state. Each public sample also carries a small recommended export path in `landing/assets/sample-runs.json`; the Guide shows the user-facing label, and Export Workbench repeats the loaded sample plus the format, scale, and target summary. The in-app switcher reuses the manifest, confirms before replacing unsaved work, and updates the URL only after the selected sample imports successfully. Project files live under `landing/assets/sample-projects/` and are copied into Pages dist as `dist/assets/sample-projects/`.

- Blank Map starter
- Modern World Japan corridor
- HOI4 1936 Europe briefing
- HOI4 1939 Europe switch
- TNO 1962 Atlantropa briefing

The manifest records `developer_preview_exclusions: ["hgo_1936"]` so the public sample set stays aligned with the public baseline policy.

Sample deep links:

- Blank Map starter: `/app/?sample=blank-base-starter&view=guide`
- Modern World Japan corridor: `/app/?sample=modern-world-japan-corridor&view=guide`
- HOI4 1936 Europe briefing: `/app/?sample=hoi4-1936-europe-briefing&view=guide`
- HOI4 1939 Europe switch: `/app/?sample=hoi4-1939-europe-switch&view=guide`
- TNO 1962 Atlantropa briefing: `/app/?sample=tno-1962-atlantropa-briefing&view=guide`

## Pages Size Status

The checked-in Pages distribution manifest currently reports `972144112` bytes, or `927.11 MiB`. That is within the preferred 950 MiB warning line and the 1 GiB hard cap.

## Public Release Smoke

The deploy workflow runs the public release smoke after `actions/deploy-pages`, using `steps.deployment.outputs.page_url`.

The release smoke covers:

- Landing root loads.
- Landing sample download cards remain readable at 375px width, expose five public sample CTAs, and keep editor links on `/app/?sample=<sample-id>&view=guide`.
- `/app/?sample=tno-1962-atlantropa-briefing&view=guide` loads, auto-imports the checked-in TNO sample project, and opens the guide path.
- The Guide shows the sample-aware card, recommended export label, five public starter samples, HGO exclusion, predictable focus return, dirty-switch confirmation behavior, and the Project tab sample-loaded banner plus original JSON download link.
- TNO 1962 reaches the interactive public scenario path.
- HGO remains hidden or unavailable on Pages.
- Export workbench opens and shows the TNO loaded-sample context with its recommended PNG 2x composite output.
- Local Phase 6E coverage also checks the sample Guide and Export context at 375px, 768px, and desktop widths, plus Simplified Chinese visible text on the TNO sample path.

## Known Local-Only Systems

- HGO 1936 runtime preview.
- Cloud Saves and community flows through the local backend preview.
- Local backend state under `.runtime/backend/`.
- Large local data preparation and developer validation flows.

## Main Data and Source Families

- Natural Earth for base geography, countries, coastlines, and small-scale reference layers.
- geoBoundaries for administrative boundary reference data.
- GeoNames for place names and settlement reference data.
- NOAA ETOPO 2022 for relief, bathymetry, and terrain context.
- NASA Black Marble for night lights and city-light texture context.
- OpenStreetMap and Geofabrik for roads, rail, facilities, and transport/context extracts.
- Japanese MLIT road data (N06) for Japan road hardening and transport preview reference data.

Detailed records live in `data/source_ledger.json`, `.provenance.json` files under `data/`, source recipes under `data/transport_layers/`, and generated asset source records.

## Known Limitations

- The public demo exposes five public baselines; HGO 1936 remains a developer/local preview.
- Roads and rail are the strongest public transport paths. Airports and ports provide overview context. Mineral resources, energy, industry, and logistics remain preview/workbench families.
- Cloud Saves, public community posts, downloads, comments, reports, and admin moderation require the local backend preview.
- Some large data preparation and validation flows require a local Windows/Python setup.

## GitHub About Recommendation

Suggested About text:

> Scenario-first political map workbench for alternate-history, strategy modding, and geopolitical storytelling.

Suggested topics:

`maps`, `cartography`, `alternate-history`, `hoi4`, `tno`, `geopolitical`, `map-editor`, `github-pages`
