# Stage 5 Visual Acceptance Context

## 2026-06-21 Start

- Loaded skills: `computer-use`, `ultragoal`, `visual-ralph`, and `code-review`.
- `computer-use` is available for Windows UI automation, but browser work should use repo-native Playwright/browser tooling first.
- `visual-ralph` is available from the user-provided path and is used as the visual verdict discipline: compare screenshots and evidence before the next edit.
- Parent checkout is `main@29c008f73348752ced55ebd56f916d734b86e37e`, behind `origin/main`, with unrelated docs/archive WIP and modified `lessons learned.md`.
- Created isolated worktree `C:\Users\raede\Desktop\dev\mapcreator-stage5-visual-acceptance` from `origin/main@8e79ea0cebb3a44d89247dc6094baca9f25b22c9`.
- `omx ultragoal create-goals` produced an over-granular 130-goal ledger from the long brief. Execution is tracked by one aggregate Codex goal plus this active task package.
- Live process owner: main Codex agent only. Subagents are limited to static mapping, acceptance planning, and final independent review.

## Stage 4 Evidence To Preserve

- Stage 4 functional commit: `cbaa47ba`.
- Stage 4 closeout commit/base in the current branch: `origin/main@8e79ea0cebb3a44d89247dc6094baca9f25b22c9`.
- Runtime sampling reported `stableVisibleRequiredLayerMissingCount=0`.
- Latest stable TNO `water` snapshot: `required=true`, `coverageStatus=present`, `sourceKind=runtime-state`, `stateFeatureCount=141`.
- Latest stable TNO `scenario_atlantropa` snapshot: `required=true`, `coverageStatus=present`, `sourceKind=runtime-state`, `stateFeatureCount=897`.
- Latest stable relief snapshot: `required=true`, `coverageStatus=present`, `sourceKind=runtime-state`, `stateFeatureCount=25`.

## Working Hypothesis

- Runtime contracts now say the required semantic layers are present.
- Stage 5 must verify the visible canvas result: screenshots, pixel samples, scenario identity, and perf smoke.
- The first implementation should be a harness and summary writer. Production renderer edits wait for failing evidence.

## Live Process Ownership

- Main Codex agent owns dev server, Playwright, performance smoke, Pages dist, and any browser sampling.
- Subagents may inspect code and recommend test shape only.
- Logs and artifacts should stay under `.runtime/`.

## 2026-06-21 Harness Implementation

- Added `tests/e2e/dev/full_visual_acceptance.dev.spec.js` as the Stage 5 evidence runner.
- Added npm entry `test:e2e:dev:stage5-visual-acceptance`.
- Rebuilt `tests/e2e/test-import-graph.json`; `verify:test-import-graph` and `verify:test:e2e-layers` passed.
- `verify:state-write-allowlist` currently fails on pre-existing unexpected writer files from `origin/main`; the new Stage 5 spec is allowlisted and is not one of the unexpected entries.
- Current live process owner remains main Codex agent. Planned live command: `npm run test:e2e:dev:stage5-visual-acceptance`.

## 2026-06-21 Stage 5 Evidence

- `npm run test:e2e:dev:stage5-visual-acceptance` passed: 1 Playwright test, 9 required acceptance cases.
- Summary artifact: `.runtime/output/visual-acceptance/stage5-summary.json`; `pass=true`, `caseCount=9`, `failedCaseCount=0`.
- Diagnostics artifact: `.runtime/output/render-diagnostics/stage5-visual-acceptance.json`.
- Screenshot artifact directory: `.runtime/browser/mcp-artifacts/stage5-visual-acceptance/`.
- Screenshot files captured: `tno_startup_idle.png`, `tno_atlantropa_mediterranean.png`, `tno_water_regions.png`, `relief_overlay_visible.png`, `fill_before_zoom.png`, `fill_after_zoom_pan.png`, `scenario_switch_final.png`, `non_tno_startup_1.png`, `non_tno_startup_2.png`.
- Required prohibited latest-stable warning codes were zero for every case: `resolved-colors-empty-with-land`, `political-visible-subset-empty-with-required-chunks`, `render-reuse-across-data-generation`, `pending-color-edit-cleared-without-render`, `visible-required-layer-missing`, `render-snapshot-scenario-mismatch`, and `scenario-apply-inflight-target-mismatch`.
- TNO startup/Atlantropa/water/relief/fill-before-zoom cases reported `water`, `scenario_atlantropa`, and `relief` as `coverageStatus=present`, `sourceKind=runtime-state`, `required=true`, `selected=true`, and `missingChunkIds=0`.
- Runtime counts in those core cases: water 141, `scenario_atlantropa` 897, relief 25.
- Visual sanity check opened `tno_startup_idle.png` and `relief_overlay_visible.png`; both show rendered political fills, water, and relief/shoreline content rather than a blank or near-black map.

## 2026-06-21 Root Fix

- First Stage 5 run exposed the real contract gap: TNO startup made relief visible, but TNO required semantic chunk selection did not require relief.
- Production source fix: add `relief` to `DEFAULT_REQUIRED_SEMANTIC_LAYERS_BY_SCENARIO.tno_1962` in `js/core/scenario_chunk_manager.js`.
- Dist mirror was regenerated by `npm run verify:pages-dist` in `dist/app/js/core/scenario_chunk_manager.js` and `dist/pages-dist-manifest.json`.
- Existing `tests/scenario_chunk_contracts.test.mjs` was extended so TNO required semantic layers and visible chunk layers include relief.

## 2026-06-21 Validation Notes

- Passed: `node --check tests/e2e/dev/full_visual_acceptance.dev.spec.js`.
- Passed: `node --test tests/scenario_chunk_contracts.test.mjs` with 55 tests.
- Passed: `npm run verify:test-import-graph`, `npm run verify:test:e2e-layers`, `npm run verify:test-timeout-guardrails`, and `npm run test:e2e:tno-contracts`.
- Passed: `npm run verify:pages-dist`; Pages startup shell ran 38 tests OK and landing showcase ran 8 Node tests OK.
- Passed: `git diff --check` with line-ending warnings only.
- Failed baseline gate: `npm run verify:architecture-boundaries` reports `js/core/map_renderer.js` has 24154 lines with a 24100 budget; this worktree does not edit `js/core/map_renderer.js`.
- Failed baseline gate: `npm run verify:state-write-allowlist` reports existing unexpected writer files; the new Stage 5 spec is allowlisted and not reported.
- Failed extra water regression suite: `npm run test:e2e:water-rendering` reported 9 passed and 3 failed. Clean `origin/main` baseline reproduced the open-ocean apply timeout and named-water inspector active-title failure, so these are tracked as baseline issues outside this Stage 5 diff.

## Anti-Slop / Fallback Review

- Scope scanned: new Stage 5 spec, `scenario_chunk_manager.js`, scenario chunk contracts, state-writer allowlist, and Stage 5 docs.
- New production code adds no fallback, retry, threshold tuning, renderer-budget tuning, worker/WebGL/default changes, or post-processing repair.
- New harness allow/ignore paths are limited to known optional local resource noise, anonymous `/api/backend/auth/me` 401, and transient console diagnostics; the harness still asserts latest stable prohibited warnings are zero.
- The simplest stable fix remains the semantic contract change: if TNO requires visible relief at startup, relief belongs in required semantic layer selection.

## 2026-06-21 Independent Review Closeout

- Code-review first pass requested changes for two harness risks: projected geo probes could be clamped to the canvas edge, and grouped pixel samples could hide a failing probe behind passing neighbors.
- Fixed the harness so offscreen projected probes fail with explicit `offscreen` evidence, empty sample windows fail with `sample-box-empty`, fixed geo probes require every entry to pass, and centroid samples require a minimum passing count.
- Code-review follow-up returned APPROVE after `node --check tests/e2e/dev/full_visual_acceptance.dev.spec.js` and static review.
- Architect lane returned WATCH only. The queued scenario-switch case now records requested scenario ids `hoi4_1939` then `modern_world`, a 30 ms request gap, and fulfilled results in the same order.
- Reran `npm run test:e2e:dev:stage5-visual-acceptance` after the review fixes: PASS, 1 Playwright test in 1.6m, 9/9 cases, summary `pass=true`.
- The rerun kept latest stable prohibited warnings at zero for every case, and stable-kind prohibited warnings since test start at zero for every case.
- Status after review: ready-for-integration. Main Codex agent remains the only live process owner; no subagent owns tests or dev server state.
