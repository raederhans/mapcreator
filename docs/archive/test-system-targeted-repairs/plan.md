# Plan

1. 已修 adaptive runner 的 import 副作用、删除文件发现、dry-run/execute 合同。
2. 已修 route registry / selector 的 helper、workflow、package、direct-e2e、guardrail 路由。
3. 已修 shared fixture failure-context 时序。
4. 已修 verify-shared 的 import-graph 顺序，并把 pr-fast workflow 拉齐当前 route 契约。
5. 已完成 targeted verification、真实场景回归、review 复核。
