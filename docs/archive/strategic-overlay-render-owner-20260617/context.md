# Strategic Overlay Render Owner Context

Last updated: 2026-06-17

## Start State

- Worktree: `C:\Users\raede\Desktop\dev\mapcreator`
- Branch: `codex/strategic-overlay-render-owner`
- Base commit: `0095aed6`
- Main status before branch: `## main...origin/main`
- Worktree list before branch: only `C:/Users/raede/Desktop/dev/mapcreator`
- Live process owner: main Codex agent only
- Subagents: static inspection/review only; no live tests, dev server, or browser processes delegated

## Read Before Work

- `AGENTS.md`
- `lessons learned.md`
- `docs/shared/agent-tiers.md`
- `docs/active/_worktree_registry.md`
- `docs/archive/city-points-render-owner-20260617/`

## Findings

- `strategic_overlay_helpers.js` already owns operational lines, operation graphics, unit counters, and special zone SVG drawing.
- `strategic_overlay_runtime_owner.js` already owns editor transactions and composes runtime domains.
- Unit Counter drag is the main mixed-responsibility risk: the renderer currently binds drag behavior and directly mutates counter anchor, attachment, history, dirty flags, and render scheduling.
- Render gating remains in `map_renderer.js`: overlay signatures, dirty checks, and `render*IfNeeded` wrappers.
- Boundary tests currently lock the old shape and must be flipped together with the implementation.
- Window 1 done: `unit_counter_runtime_domain.js` now owns render-entry selection and drag move transaction APIs. `map_renderer.js` drag handlers only collect event coordinates, update immediate SVG transform, and call runtime owner APIs.

## Boundary Decision

Runtime domain owns data mutations and history. Render owner owns when to redraw. Helpers owner owns how the SVG/DOM is drawn. `map_renderer.js` owns composition and top-level render pass order.

## Validation Matrix

- Runtime behavior:
  - `node --test tests/strategic_overlay_runtime_owner_behavior.test.mjs`
- Render owner behavior:
  - `node --test tests/strategic_overlay_render_owner_behavior.test.mjs`
- Boundary:
  - `py -3 -m unittest tests.test_map_renderer_strategic_overlay_helpers_boundary_contract tests.test_map_renderer_strategic_overlay_runtime_owner_boundary_contract tests.test_map_renderer_strategic_overlay_render_owner_boundary_contract -q`
- Integrated renderer splits:
  - `npm run test:node:renderer-splits`
- Dist and hygiene:
  - `npm run verify:pages-dist`
  - `git diff --check`

## Open Risks

- `verify:pages-dist` may need the hermes Python venv first in PATH on this Windows host.
- Long strategic overlay e2e remains final optional acceptance unless targeted tests expose behavior risk.
- `omx state write` rejected the initial active-state payload; active execution state is tracked here and through Codex goal state.

## Validation Log

- `node --test tests/strategic_overlay_runtime_owner_behavior.test.mjs` passed, 12/12, after adding Unit Counter drag transaction tests.
- `node --check js/core/renderer/strategic_overlay_render_owner.js` passed.
- `node --check js/core/map_renderer.js` passed.
- `node --check tools/test_route_registry.mjs` passed.
- `node --test tests/strategic_overlay_render_owner_behavior.test.mjs` passed, 2/2.
- `py -3 -m unittest tests.test_map_renderer_strategic_overlay_helpers_boundary_contract tests.test_map_renderer_strategic_overlay_runtime_owner_boundary_contract tests.test_map_renderer_strategic_overlay_render_owner_boundary_contract -q` passed, 6/6.
- `npm run test:node:renderer-splits` passed, 41/41.
- `npm run verify:pages-dist` passed once in the initial environment and once with `C:\Users\raede\AppData\Local\hermes\hermes-agent\venv\Scripts` prepended to PATH after the shell lost `python`.
- `git diff --check` passed after the source and dist refresh.
- Architecture review approved the split; code review found Unit Counter drag session state leaking into history snapshots.
- Review fix: Unit Counter drag session state now lives in the runtime domain closure, and the behavior test asserts neither the live counter nor the history `after.unitCounters` snapshot carries internal drag fields.

## Review Notes

- Accepted follow-up risk: `markOverlaysDirty()` still touches inspector and hover dirty flags while the new owner schedules strategic overlays. This is a later overlay scheduler boundary cleanup, not part of this render-owner split.
- Accepted follow-up risk: Operation Graphic drag still uses existing D3 datum session fields. This task only moves Unit Counter drag transaction ownership.
- Dist state was regenerated by a complete `verify:pages-dist` run after the review fix.
