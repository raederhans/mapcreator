## 任务清单

- [x] 状态：`LegendManager` 支持图例控件可见、收起、位置比例。
- [x] 展示：`map_renderer` 使用浮动控件渲染图例列表。
- [x] 交互：控件支持拖动、收起、关闭。
- [x] 入口：生成图例时重新显示控件。
- [x] 验证：测试、静态契约、dist 验证通过。
- [x] 交互：控件支持右边缘、底边缘和右下角缩放。
- [x] 设置：控件边缘选中时显示透明度条，并保存整体透明度。
- [x] 验证：新增缩放与透明度逻辑的测试、语法检查和 dist 同步。
