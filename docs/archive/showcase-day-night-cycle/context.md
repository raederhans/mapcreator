# Showcase Day-Night Cycle Context

## 2026-06-06

- User selected the `场景` tab and said it is unclear.
- Current source uses `scenario` as a fourth layer with focus markers generated by `scenario_nodes`.
- Existing product copy already references night-light context in the Japan preview and NASA Black Marble in source families.
- Chosen implementation path: replace the unclear fourth layer with a day-night visual layer in the same SVG and metadata contract.
- Implementation keeps the app-side showcase mechanism unchanged: tab selection sets `data-active-layer`, and the generated SVG owns the day-night animation through XML-valid SVG nodes.
- `npm run verify:pages-dist` passed after rebuilding dist; a direct unittest run before rebuilding dist failed because it read stale dist files and manifest sizes.
- Review fixes applied: Europe showcase SVG/JSON now have explicit LF attributes, the asset test parses SVG XML, dist root checks all four showcase tabs, and internal focus naming now uses `SHOWCASE_FOCUS_TAGS`.
- Final source search found old `scenario` showcase identifiers only in negative regression assertions.
- Final diff review watch item was resolved by deriving the night shade travel, band size, and terminator curve from the SVG canvas dimensions.

## Live Process Ownership

- Main agent owns all builds, tests, and browser/server-related live processes.
- Subagents may perform read-only review only.
