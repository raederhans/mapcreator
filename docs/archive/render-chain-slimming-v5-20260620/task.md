# Render Chain Slimming V5 Task

## Checklist

- [x] Create isolated V5 worktree from `main@f7501edf`.
- [x] Create active docs.
- [x] Update worktree registry.
- [x] Run two-step repo evidence audit.
- [x] Finalize minimal implementation plan.
- [x] Implement bridge/executor pass-language narrowing.
- [x] Update tests and static contracts.
- [x] Refresh Pages dist output.
- [x] Run final verification suite.
- [x] Run final review and first-principles bug check.
- [x] Fast-forward merge functional commit to `main`.
- [x] Run post-merge validation on `main`.
- [ ] Push, archive docs, and clean worktree.

## Delivery Package

1. What changed:
   - Removed generic `targetPasses` from FrameGraph invalidation execution plan output.
   - Runtime now passes only `targetResources`, `invalidationTargetPasses`, and `hasExplicitTargetResources` from the bridge to the visual executor.
   - Visual executor now fails fast if `executionPlan.targetPasses` appears, while keeping legacy direct `targetPasses` fallback for no-FrameGraph callers.
   - Explicit empty `targetResources` still produces empty `invalidationTargetPasses` and skips `invalidateRenderPasses`.
   - Behavior tests, Node static contracts, Python contracts, architecture boundary checks, and Pages dist mirrors were updated.

2. Files changed:
   - Core: `js/core/map_renderer/scenario_refresh_plans.js`, `js/core/map_renderer/scenario_refresh_runtime.js`, `js/core/map_renderer/scenario_visual_invalidation_executor.js`.
   - Tests/contracts: `tests/scenario_refresh_plans_behavior.test.mjs`, `tests/scenario_visual_invalidation_executor_behavior.test.mjs`, `tests/scenario_chunk_contracts.test.mjs`, `tests/test_scenario_chunk_refresh_contracts.py`.
   - Tools: `tools/check_architecture_boundaries.mjs`.
   - Dist: `dist/app/js/core/map_renderer/scenario_refresh_plans.js`, `dist/app/js/core/map_renderer/scenario_refresh_runtime.js`, `dist/app/js/core/map_renderer/scenario_visual_invalidation_executor.js`, `dist/pages-dist-manifest.json`.
   - Docs: `docs/active/_worktree_registry.md`, `docs/active/render-chain-slimming-v5/{plan,context,task}.md`.

3. Diff summary versus `main@f7501edf`:
   - 13 tracked files changed before active docs are staged.
   - Core source/dist edits are small bridge/executor contract changes.
   - Static contract edits add short source-slice checks to avoid broad regex false positives.
   - Pages manifest was regenerated by `npm run verify:pages-dist`.

4. Commit status:
   - Functional commit `39092a82` was fast-forward merged to `main`.

5. Base/main divergence:
   - Base commit: `f7501edf33faef94111e0e9e5d93e908be9fea68`.
   - `main` and `origin/main` matched `f7501edf` before integration; `main` now contains V5 functional commit `39092a82`.

6. Potential worktree conflicts:
   - Current `git worktree list` contains main plus this V5 worktree.
   - Direct overlap risk is yellow for future render refresh-chain work touching `scenario_refresh_plans.js`, `scenario_refresh_runtime.js`, `scenario_visual_invalidation_executor.js`, or Pages dist.
   - No other active implementation worktree was listed when V5 started.

7. Verification:
   - `npm run test:node:scenario-refresh-plans` -> 17/17.
   - `npm run test:node:scenario-chunk-promotion-helpers` -> 9/9.
   - `npm run test:node:scenario-chunk-contracts` -> 54/54.
   - `npm run test:node:exact-after-settle-refresh-plans` -> 8/8.
   - `npm run test:node:renderer-runtime-state-behavior` -> 10/10.
   - `npm run python -- -m unittest tests.test_scenario_chunk_refresh_contracts tests.test_map_renderer_render_pipeline_passes_boundary_contract tests.test_main_deferred_detail_promotion_boundary_contract -q` -> 46 tests.
   - `npm run verify:architecture-boundaries` -> passed.
   - `npm run verify:test-import-graph` -> passed, 48 specs.
   - `npm run verify:pages-dist` -> passed, startup shell 38/38 and landing showcase 8/8.
   - `git diff --check` -> passed.
   - Source/dist mirror checks for the three changed map_renderer modules -> no diff.
   - Post-merge `main`: `npm run test:node:scenario-refresh-plans` -> 17/17.
   - Post-merge `main`: `npm run test:node:scenario-chunk-contracts` -> 54/54.
   - Post-merge `main`: `npm run verify:architecture-boundaries` -> passed.
   - Post-merge `main`: `npm run verify:test-import-graph` -> passed, 48 specs.
   - Post-merge `main`: `npm run verify:pages-dist` -> passed, startup shell 38/38 and landing showcase 8/8.
   - Post-merge `main`: source/dist mirror checks for the three changed map_renderer modules -> no diff.

8. Remaining risks:
   - Browser inspection was intentionally skipped; this phase changes internal render refresh contracts and was covered by code-level and Pages dist gates.
   - Renderer refresh plan `targetPasses` remains by design at renderer boundaries.

9. Recommended next step:
   - Commit the closeout/archive state, push `origin/main`, and remove the V5 worktree. Future V6 work can continue from `origin/main` after this closeout.
