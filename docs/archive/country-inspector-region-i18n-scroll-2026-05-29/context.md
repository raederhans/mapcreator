# Context

User reported untranslated country inspector region cards and scroll jumping when opening sections.

Done:
- TNO virtual inspector groups now carry local zh/en labels beside their source group definitions.
- Country inspector group redraw now restores the clicked header to its previous viewport position.
- Verified by targeted unit tests, node syntax checks, dist build, and browser metrics.
