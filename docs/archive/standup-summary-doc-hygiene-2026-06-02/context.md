# Standup Summary Doc Hygiene Context

- 2026-06-02: main thread owns this docs-hygiene pass.
- Current active directories:
  - `docs/active/transport-data-rollout`
  - `docs/active/worktree-closeout-2026-06-01`
  - `docs/active/project-download-save-dialog`
  - `docs/active/project-package-options`
- `transport-data-rollout` still has explicit in-progress rollout work, so it stays active.
- `worktree-closeout-2026-06-01` still has unclosed merge/worktree cleanup conditions, so it stays active.
- `project-download-save-dialog` is an empty active shell and already has a populated archive counterpart at `docs/archive/project-download-save-dialog/`.
- `project-package-options` is an empty active shell with no current repo references.
- Current `lessons learned.md` already removed earlier duplicate async/source-dist/owner-token sections, but it still keeps a few narrow one-off entries from completed fixes.
- Candidate narrow entries for removal in this pass:
  - Quickbar proxy-button behavior rule
  - local backend API/UI state-machine rule
  - shell fallback visibility rule
  - startup chunk visual gate rule
- Root doc check:
  - `docs/EXPORT_QA_ACCEPTANCE_SCRIPT.md` and `docs/EXPORT_QA_MANUAL_CHECKLIST.md` are still referenced and stay.
  - `docs/REFACTOR_ARCHITECTURE_SPLIT_2026-04-17.md` still claims a missing archive target and stays.
  - `docs/editor-performance-water-cache-decision-record-2026-04-17.md` is old but still reads like a durable decision record, so this pass leaves it in place.
- Applied cleanup:
  - removed four resolved narrow entries from `lessons learned.md`: Quickbar proxy-button, local backend state machine, shell fallback visibility, startup chunk visual gate
  - removed empty `docs/active/project-download-save-dialog`
  - removed empty `docs/active/project-package-options`
- Verification:
  - `git diff --check -- "lessons learned.md" docs` returned only pre-existing LF/CRLF warnings, with no diff-structure errors
  - active list after cleanup contains only `transport-data-rollout` and `worktree-closeout-2026-06-01`
