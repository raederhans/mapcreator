# Arctic Shell Render Fix Context

## 2026-05-31

- Created isolated worktree `C:\Users\raede\Desktop\dev\mapcreator-arctic-render-fix` on branch `codex/arctic-shell-render-fix`.
- Main checkout had unrelated `.omx/metrics.json` and `docs/active/` changes, so this task is isolated.
- Loaded `systematic-debugging` and `ultrawork`; root-cause-first workflow is active.
- Main agent owns all live commands. Static subagents may inspect code and tests only.
- Verified root cause: `isPoliticalVisualRenderableFeature` excluded `shouldExcludeRuntimeOnlyShellFallbackPoliticalFeature`, while `getRuntimePoliticalBaseCollection` needs that same filter for collection-level shell-only payload protection.
- Added failing regression in `tests/scenario_chunk_contracts.test.mjs`; it failed before the renderer fix on `arcticShellCanRenderWithoutBecomingInteractive` and high-latitude RU Arctic visual policy.
- Implemented renderer-layer fix: visual feature gate no longer calls runtime-only shell collection filtering; political draw loop paints shell fallback as an underlay before normal features; interaction exclusion still uses `isScenarioShellFeature`.
- `npm run test:node:scenario-chunk-contracts` passes after the fix.
- After adding shared underlay ordering for full pass and partial repaint, `npm run test:node:scenario-chunk-contracts`, `npm run test:node:startup-hydration-behavior`, `npm run test:node:physical-layer-contracts`, `npm run verify:scenario-contracts`, and `npm run verify:pages-dist` pass.
- Browser smoke used static server at `http://127.0.0.1:8765/index.html?render_diag=1`; crop saved at `.runtime/browser/arctic-shell-render-russia-crop.png`. Render diagnostics showed zero skipped features; map assets and TNO chunks loaded. The static server was stopped after the smoke.
- Reviewer found no confirmed renderer logic bug. Follow-up fixes: replaced hot-path underlay sorting with linear bucket ordering, and added a VM behavior fixture that executes the current renderer shell policy functions against a minimal shell feature.
- Final state: source and `dist/app` renderer snippets match for `orderPoliticalShellUnderlayFirst`; Pages manifest was regenerated by `verify:pages-dist`.
