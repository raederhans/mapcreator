# appearance-transport-visual-audit task

当前状态：完成

已完成：
1. transport capability registry / shared resolver / workbench apply bridge 收口
2. startup 默认 warning 链、appearance transport toggle 语义、workbench open-state bug 修复
3. capability-driven copy、startup locale、transport visual mode 与 targeted tests 收口
4. reviewer 指出的 Apply bridge 语义过宽问题已收紧到主地图 overview 可表达范围
5. Apply patch 只保留主地图真正消费的 overview 字段，preview-only display config 留在 workbench
6. bridge 默认分支已收紧为显式关闭，未来新增 family 必须先声明映射规则
7. Python / Node / targeted Playwright / clean Playwright 目标集验证通过

剩余工作：
- 无
