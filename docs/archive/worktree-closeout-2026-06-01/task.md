# Worktree Closeout Task

- Preserve all meaningful code, tests, docs, and checked-in dist updates.
- Keep generated Python cache files out of Pages dist and manifest.
- Use fresh verification before commit and push.
- Delete linked worktree directories only after their branch content is merged into pushed `main`.
