# Ocean Biscay Source 2026-06-02 Context

- Main is at `3dbf3829` and pushed to origin before this worktree was created.
- Main has unrelated dirty transport/docs files; this worktree isolates ocean changes.
- Current audit from main: low_precision=0, source_replacement=5, high_precision_split=3, simplification_review=3, provenance_gap=0.
- Live process owner: main agent owns all fetch/build/test commands.
- Bay of Biscay source verified as Marine Regions IHO `mrgid=2359`.
- Full bundle-state sync was stopped before writes after an excessive wait. The narrower sync script wrote only target and dependent water assets.
- Accepted water feature changes: `tno_bay_of_biscay`, `tno_northeast_atlantic_ocean`.
- Bay of Biscay final geometry: 2147 polygonal vertices, area 19.861284, bounds (-7.8789, 43.2917, -0.4158, 47.9697).
- Derived provenance now records `tno_bay_of_biscay` in `water_extracts` with `source_layer=iho`, `source_query=mrgid=2359`, `source_record_ids=["mrgid:2359"]`, and removes it from `local_clone_extracts`.
- Audit after sync: low_precision=0, source_replacement=4, high_precision_split=3, simplification_review=3, provenance_gap=0.
- Passed validation:
  - validator report: `.runtime/reports/generated/tno_water_geometry_report.biscay_source.json`
  - targeted pytest log: `.runtime/logs/biscay_source_targeted_pytest.log`
  - full water pytest log: `.runtime/logs/biscay_source_water_pytest.log`
  - post-pretty manifest/chunk hash pytest log: `.runtime/logs/biscay_source_post_pretty_pytest.log`
- A read-only code-review subagent was launched for final review and closed after two timeout waits with no findings returned. Main-agent self-review checked the source replacement, dependent open-ocean sync, provenance, manifest/startup hashes, and validation coverage.
- Initial push was rejected because the raw IHO source snapshot exceeded GitHub's 100 MiB file limit. The final implementation records `snapshot_simplify_tolerance=0.004` in both snapshot and provenance, keeps the final Bay of Biscay geometry unchanged at 2147 vertices, and reduces the snapshot file to 99.12 MiB.
- Revalidation after size fix:
  - validator report: `.runtime/reports/generated/tno_water_geometry_report.biscay_source.size_fix.json`
  - targeted pytest log: `.runtime/logs/biscay_source_targeted_size_fix_pytest.log`
  - full water pytest log: `.runtime/logs/biscay_source_water_size_fix_pytest.log`
