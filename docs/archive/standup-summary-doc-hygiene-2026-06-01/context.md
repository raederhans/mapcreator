# Standup Summary Doc Hygiene Context

- 2026-06-01: Main thread owns the full docs-hygiene pass.
- Active task directories currently present:
  - `docs/active/transport-data-rollout`
  - `docs/active/worktree-closeout-2026-06-01`
  - `docs/active/landing-product-showcase`
- `transport-data-rollout` and `worktree-closeout-2026-06-01` both contain unfinished next steps, so they stay active.
- `landing-product-showcase` is currently an empty directory shell.
- `lessons learned.md` still contains a few duplicated durable rules:
  - async completion / observer guidance appears twice
  - source/dist scoped-sync guidance appears twice
  - owner-specific static contract guidance appears twice
- Top-level `docs/REFACTOR_ARCHITECTURE_SPLIT_AUDIT_2026-04-19.md` is currently unreferenced in the repo and reads like a one-time audit snapshot.
- Top-level `docs/REFACTOR_ARCHITECTURE_SPLIT_2026-04-17.md` claims an archive counterpart, but the archive target is still missing, so it must stay.
- Applied cleanup:
  - removed the shorter duplicated async-state section from `lessons learned.md` and kept the fuller observer contract
  - folded owner-specific token guidance into the main static-contract rule
  - folded pages-dist self-size reminder into the main source/dist rule and removed the duplicate headings
  - deleted empty `docs/active/landing-product-showcase`
  - deleted unreferenced `docs/REFACTOR_ARCHITECTURE_SPLIT_AUDIT_2026-04-19.md`
- Verification:
  - `git diff --check` reported only existing LF/CRLF conversion warnings for `.omx/metrics.json` and `lessons learned.md`
  - `git diff -- ...` confirmed the cleanup stayed inside docs plus the pre-existing `.omx/metrics.json` dirt
