const { test } = require("@playwright/test");

test.skip("controller-derived frontline overlay is retired", async () => {
  // Manual strategic overlay coverage stays in strategic_overlay_smoke/editing/roundtrip specs.
});
